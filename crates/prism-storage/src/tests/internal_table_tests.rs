// S-2.03 — Internal table test module declaration.
//
// Test bodies are written in the Test Writer dispatch (step b).
// Tests will cover: BC-2.15.011 descriptor lookup, audit capability gate
// (E-QUERY-011), scan_limit env var parsing (AC-11), VirtualField column names
// (AC-12), all_descriptors count (AC-13), diff_results metadata-only (AC-14).
