// S-2.01 — InMemoryBackend implementation (stub).
//
// BTreeMap-backed StorageBackend for use in unit tests and downstream crate
// test suites (gated behind `#[cfg(any(test, feature = "test-utils"))]`).
// Implementer fills this in during step (c) TDD.
