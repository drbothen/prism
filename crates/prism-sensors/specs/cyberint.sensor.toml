# cyberint.sensor.toml — Red Gate skeleton (TEST-WRITER scope)
#
# RED GATE STUB authored by test-writer for PLUGIN-MIGRATION-001-D
# scaffolding. The implementer (Task 5) is responsible for filling this out
# with production-grade field values reverse-engineered from the Rust adapter
# and grounded against DTU routes per ADR-028 §D1/§D2.
#
# This minimal stub satisfies:
#   - file existence (load_all() can discover it)
#   - filename-stem matches sensor_id (E-SPEC-017 not triggered)
#   - parses as valid TOML (no E-SPEC-001/E-SPEC-009 parse errors)
# This minimal stub does NOT satisfy:
#   - production table content (no [[tables]] entries → table count assertions fail)
#   - production step content (no cursor-paginated pipeline → BC-2.16.009 assertions fail)
#   - production OCSF column mappings (BC-2.16.009 validation finds no columns)
#
# auth_type = "cookie_roundtrip" is grounded from DTU enforcement (ADR-028 §D2;
# D-747 Path A architectural decision — NOT implementer scope).
# DTU extracts cyberint_session cookie in prism-dtu-cyberint::extract_session_token().

sensor_id = "cyberint"
name = "Cyberint (skeleton — implementer Task 5 to complete)"
auth_type = "cookie_roundtrip"
base_url = "https://api.cyberint.example.com"
version = "0.1.0-skeleton"
tables = []
