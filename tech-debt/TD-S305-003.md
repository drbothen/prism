# TD-S305-003: Mid-fetch timeout test requires tokio::time integration

**Filed:** 2026-05-06
**Story:** S-3.05
**Severity:** tech_debt
**Status:** open / deferred

## Finding

`test_BC_2_07_002_mid_fetch_timeout_produces_partial_results_with_sensor_errors` verifies
that a 30-second query budget expiry during multi-page fetch produces partial results with
a `sensor_errors` truncation notice.

## Why Deferred

The test body has a `todo!()` marker. Full coverage requires:
1. `tokio::time::pause()` + `tokio::time::advance(Duration::from_secs(30))` to simulate timeout.
2. Integration with the fetch loop's 30s query budget (S-3.02 / BC-2.11.005).
3. A `sensor_errors` field in the response struct (not yet defined in S-3.05 scope).

## Resolution Path

During a fetch-loop integration pass (S-3.02 extension or S-5.01): wire the 30s timeout
and verify partial results via async test with time control.

## Test Location

`crates/prism-query/src/tests/pagination_tests.rs::test_BC_2_07_002_mid_fetch_timeout_produces_partial_results_with_sensor_errors`
