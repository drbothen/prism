---
document_type: domain-spec-section
level: L2
section: "capabilities"
version: "1.0"
status: draft
producer: business-analyst
timestamp: 2026-04-14T04:00:00
phase: 1a
inputs: [product-brief.md]
input-hash: "be246a0"
traces_to: L2-INDEX.md
---

# Domain Capabilities

This section enumerates all domain capabilities exposed by Prism. Each capability maps to one or more MCP tools, resources, or internal subsystems. Priority reflects implementation order: P0 is required for MVP, P1 for full launch, P2 for post-launch enhancement.

| ID | Capability | Description | Business Rule | Priority |
|----|-----------|-------------|---------------|----------|
| CAP-001 | Sensor Query (Single Client) | Query a specific sensor's data sources (alerts, devices, vulnerabilities, etc.) for a single MSSP client via MCP tools. Supports pagination via cursor, filtering by severity/status/time range, and sorting. Each tool call requires explicit `client_id`. | Every sensor query must include a valid `client_id`. Responses are scoped exclusively to that client's sensor instance. Results are paginated with cursor-based forward-only traversal. | P0 |
| CAP-002 | Cross-Client Query | Query a sensor across all MSSP clients simultaneously by passing `client_id: null`. Returns aggregated results with per-client attribution. Enables "show me all clients with critical CrowdStrike alerts" workflows. | Cross-client queries fan out to all configured clients for the specified sensor. Each result item includes `client_id` provenance. Sensors unavailable for a client are reported as partial failures, not query-level errors. | P0 |
| CAP-003 | OCSF Normalization | Normalize all sensor records to OCSF v1.x via the DynamicMessage protobuf pattern. Enables cross-sensor correlation by mapping vendor-specific fields to a common schema (e.g., CrowdStrike `hostname` and Claroty `device_name` both map to OCSF `device.hostname`). | Every sensor record returned via MCP tools includes an `ocsf` field containing the normalized OCSF representation. Unmappable vendor-specific fields are preserved in a `raw_extensions` JSON blob. OCSF schema version is fixed per Prism release. | P0 |
| CAP-004 | Credential Management | Store, retrieve, rotate, and delete per-client per-sensor credentials via OS keyring (macOS Keychain, Windows Credential Vault, Linux libsecret) with AES-256-GCM encrypted file fallback. Namespace: `(client_id, sensor_id, credential_name)`. | Credentials are isolated by client -- accessing Client A's CrowdStrike credentials while operating in Client B's context is a type error. Credential values are never included in logs, MCP responses, or error messages. All credential access is audit-logged. | P0 |
| CAP-005 | Feature Flag Evaluation | Evaluate two-tier feature flags (compile-time cargo features + runtime per-client TOML config) to determine which write operations are available for each client. Hierarchical resolution: `sensor.crowdstrike.containment` falls back through `sensor.crowdstrike.write`, `sensor.write`, to global deny-by-default. | Deny-by-default: if no flag explicitly enables a capability, it is denied. Both tiers (compile-time and runtime) must permit the operation. Disabled tools are hidden from `tools/list`. A `list_capabilities` meta-tool exposes the full capability matrix per client. | P0 |
| CAP-006 | Write Operation Gating | Gate all write/mutation operations (host containment, alert acknowledgment, device actions) behind a three-tier risk classification: read (no gate), reversible write (dry-run default with preview), irreversible write (confirmation token with 300s expiry). | Reversible writes default to `dry_run: true`; the agent must explicitly set `dry_run: false`. Irreversible writes require a two-step flow: request returns a `ConfirmationToken`, then `confirm_action(token)` executes. Tokens are single-use, time-bounded, and include an action-specific content hash. | P1 |
| CAP-007 | Audit Logging | Log every MCP tool invocation with: client_id, sensor, tool name, parameters (secrets redacted), user identity, timestamp, feature flags evaluated, result summary, and safety flags triggered. Structured JSON via `tracing` crate, compatible with Vector pipeline. | Audit entries are append-only and include sufficient detail for SOC 2 Type II and ISO 27001 compliance. Write operations log both the capability check result and the execution outcome. Credential access events are logged with client/sensor context but never credential values. | P0 |
| CAP-008 | Sensor Health Monitoring | Expose per-sensor, per-client connectivity status via MCP resources and a dedicated health tool. Detect unreachable APIs, expired credentials, and rate limit exhaustion. | Health checks run on-demand (not polled). Each sensor adapter implements `verify_connectivity()`. Health status includes: reachable/unreachable, auth valid/expired, last successful query timestamp, and current rate limit state. | P1 |
| CAP-009 | Client Configuration | Load and validate per-client sensor mappings, credential references, and capability overrides from TOML configuration. Support `--dry-run` validation that prints redacted config and exits. Re-evaluate capabilities on client context switch with `notifications/tools/list_changed`. | Config changes require server restart (no hot-reload in stdio per-analyst model). Validation is multi-error: all problems reported in one pass. Missing required fields produce actionable error messages specifying the exact config path. | P0 |
| CAP-010 | Prompt Injection Defense | Sanitize all sensor data flowing through MCP responses to defend against attacker-controlled content (hostnames, file paths, process names) that could manipulate the LLM. Four-layer defense: structural separation, provenance framing, suspicious pattern flagging, trust-level metadata. | Untrusted sensor data is placed in `structuredContent` JSON fields, never interpolated into prose. Tool descriptions include security warnings about untrusted data. Suspicious patterns (e.g., strings containing "ignore previous instructions") are flagged in parallel `_safety_flag` fields but never stripped -- the analyst needs full forensic data. | P0 |
| CAP-011 | Cursor State Management | Persist composite cursor state per-client, per-sensor, per-source using atomic file writes (temp + fsync + rename). Enforce forward-only progress invariant. Detect query fingerprint drift at startup. | Cursor is saved AFTER successful delivery (fixes poller-cobra ordering bug). MemoryStore exists only for tests and panics in production code paths. Query fingerprint mismatch at startup is a fatal error with actionable message. | P0 |
| CAP-012 | Cross-Sensor Correlation | Enable correlation of events across different sensors for the same client using OCSF-normalized fields (IP address, hostname, timestamp). The AI agent constructs correlation queries; Prism provides the normalized data. | Correlation is performed at the AI agent layer using OCSF field alignment. Prism ensures all sensor data for a client includes consistent OCSF field mappings so the agent can join on `device.ip`, `device.hostname`, or `time` fields. | P1 |
| CAP-013 | xMP Envelope Delivery | Produce xMP-enriched payloads for backward compatibility with the existing Vector/SIEM pipeline. Batch delivery via HTTP POST with basic auth. | xMP envelope format is wire-compatible with existing Vector pipeline expectations. Record types follow `<sensor>_<entity>` naming: `crowdstrike_alert`, `claroty_device`, `armis_vulnerability`, etc. | P1 |
