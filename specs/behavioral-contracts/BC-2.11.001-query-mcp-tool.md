---
document_type: behavioral-contract
level: L3
version: "1.20"
status: active
producer: product-owner
timestamp: 2026-04-14T07:00:00
phase: 1a
origin: greenfield
subsystem: "SS-11"
capability: "CAP-015"
lifecycle_status: active
introduced: cycle-1
modified: "2026-07-13"  # v1.20: SR-006 EC-11-068→EC-11-079 namespace-collision renumbering (F-MCPNULL-P13-HIGH-001); F-MCPNULL-P13-OBS-001 modified-comment corrected from stale v1.18 reference
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
inputs: [".factory/specs/prd.md", ".factory/specs/domain-spec/capabilities.md"]
input-hash: "566def3"
traces_to: ["CAP-015"]
related_bcs: ["BC-2.11.016", "BC-2.11.017", "BC-2.11.018"]
extracted_from: ".factory/specs/prd.md"
---

# BC-2.11.001: `query` MCP Tool Accepts Scoping + PrismQL Query String

## Description

The `query` MCP tool is the primary interface for analysts to interrogate sensor data. It accepts a PrismQL query string alongside optional scoping parameters (clients, sensors, sources, time range, limit, force_refresh), fans out concurrently to all resolved sensor API and internal RocksDB sources, materializes results as Arrow RecordBatch in a DataFusion SessionContext, and returns rows projected from OCSF-normalized event data (external) or native Prism records (internal). The SessionContext is ephemeral — it is torn down when the tool call returns, so there is no cross-call pagination; instead, the `limit` parameter (max 1000) truncates results and `is_truncated`/`total_available` fields in the response tell the analyst how to narrow further.

## Preconditions
- The `query` MCP tool is invoked with at minimum a `query` string parameter (required)
- Optional scoping parameters: `clients` (array of client IDs or null for all), `sensors` (array of sensor types or null for all — includes `"prism"` for internal tables), `sources` (array of data source names or null for all — includes `prism.*` names like `"prism.alerts"`, `"prism.cases"` for internal RocksDB-backed tables), `time_range` (relative or absolute), `limit` (max results, default 25, max 1000), `force_refresh` (boolean, default false -- bypass response cache; has no effect on internal tables which always read fresh from RocksDB)
  - **Interim implementation status (2026-06-10, MCP cascade P2-04 — implementer-verified):** `clients`, `limit`, and `force_refresh` are wired end-to-end in the shipped `query` tool. `sensors`, `sources`, and `time_range` are **declared; production wiring tracked by the query scope-params story per the 2026-06-10 review** (story-writer burst registering the anchor in parallel); **interim behavior: rejected with MCP `-32602` INVALID_PARAMS (`deny_unknown_fields`) — fail-closed, never silently ignored.** The declarations remain normative (POL-1 append-only spirit): the postconditions referencing `sensors`/`sources`/`time_range` resolution (scoping resolution, predicate intersection, `time_range_applied` in `query_context`) describe the target contract behavior the wiring story must deliver, not currently-shipped behavior.
- At least one client with at least one enabled sensor exists in configuration

## Postconditions
- The query string is parsed by the Chumsky parser (auto-detecting filter/SQL/pipe mode)
- Scoping parameters (`clients`, `sensors`) are resolved to concrete client/sensor combinations from config
- If the query contains `client_id`, `sensor`, or `source` predicates, they are intersected with tool parameters (narrowing, never widening)
- Fan-out to sensor APIs occurs for all resolved (client, sensor) combinations
- For external tables: Sensor responses are normalized to OCSF, materialized as Arrow RecordBatch, registered as DataFusion MemTable
- For internal tables (`prism.*` sources): Data is read directly from RocksDB, deserialized into Arrow RecordBatch, and registered as DataFusion tables. No API fan-out or OCSF normalization occurs. Virtual fields `sensor = "prism"` and `source = "{table_name}"` are injected.
- Both external and internal tables can be queried together in a single PrismQL statement (cross-source joins are supported)
- **Table availability is validated at plan time (before fan-out).** After AST parse and before any sensor API contact, the planner checks each `source_ref` in the `QueryPlan` against the live `TableRegistry`. If a referenced table is not registered, the query is rejected immediately with `E-QUERY-037` — a structured error carrying `table` (the requested table name), `sensor` (the sensor prefix), `available_sensors` (comma-separated list of currently-registered sensor IDs), `available_tables` (comma-separated list of currently-registered table names), and `did_you_mean` (the top-1 match from edit-distance ≤ 3 comparison against the org-visible registered table names, or absent if no close match exists). This check is mode-agnostic: it applies identically to SQL mode, filter mode, and pipe mode queries. No fan-out occurs for a rejected query. **Org-scoped enumeration (ADR-039, SEC-001/CWE-200):** The `available_sensors` and `available_tables` fields are computed via `filter_to_org_visible` at error-construction time using the following rules: (a) **Multi-tenant overlay deployment** — when `QueryOptions.clients` contains one or more `OrgSlug` values AND `resolved_spec_map` (the ADR-029 per-org overlay map, type `HashMap<ResolvedSpecKey, ResolvedSensorSpec>`) is populated on `QueryEngine`, `available_sensors` and `available_tables` are filtered to the union of sensors and tables registered for those requesting org(s). The `did_you_mean` suggestion is computed only over the org-visible table set via `TableRegistry::did_you_mean_for_tables(requested, &org_visible_tables)`. (b) **Single-tenant deployment or unrestricted multi-org query** — when `QueryOptions.clients` is `None` (no client restriction) OR `resolved_spec_map` is `None` (no overlay system configured), `available_sensors` and `available_tables` are read from the global `TableRegistry::registered_tables()` and `TableRegistry::registered_sensor_ids()` without filtering — identical to pre-ADR-039 behavior. (c) **Empty org scope** — when `QueryOptions.clients` is `Some([])` (empty slice), `available_sensors` and `available_tables` are both empty strings and `did_you_mean` is absent. When no sensors are configured globally, both fields are empty strings and `did_you_mean` is absent regardless of org scope.
- Query is executed via DataFusion; results returned as rows projected from OCSF-normalized event data (external) or native Prism records (internal)
- **No cross-call pagination for query results.** The ephemeral model means the SessionContext (and all materialized data) cannot be held across calls. Each `query` call re-materializes from scratch (the response cache mitigates re-fetch cost). The `limit` parameter truncates results. If more results exist than `limit`, the response includes `is_truncated: true` and `total_available` (count of all matching records before truncation). The user narrows their query or increases `limit` (up to 1000) to see more results. There is no cursor or offset-based pagination for query results.
- **Dual limit semantics (tool-level vs SQL-level).** Tool-level `limit` is applied after DataFusion execution (which may include SQL-level LIMIT). `total_available` reflects count after DataFusion execution but before tool-level truncation. SQL LIMIT reduces `total_available`; tool-level limit causes `is_truncated: true`.
- **Tool limit (max 1000) vs materialization limit (10K).** The tool-level `limit` (max 1000) is applied post-DataFusion. Materialization may fetch up to 10K records for sorting/aggregation before truncation. This is analogous to SQL LIMIT applied after ORDER BY. Future optimization: push limit hints to sensor adapters when no aggregation is needed.
- **Query limit vs materialization guidance:** If `total_available` significantly exceeds `limit`, the agent should narrow query scope (add filters, reduce client/sensor scope) rather than increasing `limit`. The max `limit` of 1000 means at most 1000 results per query call; records beyond that threshold require a narrower query, not a higher limit. The 10K materialization cap (DI-019) is a hard ceiling on sensor fan-out — increasing `limit` beyond 1000 is not possible, and even within the 1-1000 range, large result sets consume more memory and time.
- Response includes `query_context` with: `original_query`, `expanded_query` (after alias resolution), `clients_queried`, `sensors_queried`, `time_range_applied`, `total_available` (total matching records before limit), `returned_results` (actual count returned, <= limit), `is_truncated` (true if total_available > returned_results), `execution_time_ms`
- **Successful responses MAY include `normalized_pql`** (optional field): the Chumsky-normalized PQL string the planner accepted — the canonical form the server would replay to reproduce the identical query plan. The field is ABSENT on ALL error responses (including partial-failure-only errors). Zero-row successful results still carry `normalized_pql`. See BC-2.11.018 for the full contract (field shape, exclusions, round-trip guarantee, and injection-safety boundary).
- Response includes `sensor_errors` array for any sensors that failed (partial results are valid)
- Response includes `rows` array of OCSF-normalized results with `trust_level: "untrusted_external"`. **Key name rationale:** The response array is named `rows`, not `events`: query results are tabular projections over OCSF-normalized columns — aggregate buckets (GROUP BY), joined rows, enriched columns, and internal `prism.*` records are not OCSF event objects, and even raw-event results are flattened column projections rather than OCSF event entities. Key name shipped in S-5.01-FOLLOWUP-MCP-BOOT (PR #163); ratified per human adjudication 2026-07-13 (DEFECT-MCP-ROWSHAPE-NULLS-001 F-MCPNULL-P1-MED-001).
- **Row-shape null-not-absent — agent-harness schema stability (DEFECT-MCP-ROWSHAPE-NULLS-001).** Every column present in the DataFusion result schema — including projected columns, columns produced by `| enrich` stages, and joined columns from cross-source queries — MUST appear as an explicit key in every emitted row object within the `rows` array. For NULL-valued cells, the JSON value MUST be `null` (explicit null), never key-omission. The `arrow_json` `WriterBuilder` in `prism-mcp` MUST be configured with `.with_explicit_nulls(true)` at all RecordBatch-to-JSON serialization sites in the query tool response path. This invariant applies identically to all three query modes (SQL, filter, pipe) and to both OCSF-normalized external events and internal Prism records (`prism.*` sources). **Agent-harness rationale:** LLM agents consuming `query` tool results must be able to rely on consistent key presence across all rows in a result set; a row object missing a key is indistinguishable from a row with an intentionally absent field and causes silent reasoning errors in downstream agent logic (`project_agent_harness_design.md`). **Precedents:** ADR-051 §D4 specifies that the enrich stage must produce a NULL-valued output column — not an absent column — when enrichment data is unavailable; BC-2.10.007 v1.6 establishes the same null-not-absent pattern for `retry_after_seconds` in error responses (field present as `null`, never omitted). **Acceptance-criteria grade (T13 live-audit probes [C3]/[H20]):** (a) a query projecting a nullable column returns every row with that key present — JSON `null` for NULL cells, non-null value otherwise; (b) a pipe-mode `| enrich` query where the enrichment UDF returns NULL for some rows returns those rows with the enriched column key present as `null`; (c) no row object in `rows` may omit a key that is present in the projected result schema for that query.

## Invariants
- DI-019: Query security limits enforced (64KB, nesting, pipe stages, 10K records, 30s timeout)
- DI-008: Client data separation -- cross-client results include per-event `client_id` provenance
- DI-004: Audit completeness -- exactly one AuditEntry emitted for the tool invocation

## Error Cases
| Error | Condition | Behavior |
|-------|-----------|----------|
| `E-QUERY-001` | PrismQL query string cannot be parsed | Structured error with position, context, suggestion, and syntax help. **ADR-041 v1.1 pedagogical enrichments** add `near_text` (offending token, ≤ 50 chars) and `reference_pointer: "prismql://reference"` to the structured response — see BC-2.11.017 for full payload spec. |
| `E-QUERY-036` | Query references a source table whose sensor prefix is not registered in the adapter registry OR whose prefix fails `sensor_id_from_table_name` validation — fires in `resolve_source_refs` at materialization time (after plan validation) | `PrismError::UnknownSourceTable { source_name }` surfaced as MCP `-32602` INVALID_PARAMS. Message: `"E-QUERY-036: unknown source table '{source_name}'..."`. **`available_tables` enrichment (AUDIT-001, closes misdirection):** The structured error response MUST include an `available_tables` field (comma-separated, org-scoped, same `filter_to_org_visible` principle as E-QUERY-037) listing the currently-registered table names visible to the requesting client. This field MUST NOT be empty when tables are registered — the absence of `available_tables` is what caused AUDIT-001 to misdirect the agent to edit config rather than showing valid table names. When no tables are registered, `available_tables` is `""`. See error-taxonomy.md §E-QUERY-036 for the `PrismError::UnknownSourceTable` variant definition; this BC extends the payload requirement. |
| `E-QUERY-037` | Query references a table whose sensor is not currently configured in the live `TableRegistry` (plan-time check, before fan-out) | `PrismError::TableNotAvailable(Box<TableNotAvailableDetails>)` surfaced as MCP `-32602` INVALID_PARAMS. Message: `"E-QUERY-037: table '{table}' is not available — sensor '{sensor}' is not configured. Available sensors: [{available_sensors}]. Available tables: [{available_tables}].{did_you_mean}"` where `{did_you_mean}` = `" Did you mean: '{best_match}'?"` (leading space) if edit-distance ≤ 3 match exists, else absent. Check applies in all three query modes (SQL / filter / pipe). No fan-out occurs. **Org-scoping invariant (ADR-039):** `available_sensors` and `available_tables` MUST NOT enumerate sensors or tables belonging to org(s) other than the requesting client's org scope. In multi-tenant overlay deployments (`QueryOptions.clients` is non-empty AND `resolved_spec_map` is populated), both fields are filtered to the requesting org(s)' registered sensors/tables via `filter_to_org_visible`. In single-tenant deployments or unrestricted queries, both fields reflect the global registry. The `did_you_mean` suggestion is computed only over the org-visible table set. **ADR-041 v1.1 pedagogical enrichment** adds a `suggestion` field pointing to `prism_describe` as the discovery path ("Call prism_describe('<client_id>') to see available tables and columns...") — see BC-2.11.017 for the exact suggestion string per `did_you_mean` present/absent cases. |
| `E-QUERY-002` | Type error: field used with an operator incompatible with its column type | Structured error. **ADR-041 v1.1 pedagogical enrichment** adds `valid_operators_for_type` (array of valid operators for the actual column type) to the structured response — see BC-2.11.017 for the per-type operator table. |
| `E-QUERY-003` | Query violates a security limit (size, nesting depth, pipe stages, regex length) | Structured error. **ADR-041 v1.1 pedagogical enrichment** adds `how_to_fix` (actionable per-limit suggestion string) to the structured response — see BC-2.11.017 for the per-limit suggestion table. |
| `E-QUERY-038` | Query references a column name that does not exist in the `TableRegistry` schema for the specified table and client (plan-time gate, fires after E-QUERY-037 table check, before fan-out) | `PrismError::ColumnNotFound` surfaced as MCP `-32602` INVALID_PARAMS. Payload: `column` (as submitted), `table`, `client_id`, `available_columns` (org-scoped, always present), `did_you_mean` (when Levenshtein distance ≤ 3 match exists). Full contract: see BC-2.11.016. |
| `E-QUERY-039` | Pipe-mode query contains `\| enrich <infusion>(...)` OR SQL-mode query contains `ScalarFunc::Unknown(name)` (SELECT projection or WHERE) where the enrichment function name is not registered in the process-global `InfusionRegistry` (plan-time gate, fires after E-QUERY-037 and E-QUERY-038 checks, before fan-out) | `PrismError::EnrichUdfNotFound(Box<EnrichUdfNotFoundDetails>)` surfaced as MCP `-32602` INVALID_PARAMS. Payload: `infusion` (as submitted), `available_infusions` (comma-separated, global `InfusionField.name` set, always present), `did_you_mean` (when Levenshtein distance ≤ 3 match exists against global set). Closes AUDIT-005 (opaque E-INT-001 for unregistered enrichment calls). Full contract: see BC-2.11.019. |
| `E-QUERY-006` | Materialization would exceed 10K records | Structured error with estimated counts and narrowing suggestions (DEC-023) |
| `E-QUERY-004` | Execution exceeds 30 seconds | Structured error with timeout duration and narrowing suggestions (DEC-026) |
| `E-QUERY-033` | Tool-level `limit` parameter exceeds the 1000 maximum (`limit` default 25, max 1000) | Pre-execution parameter validation rejection; `PrismError::QueryLimitExceeded { requested, max }` surfaced via `map_prism_error` as MCP `-32602` INVALID_PARAMS with message `"E-QUERY-033: limit {requested} exceeds maximum of {max} (BC-2.11.001)"` (taxonomy v1.70 verbatim shipped display) |
| `E-MCP-004` | `clients` array contains invalid client ID | Structured error with rejected value |
| `E-CFG-100` | No matching clients/sensors found for scoping parameters | Structured error listing configured clients/sensors |

## Edge Cases
| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| DEC-022 | All sensor API calls return empty results | Empty result set with `total_results: 0`, not an error |
| DEC-023 | Fan-out produces more than 10K records | Fetch aborted when streaming counter hits 10K; error with per-sensor counts and narrowing suggestions |
| DEC-026 | Query execution exceeds 30s timeout | Error, no partial results |
| EC-11-001 | `clients: ["acme"]` but query contains `client_id = "globex"` | Intersection is empty; return empty result set (not error) with metadata explaining intersection was empty |
| EC-11-002 | All sensors error for a single client in cross-client query | Client's results omitted; other clients' results returned; failed client listed in `sensor_errors` |
| EC-11-032 | Query matches 500 records but `limit` is 25 | Returns 25 records with `is_truncated: true`, `total_available: 500`. User can re-query with `limit: 500` or narrow the query. No cross-call pagination state is held. |
| EC-11-033 | Query references `crowdstrike_alerts` when CrowdStrike is not configured but Armis and Claroty are | Returns `E-QUERY-037` with `sensor="crowdstrike"`, `available_sensors="armis, claroty"`, `available_tables="armis_alerts, armis_devices, claroty_devices, ..."`. No fan-out occurs. |
| EC-11-034 | Query references `crowdstrike_alert` (typo — missing 's') when `crowdstrike_alerts` is registered | Returns `E-QUERY-037` with `did_you_mean="crowdstrike_alerts"` (Levenshtein distance = 1). The suggestion field is present in the error. |
| EC-11-035 | Query references an unregistered table and no sensors are configured at all | Returns `E-QUERY-037` with `available_sensors=""`, `available_tables=""`, `did_you_mean` absent. |
| EC-11-036 | SQL mode, filter mode, and pipe mode queries all reference an unregistered table | All three return `E-QUERY-037` — the plan-time table-availability gate is mode-agnostic. |
| EC-11-037 | Multi-tenant overlay deployment: org A (`clients=["acme"]`) queries an unknown table; orgs "acme" and "contoso" each have distinct sensor sets configured via ADR-029 per-org overlays (`resolved_spec_map` populated) | Returns `E-QUERY-037` where `available_sensors` contains ONLY acme's configured sensor IDs, NOT contoso's sensors. `available_tables` contains ONLY acme's registered tables. `did_you_mean` suggestion is drawn only from acme's table set. Contoso's sensor vendor names MUST NOT appear in the error response. (SEC-001/CWE-200 cross-tenant enumeration prevention; ADR-039 §Consequences.) |
| EC-11-038 | Single-tenant deployment or `clients: None` query (no `resolved_spec_map`, or `QueryOptions.clients` is `None`) encounters an unregistered table | Returns `E-QUERY-037` with `available_sensors` and `available_tables` reflecting the full global `TableRegistry` — identical to pre-ADR-039 behavior. No filtering occurs. `filter_to_org_visible` short-circuits at the `resolved_spec_map is None` / `org_scope is None` check. Backward compatibility preserved for all existing single-tenant deployments. |
| EC-11-065 | Query passes plan-time checks (E-QUERY-037 passes) but then `resolve_source_refs` encounters an unregistered sensor prefix at materialization time → E-QUERY-036 fires | `E-QUERY-036` response MUST include `available_tables` (comma-separated, org-scoped list of registered table names). The error MUST NOT omit `available_tables` — its absence was the root cause of AUDIT-001 misdirection (agent given "check config" instead of the list of valid tables). (AUDIT-001) |
| EC-11-066 | Pipe-mode query `FROM cyberint_alerts \| enrich unknown_udf(ioc_value)` where `cyberint_alerts` is registered and `ioc_value` is a registered column, but `unknown_udf` is not registered in `InfusionRegistry` | `E-QUERY-039` with `infusion: "unknown_udf"`, `available_infusions` listing all global `InfusionField.name` values, `did_you_mean` present if a close match exists (Levenshtein ≤ 3 against global set). Gate fires at plan time; no sensor fan-out occurs. (AUDIT-005) |
| EC-11-067 | **Dot-notation in FROM target position** — query uses dot-notation as the table name in a `FROM` clause (SQL, Pipe, or SqlPipe mode), e.g. `FROM cyberint.alerts` or `SELECT * FROM cyberint.alerts`. This is DISTINCT from filter-mode source-qualified refs (e.g. `crowdstrike_detections \| severity='HIGH'`) which use underscore-qualified table names with a `\|` separator. | `E-QUERY-037` with `table: "cyberint.alerts"`, `sensor: "cyberint"`, `available_tables` listing org-visible underscore-qualified tables, and `did_you_mean: "cyberint_alerts"` (Levenshtein distance ≤ 3). The dot-notation string `"cyberint.alerts"` is NOT a valid registered table key in `TableRegistry` (which stores only underscore-qualified keys like `"cyberint_alerts"`). Therefore `TableRegistry::is_registered("cyberint.alerts")` → false and E-QUERY-037 fires at plan time. The gate MUST NOT silently extract `"cyberint"` as a sensor prefix and route to the sensor adapter — that silent extraction causes E-SENSOR-030 partial failures and is the N2 regression from PR #203. **SqlPipe is NOT exempt (HIGH-1 adjudication, 2026-06-27):** `SELECT * FROM crowdstrike.detections \| enrich fn(x)` is a SqlPipe query whose SQL head carries an `Ast::SourceRefKind::External` reference — the same AST kind as SQL/Pipe mode. Dot-notation is NOT a defined SqlPipe source syntax (BC-2.11.020 edge cases EC-11-020-001 through EC-11-020-009 all use underscore-qualified `FROM t` form; no SqlPipe BC names dot-notation as accepted syntax). Silently converting the external dot form to underscore and proceeding produces the same inconsistency EC-11-067 exists to eliminate: an analyst writing `FROM crowdstrike.detections` gets E-QUERY-037 in Pipe mode but silent success in SqlPipe mode for the same token. The `check_availability_gate` SqlPipe exemption branch introduced post-spec MUST be removed; SqlPipe dot-notation FROM targets return E-QUERY-037 identically to SQL and Pipe modes. **Disambiguation (N2 / AUDIT-004):** dot-notation in a FROM clause TARGET is always an error. dot-notation for filter-mode source refs is out-of-scope (filter mode uses `<table_name> \| <predicate>` syntax per BC-2.11.002, not `<sensor>.<table>` dot-syntax). The implementer fix must ensure the E-QUERY-037 plan-time gate runs BEFORE `sensor_id_from_table_name` dot-notation extraction so `cyberint.alerts` never reaches the sensor fan-out path. |
| EC-11-079 | Query result RecordBatch contains rows with NULL values in one or more schema columns (e.g., `sensor_ip` is NULL for some events but the column is present in the projected result schema) | Every row in the `rows` JSON array MUST contain the key `"sensor_ip"` with JSON value `null` — never key-omission. A row object `{"severity":"high"}` when `sensor_ip` is NULL is a contract violation; the correct serialization is `{"severity":"high","sensor_ip":null}` (key order unconstrained). The `arrow_json` `WriterBuilder` MUST use `.with_explicit_nulls(true)`. Applies to all three query modes (SQL, filter, pipe) and to both OCSF-normalized external events and internal `prism.*` result sets. (DEFECT-MCP-ROWSHAPE-NULLS-001, T13 audit probes [C3]/[H20]) |

## Canonical Test Vectors

> See `.factory/specs/prd-supplements/test-vectors.md` for the canonical test vector tables.

| Input | Expected Output | Category |
|-------|----------------|----------|
| `query(query="severity = 'critical'", clients=null)` | Rows array projected from OCSF-normalized event data; `is_truncated` reflects limit state | happy-path |
| `query(query="SELECT count(*) FROM events")` | Single-row result with aggregate count | happy-path |
| `query(query="severity = 'critical'", clients=["nonexistent"])` | `Err(E-MCP-004)` with rejected value | error |
| `query(query="<64KB+1 string>")` | `Err(E-QUERY-003)` query length exceeded | error |
| `query(query="severity = 'critical'", limit=25)` with 500 matching records | Returns 25 records, `is_truncated: true`, `total_available: 500` | edge-case |
| `query(query="SELECT * FROM crowdstrike_alerts")` when CrowdStrike is not configured and Armis+Claroty are | `Err(E-QUERY-037)` with `table="crowdstrike_alerts"`, `sensor="crowdstrike"`, `available_sensors="armis, claroty"`, `available_tables` listing armis and claroty tables, `did_you_mean` absent (no close match) | error (table-availability, EC-11-033) |
| `query(query="SELECT * FROM crowdstrike_alert")` when `crowdstrike_alerts` is registered | `Err(E-QUERY-037)` with `did_you_mean="crowdstrike_alerts"` (Levenshtein distance 1) | error (table-availability did_you_mean, EC-11-034) |
| `query(query="SELECT * FROM armis_alerts")` in all three modes (filter: `armis_severity = 'high'`, pipe: `FROM armis_alerts \| ...`) when Armis is not configured | All three return `Err(E-QUERY-037)` with `sensor="armis"` | error (table-availability mode-agnostic, EC-11-036) |
| `query(query="SELECT * FROM unknown_table", clients=["acme"])` in multi-tenant overlay deployment where acme has `crowdstrike` and contoso has `claroty` (distinct non-overlapping sensor sets) | `Err(E-QUERY-037)` with `available_sensors` containing `"crowdstrike"` ONLY (not `"claroty"`); `available_tables` lists acme's crowdstrike tables ONLY; contoso sensor names absent from error | error (org-scoped enumeration, EC-11-037, SEC-001) |
| `query(query="SELECT * FROM unknown_table")` (`clients=None`) in same multi-tenant overlay deployment | `Err(E-QUERY-037)` with `available_sensors` listing ALL globally configured sensors (`"crowdstrike, claroty"`); `available_tables` listing all registered tables; `filter_to_org_visible` bypassed (org_scope=None) | error (org-scoped enumeration backward-compat, EC-11-038) |
| `query(query="SELECT * FROM alerts")` when `cyberint_alerts` and `claroty_alerts` are registered but `alerts` is not → E-QUERY-036 fires in `resolve_source_refs` | `Err(E-QUERY-036)` with `source_name: "alerts"` AND `available_tables` containing `"cyberint_alerts, claroty_alerts"` (org-scoped). `available_tables` MUST NOT be absent. (AUDIT-001, EC-11-065) | error (unknown-source-table available_tables) |
| `query("FROM cyberint_alerts \| enrich nvd_cvss(alert_id)", clients=["acme"])` where `nvd_cvss` is not registered in `InfusionRegistry` | `Err(E-QUERY-039)` with `infusion: "nvd_cvss"`, `available_infusions` listing all global `InfusionField.name` values, `did_you_mean` absent if no close match (Levenshtein > 3 against global set). (AUDIT-005, EC-11-066) | error (enrich-udf-not-found) |
| `query("FROM cyberint.alerts LIMIT 5", clients=["org-c"])` where `cyberint_alerts` is registered for org-c | `Err(E-QUERY-037)` with `table: "cyberint.alerts"`, `sensor: "cyberint"`, `did_you_mean: "cyberint_alerts"`. The dot-notation form `cyberint.alerts` is NOT in `TableRegistry`; the gate fires before `sensor_id_from_table_name` dot-extraction. `isError: true` — NOT a silent 0-row result. (N2 / EC-11-067) | error (dot-notation-from-target) |
| `query("SELECT * FROM crowdstrike.detections", clients=["org-c"])` where `crowdstrike_detections` is registered | `Err(E-QUERY-037)` with `table: "crowdstrike.detections"`, `did_you_mean: "crowdstrike_detections"`. Same gate as EC-11-067 — dot-notation in SQL-mode FROM is rejected at plan time, not silently fan-outed. (N2 / EC-11-067) | error (dot-notation-from-target-sql) |
| `query("SELECT * FROM crowdstrike.detections \| enrich threat_score(src_ip)", clients=["org-c"])` where `crowdstrike_detections` is registered | `Err(E-QUERY-037)` with `table: "crowdstrike.detections"`, `sensor: "crowdstrike"`, `did_you_mean: "crowdstrike_detections"`. SqlPipe mode is NOT exempt from EC-11-067 — the dot-notation FROM target is `SourceRefKind::External` in the SQL head; E-QUERY-037 fires before any pipe-stage processing. The query does NOT silently succeed by converting dot to underscore. (HIGH-1 adjudication / EC-11-067 SqlPipe) | error (dot-notation-from-target-sqlpipe) |
| `query(query="SELECT severity, sensor_ip FROM crowdstrike_alerts")` where some rows in the sensor response have `sensor_ip = NULL` | Every row in `rows` array contains BOTH `"severity"` and `"sensor_ip"` keys. Rows with NULL `sensor_ip` serialize as `{"severity":"...","sensor_ip":null}` — key present, value `null`, not absent. No row object omits `"sensor_ip"` regardless of its value. `WriterBuilder.with_explicit_nulls(true)` is the required serialization configuration. (EC-11-079) | row-shape null-not-absent (DEFECT-MCP-ROWSHAPE-NULLS-001, T13 [C3]/[H20]) |

## Verification Properties

| VP ID | Property | Proof Method |
|-------|----------|-------------|
| VP-014 | Query security limits: rejects oversized queries | kani |
| VP-015 | Query security limits: rejects excessive nesting depth | kani |
| VP-021 | PrismQL parser: never panics on arbitrary input | fuzz |

## Traceability
| Field | Value |
|-------|-------|
| L2 Capability | CAP-015 |
| L2 Invariants | DI-004, DI-008, DI-019 |
| L2 Edge Cases | DEC-022, DEC-023, DEC-026 |
| Priority | P0 |

## Related BCs

- BC-2.11.016 — composes with: E-QUERY-038 column-not-found plan-time gate; the full payload contract (column, table, client_id, available_columns, did_you_mean), gate ordering (fires after E-QUERY-037), and org-scoping guarantee live in BC-2.11.016. Error Cases table above is a cross-reference summary only.
- BC-2.11.019 — composes with: E-QUERY-039 enrich-function-not-found plan-time gate; the full payload contract (`infusion`, `available_infusions` global set, `did_you_mean`), gate ordering (fires after E-QUERY-037/038), both AST paths (pipe-mode `EnrichStage.infusion` and SQL-mode `ScalarFunc::Unknown(name)`), and global-registry scoping live in BC-2.11.019. Error Cases row above is a cross-reference summary only.
- BC-2.11.017 — composes with: ADR-041 v1.1 pedagogical enrichments to E-QUERY-001/002/003/037; the additional structured fields (near_text, reference_pointer, valid_operators_for_type, how_to_fix, prism_describe suggestion) and their per-code payload specs live in BC-2.11.017. Error Cases annotations above are cross-reference summaries only.
- BC-2.11.018 — composes with: the optional normalized_pql field on successful query responses; the field's inclusion/exclusion invariants, wire format, round-trip guarantee, injection-safety boundary, and token cost acceptance live in BC-2.11.018. Postcondition note above is a cross-reference summary only.
- BC-2.11.006 — composed by: query security limits (DI-019) enforce 64KB, nesting, pipe stages, 10K records, 30s timeout; E-QUERY-006/004 complement E-QUERY-003.
- BC-2.11.010 — composed by: explain_query tool traverses the same parse/plan validation pipeline; E-QUERY-037/038 and pedagogical enrichments apply identically to explain_query calls.

## Changelog

| Version | Burst | Date | Author | Change |
|---------|-------|------|--------|--------|
| 1.20 | pass-13-ec-namespace-collision-sr-006-renumber | 2026-07-13 | product-owner | **SR-006 EC-11 namespace collision fix (F-MCPNULL-P13-HIGH-001) — EC-11-068→EC-11-079 for null-not-absent row-shape edge case; F-MCPNULL-P13-OBS-001 modified-comment correction.** This BC introduced EC-11-068 at v1.16 (2026-07-13) for the null-not-absent row-shape postcondition (DEFECT-MCP-ROWSHAPE-NULLS-001). BC-2.11.016 allocated EC-11-068 earlier at v1.15 (2026-07-09) for the FIELDS TRANSITION green-lock — BC-2.11.016 is the oldest allocator and retains the ID per append-only precedence (SR-006). BC-2.11.019 simultaneously introduced a third EC-11-068 at v1.7 (2026-07-13), resolved to EC-11-080 in BC-2.11.019 v1.11. **Normative sites updated in this BC:** (1) §Edge Cases row heading: `EC-11-068` → `EC-11-079`; (2) §Canonical Test Vectors category column tag: `(EC-11-068)` → `(EC-11-079)`. §Postconditions row-shape null-not-absent bullet does not cite the EC ID by number — no change there. Changelog references to EC-11-068 in v1.16/v1.17/v1.18/v1.19 entries are historical and left intact per append-only audit trail policy (TD-VSDD-091). Semantic content of edge case unchanged. **F-MCPNULL-P13-OBS-001:** frontmatter `modified:` comment updated from stale v1.18 reference to current v1.20. **POL-25 downstream propagation required (report only — not edited here):** code test file `.worktrees/DEFECT-MCP-ROWSHAPE-NULLS-001/crates/prism-mcp/tests/bc_2_11_001_null_row_shape_test.rs` (test names `test_BC_2_11_001_EC_11_068_*`, doc comments, assertion strings); server.rs comment at line 1950; story S-TEST-WIRESHAPE-SWEEP-001 AC-002 heading and BC table; BC-INDEX synopsis row for BC-2.11.001 (historical sub-record citing `EC-11-068 added`). |
| 1.19 | DEFECT-MCP-ROWSHAPE-NULLS-001-pass10-apiname-fix | 2026-07-13 | product-owner | **F-MCPNULL-P10-OBS-001 closure — `.explicit_nulls(true)` → `.with_explicit_nulls(true)` API name corrected at all spec sites.** The actual arrow-json v58 builder setter is `.with_explicit_nulls(true)`, not `.explicit_nulls(true)`. Three normative sites updated: (1) §Postconditions row-shape null-not-absent bullet; (2) §Edge Cases EC-11-068; (3) §Canonical Test Vectors EC-11-068 vector. Historical v1.16 changelog reference also corrected (full-file sweep). No contract semantics changed — the null-not-absent invariant is unchanged; only the cited API symbol name is corrected to match the actual arrow-json v58 method signature. (Orchestrator-adjudicated: name the real API, not shorthand.) |
| 1.18 | DEFECT-MCP-ROWSHAPE-NULLS-001-pass3-prose-harmonize | 2026-07-13 | product-owner | **F-MCPNULL-P3-LOW-002 closure — "events"-as-result-noun prose harmonized (POL-25 full-file sweep).** Internal contradiction between §Description ("returns OCSF-normalized events"), §Postconditions ("results returned as OCSF-normalized events (external)"), and the v1.17 rows-rationale ("flattened column projections rather than OCSF event entities"). The `rows` key rationale established in v1.17 is authoritative — results are tabular projections (rows) from OCSF-normalized data, not OCSF event objects. The "events" terminology for the result noun contradicts that rationale and perpetuates the design confusion that led to the original `events`→`rows` key rename. **Changes (3 sites):** (1) §Description: "returns OCSF-normalized events" → "returns rows projected from OCSF-normalized event data (external) or native Prism records (internal)". (2) §Postconditions: "results returned as OCSF-normalized events (external) or native Prism records (internal)" → "results returned as rows projected from OCSF-normalized event data (external) or native Prism records (internal)". (3) §Canonical Test Vectors happy-path row: "Events array with OCSF records" → "Rows array projected from OCSF-normalized event data". **POL-25 full-file sweep — all sites checked:** `events`-as-table-name in `FROM events` query string (line 108): left unchanged — SQL table name, not result noun. `events` in ADR-041 OCSF-concept references (line 110 traceability): clean. `OCSF-normalized event data` and `OCSF event objects` in §Postconditions `rows` key rationale (line 57): kept — these correctly describe the DATA BEING PROJECTED FROM, not the result noun. `audit events` in §Invariants: clean. `tracing events` in Logging conventions: clean. All other `events` usages reference the concept or SQL table names, not the result noun — left unchanged. No BC contract semantics changed; `rows` key name, null-not-absent invariant (EC-11-068), trust_level, and all error-case contracts UNCHANGED. |
| 1.17 | DEFECT-MCP-ROWSHAPE-NULLS-001-events-to-rows | 2026-07-13 | product-owner | **F-MCPNULL-P1-MED-001 closure — `events` → `rows` response-array key name ratified; semantic rationale codified.** The shipped `query` MCP tool response uses the JSON key `rows` (not `events`) for the result array. This name was shipped in S-5.01-FOLLOWUP-MCP-BOOT (PR #163) but was never reflected in the BC spec — the postcondition, null-not-absent invariant (§Row-shape postcondition), EC-11-068, and canonical test vector all still used `events`. Human adjudication 2026-07-13 ratified `rows` as the authoritative key name under Source-of-Truth rule 7 (code-wins for key names; DEFECT-MCP-ROWSHAPE-NULLS-001 F-MCPNULL-P1-MED-001). **Changes:** (1) §Postconditions — `"events"` → `"rows"` in the response-array bullet; semantic rationale sentence added (tabular projection rationale). (2) §Row-shape null-not-absent postcondition — two occurrences of `"events array"` → `"rows array"`. (3) §Edge Cases EC-11-068 — `"events JSON array"` → `"rows JSON array"`. (4) §Canonical Test Vectors — EC-11-068 vector `"events array"` → `"rows array"`. Postcondition semantics (trust_level, null-not-absent invariant) unchanged. |
| 1.16 | DEFECT-MCP-ROWSHAPE-NULLS-001-bc-codification | 2026-07-13 | product-owner | **Row-shape null-not-absent postcondition codified (DEFECT-MCP-ROWSHAPE-NULLS-001, T13 live-audit findings [C3]/[H20]).** Root cause of defect: `prism-mcp` serialized RecordBatch rows via `arrow_json WriterBuilder` with default `explicit_nulls=false`, causing NULL-valued columns to be OMITTED from JSON row objects — violating agent-harness schema-stability. No existing BC stated this invariant for query-result row shapes (BC-2.10.007 established null-not-absent for error fields only; ADR-051 §D4 established it for enrich-stage output columns but not for the tool-response serialization layer). Changes: (1) §Postconditions — added "Row-shape null-not-absent" postcondition: `WriterBuilder.with_explicit_nulls(true)` required; every result-schema column key present in every row; JSON `null` for NULL values; all three query modes; both external OCSF-normalized events and internal `prism.*` records. Cites ADR-051 §D4 and BC-2.10.007 v1.6 as precedents. (2) §Edge Cases — added EC-11-068 (NULL-valued column in projected result schema → key present as `null`, never absent). (3) §Canonical Test Vectors — added SELECT-with-NULL-column vector. Implementer instruction: set `.with_explicit_nulls(true)` on `WriterBuilder` at all RecordBatch-to-JSON serialization sites in `prism-mcp` query tool response path. |
| 1.15 | demo-fidelity-remediation-pass-4-high1-adjudication | 2026-06-27 | product-owner | **HIGH-1 adjudication — SqlPipe is NOT exempt from EC-11-067.** LOCAL adversary Pass 4 found that `check_availability_gate` contained a post-spec `is_sqlpipe` exemption branch that silently converted `crowdstrike.detections` to `crowdstrike_detections` and returned `Ok(())` for SqlPipe queries. Decision: OPTION A (spec-as-written). Rationale: (1) `SourceRefKind::External` is the same AST kind in SqlPipe SQL heads as in SQL/Pipe mode — no structural distinction justifies different gate behavior; (2) BC-2.11.020 defines no dot-notation FROM syntax for SqlPipe (all edge cases EC-11-020-001..009 use underscore form); (3) the silent conversion produces analyst-visible inconsistency: same `FROM crowdstrike.detections` token gets E-QUERY-037 in Pipe mode but silent success in SqlPipe mode. Changes: (1) EC-11-067 expanded with explicit SqlPipe-not-exempt clause naming the HIGH-1 adjudication; (2) new canonical test vector for SqlPipe dot-notation rejection added. Implementer instruction: remove the `is_sqlpipe` exemption branch in `check_availability_gate`; the `SourceRefKind::External` rejection block must execute for ALL non-filter AST modes including SqlPipe. Add a Red Gate test `test_bc_2_11_001_n2_dot_notation_sqlpipe_e_query_037` covering `SELECT * FROM crowdstrike.detections \| enrich fn(x)` → E-QUERY-037. |
| 1.14 | demo-fidelity-remediation-2026-06-26 | 2026-06-26 | product-owner | **N2 / AUDIT-004 contract fix (S-DEMO-FIDELITY-REMEDIATION-001):** Added EC-11-067 (dot-notation in FROM target position → E-QUERY-037, not E-SENSOR-030) and two canonical test vectors (`FROM cyberint.alerts` and `SELECT * FROM crowdstrike.detections`). This closes the N2 spec ambiguity introduced by PR #203's dot-notation fallback in `sensor_id_from_table_name`: the spec now explicitly requires E-QUERY-037 to fire at plan time for dot-notation FROM targets BEFORE `sensor_id_from_table_name` extraction runs. Disambiguates from filter-mode source refs (which use `<table_name> \| <predicate>` not `<sensor>.<table>` dot-syntax). The implementer must ensure `TableRegistry::is_registered` runs on the raw source_ref string before any dot-extraction fallback; `"cyberint.alerts"` is not a registered table key and must trigger E-QUERY-037. |
| 1.13 | onboarding-001-C-sr-006-ec-renumber-2026-06-23 | 2026-06-23 | product-owner | SR-006 EC-11 namespace collision fix: renumbered the two 001-C-introduced edge-case IDs in this BC — EC-11-053→065 (E-QUERY-036 available_tables AUDIT-001 edge case) and EC-11-054→066 (E-QUERY-039 enrich unknown_udf AUDIT-005 edge case). These IDs were added by 001-C bursts v1.11/v1.12 and collided with committed BC-2.11.018's EC-11-053..056 range. Updated §Edge Cases table rows, §Canonical Test Vectors category column tags, and frontmatter comment. Changelog references to old IDs in v1.11/v1.12 entries are historical and left intact per append-only audit trail policy. Semantic content of edge cases unchanged. |
| 1.12 | onboarding-001-C-sr-resolution-2026-06-23 | 2026-06-23 | product-owner | SR-001/SR-002/SR-003 vocabulary consistency sweep (BC-2.11.019 architect adjudication): (1) E-QUERY-039 Error Cases row updated — `udf_name`→`infusion`, `available_udfs`→`available_infusions`; description extended to cover both pipe-mode `EnrichStage.infusion` AND SQL-mode `ScalarFunc::Unknown(name)` paths; added global-registry scoping note. (2) EC-11-054 updated — `udf_name`/`available_udfs` → `infusion`/`available_infusions`; bound to global `InfusionField.name` set. (3) Test vector updated — same vocabulary fix + global-set note. (4) Related BCs BC-2.11.019 entry updated — new vocabulary and both-AST-paths description. No structural BC semantics changed — this is a consistency-only sweep following BC-2.11.019 v1.1 revision. |
| 1.11 | onboarding-001-C-spec-burst-2026-06-23 | 2026-06-23 | product-owner | S-DEMO-PRISMQL-ONBOARDING-001-C — two additive changes: (1) **E-QUERY-036 `available_tables` enrichment (AUDIT-001):** Added explicit E-QUERY-036 Error Cases row specifying that the structured error response MUST include `available_tables` (comma-separated, org-scoped) — its absence was the root cause of AUDIT-001 misdirection (agent received "check config" instead of the list of valid table names). Added EC-11-053 edge case and test vector. (2) **E-QUERY-039 cross-reference (BC-2.11.019):** Added E-QUERY-039 Error Cases row (enrich-UDF-not-found plan-time gate; full contract in new BC-2.11.019), EC-11-054 edge case, test vector, and BC-2.11.019 to Related BCs. No existing postconditions or invariants modified. |
| 1.10 | ADR-041-v1.1-coherence | 2026-06-19 | product-owner | **ADR-041 v1.1 / S-DEMO-PRISMQL-ONBOARDING-001 cross-reference addition.** Additive coherence micro-edit only — no query semantics changed. Changes: (1) §Postconditions — added `normalized_pql` optional field note (present on success, absent on all errors; full contract in BC-2.11.018). (2) §Error Cases — added `E-QUERY-038` row (column-not-found plan-time gate; full contract in BC-2.11.016); added `E-QUERY-002` row (type error; full contract in BC-2.11.017); added `E-QUERY-003` row (security limits; full contract in BC-2.11.017); annotated `E-QUERY-001` row with ADR-041 v1.1 pedagogical enrichment reference (near_text, reference_pointer; BC-2.11.017); annotated `E-QUERY-037` row with ADR-041 v1.1 pedagogical enrichment reference (prism_describe suggestion field; BC-2.11.017). (3) §Related BCs — new section added with BC-2.11.016/017/018 as "composes with" entries plus pre-existing BC-2.11.006/010 sibling relationships. (4) Frontmatter — `related_bcs` array added; `modified` date updated; version bumped 1.9→1.10. |
| 1.9 | ADR-039-SEC-001 | 2026-06-16 | product-owner | **ADR-039 org-scoped enumeration invariant.** In-place strengthening of the E-QUERY-037 postcondition to close SEC-001 / CWE-200 (cross-tenant information disclosure). Changes: (1) §Postconditions — E-QUERY-037 plan-time availability bullet extended with `filter_to_org_visible` org-scoping clause specifying the three-way dispatch: (a) multi-tenant overlay deployment (`QueryOptions.clients` non-empty AND `resolved_spec_map` populated) → `available_sensors` and `available_tables` filtered to requesting org(s) only via `filter_to_org_visible`; (b) single-tenant or unrestricted query (`clients=None` or `resolved_spec_map=None`) → global registry, backward-compatible; (c) empty org scope → both fields empty. (2) §Error Cases — E-QUERY-037 row extended with explicit invariant: `available_sensors` and `available_tables` MUST NOT enumerate sensors/tables belonging to orgs other than the requesting client's org scope; `did_you_mean` computed only over org-visible table set via `TableRegistry::did_you_mean_for_tables`. (3) §Edge Cases — added EC-11-037 (multi-tenant: only acme's sensors visible when acme queries) and EC-11-038 (single-tenant / clients=None: global registry, unfiltered, backward-compat). (4) §Canonical Test Vectors — added two vectors for EC-11-037 and EC-11-038. No new BC created — ADR-039 explicitly specifies in-place strengthening. Implementer: `check_table_availability` and `check_availability_gate` signature changes specified in ADR-039 §Design Specification; sibling-site sweep required (TD-VSDD-060). S-3.13 traces to this BC; story-writer must propagate BC table and ACs under `bc_array_changes_propagate_to_body_and_acs` policy. |
| 1.8 | S-3.13-pre-TDD | 2026-06-14 | product-owner | S-3.13 pre-TDD error-contract reconciliation. Added plan-time table-availability postcondition: planner validates each `source_ref` against live `TableRegistry` before fan-out; unconfigured-sensor tables return `E-QUERY-037` (new code, error-taxonomy v1.79) with `table`, `sensor`, `available_sensors`, `available_tables`, `did_you_mean` (edit-distance ≤ 3) fields. Added E-QUERY-037 Error Cases row. Replaces the incorrect E-QUERY-001 reference in S-3.13 ACs (E-QUERY-001 = parse error, wrong semantics and wrong fields for a plan-time availability check). Added EC-11-033..036 edge cases covering the unconfigured-sensor, did_you_mean, empty-registry, and mode-agnostic scenarios. Added four canonical test vectors for the new error cases. Story-writer must update S-3.13 body (AC-2/AC-3/AC-8 and error-handling section) to reference E-QUERY-037; BC-INDEX title-column bump noted for state-manager. Architect note (NOT product-owner scope): new `PrismError::TableNotAvailable` variant required in `prism-core/src/error.rs`; edit-distance vs strsim crate decision delegated to architect. |
| 1.7 | mcp-cascade-P2-04 | 2026-06-10 | product-owner | MCP cascade P2-04: interim-status annotation added under the optional-scoping-parameters precondition for the declared-but-unwired params. Shipped `query` tool (implementer-verified 2026-06-10) wires `clients`/`limit`/`force_refresh` only; `sensors`/`sources`/`time_range` have no engine plumbing and are rejected fail-closed with MCP `-32602` INVALID_PARAMS via `deny_unknown_fields` — never silently ignored. Declarations NOT deleted (POL-1 append-only spirit): they remain the normative target contract; production wiring tracked by the query scope-params story per the 2026-06-10 review (story-writer anchor burst in parallel). No postcondition/error-case/edge-case/vector changes; the annotation marks the affected postconditions as target behavior. |
| 1.6 | mcp-cascade-P2-01 | 2026-06-10 | product-owner | MCP cascade P2-01 (architect adjudication, error-taxonomy v1.70): added missing Error Cases row for tool-level `limit` > 1000 rejection — code `E-QUERY-033`, surfaced as MCP `-32602` INVALID_PARAMS via `map_prism_error`, Message Format verbatim `"E-QUERY-033: limit {requested} exceeds maximum of {max} (BC-2.11.001)"`. The condition was prose-only in Preconditions/Postconditions ("max 1000") while code (`PrismError::QueryLimitExceeded`) + 9 tests enforce it; the row closes the BC↔taxonomy gap. Default-25/max-1000 semantics unchanged (cross-referenced in the row). No precondition/postcondition/edge-case changes. |
| 1.5 | ADR-038-D6-sweep | 2026-06-10 | product-owner | ADR-038 D6 number sweep: Error Cases row "No matching clients/sensors found" code `E-CFG-001` → `E-CFG-100` (ClientNotFound). The BC froze the pre-v1.8 number; error-taxonomy v1.8 renumbered the client-not-found condition to E-CFG-100, and ADR-037 tombstoned the low number for the retired customer-config semantics. Condition text and response shape unchanged. Per ADR-038 (error-taxonomy v1.66). |
| 1.4 | D-987-POL-14 | 2026-06-04 | state-manager | **[reconstructed-tombstone]** POL-14 auto-promotion: `status:` field synced draft→active when anchor story S-DEMO-002 merged PR #171 develop@fdd12251 2026-06-04. `lifecycle_status` was already `active` prior to this row; only the legacy `status:` field was synced. No contract content change. Recorded in BC-INDEX v5.79. |
| 1.3 | pass-73-fix | 2026-04-20 | state-manager | Deterministic changelog reorder: sorted all rows to descending version order (pass-73 bash script). |
| 1.2 | pass-69-housekeeping | 2026-04-20 | product-owner | Normalized changelog schema to canonical 5-col schema. |
| 1.1 | pre-build-sweep | 2026-04-20 | product-owner | Template-compliance sweep: added extracted_from/inputs/input-hash/traces_to frontmatter; added ## Description synthesized from body; added ## Canonical Test Vectors scaffolding; added ## Verification Properties cross-ref; added ## Changelog. |
| 1.0 | cycle-1 | 2026-04-14 | product-owner | Initial contract |
