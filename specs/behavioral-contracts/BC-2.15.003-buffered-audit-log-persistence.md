---
document_type: behavioral-contract
level: L3
version: "1.4"
status: draft
producer: product-owner
timestamp: 2026-04-13T12:00:00
phase: 1a
origin: greenfield
subsystem: "SS-15"
capability: "CAP-025"
lifecycle_status: active
introduced: cycle-1
modified: null
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
inputs:
  - ".factory/specs/prd.md"
  - ".factory/specs/domain-spec/capabilities.md"
input-hash: "572c2a9"
traces_to:
  - "CAP-025"
extracted_from: ".factory/specs/prd.md"
---

# BC-2.15.003: Buffered Audit Log Persistence — Write to RocksDB Before stderr/Vector, Exponential Backoff on Forward Failure

## Description

Every audit entry is first durably written to the RocksDB `audit_buffer` column family
before any forwarding attempt. This persist-then-forward ordering guarantees no audit
entry is lost even if Vector is unreachable or stderr fails. Forwarding to stderr and
the configured Vector endpoint runs asynchronously in a background task; the calling
tool does not wait for forwarding to complete.

Entries that fail forwarding are retried with exponential backoff (1s initial, 2x
multiplier, 60s cap, 10 retries). Entries remaining in the buffer after Prism restarts
are re-forwarded automatically on the next startup. Overflow handling is defined
separately in BC-2.15.004.

## Preconditions
- An audit entry has been generated by any MCP tool invocation or internal operation (BC-2.05.001)
- The RocksDB `audit_buffer` column family is initialized (BC-2.15.001)

## Postconditions
- The audit entry is first persisted to the RocksDB `audit_buffer` column family with a time-ordered key: `audit:{timestamp_nanos}:{uuid}`
- After persistence to RocksDB, the entry is forwarded to two sinks in parallel:
  1. **stderr** (structured JSON via `tracing` crate) -- for local consumption and Vector pipeline pickup
  2. **Direct Vector sink** (if configured) -- TCP/HTTP forward to a Vector endpoint
- If stderr write fails: entry remains in RocksDB buffer; warning logged to an internal channel
- If Vector forward fails: exponential backoff retry with parameters:
  - Initial delay: 1 second
  - Max delay: 60 seconds
  - Backoff multiplier: 2x
  - Max retries: 10 (then entry is marked as "forward_failed" in buffer)
- Successfully forwarded entries are deleted from the RocksDB buffer
- On startup, any entries remaining in the `audit_buffer` (from previous crashes or forward failures) are re-forwarded using the same retry logic

## Invariants
- Persist-then-forward ordering: every audit entry is in RocksDB before any forwarding attempt
- No audit entry is lost: even if Vector is unreachable and stderr fails, entries survive in RocksDB
- Forwarding does not block the calling tool; the buffer forwarder runs as a background task

## Error Conditions
| Error | Condition | Behavior |
|-------|-----------|----------|
| `E-AUDIT-001` | RocksDB write failure for audit entry | For write operations: tool invocation fails (fail-closed per BC-2.05.001). For read operations: entry logged to stderr only (best-effort) |
| `E-AUDIT-002` | Vector endpoint unreachable | Exponential backoff retry; entries accumulate in buffer |
| `E-AUDIT-003` | Buffer exceeds 100K entries | Overflow handling per BC-2.15.004 |

## Edge Cases
| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-15-041 | Vector endpoint goes down for 30 minutes | Entries accumulate in RocksDB; when Vector recovers, all buffered entries are forwarded in order |
| EC-15-012 | Server crash with 500 buffered entries | On restart, all 500 entries are re-forwarded |
| EC-15-013 | No Vector endpoint configured | Entries forwarded to stderr only; deleted from buffer after stderr write |

## Canonical Test Vectors

See `.factory/specs/prd-supplements/test-vectors.md` for full canonical vectors.

| Scenario | Input | Expected Output |
|----------|-------|-----------------|
| Happy path — Vector reachable | audit entry generated | Entry in RocksDB; forwarded to stderr + Vector; deleted from buffer after success |
| Vector unreachable | audit entry generated; Vector down | Entry in RocksDB; retry with backoff; entry persists until Vector recovers |
| Startup replay | 500 buffered entries from crash | All 500 forwarded in order on next startup |
| Write operation + RocksDB fail | audit write to RocksDB fails | Tool invocation aborted (fail-closed) |
| No Vector config | only stderr configured | Entry forwarded to stderr; deleted from buffer |

## Verification Properties

| VP ID | Description |
|-------|-------------|
| VP-033 | Audit buffer: RocksDB write completes before delivery attempt |

## Traceability
| Field | Value |
|-------|-------|
| L2 Capability | CAP-025 |
| L2 Invariants | DI-004 |
| Priority | P0 |

## Changelog

| Version | Burst | Date | Author | Change |
|---------|-------|------|--------|--------|
| 1.4 | pass-87-remediation | 2026-04-21 | architect | F87-005: replaced (none) placeholder VP row with proper VP-033 row per VP-INDEX canonical. |
| 1.3 | pass-74-fix | 2026-04-20 | product-owner | Resolved (placeholder) row in ## Verification Properties per pass-74 VP-TBD decision matrix extension. |
| 1.2 | pass-73-fix | 2026-04-20 | state-manager | Deterministic changelog reorder: sorted all rows to descending version order (pass-73 bash script). |
| 1.1 | pre-build-sweep | 2026-04-20 | product-owner | Template-compliance sweep: added extracted_from/inputs/input-hash/traces_to frontmatter; added ## Description synthesized from body; added ## Canonical Test Vectors scaffolding; added ## Verification Properties cross-ref; renamed Error Cases → Error Conditions; added ## Changelog. |
| 1.0 | cycle-1 | 2026-04-13 | product-owner | Initial draft |
