---
document_type: behavioral-contract
level: L3
version: "1.45"
status: active
producer: product-owner
timestamp: 2026-04-14T07:00:00
phase: 1a
origin: greenfield
subsystem: "SS-11"
capability: "CAP-015"
lifecycle_status: active
introduced: cycle-1
modified: 2026-07-14
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
inputs: [".factory/specs/prd.md", ".factory/specs/domain-spec/capabilities.md", ".factory/specs/architecture/decisions/ADR-052-prismql-native-temporal-typing-utf8-to-arrow-timestamp.md", ".factory/specs/architecture/decisions/ADR-047-prismql-case-sensitivity-policy-ieq-iin-and-adapter-boundary-normalization.md", ".factory/specs/architecture/decisions/ADR-048-prismql-having-predicate-grammar-divergence-aggregate-fn-predicate-lhs.md"]
input-hash: "c36ec87"
traces_to: ["CAP-015"]
extracted_from: ".factory/specs/prd.md"
---

# BC-2.11.004: PrismQL Pipe Mode Parsing

## Description

Pipe mode is the highest-priority query mode: it activates whenever the query contains a `|` operator outside string literals, overriding SQL or filter mode detection. The Chumsky parser produces a `PipeExpr` AST representing a linear chain of transformation stages applied left-to-right. Each stage (`where`, `sort`, `head`, `tail`, `stats`, `dedup`, `fields`) is translated to a DataFusion `DataFrame` API call in sequence. The 32-stage limit and security limits from DI-019 apply. Pipe mode is the recommended mode for multi-step analysis workflows.

S-3.06 extends pipe mode with write stages: a write stage (sensor-registered verb in terminal pipe position) routes to the write-parser extension. Write verb matching is case-insensitive (see §Write Verb Case Sensitivity below). Unbounded DML writes (DELETE/UPDATE without WHERE) are rejected with `E-QUERY-022` before any API call is made. When `WriteVerbRegistry` is empty, `reject_write_verbs_in_filter` always returns `Ok(())` — no false positives on read-only installations.

## Preconditions
- A query string contains `|` outside of string literals -- pipe mode has highest precedence in mode auto-detection (see BC-2.11.002 for full precedence rules). Pipe mode wins even if the query also starts with `SELECT` or `FROM`.
- The query string has passed the 64KB length check

## Postconditions
- The Chumsky parser produces a `PipeExpr` AST consisting of an optional source followed by a chain of pipe stages
- Supported pipe stages:
  - `where <filter_expr>` -- filter rows using the same filter grammar as filter mode (ADR-046 D7 shared-predicate-grammar invariant, BC-2.11.023). This includes the `IEQ`/`IIN`/`INE` case-insensitive operators added by ADR-047 D.2 (see BC-2.11.024): `| where severity IEQ 'high'` is valid pipe-mode syntax lowered to `lower(severity) = lower('high')` in the DataFusion SQL plan.
  - `sort <field> [asc|desc] [, <field> [asc|desc]]` -- sort results
  - `head <N>` -- take first N rows (equivalent to `LIMIT N`)
  - `tail <N>` -- take last N rows (reverse sort + limit + reverse)
  - `stats <agg_func> [by <field>]` -- aggregation (count, sum, avg, min, max) with optional GROUP BY
  - `dedup <field> [, <field>]` -- deduplicate by specified fields
  - `fields [+|-] <field> [, <field>]` -- include (`+`) or exclude (`-`) specific fields from output
- Each pipe stage is translated to a DataFusion `DataFrame` API call in sequence
- Pipe stages are applied left-to-right (first stage operates on the full dataset, each subsequent stage operates on the previous stage's output)
- **ADR-052 D2 — Datetime column Arrow type:** `ColumnType::Datetime` sensor columns are registered in the DataFusion execution schema as `DataType::Timestamp(Microsecond, Some("UTC"))` (not `DataType::Utf8`). Temporal predicates in `| where` stages such as `| where timestamp > arrow_cast('2026-06-24T00:00:00Z', 'Timestamp(Microsecond, Some("UTC"))')` compare typed Timestamp values against Timestamp columns. The PrismQL parser accepts the 7 canonical offset-less date/datetime forms in the `is_date_like` Acceptance Set (full set defined in BC-2.11.021 §Error Cases and ADR-052 §D4; representative examples: `'2026-06-24'`, `'2026-06-24T12:00:00'`) as `Literal::RawTemporalLiteral` AST nodes (parse succeeds — no E-QUERY-001 at parse time). The plan-time AST walker `check_temporal_literals` resolves each `RawTemporalLiteral` in `| where` clauses with a four-arm dispatch: (1) vs `Timestamp(Microsecond, UTC)` (Datetime) column (bare `Field` LHS) → **E-QUERY-041**; (2) vs String/Utf8 column (bare `Field` LHS) → **COERCE** to `Literal::String(s)` and compare as ordinary string literal (SUCCESS — no error; byte-identical to pre-ADR-052 behavior; see Error Cases); (3) vs Integer/Float/Bool column (bare `Field` LHS) → **E-QUERY-002 (QueryTypeMismatch)**; (4) in `| where` comparison where LHS is a function call (post-DEFECT-PQL-FNCALL-LHS-001 grammar extension: predicate grammar (`build_predicate_parser`, shared by six callers) now produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: RawTemporalLiteral, ... }` for fn-call LHS inputs; parse SUCCEEDS; prior to fix, arm (4) was defense-in-depth only — grammar rejected fn-call LHS at parse time with generic E-QUERY-001) or compound expression (non-`Field`) → **E-QUERY-042** (`TemporalLiteralInvalidPosition`, NonColumnLhsComparison — LHS type cannot be resolved at plan time; silent coercion would reintroduce RISK-1 for datetime-valued expressions). **SHARED-PARSER SCOPE (F-PQLFN-P1-MED-001):** `build_predicate_parser` is shared by six callers — filter-mode root predicate, pipe `| where` stage, SqlPipe pipe-stage `| where`, SQL WHERE, SQL HAVING, and SQL DML WHERE (via `build_delete_parser`/`build_update_parser`, ADR-048 v1.6 OD-6 §D.7.5). The `fn_call_comparison` production therefore extends fn-call LHS grammar to ALL six Predicate-typed query positions (filter mode, pipe `| where` incl. SqlPipe, SQL WHERE, SQL HAVING, SQL DML WHERE (via `build_delete_parser`/`build_update_parser`, ADR-048 v1.6 OD-6), and INSERT source_select WHERE (via `build_insert_parser` → `build_sql_parser` → `build_sql_predicate_parser`, ADR-048 v1.13 OD-7 §D.7.6)) — not pipe-only. BC-2.11.003 EC-11-003-007 is the authoritative EC for the SQL WHERE position (same E-QUERY-042 NonColumnLhsComparison arm). Aggregate fn-calls remain invalid in pipe `| where` (HAVING path per ADR-048 D.3 is SQL-mode only). **Aggregate-gate enforcement mechanism (ADR-048 v1.13 §D.7, v1.33):** former parser-level `AGGREGATE_FUNC_NAMES` `try_map` guard in `fn_call_comparison` is REMOVED (ADR-048 §D.7.2) — message was swallowed by Chumsky backtrack; post-v1.2 aggregate fn-call names parse via `fn_call_comparison` as `FuncCall::Scalar(Unknown(name))` (parse SUCCEEDS); `DATAFUSION_BUILTIN_AGGREGATE_NAMES` gate in `check_enrich_udf_availability` is the SOLE enforcement point across all seven predicate positions; empty-arg forms (`count() = 5`) take the same path; HAVING exempt (ADR-048 §D.7.1). See EC-11-013. **Explicit scope limits (LOW-001, LOW-002, LOW-004, LOW-005):** (LOW-001) `fn_call_arg` in `fn_call_comparison` admits `literal | field_path` only; nested fn-calls in args (e.g., `upper(trim(col))`) are NOT admitted in this version — E-QUERY-001 at parse time; candidate follow-up feature if analyst use cases emerge. (LOW-002) `fn_call_comparison` is the SOLE production admitting fn-call LHS; it wires `compare_op` (`=`, `==`, `!=`, `<`, `>`, `<=`, `>=`; `==` is an alias of `=`, both map to `CompareOp::Eq`) only — all other predicate-operator productions require `field_path` on the LHS and reject fn-call LHS with E-QUERY-001 at parse time: `regex_match` (`=~`/`MATCHES`), `cidr_match` (`IN CIDR`), `not_in_list` (`NOT IN`), `iin_list` (`IIN` — ADR-047), `in_list` (`IN`), `between` (`BETWEEN`), `is_null` (`IS NULL`/`IS NOT NULL`), `has_check` (`HAS`), `missing_check` (`MISSING`), `string_op_match` (`CONTAINS`/`STARTSWITH`/`ENDSWITH`/`ICONTAINS`/`ISTARTSWITH`/`IENDSWITH`), `cidr_bare` (`CIDR`), `like_match` (`LIKE`), `ieq_compare` (`IEQ`), `ine_compare` (`INE`); e.g., `| where lower(x) LIKE '%foo%'` → E-QUERY-001 at parse time (generic parse failure; no scope-limit citation in the message). (LOW-004) fn-name identifiers in `fn_call_comparison` must begin with an ASCII alphabetic character or `_`; digit-leading names (e.g., `123abc(x)`) produce E-QUERY-001 at parse time (ADR-048 v1.13 §D.7.2). (LOW-005) fn-call on RHS is not admitted — `rhs_expr` in `fn_call_comparison` and `field_comparison` accepts `temporal_rhs | literal` only; fn-call expressions are not a valid RHS alternative; `| where x = upper(y)` and `| where lower(x) = upper(y)` both fail at parse time with E-QUERY-001 (generic parse failure; no scope-limit citation). (LOW-006) fn-call names in `fn_call_comparison` must not match PrismQL predicate-level reserved keywords (case-insensitive exclusion): `NOT`, `AND`, `OR`, `IN`, `IIN`, `IEQ`, `INE`, `IS`, `BETWEEN`, `LIKE`, `CIDR`, `MATCHES`, `HAS`, `MISSING`, `CONTAINS`, `ICONTAINS`, `STARTSWITH`, `ISTARTSWITH`, `ENDSWITH`, `IENDSWITH` — keyword-matching fn-names produce E-QUERY-001 at parse time (message: `"E-QUERY-001: '{name}' is a PrismQL keyword and cannot be used as a function name"`); e.g., `| where NOT(x) = 5` (typo for `NOT (x = 5)`) → E-QUERY-001 at parse time. Rationale: these names are structurally unreachable as UDF names in predicate position; admitting them turns pre-branch parse-time errors for analyst typos into silent fn-call parses that fail late with unhelpful execution errors (DataFusion `"Invalid function"`) on no-registry installations. **Implementer directive (LOW-006): add a keyword exclusion check (case-insensitive `eq_ignore_ascii_case` against the 20-keyword list above) immediately after the identifier-slice capture in `fn_call_comparison` in `build_predicate_parser` (`filter_parser.rs`); return E-QUERY-001 citing the keyword name.** (LOW-007) `Expr::Star` (`*`) is not admissible as a fn-call argument in predicate position — `| where count(*) = 5` fails closed at parse time with a generic E-QUERY-001 (`fn_call_arg` in `fn_call_comparison` admits `literal | field_path` only; `*` is neither); the aggregate gate's canonical HAVING-redirect message (ADR-048 §D.7.2) applies only to parseable forms like `count() = 5` / `count(col) = 5`; extending `fn_call_arg` to admit `*` (for canonical-message UX parity with `count()`) is a possible future enhancement requiring architect adjudication. All six limits documented explicitly (not silent gaps); surface in PR body for human feature-decision. **Note — `stats by` and `sort` positions (pipe-mode parse-time rejection):** Bare date-like literals in `stats <agg_func> by '2026-06-24'` and `sort '2026-06-24'` positions are rejected AT PARSE TIME with E-QUERY-001 (enhanced message per ADR-052 §D4 v1.10 Option (a): "pipe stats by expects column references (field names), not literal values") — the `check_temporal_literals` walker is never invoked for these positions; they are not walker-level arms. The `chrono::DateTime::parse_from_rfc3339` strictness is preserved at the sensor-boundary datetime parsing path (AC-013) — both the AST-walk plan-time gate and the sensor-boundary parser reject non-RFC-3339 datetime forms; mechanisms differ but the contract is preserved.

### Write Parser Extension (S-3.06)

- A write stage is recognized in terminal pipe position: `| <verb> <target> [args]` where `<verb>` is a sensor-registered write verb from `WriteVerbRegistry`
- Write stages are ONLY valid in terminal position (the last stage of a pipe); a write stage in non-terminal position returns `E-QUERY-024`
- Non-terminal pipe stages that contain write verbs are rejected with `E-QUERY-024` before DataFusion planning

#### Write Verb Case Sensitivity (Gap a)

Write verb matching is **case-insensitive**. `WriteVerbRegistry` normalizes verb names to lowercase on insert (via `from_source`) and on lookup (via `is_write_verb`). The parser calls `is_write_verb` on the lowercased form of the identifier seen in the query. This is consistent with PrismQL's SQL-style identifier conventions (SQL keywords are case-insensitive) and aligns with the SQL/filter parser convention established in S-3.01.

Examples:
- `| ISOLATE crowdstrike_hosts id = "abc"` — recognized (normalized to `isolate`)
- `| Isolate crowdstrike_hosts id = "abc"` — recognized (normalized to `isolate`)
- `| isolate crowdstrike_hosts id = "abc"` — canonical form

#### Unbounded Write Protection (Gap b)

DML write operations (DELETE, UPDATE, INSERT INTO ... SELECT) without a WHERE clause are rejected at parse time with `E-QUERY-022`. This prevents accidental mass-modification of sensor-managed data.

- `DELETE FROM <table>` (no WHERE) → `E-QUERY-022`
- `UPDATE <table> SET <col>=<val>` (no WHERE) → `E-QUERY-022`
- `INSERT INTO <table> SELECT ...` without LIMIT or WHERE on source SELECT → `E-QUERY-022`

The `--all` opt-in flag (or equivalent explicit bypass mechanism) may be defined per-sensor-spec to allow intentional unbounded operations — the BC requires that unbounded writes MUST be explicit, never the default.

#### Empty WriteVerbRegistry Behavior (Gap c)

When `WriteVerbRegistry` is empty (no sensor write endpoints registered), `reject_write_verbs_in_filter` always returns `Ok(())`. No false-positive errors are raised on read-only Prism installations or during test scenarios where write specs are not loaded. An empty registry means no write verbs exist to reject — filter parsing proceeds normally.

## Invariants
- DI-019: Maximum 32 pipe stages enforced; exceeding returns structured error
- DI-019: Nesting depth within `where` expressions tracked against the 64 depth limit
- Each pipe stage produces a valid intermediate `DataFrame`; stage ordering errors (e.g., `head` before `stats`) are not compile errors -- DataFusion evaluates them in order
- **BC-2.11.004 §INV-FILTER-EMPTY-REGISTRY:** When `WriteVerbRegistry` is empty, `reject_write_verbs_in_filter` always returns `Ok(())`. No false positives on read-only installations or test environments without write verb specs loaded.
- **BC-2.11.004 §INV-WRITE-VERB-CASE-INSENSITIVE:** Write verb names are normalized to lowercase at registry-insert time and at lookup time. The parser never performs case-sensitive verb matching. `"ISOLATE"` and `"isolate"` are identical in the write parser.
- **BC-2.11.004 §INV-UNBOUNDED-WRITE-REJECTED:** DML operations without a WHERE clause (DELETE, UPDATE) or an INSERT INTO SELECT without LIMIT or WHERE are rejected at parse time before any API call. The rejection is not bypassable without an explicit opt-in mechanism defined in the sensor spec.

## Error Cases
| Error | Condition | Behavior |
|-------|-----------|----------|
| `E-QUERY-001` | Unknown pipe stage keyword | Error with list of supported stages |
| `E-QUERY-003` | More than 32 pipe stages | Structured error: "Query has N pipe stages (max 32). Simplify the query pipeline." |
| `E-QUERY-001` | `stats` with invalid aggregation function | Error listing supported aggregation functions |
| `E-QUERY-001` | `head` or `tail` with non-integer argument | Error: "head/tail requires a positive integer argument" |
| `E-QUERY-010` | Write stage targets an internal Prism table (`prism_audit`, `prism_metrics`, etc.) | Parse-time rejection: `"E-QUERY-010: write to internal table '{table}' is not permitted"` |
| `E-QUERY-022` | Unbounded DML write: DELETE or UPDATE without WHERE clause, or INSERT INTO SELECT without LIMIT or WHERE | `"E-QUERY-022: unbounded {verb} rejected — add a WHERE clause (or LIMIT for INSERT...SELECT) to scope the operation, or use explicit opt-in if provided by the sensor spec"` — rejected at parse time before any API call |
| `E-QUERY-023` | Unknown write verb in terminal pipe position | `"E-QUERY-023: unknown write verb '{verb}' for sensor '{sensor}'"` — includes suggestion list of available verbs from `WriteVerbRegistry::verbs_for_sensor` |
| `E-QUERY-024` | Write stage in non-terminal pipe position | `"E-QUERY-024: write stage must be in terminal pipe position"` — write stages cannot be followed by additional stages |
| `E-QUERY-038` | Column referenced in a pipe-mode stage position not found in the `TableRegistry` schema for the table and client (or not in the running derived-column binding context). Covered positions: (8) `\| where` stage predicate (every `PipeStage::Where(Predicate)`, `Predicate` tree walked recursively), (10) `\| sort` field key references (`PipeStage::Sort` sort-key list), (11) `\| stats ... by` grouping field references AND aggregate function argument field paths (`PipeStage::Stats(StatsStage)` `by_fields: Vec<FieldPath>` grouping list AND aggregate func args such as `sum(col)`, `count(col)` — checked before Stats replaces binding context), (12) `\| fields` column refs (`PipeStage::Fields` inclusion/exclusion list), (13) `\| enrich` input column (`PipeStage::Enrich(EnrichStage)` — `EnrichStage.field: FieldPath` — checked before Enrich updates binding context), (14) `\| dedup` field key references (`PipeStage::Dedup(Vec<FieldPath>)` dedup-key list). For SqlPipe (`Ast::SqlPipe`), head SQL positions 1–6 are also gated (see BC-2.11.003); `\| where` stages are position 9 (same walking as position 8). **Derived-column binding rule:** the gate maintains a running `{ available, suspended }` context — Enrich stages add infusion output columns when resolvable (fail-open otherwise with `suspended = true`); Stats stages replace `available` with `{explicit_aliases ∪ by_fields}`; once suspended all remaining stages skip E-QUERY-038. False-positive prohibition (FP-001): when binding provenance is uncertain the gate fails open. Full spec: BC-2.11.016 v1.25 §Preconditions DERIVED-COLUMN BINDING RULE. | `E-QUERY-038` with `column`, `available_columns` (always present, org-scoped), `did_you_mean` (Levenshtein ≤ 3); MCP `-32602 INVALID_PARAMS`. Full spec: BC-2.11.016 v1.25. |
| `E-QUERY-041` | A `Timestamp(Microsecond, UTC)` datetime column compared against a date-like string literal in a `| where` stage (bare `Field` LHS). The PrismQL parser emits `Literal::RawTemporalLiteral` for any of the 7 canonical offset-less date/datetime forms in the `is_date_like` Acceptance Set (see BC-2.11.021 §Error Cases / ADR-052 §D4; e.g., `'2026-06-24'`, `'2026-06-24T12:00:00'`) (parse succeeds); the plan-time AST walker `check_temporal_literals` raises E-QUERY-041 upon resolving the column type as `Timestamp(Microsecond, UTC)`. **Does NOT fire for String/Utf8 columns** — `check_temporal_literals` coerces `RawTemporalLiteral` to `Literal::String(s)` for String columns in `| where` comparisons (SUCCESS, no error; `\| where string_col = '2026-06-24'` is valid — arm (2) of four-arm dispatch). **Does NOT fire for `stats by` or `sort` positions** — those positions reject at parse time with E-QUERY-001 (enhanced message per Option (a)); `check_temporal_literals` walker is never reached. **Does produce E-QUERY-042** for non-`Field` LHS comparisons in `| where` (arm (4) — see E-QUERY-042 row). | `"E-QUERY-041: The value '{first_50_chars}' cannot be interpreted as a UTC timestamp. Expected RFC-3339 format with UTC offset (e.g., '2026-07-03T00:00:00Z'). Date-only and offset-less forms are not accepted. For relative time filters, use NOW() - INTERVAL 'Nh' (e.g., WHERE timestamp > NOW() - INTERVAL '24h')."` |
| `E-QUERY-042` | A date-like string literal in a Predicate comparison where the LHS is a function call or compound expression (non-`Field`). Post-DEFECT-PQL-FNCALL-LHS-001 grammar extension (architect Option A): predicate grammar (`build_predicate_parser`) now produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: Literal::RawTemporalLiteral, ... }` for fn-call LHS inputs (parse SUCCEEDS); prior to fix, arm (4) was defense-in-depth only — grammar rejected fn-call LHS at parse time with generic E-QUERY-001 (`-32000 INTERNAL_ERROR` to callers); that was the DEFECT. The plan-time AST walker `check_temporal_literals` detects non-`Field` LHS → `TemporalLiteralInvalidPosition { position: NonColumnLhsComparison, value_prefix: <first_50_chars> }`. LHS type unresolvable at plan time; silent coercion would reintroduce RISK-1 for datetime-valued expressions. Example (pipe `\| where`): `\| where lower(hostname) = '2026-06-24'`; for the SQL WHERE position see BC-2.11.003 EC-11-003-007. **Does NOT fire** when RHS is not a date-like literal (e.g., `\| where lower(device_id) = 'active'` — no `RawTemporalLiteral` emitted; passes to DataFusion). **Does NOT fire** for `stats by` or `sort` literals — those are parse-time rejections (E-QUERY-001, not this error). **SHARED-PARSER SCOPE (F-PQLFN-P1-MED-001):** `build_predicate_parser` is shared by six callers; arm (4) fires in filter-mode root predicate, pipe `\| where`, SqlPipe `\| where`, SQL WHERE (BC-2.11.003 EC-11-003-007), SQL HAVING, SQL DML WHERE (via `build_delete_parser`/`build_update_parser`, ADR-048 v1.6 OD-6 §D.7.5), and INSERT source_select WHERE (via `build_insert_parser` → `build_sql_parser` → `build_sql_predicate_parser`, ADR-048 v1.13 OD-7 §D.7.6) positions. **Scope limits (LOW-001, LOW-002, LOW-004, LOW-005, LOW-006, LOW-007):** (LOW-001) `fn_call_arg` admits `literal \| field_path` only; nested fn-calls in args (e.g., `upper(trim(col))`) produce E-QUERY-001 at parse time. (LOW-002) `fn_call_comparison` is the SOLE production admitting fn-call LHS; it wires `compare_op` (`=`, `==`, `!=`, `<`, `>`, `<=`, `>=`; `==` is an alias of `=`, both map to `CompareOp::Eq`) only — all other predicate-operator productions require `field_path` on the LHS and reject fn-call LHS with E-QUERY-001 at parse time: `regex_match` (`=~`/`MATCHES`), `cidr_match` (`IN CIDR`), `not_in_list` (`NOT IN`), `iin_list` (`IIN` — ADR-047), `in_list` (`IN`), `between` (`BETWEEN`), `is_null` (`IS NULL`/`IS NOT NULL`), `has_check` (`HAS`), `missing_check` (`MISSING`), `string_op_match` (`CONTAINS`/`STARTSWITH`/`ENDSWITH`/`ICONTAINS`/`ISTARTSWITH`/`IENDSWITH`), `cidr_bare` (`CIDR`), `like_match` (`LIKE`), `ieq_compare` (`IEQ`), `ine_compare` (`INE`); e.g., `\| where lower(x) LIKE '%foo%'` → E-QUERY-001 at parse time. (LOW-004) fn-name identifiers in `fn_call_comparison` must begin with an ASCII alphabetic character or `_`; digit-leading names (e.g., `123abc(x)`) produce E-QUERY-001 at parse time (ADR-048 v1.13 §D.7.2). (LOW-005) fn-call on RHS is not admitted — `rhs_expr` accepts `temporal_rhs \| literal` only; fn-call expressions are not a valid RHS alternative; `\| where x = upper(y)` and `\| where lower(x) = upper(y)` both fail at parse time with E-QUERY-001 (generic parse failure). (LOW-006) fn-call names in `fn_call_comparison` must not match PrismQL predicate-level reserved keywords (case-insensitive): `NOT`, `AND`, `OR`, `IN`, `IIN`, `IEQ`, `INE`, `IS`, `BETWEEN`, `LIKE`, `CIDR`, `MATCHES`, `HAS`, `MISSING`, `CONTAINS`, `ICONTAINS`, `STARTSWITH`, `ISTARTSWITH`, `ENDSWITH`, `IENDSWITH` — keyword-matching fn-names produce E-QUERY-001 at parse time; e.g., `\| where NOT(x) = 5` (typo for `NOT (x = 5)`) → E-QUERY-001 (reserved keyword). (LOW-007) `Expr::Star` (`*`) is not admissible as a fn-call argument in predicate position; `\| where count(*) = 5` fails at parse time with a generic E-QUERY-001 before the aggregate gate is reached. All six limits are documented scope limits (not silent gaps); surface in PR body for human feature-decision. | `"E-QUERY-042: A date-like literal compared against a computed expression cannot be type-checked at plan time. Compare against a bare datetime column using RFC-3339 (e.g., '2026-07-03T00:00:00Z'), against a string column using a non-date-shaped value, or wrap the expression in an explicit CAST."` |

## Edge Cases
| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-11-009 | Pipe mode with no source prefix (starts with `| where ...`) | Valid; operates on the full materialized events table |
| EC-11-010 | `head 0` | Returns empty result set (valid but unusual) |
| EC-11-011 | `dedup` on a field with all unique values | Returns all rows (no deduplication occurs) |
| EC-11-012 | Multiple `where` stages in sequence | Valid; equivalent to AND-ing the conditions. Each `where` narrows the previous result. |
| EC-11-004-001 | `| where timestamp > '2026-06-24'` (date-only bare string literal in datetime `| where` predicate against a Datetime/Timestamp column) | `Err(E-QUERY-041)`: PrismQL parser emits `Literal::RawTemporalLiteral("2026-06-24")` (parse succeeds); plan-time walker `check_temporal_literals` resolves `timestamp` as `Timestamp(Microsecond, UTC)` → E-QUERY-041. Use full RFC-3339 UTC form `'2026-06-24T00:00:00Z'` or `NOW() - INTERVAL 'Nh'` |
| EC-11-004-002 | `| where timestamp > '2026-06-24T00:00:00Z'` (valid RFC-3339 UTC string literal in datetime `| where` predicate) | Valid; full RFC-3339 form parses directly to `Literal::Timestamp` (not `RawTemporalLiteral`) — `check_temporal_literals` does not intercept it; emitted as `arrow_cast('2026-06-24T00:00:00Z', 'Timestamp(Microsecond, Some("UTC"))')` — proceeds to DataFusion without error |
| EC-11-004-003 | `| where report_date = '2026-06-24'` where `report_date` is a String/Utf8 column (not a Datetime column) | Valid — PrismQL parser emits `Literal::RawTemporalLiteral("2026-06-24")`; `check_temporal_literals` resolves `report_date` as `DataType::Utf8` (String column) → COERCE to `Literal::String("2026-06-24")`. Processed as a normal string comparison in the `| where` stage. No E-QUERY-041 emitted. Byte-identical to pre-ADR-052 behavior. |
| EC-11-004-004 | `FROM crowdstrike_detections \| stats count by '2026-06-24'` (temporal literal as GROUP BY literal in `stats` stage) | **FORBIDDEN — parse-time rejection.** The PrismQL parser detects a bare literal in the `stats by` position and rejects immediately with E-QUERY-001 (enhanced message per ADR-052 §D4 v1.10 Option (a)): `"E-QUERY-001: pipe stats by expects column references (field names), not literal values — '2026-06-24' looks like a date-shaped literal, not a column name. Grouping by a literal constant has no effect. Did you mean to reference a column, or to add a \| where filter before the stats stage?"`. The `check_temporal_literals` walker is never invoked. **F-MED-1 correction: prior v1.11 description was wrong** — this case does NOT coerce to success; it is a parse-time E-QUERY-001. |
| EC-11-004-005 | `FROM crowdstrike_detections \| where lower(hostname) = '2026-06-24'` (fn-call LHS in `| where` comparison with date-like RHS string literal) | `Err(E-QUERY-042)` NonColumnLhsComparison — post-DEFECT-PQL-FNCALL-LHS-001 grammar extension (architect Option A): predicate grammar (`build_predicate_parser`) produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: Literal::RawTemporalLiteral("2026-06-24"), ... }`; parse SUCCEEDS; `check_temporal_literals` arm (4) fires at plan time → E-QUERY-042 `TemporalLiteralInvalidPosition { position: NonColumnLhsComparison, value_prefix: "2026-06-24" }`. **Prior defect (DEFECT-PQL-FNCALL-LHS-001):** arm (4) was defense-in-depth only; grammar previously rejected fn-call LHS inputs at parse time with generic E-QUERY-001 (appeared as `-32000 INTERNAL_ERROR` to callers). Grammar extension (Option A) fixes the parse path; plan-time arm (4) is now the authoritative E-QUERY-042 gate. **SHARED-PARSER SCOPE (F-PQLFN-P1-MED-001):** `build_predicate_parser` is shared by six callers; this EC covers the pipe `\| where` position. For the SQL WHERE position, see BC-2.11.003 EC-11-003-007 (same E-QUERY-042 NonColumnLhsComparison arm, same mechanism). Aggregate fn-calls remain invalid in pipe `\| where` (HAVING path per ADR-048 D.3 is SQL-mode only). INSERT source_select WHERE is an additional predicate position where this arm fires (via `build_insert_parser` → `build_sql_parser` → `build_sql_predicate_parser`, ADR-048 v1.13 OD-7 §D.7.6). |
| EC-11-004-006 | `FROM crowdstrike_detections \| where lower(device_id) = 'active'` (fn-call LHS in `| where` with NON-date-like RHS string literal, post-DEFECT-PQL-FNCALL-LHS-001 grammar extension) | Valid — grammar produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: Literal::String("active"), ... }`; `check_temporal_literals` does NOT intercept (RHS `"active"` is not in the `is_date_like` Acceptance Set — no `RawTemporalLiteral` emitted); predicate passes to DataFusion execution as-is. **E-QUERY-038 interaction:** fn-call arguments are walked by `collect_predicate_columns` FuncCall arm — `lower(nonexistent_col) = 'active'` → E-QUERY-038 (column `nonexistent_col` not found in schema). **Scope limits (LOW-001, LOW-002, LOW-004, LOW-005, LOW-006, LOW-007 — explicit, not silent gaps):** (LOW-001) `fn_call_arg` in `fn_call_comparison` admits `literal \| field_path` only; nested fn-calls in args (e.g., `\| where upper(trim(device_id)) = 'active'`) are NOT admitted in this version — E-QUERY-001 at parse time; candidate follow-up feature if analyst use cases emerge. (LOW-002) `fn_call_comparison` is the SOLE production admitting fn-call LHS; it wires `compare_op` (`=`, `==`, `!=`, `<`, `>`, `<=`, `>=`; `==` is an alias of `=`, both map to `CompareOp::Eq`) only — all other predicate-operator productions (`regex_match`, `cidr_match`, `not_in_list`, `iin_list`, `in_list`, `between`, `is_null`, `has_check`, `missing_check`, `string_op_match`, `cidr_bare`, `like_match`, `ieq_compare`, `ine_compare`) require `field_path` on the LHS and reject fn-call LHS with E-QUERY-001 at parse time (e.g., `\| where lower(x) LIKE '%foo%'` → E-QUERY-001). (LOW-004) fn-name identifiers in `fn_call_comparison` must begin with an ASCII alphabetic character or `_`; digit-leading names (e.g., `123abc(x)`) produce E-QUERY-001 at parse time (ADR-048 v1.13 §D.7.2). (LOW-005) fn-call on RHS is not admitted — `rhs_expr` accepts `temporal_rhs \| literal` only; `\| where x = upper(y)` → E-QUERY-001 at parse time. (LOW-006) fn-call names in `fn_call_comparison` must not match PrismQL predicate-level reserved keywords (case-insensitive): `NOT`, `AND`, `OR`, `IN`, `IIN`, `IEQ`, `INE`, `IS`, `BETWEEN`, `LIKE`, `CIDR`, `MATCHES`, `HAS`, `MISSING`, `CONTAINS`, `ICONTAINS`, `STARTSWITH`, `ISTARTSWITH`, `ENDSWITH`, `IENDSWITH` — keyword-matching fn-names produce E-QUERY-001 at parse time; e.g., `\| where NOT(x) = 5` (typo for `NOT (x = 5)`) → E-QUERY-001 (reserved keyword). (LOW-007) `Expr::Star` (`*`) is not admissible as a fn-call argument in predicate position; `\| where count(*) = 5` fails at parse time with a generic E-QUERY-001 before the aggregate gate is reached. All six limits surface in the PR body for human feature-decision. **Pipe-mode source pre-registration:** when the source table returns 0 batches, `pre_register_source_table` ensures the table is present in the DataFusion catalog before planning — see BC-2.11.005 DEC-022 (F-PQLFN-P19-MED-001) for the full pre-registration contract (Priority-1/2/3 schema ladder, virtual-field append, double-registration guard). |
| EC-11-013 | `FROM crowdstrike_detections \| where count(device_id) = 5` (aggregate fn-call LHS in pipe `\| where`; ADR-048 v1.13 §D.7 unified plan-time gate) | `Err(E-QUERY-001)` at PLAN time — `count` ∈ `DATAFUSION_BUILTIN_AGGREGATE_NAMES`; gate in `check_enrich_udf_availability` fires (gate ordering per ADR-048 §D.7.4: aggregate gate → `check_query_column_availability` → `check_temporal_literals`). Parse SUCCEEDS via `fn_call_comparison` as `FuncCall::Scalar(Unknown("count"))` — parser-level `AGGREGATE_FUNC_NAMES` `try_map` guard is REMOVED per ADR-048 v1.13 §D.7.2 (former guard message was swallowed by Chumsky backtrack). Canonical message (ADR-048 v1.13 §D.7.2, byte-verbatim per POL-24): `"E-QUERY-001: query parse error at offset {offset}: 'count' is an aggregate function; aggregate fn-calls are not valid in WHERE/where predicates (use HAVING for post-aggregation filters, ADR-048 D.3)"`. **Empty-arg form** (`FROM crowdstrike_detections \| where count() = 5`): same path — parse SUCCEEDS; plan-time gate fires; same E-QUERY-001 message (name: `'count'`). **HAVING exemption (ADR-048 §D.7.1):** HAVING predicates are NOT walked into `predicate_fncall_names`; aggregate fn-calls in HAVING do NOT trigger this gate — HAVING remains the correct path for post-aggregation filters. **Scope limit (LOW-003 — explicit, not silent gap):** An infusion whose name collides with DataFusion's default aggregate registry or the PrismQL-specific aggregate names (`distinct_count`, `percentile`) CANNOT be invoked as a predicate-comparison LHS via `\| where infusion(x) op y` — the plan-time aggregate gate (`DATAFUSION_BUILTIN_AGGREGATE_NAMES` in `check_enrich_udf_availability`) rejects it unconditionally with E-QUERY-001 before infusion resolution. Use `\| enrich infusion(x)` at pipe-stage level or a SELECT projection instead. Documented scope limit, not silent gap; surfaces in PR body for human feature-decision. |

## Canonical Test Vectors

> See `.factory/specs/prd-supplements/test-vectors.md` for the canonical test vector tables.

| Input | Expected Output | Category |
|-------|----------------|----------|
| `| where severity = 'critical' \| stats count by _sensor` | Aggregate counts per sensor for critical events | happy-path |
| `| where severity = 'high' \| sort event_time desc \| head 10` | Top 10 recent high-severity events | happy-path |
| 33 pipe stages chained | `Err(E-QUERY-003)` pipe stage limit exceeded | error |
| `| stats invalid_func by severity` | `Err(E-QUERY-001)` invalid aggregation function | error |
| `| head 0` | Empty result set (valid) | edge-case |
| `FROM crowdstrike_detections \| where timestamp > '2026-06-24'` | `Err(E-QUERY-041)` — `check_temporal_literals` resolves `RawTemporalLiteral` vs Datetime column → E-QUERY-041 (use `'2026-06-24T00:00:00Z'` or `NOW() - INTERVAL 'Nh'`) | error |
| `FROM crowdstrike_detections \| where timestamp > '2026-06-24T00:00:00Z'` | Valid; parses to `Literal::Timestamp` (not `RawTemporalLiteral`); emitted as `arrow_cast('2026-06-24T00:00:00Z', 'Timestamp(Microsecond, Some("UTC"))')` — no `RawTemporalLiteral` emitted, no E-QUERY-041 | happy-path |
| `FROM crowdstrike_detections \| where report_date = '2026-06-24'` (String/Utf8 column `report_date`) | Valid — `check_temporal_literals` coerces `RawTemporalLiteral` → `Literal::String("2026-06-24")`; processed as string comparison in `\| where` (no E-QUERY-041) | edge-case |
| `FROM crowdstrike_detections \| stats count by '2026-06-24'` (temporal literal as GROUP BY literal in `stats`) | `Err(E-QUERY-001)` — parse-time rejection (enhanced message per ADR-052 §D4 v1.10 Option (a)): `stats by` expects column references, not literal values. **F-MED-1 fix:** prior v1.11 vector was wrong (showed COERCE). | error |
| `FROM crowdstrike_detections \| where lower(hostname) = '2026-06-24'` (fn-call LHS in `\| where`, date-like RHS; post-DEFECT-PQL-FNCALL-LHS-001 grammar extension) | `Err(E-QUERY-042)` NonColumnLhsComparison — grammar produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: Literal::RawTemporalLiteral("2026-06-24"), ... }`; parse SUCCEEDS; `check_temporal_literals` arm (4) → E-QUERY-042 `TemporalLiteralInvalidPosition { position: NonColumnLhsComparison, value_prefix: "2026-06-24" }` | error |
| `FROM crowdstrike_detections \| where lower(device_id) = 'active'` (fn-call LHS in `\| where`, NON-date-like RHS; post-DEFECT-PQL-FNCALL-LHS-001 grammar extension) | Valid — grammar produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), rhs: Literal::String("active"), ... }`; `check_temporal_literals` does not intercept (no `RawTemporalLiteral`); passes to DataFusion execution | happy-path |
| `FROM crowdstrike_detections \| where severity IEQ 'high' \| head 10` | Returns rows where `lower(severity)='high'`; `normalized_pql` contains `severity IEQ 'high'` (uppercase canonical form) — IEQ available in `\| where` via shared filter grammar (ADR-046 D7, BC-2.11.023) | happy-path (IEQ) |
| `FROM crowdstrike_detections \| where status IIN ('open', 'new') \| stats count by _sensor` | Returns aggregate counts for records where `lower(status)` is `'open'` or `'new'` — IIN case-insensitive membership in pipe `\| where` stage | happy-path (IIN) |
| `FROM crowdstrike_detections \| where upper(trim(device_id)) = 'active'` (nested fn-call in arg — LOW-001 scope limit) | `Err(E-QUERY-001)` — `fn_call_arg` production in `fn_call_comparison` admits only `literal \| field_path`; nested fn-call `trim(device_id)` as argument is not admitted by this version's grammar; parse-time rejection. Candidate follow-up feature. | scope-limit (LOW-001) |
| `FROM crowdstrike_detections \| where lower(severity) IEQ 'high'` (IEQ with fn-call LHS — LOW-002 scope limit) | `Err(E-QUERY-001)` — `fn_call_comparison` production uses the standard `compare_op` set (`=`, `==`, `!=`, `<`, `>`, `<=`, `>=`; `==` is an alias of `=`, both map to `CompareOp::Eq`); `IEQ` is not in `compare_op` for this production; parse-time rejection. Use `\| where lower(severity) = 'high'` instead. | scope-limit (LOW-002) |
| `FROM crowdstrike_detections \| where lower(host) LIKE '%server%'` (LIKE with fn-call LHS — LOW-002 extended scope, `like_match` production) | `Err(E-QUERY-001)` — `like_match` production wires `field_path` on the LHS; fn-call LHS is not admitted; parse-time rejection (generic parse failure; no scope-limit citation in the message). | scope-limit (LOW-002) |
| `FROM crowdstrike_detections \| where status = upper(severity)` (fn-call on RHS — LOW-005 scope limit) | `Err(E-QUERY-001)` — `rhs_expr` in `fn_call_comparison` and `field_comparison` accepts `temporal_rhs \| literal` only; `upper(severity)` is not a valid RHS alternative; parse-time rejection (generic parse failure; no scope-limit citation in the message). | scope-limit (LOW-005) |
| `FROM crowdstrike_detections \| where NOT(x) = 5` (keyword-shaped fn-name — LOW-006 reserved-word exclusion) | `Err(E-QUERY-001)` — parse-time rejection; `NOT` is a PrismQL predicate-level reserved keyword and cannot be used as a fn-name in `fn_call_comparison`; keyword exclusion fires before aggregate-gate and plan-time checks. The analyst likely intended `NOT (x = 5)` or `NOT x = 5`. Applies to all 20 reserved keywords: `NOT`, `AND`, `OR`, `IN`, `IIN`, `IEQ`, `INE`, `IS`, `BETWEEN`, `LIKE`, `CIDR`, `MATCHES`, `HAS`, `MISSING`, `CONTAINS`, `ICONTAINS`, `STARTSWITH`, `ISTARTSWITH`, `ENDSWITH`, `IENDSWITH`. **Implemented (fix-burst 20): LOW-006 keyword exclusion shipped via .validate() in fn_call_comparison; test vector GREEN.** | scope-limit (LOW-006) |
| `FROM crowdstrike_detections \| where count(*) = 5` (star-arg form of aggregate fn-call in pipe `\| where`; LOW-007 scope limit) | `Err(E-QUERY-001)` at parse time — `fn_call_arg` in `fn_call_comparison` admits `literal \| field_path` only; `Expr::Star` (`*`) is not a valid argument form; parse fails with generic E-QUERY-001 before the aggregate gate in `check_enrich_udf_availability` is reached. The aggregate HAVING-redirect message (ADR-048 v1.13 §D.7.2) applies only to parseable forms (`count() = 5`, `count(col) = 5`). Extending `fn_call_arg` to admit `*` is a possible future enhancement requiring architect adjudication. | scope-limit (LOW-007) |
| `FROM crowdstrike_detections \| where count(device_id) = 5` (aggregate fn-call LHS in pipe `\| where`, ADR-048 v1.13 §D.7 unified plan-time gate) | `Err(E-QUERY-001)` at plan time — `DATAFUSION_BUILTIN_AGGREGATE_NAMES` gate in `check_enrich_udf_availability`; parse SUCCEEDS via `fn_call_comparison` (parser-level `AGGREGATE_FUNC_NAMES` blocklist REMOVED per ADR-048 §D.7.2); canonical message (ADR-048 v1.13 §D.7.2, POL-24): `"E-QUERY-001: query parse error at offset {offset}: 'count' is an aggregate function; aggregate fn-calls are not valid in WHERE/where predicates (use HAVING for post-aggregation filters, ADR-048 D.3)"` | error (aggregate-in-where, EC-11-013) |

## Verification Properties

| VP ID | Property | Proof Method |
|-------|----------|-------------|
| VP-021 | PrismQL parser: never panics on arbitrary input | fuzz |

## Traceability
| Field | Value |
|-------|-------|
| L2 Capability | CAP-015 |
| L2 Invariants | DI-019 |
| Priority | P0 |

## Changelog

| Version | Burst | Date | Author | Change |
|---------|-------|------|--------|--------|
| 1.45 | DEFECT-PQL-FNCALL-LHS-001 / fix-burst 24 / F-PQLFN-P32-OBS-001 | 2026-07-14 | product-owner | **F-PQLFN-P32-OBS-001 closure — Position 7 INSERT source_select WHERE scope + ADR-048 v1.12→v1.13 pin sweep (10 sites).** ADR-048 is now v1.13 (INSERT source_select WHERE added as seventh gated position, OD-7 §D.7.6). (1) §Postconditions SHARED-PARSER SCOPE: "ALL five Predicate-typed query positions (...)" → "ALL six Predicate-typed query positions (..., and INSERT source_select WHERE (via `build_insert_parser` → `build_sql_parser` → `build_sql_predicate_parser`, ADR-048 v1.13 OD-7 §D.7.6))". (2) §Postconditions aggregate-gate enforcement: "SOLE enforcement point across all six predicate positions" → "all seven predicate positions". (3) §Error Cases E-QUERY-042 SHARED-PARSER SCOPE: INSERT source_select WHERE added as seventh position after SQL DML WHERE. (4) §Edge Cases EC-11-004-005 SHARED-PARSER SCOPE: INSERT source_select WHERE note appended. (5–14) ADR-048 v1.12 §D.7 pin currency sweep — 10 live normative body pins advanced to v1.13: (5) §Postconditions aggregate-gate enforcement header `(ADR-048 v1.12 §D.7, v1.33)` → `v1.13`; (6) §Postconditions LOW-004 scope-limit note `(ADR-048 v1.12 §D.7.2)` → `v1.13`; (7) §Error Cases E-QUERY-042 LOW-004 `(ADR-048 v1.12 §D.7.2)` → `v1.13`; (8) §Edge Cases EC-11-004-006 LOW-004 `(ADR-048 v1.12 §D.7.2)` → `v1.13`; (9) EC-11-013 description `ADR-048 v1.12 §D.7 unified plan-time gate` → `v1.13`; (10) EC-11-013 `REMOVED per ADR-048 v1.12 §D.7.2` → `v1.13`; (11) EC-11-013 `Canonical message (ADR-048 v1.12 §D.7.2, byte-verbatim per POL-24)` → `v1.13`; (12) §Canonical Test Vectors LOW-007 `(ADR-048 v1.12 §D.7.2)` → `v1.13`; (13) §Canonical Test Vectors EC-11-013 input `ADR-048 v1.12 §D.7 unified plan-time gate` → `v1.13`; (14) §Canonical Test Vectors EC-11-013 `(ADR-048 v1.12 §D.7.2, POL-24)` → `v1.13`. Historical changelog rows left intact per TD-VSDD-091. Note: "six callers" of `build_predicate_parser` is UNCHANGED — `build_sql_predicate_parser` (already counted) handles INSERT source_select WHERE through the same call chain as SQL SELECT WHERE. POL-23: BC-2.11.004 v1.44→v1.45 pin propagation at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.69→v1.70. `modified: 2026-07-14`. |
| 1.44 | DEFECT-PQL-FNCALL-LHS-001 / fix-burst 23 / F-PQLFN-P31-OBS-001 | 2026-07-14 | product-owner | **F-PQLFN-P31-OBS-001 closure — LOW-007 star-arg scope limit.** `Expr::Star` (`*`) is not admissible as a fn-call argument in predicate position — `\| where count(*) = 5` fails closed at parse time with a generic E-QUERY-001; `fn_call_arg` in `fn_call_comparison` admits `literal \| field_path` only; `*` is neither. LOW-001 covered this implicitly; LOW-007 makes it an explicit documented limit. Three scope-limit sites updated: (1) §Postconditions explicit-scope-limits block — LOW-007 appended inline after LOW-006 text; "All five limits" → "All six limits"; (2) §Error Cases E-QUERY-042 heading `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006)` → `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006, LOW-007)`; LOW-007 appended; "All five limits" → "All six limits"; (3) EC-11-004-006 heading `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006 — explicit, not silent gaps)` → `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006, LOW-007 — explicit, not silent gaps)`; LOW-007 appended; "All five limits" → "All six limits". §Canonical Test Vectors: one row added for `\| where count(*) = 5` → parse-time E-QUERY-001, scope-limit LOW-007. Note: extending `fn_call_arg` to admit `*` (for canonical-message UX parity with `count()`) is a possible future enhancement requiring architect adjudication. POL-23: BC-2.11.004 v1.43→v1.44 pin propagation at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.68→v1.69. `modified: 2026-07-14`. |
| 1.43 | DEFECT-PQL-FNCALL-LHS-001 / fix-burst 21 / F-PQLFN-P27-LOW-001 | 2026-07-14 | product-owner | **F-PQLFN-P27-LOW-001 closure + POL-23 ADR-048 v1.11→v1.12 pin currency.** (Item 1 — POL-23 ADR-048 v1.12 pin advance) ADR-048 is now v1.12 (amended §D.7.2 to codify two-form E-QUERY-001 Display convention + broad recovery-guard semantics; no behavioral change to this BC's own contracts). 9 live normative body pins advanced to v1.12: (1) §Postconditions aggregate-gate enforcement header `(ADR-048 v1.11 §D.7, v1.33)` → `v1.12`; (2) §Postconditions LOW-004 scope-limit note `(ADR-048 v1.11 §D.7.2)` → `v1.12`; (3) §Error Cases E-QUERY-042 LOW-004 scope-limit note `(ADR-048 v1.11 §D.7.2)` → `v1.12`; (4) §Edge Cases EC-11-004-006 LOW-004 scope-limit note `(ADR-048 v1.11 §D.7.2)` → `v1.12`; (5) §Edge Cases EC-11-013 description `ADR-048 v1.11 §D.7 unified plan-time gate` → `v1.12`; (6) §Edge Cases EC-11-013 expected behavior `REMOVED per ADR-048 v1.11 §D.7.2` → `v1.12`; (7) §Edge Cases EC-11-013 canonical-message citation `Canonical message (ADR-048 v1.11 §D.7.2, byte-verbatim per POL-24)` → `v1.12`; (8) §Canonical Test Vectors EC-11-013 input `ADR-048 v1.11 §D.7 unified plan-time gate` → `v1.12`; (9) §Canonical Test Vectors EC-11-013 expected-output message citation `(ADR-048 v1.11 §D.7.2, POL-24)` → `v1.12`. Historical changelog rows (v1.39 audit-trail entry, v1.38 nine-pin sweep, etc.) left intact per TD-VSDD-091. (Item 2 — F-PQLFN-P27-LOW-001 LOW-006 implemented-marker truth) §Canonical Test Vectors LOW-006 row: stale "Red Gate (implementer): grammar excludion not yet implemented; this test vector is a failing test until LOW-006 implementer directive is executed" marker rewritten to reflect implemented status — "Implemented (fix-burst 20): LOW-006 keyword exclusion shipped via .validate() in fn_call_comparison; test vector GREEN." LOW-006 keyword exclusion shipped in fix-burst 20 (filter_parser.rs fn_call_comparison .validate() gate, commit 1a07a5f9); all 5 rejection tests GREEN. POL-23: BC-2.11.004 v1.42→v1.43 pin propagation at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.67→v1.68. `modified: 2026-07-14`. |
| 1.42 | DEFECT-PQL-FNCALL-LHS-001 / fix-burst 20 / F-PQLFN-P26-OBS-001/002 | 2026-07-14 | product-owner | **F-PQLFN-P26-OBS-001 + F-PQLFN-P26-OBS-002 closure — LOW-002 compare-op alias truth + LOW-006 keyword-fn-name adjudication.** (OBS-001) LOW-002 corrected: `fn_call_comparison` wires `compare_op` 7 alternatives (`=`, `==`, `!=`, `<`, `>`, `<=`, `>=`), not 6 — `==` is a pre-existing long-standing alias of `=` mapping to `CompareOp::Eq` (grammar shared with `field_comparison`; verified POL-22 against filter_parser.rs ~1001-1010 in worktree DEFECT-PQL-FNCALL-LHS-001). All 4 LOW-002 operator-set enumeration sites updated: §Postconditions explicit-scope-limits block, §Error Cases E-QUERY-042 scope-limits block, EC-11-004-006 scope-limits block, §Canonical Test Vectors LOW-002 row. (OBS-002) Adjudicated as option **(b) — reserved-word exclusion required**. Rationale: (1) `NOT(x) = 5` (typo for `NOT (x = 5)`) now silently parses as fn-call post-branch, was clean parse-time E-QUERY-001 pre-branch — behavioral regression for analyst typos; (2) no legitimate infusion uses PrismQL predicate keywords as names (structurally unreachable in predicate position anyway — grammar trace: `kw("NOT").ignore_then(not.clone())` fails on `NOT(x) = 5` because `(x)` alone is not a valid predicate, then Chumsky backtracks to atom where `fn_call_comparison` matches); (3) parse-time E-QUERY-001 citing the keyword is unambiguously better UX than reaching DataFusion with `"Invalid function"` on no-registry installations; (4) `EXISTS` excluded from reserved list — NOT a PrismQL predicate keyword (finding enumerated it but grammar confirms no such production). LOW-006 added to three scope-limit sites (§Postconditions, §Error Cases E-QUERY-042, EC-11-004-006) and §Canonical Test Vectors: fn-call names must not match 20 PrismQL predicate-level reserved keywords (case-insensitive): `NOT`, `AND`, `OR`, `IN`, `IIN`, `IEQ`, `INE`, `IS`, `BETWEEN`, `LIKE`, `CIDR`, `MATCHES`, `HAS`, `MISSING`, `CONTAINS`, `ICONTAINS`, `STARTSWITH`, `ISTARTSWITH`, `ENDSWITH`, `IENDSWITH` — keyword-matching fn-names → E-QUERY-001 at parse time. Implementer directive included in §Postconditions LOW-006 text. Scope-limit headings updated: §Postconditions `(LOW-001, LOW-002, LOW-004, LOW-005)` → `(LOW-001, LOW-002, LOW-004, LOW-005)` (heading unchanged; LOW-006 appended inline); E-QUERY-042 `(LOW-001, LOW-002, LOW-004, LOW-005)` → `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006)`; EC-11-004-006 `(LOW-001, LOW-002, LOW-004 — explicit, not silent gaps)` → `(LOW-001, LOW-002, LOW-004, LOW-005, LOW-006 — explicit, not silent gaps)`. "All four limits" → "All five limits" at three sites. LOW-006 test vector added (Red Gate pending implementer fix-burst). POL-23: BC-2.11.004 v1.41→v1.42 pin propagation at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.66→v1.67. `modified: 2026-07-14`. |
| 1.41 | DEFECT-PQL-FNCALL-LHS-001 / fix-burst 16 / F-PQLFN-P21-OBS-001/002 | 2026-07-13 | product-owner | **F-PQLFN-P21-OBS-001 + F-PQLFN-P21-OBS-002 closure — LOW-002 full non-composable operator enumeration + LOW-005 fn-call-on-RHS scope limit.** (OBS-001) LOW-002 extended: `fn_call_comparison` is the SOLE production admitting fn-call LHS; it wires `compare_op` (`=`, `!=`, `<`, `>`, `<=`, `>=`) only; ALL 14 other predicate-operator productions (`regex_match`, `cidr_match`, `not_in_list`, `iin_list`, `in_list`, `between`, `is_null`, `has_check`, `missing_check`, `string_op_match`, `cidr_bare`, `like_match`, `ieq_compare`, `ine_compare`) require `field_path` on LHS and reject fn-call LHS with E-QUERY-001 at parse time; e.g., `\| where lower(x) LIKE '%foo%'` → E-QUERY-001 (generic parse failure; no scope-limit citation in message). (OBS-002) LOW-005 added: fn-call on RHS not admitted — `rhs_expr` accepts `temporal_rhs | literal` only; `\| where x = upper(y)` and `\| where lower(x) = upper(y)` → E-QUERY-001 at parse time (generic parse failure). Three body sites updated: §Postconditions explicit-scope-limits block, §Error Cases E-QUERY-042 scope-limits block, EC-11-004-006 scope-limits block; headings updated from `(LOW-001, LOW-002, LOW-004)` to `(LOW-001, LOW-002, LOW-004, LOW-005)`; "All three limits" → "All four limits" at all three sites. Two §Canonical Test Vectors added (LIKE fn-call LHS for extended LOW-002 scope, fn-call-on-RHS for LOW-005). POL-22 grammar verification: all 14 production names verified against `build_predicate_parser` `atom` choice in `filter_parser.rs` (worktree DEFECT-PQL-FNCALL-LHS-001). POL-23: BC-2.11.004 v1.40→v1.41 pin propagation at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.65→v1.66. `modified: 2026-07-13`. |
| 1.40 | DEFECT-PQL-FNCALL-LHS-001 / F-PQLFN-P19-MED-001 fix-burst pass-19 traceability tightening | 2026-07-13 | product-owner | **F-PQLFN-P19-MED-001 traceability tightening (adversary recommended) — EC-11-004-006 cross-reference to BC-2.11.005 DEC-022 Pipe-mode source pre-registration.** Added one-sentence cross-reference at the end of EC-11-004-006 Expected Behavior: `pre_register_source_table` pipe-mode source pre-registration is the mechanism that prevents `QueryExecutionFailed` when the source table returns 0 batches — BC-2.11.005 DEC-022 (F-PQLFN-P19-MED-001) is the authoritative contract. No other body changes. POL-23: BC-2.11.004 v1.39→v1.40 pin propagation required at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.64→v1.65. `modified: 2026-07-13`. |
| 1.39 | DEFECT-PQL-FNCALL-LHS-001-pass-16-F-PQLFN-P16-OBS-001 | 2026-07-13 | product-owner | **F-PQLFN-P16-OBS-001 closure — audit-trail correction (POL-26): v1.38 changelog undercounted the ADR-048 v1.9→v1.11 pin sweep.** v1.38 stated "6 live ADR-048 v1.9 normative body pins advanced to v1.11" but the sweep actually advanced **9 live citations across 5 lines**. Body content is CORRECT (zero v1.9 residue confirmed in v1.38); only the audit-trail enumeration was wrong. The three unenumerated sites advanced in v1.38 (not listed in its changelog row): (7) §Edge Cases EC-11-004-006 LOW-004 scope-limit note (line ~117): `(ADR-048 v1.11 §D.7.2)` — fn-name identifier-start constraint cite; (8) §Edge Cases EC-11-013 canonical-message citation (line ~118): `"Canonical message (ADR-048 v1.11 §D.7.2, byte-verbatim per POL-24)"` — aggregate-gate E-QUERY-001 message anchor; (9) §Canonical Test Vectors EC-11-013 expected-output canonical-message citation (line ~141): `(ADR-048 v1.11 §D.7.2, POL-24)` — test-vector message anchor. No body-content change in this version — all nine sites have been at v1.11 since v1.38. This v1.39 bump corrects the audit trail only. Citing F-PQLFN-P16-OBS-001, pass 16, POL-26 audit-trail integrity. POL-23: BC-2.11.004 v1.38→v1.39 pin propagation required at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.63→v1.64. `modified: 2026-07-13`. |
| 1.38 | DEFECT-PQL-FNCALL-LHS-001-pass-15-F-PQLFN-P15-OBS-001 | 2026-07-13 | product-owner | **F-PQLFN-P15-OBS-001 closure (F-PQLFN-P10-LOW-001 precedent: pins advance when anchored ADR version advances) — 6 live ADR-048 v1.9 normative body pins advanced to v1.11.** ADR-048 is currently v1.11. Per F-PQLFN-P10-LOW-001 precedent, live-normative body citations to an ADR must advance to the current ADR version when the ADR version advances. **Classification (per-site audit):** (1) §Postconditions aggregate-gate enforcement header `(ADR-048 v1.9 §D.7, v1.33)` → `(ADR-048 v1.11 §D.7, v1.33)` — live normative. (2) §Postconditions LOW-004 scope-limit note `(ADR-048 v1.9 §D.7.2)` — live normative. (3) §Error Cases E-QUERY-042 LOW-004 scope-limit note `(ADR-048 v1.9 §D.7.2)` — live normative. (4) §Edge Cases EC-11-013 description `ADR-048 v1.9 §D.7 unified plan-time gate` — live normative. (5) §Edge Cases EC-11-013 expected behavior `REMOVED per ADR-048 v1.9 §D.7.2` — live normative. (6) §Canonical Test Vectors EC-11-013 input `(ADR-048 v1.9 §D.7.2, POL-24)` — live normative. Historical changelog rows (v1.37 entry describing "advanced to `v1.9`"; v1.35 entry mentioning "ADR-048 is currently v1.4"; v1.33 entry citing "ADR-048 v1.2") left intact per TD-VSDD-091 append-only audit trail policy. Body-text behavior unchanged — all six sites are citation-currency updates only. POL-23: BC-2.11.004 v1.37→v1.38 pin propagation required at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites); story v1.62→v1.63. `modified: 2026-07-13`. |
| 1.37 | DEFECT-PQL-FNCALL-LHS-001-pass-10-F-PQLFN-P10-HIGH-001-LOW-001-grammar-constraint | 2026-07-13 | product-owner | **F-PQLFN-P10-HIGH-001 + F-PQLFN-P10-LOW-001 + fn-name identifier-start constraint (pass 10).** (HIGH-001) Two live sites that quoted the OLD canonical message form updated to the Full Display form per ADR-048 v1.9 §D.7.2: (a) EC-11-013 Expected Behavior cell; (b) §Canonical Test Vectors EC-11-013 row Expected Output. Old form: `"E-QUERY-001: 'count' is an aggregate function; aggregate fn-calls are not valid in pipe \| where ..."` — stale on two axes: (1) "pipe \| where" → "WHERE/where predicates"; (2) missing "query parse error at offset {offset}: " interposed prefix. New Full Display form: `"E-QUERY-001: query parse error at offset {offset}: 'count' is an aggregate function; aggregate fn-calls are not valid in WHERE/where predicates (use HAVING for post-aggregation filters, ADR-048 D.3)"`. Both citations updated to `(ADR-048 v1.9 §D.7.2, byte-verbatim per POL-24)` / `(ADR-048 v1.9 §D.7.2, POL-24)`. (LOW-001) Four `ADR-048 v1.4` live-normative pins advanced to `v1.9`: (1) §Postconditions aggregate-gate enforcement header `(ADR-048 v1.4 §D.7, v1.33)` → `v1.9`; (2) EC-11-013 description `ADR-048 v1.4 §D.7 unified plan-time gate` → `v1.9`; (3) EC-11-013 expected behavior `REMOVED per ADR-048 v1.4 §D.7.2` → `v1.9`; (4) §Canonical Test Vectors EC-11-013 input `ADR-048 v1.4 §D.7 unified plan-time gate` → `v1.9`. Historical changelog rows at lines 160–163 citing `ADR-048 v1.4` left intact per TD-VSDD-091 append-only audit trail policy. (Grammar constraint) `fn_call_comparison` fn-name identifier-start constraint added as LOW-004 to three scope-limit sites: §Postconditions explicit-scope-limits block (heading `LOW-001, LOW-002` → `LOW-001, LOW-002, LOW-004`; "Both limits documented" → "All three limits documented"), §Error Cases E-QUERY-042 scope-limits block (same heading + "both"→"all three"), EC-11-004-006 scope-limits block (same). LOW-004 text: fn-name identifiers must begin with ASCII alphabetic or `_`; digit-leading names (e.g., `123abc(x)`) produce E-QUERY-001 at parse time (ADR-048 v1.9 §D.7.2). **POL-23 sibling pin sweep:** `BC-2.11.004 v1.36` → `v1.37` at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 171, 189, 407 (4 sites; line 171 table-cell was pass-9 false-close site); story v1.61→v1.62 required. `modified: 2026-07-13`. |
| 1.36 | DEFECT-PQL-FNCALL-LHS-001-pass-8-F-PQLFN-P8-HIGH-002-sibling-sweep | 2026-07-13 | product-owner | **F-PQLFN-P8-HIGH-002 sibling-sweep closure (POL-29) — `build_predicate_parser` caller count updated from five to six (ADR-052 v1.14 / ADR-048 v1.7 sync); "ALL four Predicate-typed" updated to "ALL five".** Four normative body sites corrected: (1) §Postconditions arm (4) SHARED-PARSER SCOPE — `build_predicate_parser` is shared by **six callers**: filter-mode root predicate, pipe `\| where` stage, SqlPipe pipe-stage `\| where`, SQL WHERE, SQL HAVING, and SQL DML WHERE (NEW — via `build_delete_parser`/`build_update_parser`, ADR-048 v1.6 OD-6 §D.7.5); "ALL four Predicate-typed query positions" → "ALL five Predicate-typed query positions" (list extended to include SQL DML WHERE position). (2) §Postconditions aggregate-gate enforcement line: "`DATAFUSION_BUILTIN_AGGREGATE_NAMES` gate ... is the SOLE enforcement point across all **five** predicate positions" → "all **six** predicate positions" (per ADR-048 v1.7 §D.7.5 the DML gate uses the same enforcement). (3) §Error Cases E-QUERY-042 SHARED-PARSER SCOPE note: "five callers; arm (4) fires in ..., SQL HAVING positions" → "six callers; arm (4) fires in ..., SQL HAVING, and SQL DML WHERE (via `build_delete_parser`/`build_update_parser`, ADR-048 v1.6 OD-6 §D.7.5) positions". (4) EC-11-004-005 SHARED-PARSER SCOPE note: "five callers" → "six callers". Also: §Postconditions arm (4) first mention "shared by five callers" → "shared by six callers". Historical changelog rows (lines 162–163 referencing "five callers" from v1.32/v1.33) left intact per TD-VSDD-091. **"ALL four Predicate-typed" reasoning:** "ALL four" was a positions-count (4 grouped positions), not a callers-count (was 5 callers). SQL DML WHERE adds a fifth query position; "ALL four" → "ALL five". **POL-23 story-pin sweep:** `BC-2.11.004 v1.35` → `v1.36` at S-PRISMQL-CASE-INSENSITIVE-001 lines 74, 189, 407 (3 sites; pre-existing deferral from v1.33 no longer applies — those were v1.30 pins that have since been swept by story-writer). `modified: 2026-07-13` (unchanged). |
| 1.35 | DEFECT-PQL-FNCALL-LHS-001-adversary-pass-5 | 2026-07-13 | product-owner | **F-PQLFN-P5-LOW-002 closure — three live "ADR-048 v1.2" body citations updated to "ADR-048 v1.4" (POL-25 pin-currency sweep).** ADR-048 is currently v1.4 (modified 2026-07-13). Four body pin occurrences across three locations updated: (1) §Postconditions aggregate-gate enforcement mechanism line: `(ADR-048 v1.2 §D.7, v1.33):` → `(ADR-048 v1.4 §D.7, v1.33):`; (2) EC-11-013 Description cell: `\`\| where\`; ADR-048 v1.2 §D.7 unified plan-time gate)` → `v1.4`; (3) EC-11-013 Expected Behavior cell: `REMOVED per ADR-048 v1.2 §D.7.2` → `REMOVED per ADR-048 v1.4 §D.7.2`; (4) §Canonical Test Vectors EC-11-013 row Input: `\`\| where\`, ADR-048 v1.2 §D.7 unified plan-time gate)` → `v1.4`. §D references unchanged — D.7 content is stable across v1.2→v1.4. Historical changelog rows (including v1.33 entry mentioning "ADR-048 v1.2 §D.7 unified plan-time gate") left untouched per TD-VSDD-091 append-only audit trail policy. **POL-23 sibling pin sweep grep evidence:** `grep -n "BC-2.11.004" .factory/stories/S-PRISMQL-CASE-INSENSITIVE-001*.md` → 4 live v1.34 pin sites at lines 74, 171, 189, 407; story-writer update to v1.35 required under `bc_array_changes_propagate_to_body_and_acs` policy — NOT done by product-owner in this burst. `modified: 2026-07-13`. |
| 1.34 | DEFECT-PQL-FNCALL-LHS-001-adversary-pass-3 | 2026-07-13 | product-owner | **F-PQLFN-P3-LOW-003 closure — EC-11-013 scope-limit note (LOW-003) added.** EC-11-013 (aggregate fn-call LHS in pipe `\| where`) amended: appended explicit LOW-003 scope-limit note documenting that an infusion whose name collides with DataFusion's default aggregate registry or the PrismQL-specific aggregate names (`distinct_count`, `percentile`) CANNOT be invoked as a predicate-comparison LHS via `\| where infusion(x) op y` — the plan-time aggregate gate (`DATAFUSION_BUILTIN_AGGREGATE_NAMES` in `check_enrich_udf_availability`) rejects it unconditionally with E-QUERY-001 before infusion resolution; use `\| enrich infusion(x)` at pipe-stage level or SELECT projection instead. Documented scope limit, not silent gap; surfaces in PR body for human feature-decision. Pattern mirrors LOW-001 (nested fn-call args not admitted at parse time) and LOW-002 (IEQ/IIN/INE not composable with fn-call LHS). **POL-23 sibling pin sweep grep evidence:** `grep -n "BC-2.11.004" .factory/stories/S-PRISMQL-CASE-INSENSITIVE-001*.md` → 4 live v1.33 pin sites at lines 74, 171, 189, 407; story-writer update to v1.34 required under `bc_array_changes_propagate_to_body_and_acs` policy — NOT done by product-owner in this burst. `modified: 2026-07-13`. |
| 1.33 | DEFECT-PQL-FNCALL-LHS-001-fix-burst-2 | 2026-07-13 | product-owner | **F-PQLFN-P2-MED-002 + F-PQLFN-P2-HIGH-001 spec anchor (ADR-048 v1.2 §D.7 unified plan-time gate).** Added EC-11-013: aggregate fn-call LHS in pipe `\| where` (`FROM crowdstrike_detections \| where count(device_id) = 5` → `Err(E-QUERY-001)` at plan time via `DATAFUSION_BUILTIN_AGGREGATE_NAMES` gate in `check_enrich_udf_availability`; canonical ADR-048 §D.7.2 message byte-verbatim per POL-24; empty-arg form `count() = 5` takes same path; HAVING exempt per ADR-048 §D.7.1 permit ruling). Canonical test vector added (category: `error/aggregate-in-where`). Mechanism change note added to §Postconditions SHARED-PARSER SCOPE: parser-level `AGGREGATE_FUNC_NAMES` `try_map` guard REMOVED (ADR-048 §D.7.2, message was swallowed by Chumsky backtrack); `DATAFUSION_BUILTIN_AGGREGATE_NAMES` gate is SOLE enforcement across all five predicate positions. ADR-048 added to `inputs:`. POL-23 sibling pin sweep: `S-PRISMQL-CASE-INSENSITIVE-001` has live pins at BC-2.11.004 v1.30 (3 live sites: lines 74, 189, 407; already stale since v1.31/v1.32); story-writer update deferred per `bc_array_changes_propagate_to_body_and_acs` policy. `modified: 2026-07-13`. |
| 1.32 | DEFECT-PQL-FNCALL-LHS-001-adversary-pass-1 | 2026-07-13 | product-owner | **F-PQLFN-P1-MED-001 + F-PQLFN-P1-LOW-001 + F-PQLFN-P1-LOW-002 closure (adversary pass-1 findings).** (MED-001) SHARED-PARSER SCOPE note added throughout: `build_predicate_parser` is shared by five callers — filter-mode root predicate, pipe `\| where` stage, SqlPipe pipe-stage `\| where`, SQL WHERE, and SQL HAVING. The `fn_call_comparison` production extends fn-call LHS grammar to ALL four Predicate-typed query positions, not pipe-only. BC-2.11.003 EC-11-003-007 cross-referenced as the authoritative EC for the SQL WHERE position. **Changes:** (1) §Postconditions arm (4): "pipe `\| where` grammar now produces" → "predicate grammar (`build_predicate_parser`, shared by five callers) now produces"; old "Scope: FuncCall::Scalar in pipe `\| where` only" replaced with SHARED-PARSER SCOPE note + LOW-001/LOW-002 explicit scope limits; (2) §Error Cases E-QUERY-042 row: description updated ("pipe `\| where` comparison" → "Predicate comparison"), grammar description updated, cross-reference to BC-2.11.003 EC-11-003-007 added, SHARED-PARSER SCOPE note + LOW-001/LOW-002 scope limits added, old "Scope" note removed; (3) EC-11-004-005: grammar description updated, SHARED-PARSER SCOPE note + BC-2.11.003 EC-11-003-007 cross-reference added, old scope note replaced; (4) EC-11-004-006: LOW-001 + LOW-002 explicit scope-limit notes added (nested fn-calls in args not admitted; IEQ/IIN/INE not composable with fn-call LHS); old "Scope" note replaced; (5) §Canonical Test Vectors: two scope-limit test vectors added — `upper(trim(device_id)) = 'active'` → E-QUERY-001 (LOW-001); `lower(severity) IEQ 'high'` → E-QUERY-001 (LOW-002). `modified: 2026-07-13`. |
| 1.31 | DEFECT-PQL-FNCALL-LHS-001-spec-amendment | 2026-07-13 | product-owner | **DEFECT-PQL-FNCALL-LHS-001 spec amendment — architect-adjudicated Option A (grammar extension, spec-wins rule). Live-audit [H5c].** Post-fix: pipe `\| where` grammar produces `Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), ... }` for fn-call LHS inputs; parse SUCCEEDS; `check_temporal_literals` arm (4) fires E-QUERY-042 (NonColumnLhsComparison) at plan time when RHS is a date-like literal. Prior state (the DEFECT): arm (4) was defense-in-depth only — grammar rejected fn-call LHS at parse time with generic E-QUERY-001 (`-32000 INTERNAL_ERROR` to callers). **Changes:** (1) §Postconditions arm (4) amended: grammar extension noted (`Predicate::Compare { lhs: Expr::FuncCall(FuncCall::Scalar), ... }`), prior defense-in-depth-only status documented, scope note added (FuncCall::Scalar only; aggregate fn-calls remain invalid in pipe `\| where`; HAVING path per ADR-048 D.3 is SQL-mode only); (2) §Error Cases E-QUERY-042 row: grammar extension and prior defect noted, non-date-like RHS negative case clarified (no `RawTemporalLiteral` → no intercept → valid), scope note added; (3) EC-11-004-005 mechanism note updated — grammar extension clarified, "Prior defect" note added, "Closes prior -32000" replaced with accurate history; (4) EC-11-004-006 ADDED — fn-call LHS with NON-date-like RHS (`\| where lower(device_id) = 'active'`) → Valid; `check_temporal_literals` does not intercept; E-QUERY-038 interaction via `collect_predicate_columns` FuncCall arm documented; (5) §Canonical Test Vectors: EC-11-004-005 vector description updated to cite grammar AST form; EC-11-004-006 happy-path vector added. `modified: 2026-07-13`. |
| 1.30 | FIX-IEQ-ERRPATH-001-ADV-PR-P5 | 2026-07-09 | product-owner | **ADV-PR-P5 companion to BC-2.11.016 v1.25 — pin update + POL-22 type-name body-text corrections (authorized in-scope fold, same burst).** BC-2.11.016 v1.25 closes ADV-PR-P5-MED-001/002 (position 8 prose `FilterExpr` → `Predicate`; position 11 prose struct-variant + field + type corrections) and fixes proactive position 4 table `OrderBy::expr` → `OrderExpr::expr`. The same class of POL-22 type-name inaccuracy existed in this file's §Error Cases E-QUERY-038 Condition column position summary; all four corrected in this same v1.30 bump (verified against ast.rs POL-22 Phase C): (1) **pos 8:** `` every `PipeStage::Where`, `FilterExpr` tree walked recursively `` → `` every `PipeStage::Where(Predicate)`, `Predicate` tree walked recursively `` (ast.rs line 319: `Where(Predicate)`); (2) **pos 11:** `` `PipeStage::Stats { by }` `` → `` `PipeStage::Stats(StatsStage)` `by_fields: Vec<FieldPath>` `` (ast.rs lines 327/379-385: tuple variant `Stats(StatsStage)`; field `by_fields: Vec<FieldPath>`); (3) **pos 13:** `` `PipeStage::Enrich { input_col }` `` → `` `PipeStage::Enrich(EnrichStage)` — `EnrichStage.field: FieldPath` `` (ast.rs lines 335/479-482: tuple variant `Enrich(EnrichStage)`; field `field: FieldPath`; no `input_col` field exists); (4) **pos 14:** `` `PipeStage::Dedup { fields }` `` → `` `PipeStage::Dedup(Vec<FieldPath>)` `` (ast.rs line 329: tuple variant with `Vec<FieldPath>` directly; no named struct). **Pin update:** (1) Condition column: `v1.24 §Preconditions DERIVED-COLUMN BINDING RULE` → `v1.25`; (2) Behavior column: `v1.24` → `v1.25`. BC-2.11.017 v1.13 and BC-2.11.020 v1.18 body text swept — no additional AST-shape inaccuracies found. Companions: BC-2.11.016 v1.25, BC-2.11.020 v1.18, BC-2.11.017 v1.13. `modified: 2026-07-09`. |
| 1.29 | FIX-IEQ-ERRPATH-001-ADV-PR-P4-PROACTIVE | 2026-07-09 | product-owner | **ADV-PR-P4 proactive pin-currency sweep: BC-2.11.016 v1.23 → v1.24 (AST type-name corrections, POL-22 Phase C).** BC-2.11.016 v1.24 corrects AST type-name inaccuracies: position 7 `And`/`Or`/`Not` → `Predicate::Logical`/`Predicate::Not`; position 8 `PipeStage::Where(FilterExpr)` → `PipeStage::Where(Predicate)`; position 10 `SortEntry` → `SortExpr`. E-QUERY-038 gate semantics and DERIVED-COLUMN BINDING RULE unchanged. **Amendment (pin-only):** (1) Condition column: `v1.23 §Preconditions DERIVED-COLUMN BINDING RULE` → `v1.24`; (2) Behavior column: `v1.23` → `v1.24`. Companions: BC-2.11.016 v1.24, BC-2.11.020 v1.17, BC-2.11.017 v1.12. `modified: 2026-07-09`. |
| 1.28 | FIX-IEQ-ERRPATH-001-ADV-PR-P3-LOW-001 | 2026-07-09 | product-owner | **ADV-PR-P3-LOW-001 POL-25 pin-currency sweep: BC-2.11.016 v1.22 → v1.23 (implementation location function name corrections, POL-22 Phase C).** BC-2.11.016 v1.23 corrects §Implementation location table function names: positions 1/3/4/5 → `extract_field_paths_with_bareness`; positions 2/6 → `extract_predicate_columns_with_bareness`; positions 10–14 → `extract_column_name_from_field_path`. This is an internal implementation-location correction; the E-QUERY-038 gate semantics and the DERIVED-COLUMN BINDING RULE are unchanged. BC-2.11.004 §Error Cases delegates to the full spec via pin. **Amendment (pin-only):** §Error Cases E-QUERY-038 row: (1) Condition column: `"Full spec: BC-2.11.016 v1.22 §Preconditions DERIVED-COLUMN BINDING RULE"` → `v1.23`; (2) Behavior column: `"Full spec: BC-2.11.016 v1.22"` → `v1.23`. Companions: BC-2.11.016 v1.23, BC-2.11.020 v1.16, BC-2.11.017 v1.11. `modified: 2026-07-09`. |
| 1.27 | FIX-IEQ-ERRPATH-001-ADV-PR-P1-OBS-001 | 2026-07-09 | product-owner | **ADV-PR-P1-OBS-001 POL-25 pin-currency sweep: BC-2.11.016 v1.21 → v1.22 (injection-safety note for `column` MCP-facing payload).** BC-2.11.016 v1.22 adds `### Injection-safety of \`column\` (MCP-facing payload)` documenting `sanitize_for_log` control-char sanitization (CWE-116/CWE-117; ADV-PR-P1-OBS-001). This is a payload postcondition; BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate raw-echo semantics. **Amendment (pin-only):** §Error Cases E-QUERY-038 row: (1) Condition column: `"Full spec: BC-2.11.016 v1.21 §Preconditions DERIVED-COLUMN BINDING RULE"` → `v1.22`; (2) Behavior column: `"Full spec: BC-2.11.016 v1.21"` → `v1.22`. Companions: BC-2.11.016 v1.22, BC-2.11.020 v1.15, BC-2.11.017 v1.10, error-taxonomy.md v2.35. `modified: 2026-07-09`. |
| 1.26 | FIX-IEQ-ERRPATH-001-ADV-FIX-P16-MED-001 | 2026-07-09 | product-owner | **ADV-FIX-P16-MED-001 POL-25 pin-currency sweep: BC-2.11.016 v1.20 → v1.21 (PER-REFERENCE SCOPING clarification to HEAD-JOIN SUSPENSION RULE).** §Error Cases E-QUERY-038 row: (1) Condition column: `"Full spec: BC-2.11.016 v1.20 §Preconditions DERIVED-COLUMN BINDING RULE"` → `v1.21`; (2) Behavior column: `"Full spec: BC-2.11.016 v1.20"` → `v1.21`. Pin-only update — BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate the HEAD-JOIN SUSPENSION RULE (HEAD-JOIN is a positions-1–6 SQL-head rule; BC-2.11.004 is a pipe-mode BC covering positions 8–14; the HEAD-JOIN restatement including PER-REFERENCE SCOPING for SqlPipe head forms is in BC-2.11.020 v1.14). Companions: BC-2.11.016 v1.21, BC-2.11.020 v1.14, BC-2.11.017 v1.9, error-taxonomy.md v2.34. `modified: 2026-07-09`. |
| 1.25 | FIX-IEQ-ERRPATH-001-ADV-FIX-P15-MED-001 | 2026-07-09 | product-owner | **ADV-FIX-P15-MED-001 POL-25 pin-currency sweep: BC-2.11.016 v1.19 → v1.20 (HEAD-JOIN SUSPENSION RULE addition).** §Error Cases E-QUERY-038 row: (1) Condition column: `"Full spec: BC-2.11.016 v1.19 §Preconditions DERIVED-COLUMN BINDING RULE"` → `v1.20`; (2) Behavior column: `"Full spec: BC-2.11.016 v1.19"` → `v1.20`. Pin-only update — BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate the HEAD-JOIN SUSPENSION RULE (HEAD-JOIN is a positions-1–6 SQL-head rule; BC-2.11.004 is a pipe-mode BC covering positions 8–14; the HEAD-JOIN restatement for SqlPipe head forms is in BC-2.11.020 v1.13). Companions: BC-2.11.016 v1.20, BC-2.11.020 v1.13, BC-2.11.017 v1.8, error-taxonomy.md v2.33. `modified: 2026-07-09`. |
| 1.24 | FIX-IEQ-ERRPATH-001-ADV-FIX-P14-OBS-001-002-003 | 2026-07-09 | product-owner | **ADV-FIX-P14-OBS-001-003 POL-25 pin-currency sweep: BC-2.11.016 v1.18 → v1.19 (STAGE-JOIN SUSPENSION RULE addition).** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.18 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.19; Behavior column "Full spec: BC-2.11.016 v1.18" → v1.19. Pin-only update — BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate the STAGE-JOIN SUSPENSION RULE (that restatement is in BC-2.11.020 v1.12). Companions: BC-2.11.016 v1.19, BC-2.11.020 v1.12, BC-2.11.017 v1.7, error-taxonomy.md v2.32. `modified: 2026-07-09`. |
| 1.23 | FIX-IEQ-ERRPATH-001-ADV-FIX-P12-OBS-002 | 2026-07-09 | product-owner | **ADV-FIX-P12-OBS-002 POL-25 pin-currency sweep: BC-2.11.016 v1.17 → v1.18 (STAR-WITH-JOIN SUSPENSION RULE addition).** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.17 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.18; Behavior column "Full spec: BC-2.11.016 v1.17" → v1.18. This is a pin-only update — BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate the STAR-WITH-JOIN SUSPENSION RULE (that restatement is in BC-2.11.020 v1.11). Companions: BC-2.11.016 v1.18, BC-2.11.020 v1.11, BC-2.11.017 v1.6, error-taxonomy.md v2.31. `modified: 2026-07-09`. |
| 1.22 | FIX-IEQ-ERRPATH-001-ADV-FIX-P10-OBS-001 | 2026-07-09 | product-owner | **ADV-FIX-P10-OBS-001 POL-25 pin-currency sweep: BC-2.11.016 v1.15 → v1.17 (LAST-SEGMENT OUTPUT-NAME RULE addition).** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.15 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.17; Behavior column "Full spec: BC-2.11.016 v1.15" → v1.17. This is a pin-only update — BC-2.11.004 §Error Cases delegates to the full spec via pin and does not restate the LAST-SEGMENT OUTPUT-NAME RULE or branch-enumeration extraction detail (those are restated in BC-2.11.020 v1.10). Note: v1.16 was a narrative-only fix (ADV-FIX-P8-OBS-002 — Enrich bullet gate-ordering note); siblings were intentionally NOT updated then (no rule change per BC-2.11.016 v1.16 changelog). This pin jump v1.15→v1.17 skips v1.16 by design. Companions: BC-2.11.016 v1.17, BC-2.11.020 v1.10, BC-2.11.017 v1.5, error-taxonomy.md v2.30. `modified: 2026-07-09`. |
| 1.21 | FIX-IEQ-ERRPATH-001-ADV-FIX-P7-MED-001-OBS-001-OBS-002 | 2026-07-09 | product-owner | **ADV-FIX-P7 POL-25 pin-currency sweep: BC-2.11.016 v1.14 → v1.15 (FIELDS TRANSITION RULE + FROM-ALIAS RESOLUTION + SIBLING-GATE CONSISTENCY).** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.14 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.15; Behavior column "Full spec: BC-2.11.016 v1.14" → v1.15. This is a pin-only update — BC-2.11.004 §Error Cases does not restate the DERIVED-COLUMN BINDING RULE new clauses (delegates to the full spec via pin). Companions: BC-2.11.016 v1.15, BC-2.11.020 v1.9, BC-2.11.017 v1.4, error-taxonomy.md v2.29. `modified: 2026-07-09`. |
| 1.20 | FIX-IEQ-ERRPATH-001-ADV-FIX-P6-MED-002 | 2026-07-09 | product-owner | **ADV-FIX-P6-MED-002 POL-25 pin-currency sweep: BC-2.11.016 v1.13 → v1.14 (MIXED-STAR branch addition).** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.13 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.14; Behavior column "Full spec: BC-2.11.016 v1.13" → v1.14. This is a pin-only update — BC-2.11.004 §Error Cases does not restate the SQLPIPE HEAD-PROJECTION BINDING RULE branch enumeration (it delegates to the full spec via pin). Companions: BC-2.11.016 v1.14, BC-2.11.020 v1.8, error-taxonomy.md v2.28. `modified: 2026-07-09`. |
| 1.19 | FIX-IEQ-ERRPATH-001-ADV-FIX-P5-FP-001 | 2026-07-09 | product-owner | **POL-25 pin-currency sweep: BC-2.11.016 v1.12 → v1.13.** §Error Cases E-QUERY-038 row: Condition column "Full spec: BC-2.11.016 v1.12 §Preconditions DERIVED-COLUMN BINDING RULE" → v1.13; Behavior column "Full spec: BC-2.11.016 v1.12" → v1.13. Reflects BC-2.11.016 v1.13 SQLPIPE HEAD-PROJECTION BINDING RULE addition (ADV-FIX-P5-OBS-1 upgraded-to-FP-001-violation; initial `available` for SqlPipe stage positions seeded from head projection output, not raw schema). Same-burst discipline (Lesson 18/L20): pin currency sweep completed in the same burst as the BC-2.11.016 v1.13 authoring burst. `modified: 2026-07-09`. |
| 1.18 | FIX-IEQ-ERRPATH-001-ADV-FIX-P4-MED-002-OBS-001 | 2026-07-08 | product-owner | **ADV-FIX-P4-MED-002 + OBS-001 POL-25 closure.** §Error Cases E-QUERY-038 row: (1) **MED-002 (pin currency):** both "BC-2.11.016 v1.9" BC anchor references updated to BC-2.11.016 v1.12 — two pins in this row (Condition column tail and Behavior column tail) were 3 versions stale (v1.9 → v1.12); (2) **OBS-001 (position-11 scope expansion):** position-11 description updated from "`\| stats ... by` grouping field references (`PipeStage::Stats { by }` grouping list)" to also cover aggregate function argument field paths (`sum(col)`, `count(col)`, etc., checked before Stats replaces binding context) — consistent with BC-2.11.016 v1.12 position-11 enumeration and the existing DERIVED-COLUMN BINDING RULE prose. `modified: 2026-07-08`. |
| 1.17 | FIX-IEQ-ERRPATH-001-grammar-keyword-correction-sort-by | 2026-07-08 | product-owner | **Grammar keyword correction: `\| sort by` → `\| sort` in §Error Cases E-QUERY-038 row (position 10).** Test-writer grammar verification confirmed no `by` keyword exists between `sort` and its field list in the PrismQL pipe parser. Fixed position 10 label in the E-QUERY-038 covered-positions list. BC anchor reference updated to BC-2.11.016 v1.9. |
| 1.16 | ADV-FIX-P2-CRIT-001-CRIT-002-HIGH-002-HIGH-003 | 2026-07-08 | product-owner | **ADV-FIX-P2-CRIT-001/002 + HIGH-002/003 closure (FIX-IEQ-ERRPATH-001 LOCAL pass-2) — POL-25 multi-cite propagation.** §Error Cases E-QUERY-038 row: (1) added positions (13) `\| enrich` input column and (14) `\| dedup` field keys to covered positions list; (2) added Derived-column binding rule note — running `{ available, suspended }` context; Enrich fail-open on unresolvable infusion output; Stats replaces available with `{aliases ∪ by_fields}`; FP-001 false-positive prohibition. BC anchor reference updated to BC-2.11.016 v1.8. `modified: 2026-07-08`. |
| 1.15 | FIX-IEQ-ERRPATH-001-grammar-keyword-correction | 2026-07-08 | product-owner | **Grammar keyword correction: `\| project` → `\| fields` in §Error Cases E-QUERY-038 row (position 12).** Test-writer grammar verification (fix-PR FIX-IEQ-ERRPATH-001) confirmed no `\| project` keyword exists in the PrismQL pipe parser; the projection stage keyword is `\| fields` (`PipeStage::Fields`). Fixed position 12 label in the E-QUERY-038 covered-positions list. |
| 1.14 | ADV-FIX-P1-HIGH-001-HIGH-002-DRIFT-IEQ-NONEXISTENT-COL-ERRPATH-001 | 2026-07-08 | product-owner | **POL-25 multi-cite propagation (ADV-FIX-P1-HIGH-001 / HIGH-002 closure).** §Error Cases: added `E-QUERY-038` row documenting the gate positions now covered in pipe mode: (8) `\| where` stage predicates, (10) `\| sort by` field keys, (11) `\| stats ... by` grouping refs, (12) `\| project` column refs; SqlPipe positions 9/1–6 cross-referenced. Positions added by BC-2.11.016 v1.6 (ADV-FIX-P1-HIGH-001/HIGH-002/OBS-002 closure). Cross-reference to BC-2.11.016 for full postcondition spec. `modified: 2026-07-08`. |
| 1.13 | S-PRISMQL-CASE-INSENSITIVE-001-bc-burst | 2026-07-06 | product-owner | **ADR-047 D.2 amendment: IEQ/IIN/INE operators available in `\| where` stages via shared filter grammar.** §Postconditions `where <filter_expr>` bullet: expanded to note that shared filter grammar (ADR-046 D7, BC-2.11.023) includes `IEQ`/`IIN`/`INE` case-insensitive operators; inline example `\| where severity IEQ 'high'` added. §Canonical Test Vectors: two IEQ/IIN pipe-mode vectors added. inputs: ADR-047 added. |
| 1.12 | adr-052-d4-v1.10-seven-arm-f-med-1 | 2026-07-05 | product-owner | **FIX 4 (F-MED-1 + ADR-052 §D4 v1.10 Option (a)): reclassify `stats by` / `sort` positions as parse-time rejections; add non-`Field` LHS E-QUERY-042.** §Postconditions ADR-052 D2 bullet: "four-way dispatch" → "four-arm dispatch" (pipes have no SQL-mode SELECT/GROUP BY/ORDER BY arms); arm (4) changed from `stats by / sort` non-comparison COERCE to non-`Field` LHS in `\| where` → E-QUERY-042 NonColumnLhsComparison; added explicit note that `stats by` and `sort` positions reject AT PARSE TIME with E-QUERY-001 (enhanced message, Option (a)) — walker is never reached. §Error Cases E-QUERY-041: "Does NOT fire" note updated — removed `stats by` / `sort` coerce claim; clarified those are parse-time rejections; added forward-ref to E-QUERY-042 for non-`Field` LHS. E-QUERY-042 row ADDED (NonColumnLhsComparison variant only for pipe mode; verbatim ADR-052 §D4 v1.10 message, POL-24). §Edge Cases: **EC-11-004-004 F-MED-1 RECLASSIFIED** from "Valid — arm (4) COERCE" to "FORBIDDEN — parse-time E-QUERY-001 with enhanced message"; EC-11-004-005 ADDED (`\| where lower(hostname) = '2026-06-24'` → E-QUERY-042 NonColumnLhsComparison). §Canonical Test Vectors: `stats count by '2026-06-24'` vector corrected from "COERCE → success" to "Err(E-QUERY-001) parse-time rejection"; `\| where lower(hostname)` vector ADDED (E-QUERY-042). |
| 1.11 | adr-052-d4-v1.8-four-way-dispatch | 2026-07-05 | product-owner | **HIGH-1: propagate ADR-052 §D4 v1.8 four-way dispatch (non-comparison coercion arm) to pipe-mode BC.** §Postconditions ADR-052 D2 bullet: `three-way dispatch` → `four-way dispatch`; added arm (4): non-comparison position (pipe stage function argument, `stats` GROUP BY literal, `sort` literal, `fields` literal — no column type in scope) → COERCE to `Literal::String(s)` (SUCCESS). §Error Cases E-QUERY-041: expanded "Does NOT fire" note to include non-comparison position (arm (4) of four-way dispatch). §Edge Cases: EC-11-004-004 ADDED (GROUP BY literal coerce → success in pipe mode). §Canonical Test Vectors: non-comparison-position coerce vector ADDED. |
| 1.10 | med-1-e-query-002-propagation | 2026-07-04 | product-owner | **MED-1 E-QUERY-001→E-QUERY-002 correction: numeric/bool temporal dispatch arm.** §Postconditions ADR-052 D2 bullet: corrected three-way dispatch arm (3) from "Integer/Float/Bool column → **E-QUERY-001**" to "**E-QUERY-002 (QueryTypeMismatch)**". Aligns to error-taxonomy.md v2.12 (E-QUERY-002 QueryTypeMismatch) and ADR-052 §D4 v1.5. Datetime→E-QUERY-041 and String/Utf8→COERCE arms UNCHANGED. All other §Error Cases E-QUERY-001 entries (unknown pipe stage keyword, invalid aggregation function, head/tail non-integer argument) are for distinct non-temporal conditions and are UNCHANGED. |
| 1.9 | ADR-052-d4-is-date-like-canonical-ref | 2026-07-04 | product-owner | Align `is_date_like` enumeration to ADR-052 §D4 v1.4 canonical 7-form set (reference, not re-enumeration). §Postconditions D2 bullet and §Error Cases E-QUERY-041 condition: replaced stale 2-form partial enumeration ("date-only and offset-less datetime") with reference to BC-2.11.021 §Error Cases / ADR-052 §D4 `is_date_like` Acceptance Set; two representative examples retained. Three-way dispatch (Datetime→E-QUERY-041; String/Utf8→coerce; numeric/bool→E-QUERY-001) unchanged. |
| 1.8 | ADR-052-d4-v1.3-bc-amendment | 2026-07-04 | product-owner | **ADR-052 §D4 v1.3 amendment (human-ratified 2026-07-04, Option A — lenient-parse-then-AST-walk + String-column coercion modification).** E-QUERY-041 detection mechanism redesigned from chrono plan-time pre-validator (v1.7) to `Literal::RawTemporalLiteral` AST node + `check_temporal_literals` plan-time walker. **Changes:** §Postconditions ADR-052 D2 bullet: detection description updated — parser emits `RawTemporalLiteral` (parse succeeds), `check_temporal_literals` three-way dispatch: (1) Datetime col → E-QUERY-041; (2) String/Utf8 col → COERCE to `Literal::String(s)` (SUCCESS, byte-identical to pre-ADR-052); (3) Integer/Float/Bool col → E-QUERY-001. §Error Cases E-QUERY-041: condition updated to AST-walk mechanism; added "Does NOT fire for String/Utf8 columns" note. §Edge Cases: EC-11-004-001 mechanism updated; EC-11-004-002 mechanism updated; EC-11-004-003 ADDED (String-column coercion → success in `| where` stage). §Canonical Test Vectors: E-QUERY-041 and RFC-3339 vector descriptions updated; String-column coercion vector ADDED. Message format (POL-24), column type assertion (D2), write parser extension (S-3.06), and security invariants UNCHANGED. RISK-5 eliminated by design. |
| 1.7 | ADR-052-bc-amendment-burst | 2026-07-03 | product-owner | **ADR-052 v1.1 correction (remove-uncertainty PASS-1 amendments).** §Postconditions ADR-052 D2 bullet: corrected E-QUERY-041 detection mechanism from "DataFusion cannot implicitly cast" to Prism plan-time literal pre-validator using `chrono::DateTime::parse_from_rfc3339` strictness — arrow-cast 58.2.0 is LENIENT (accepts date-only and offset-less strings via coercion); Prism must validate at parse/plan time before DataFusion sees the query. Added: same chrono strictness applies at sensor-boundary datetime parsing (AC-013 consistency). Postcondition `| where` example updated to `arrow_cast(...)` form. Error Cases E-QUERY-041: trigger condition corrected to "Prism plan-time pre-validator rejects" (not "DataFusion cannot cast"); offset-less ISO form example added. Edge cases EC-11-004-001/002: mechanism descriptions updated (chrono pre-validator, not DataFusion implicit cast). Test vectors: descriptions updated to reflect pre-validator language. |
| 1.6 | ADR-052-bc-amendment-burst | 2026-07-03 | product-owner | **ADR-052 amendment (ratified 2026-07-03).** §Postconditions: added ADR-052 D2 datetime column Arrow type assertion — `ColumnType::Datetime` registers as `DataType::Timestamp(Microsecond, Some("UTC"))` (not `Utf8`); typed Timestamp-vs-Timestamp comparison in `| where` stages; bare string literal cast failure returns `Err(E-QUERY-041)`. Error Cases: E-QUERY-041 `TemporalLiteralUnparseable` added. Edge Cases: EC-11-004-001 (date-only bare string → E-QUERY-041) and EC-11-004-002 (valid RFC-3339 UTC string → OK) added. Canonical Test Vectors: two E-QUERY-041 / RFC-3339 vectors added. inputs: ADR-052 file added. |
| 1.5 | bundle-a.2.2 | 2026-05-08 | state-manager | POL-14 promotion: draft → active. S-3.06 flipped to merged (D-304 / Bundle A.2). |
| 1.4 | pre-impl-amendments | 2026-05-06 | product-owner | AMENDMENT 4 — three S-3.06 implementer gaps: (a) write verb case-insensitive matching specified (normalize to lowercase on insert+lookup, consistent with SQL conventions); (b) E-QUERY-022 added to Error Cases table (unbounded DML write rejected at parse time); (c) INV-FILTER-EMPTY-REGISTRY specified (empty WriteVerbRegistry → reject_write_verbs_in_filter always Ok(())). Also added E-QUERY-010/023/024 rows to Error Cases for completeness. Three new invariants added to §Invariants. |
| 1.3 | pass-73-fix | 2026-04-20 | state-manager | Deterministic changelog reorder: sorted all rows to descending version order (pass-73 bash script). |
| 1.2 | pass-69-housekeeping | 2026-04-20 | product-owner | Normalized changelog schema to canonical 5-col schema. |
| 1.1 | pre-build-sweep | 2026-04-20 | product-owner | Template-compliance sweep: added extracted_from/inputs/input-hash/traces_to frontmatter; added ## Description synthesized from body; added ## Canonical Test Vectors scaffolding; added ## Verification Properties cross-ref; added ## Changelog. |
| 1.0 | cycle-1 | 2026-04-14 | product-owner | Initial contract |
