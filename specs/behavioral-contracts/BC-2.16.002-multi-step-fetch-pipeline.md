---
document_type: behavioral-contract
level: L3
version: "2.13"
status: active
producer: product-owner
timestamp: 2026-04-14T00:00:00Z
phase: 1a
origin: greenfield
subsystem: "SS-16"
capability: "CAP-029"
lifecycle_status: active
introduced: cycle-1
modified: "2026-07-28"
deprecated: ~
deprecated_by: ~
replacement: null
retired: null
removed: null
removal_reason: null
inputs:
  - ".factory/specs/prd.md"
  - ".factory/specs/domain-spec/capabilities.md"
input-hash: "a21f92e"
traces_to:
  - "CAP-029"
extracted_from: ".factory/specs/prd.md"
---

# BC-2.16.002: Multi-Step Fetch Pipeline Execution — Sequential Steps with Variable Interpolation

## Description

Config-driven sensor tables may require multi-step fetch sequences where each step's
output feeds into subsequent steps via variable interpolation. Steps execute sequentially
in spec-declared order; variables produced by a step are available to all later steps
but not earlier ones. When a variable resolves to an array, the step is executed in
batches (default batch size 100), with all batches concatenated into a single result set.

The final step's response records are collected into an Arrow RecordBatch using the
table's column definitions. The 10K materialization limit (DI-019) applies to the
final collected records, not intermediate step results. Rate limit hints from the
`SensorSpec` apply between API calls.

## Preconditions
- A spec-driven table has been registered (BC-2.16.001) with one or more `FetchStep` entries in its `steps` array
- A query targeting this table has been dispatched by the query engine (CAP-015)
- An `AuthProvider` implementation is available to resolve credentials for the sensor's `auth_type`. `PipelineExecutor::execute` invokes `AuthProvider::acquire_token` EAGERLY at pipeline start for any sensor whose `auth_type` requires credentials (currently: all real `AuthType` variants — `Oauth2ClientCredentials`, `BearerStatic`, `CookieRoundtrip`, `ApiKey`). The acquired token is used on the FIRST HTTP request of the pipeline. If a subsequent HTTP request receives a 401-Unauthorized, `AuthProvider::acquire_token` is invoked again (refresh) and the failed step is retried once. If the retry also returns 401, the pipeline aborts with `SpecEngineError::AuthRefreshFailed`.
  - **Rationale (F-LP5-LOW-003 closure):** The prior lazy-token-on-401 design forced a guaranteed 401 round-trip on every production execution against bearer-auth APIs, polluting the audit signal (every legitimate execution emitted `auth_refresh_triggered`) and inflating both `request_count` and API quota usage. Eager-token acquisition restores the audit signal to its intended semantic ("refresh event = rare mid-pipeline token expiry") while preserving the 401-retry path for genuine token expiry events.

## Postconditions
- Steps are executed sequentially in the order defined in the spec's `[[table.steps]]` array
- Each step produces an HTTP request using:
  - `method`: GET or POST as declared
  - `path_template`: interpolated against variables from prior steps and query parameters (e.g., `${query_ids.resource_ids}` resolves to the `resource_ids` field from the step named `query_ids`)
  - `body_template` (if present): interpolated using `JsonBody` escaping context (`InterpolationContext::JsonBody` via `Interpolator::interpolate`), then sent as the request body. The escaping strategy differs from `path_template` — see the **Interpolation Escaping Contract** postcondition below.
- **Interpolation Escaping Contract** — `Interpolator::interpolate` applies context-specific escaping to every resolved variable value before substitution:
  - **`UrlPath` context** (used for `path_template`): the resolved value is converted to its string representation via `value_to_string` (String → raw chars; Number/Bool/Null → `to_string()`; Array/Object → compact JSON via `serde_json::Value::to_string()`), then **percent-encoded** per RFC 3986 unreserved charset (`[A-Za-z0-9._~-]`). All other characters — including space (`%20`), `&` (`%26`), `=` (`%3D`), `/` (`%2F`), `"` (`%22`) — are encoded. Array serialization encodes the surrounding brackets and inner quotes, making `["a","b"]` into `%5B%22a%22%2C%22b%22%5D`.
  - **`JsonBody` context** (used for `body_template`): `Interpolator::interpolate` dispatches on the `serde_json::Value` type in its `JsonBody` arm with **three distinct branches** (confirmed against the `Interpolator::interpolate` `JsonBody` match arm in `prism-spec-engine/src/interpolation.rs`):
    - **`Array` | `Object`**: inserted **verbatim** via `value.to_string()` — no `json_escape` post-processing. The result is a **valid JSON literal** that can be embedded directly in a `body_template` string. Example: a variable resolving to `["id1","id2"]` produces the literal text `["id1","id2"]` in the request body, making `'{"ids": ${query_detection_ids.resources}}'` yield the valid JSON `{"ids":["id1","id2"]}`. This is the mechanism that allows spec authors to embed array-valued step variables directly in `body_template` fields.
    - **`String` scalar**: passed through `Interpolator::json_escape`, which escapes `"` → `\"`, `\` → `\\`, `\n` → `\n`, `\r` → `\r`, `\t` → `\t`, control chars `U+0000`–`U+001F` → `\uXXXX`. The result is safe for embedding inside a JSON string context (e.g., `"key": "${step.field}"`).
    - **`Number` | `Bool` | `Null`**: passed through `value_to_string` → verbatim JSON literal (`42`, `true`, `null`). No special escaping needed — these types contain no special characters in their `to_string()` forms.
  - **`${params.KEY}` write params path** (`Interpolator::interpolate_write_params`): operates on pre-stringified `params: &HashMap<String, String>` values and applies `json_escape` (JsonBody) or `percent_encode` (UrlPath) identically to the main `Interpolator::interpolate` path. These are always string scalars (the map value type is `String`); no Array or Object type can appear on this path.
  - **`${record_ids}` write-side path** (`Interpolator::interpolate_record_ids`): the `JsonBody` arm calls `serde_json::to_string(record_ids)` directly — no `json_escape` post-processing. This produces a verbatim compact JSON array (e.g. `["id1","id2"]`) that is a valid JSON literal. This function is distinct from `Interpolator::interpolate` and handles only the `${record_ids}` placeholder in write-endpoint templates (S-1.13). It is NOT available in fetch-pipeline `path_template` or `body_template` steps.
  - **Verification**: a string value containing `"` (e.g. `abc"def`) must produce `abc\"def` in `body_template` and `abc%22def` in `path_template`. A number value `42` must produce `42` in both contexts. An array `["id1","id2"]` in a fetch-pipeline `body_template` variable must produce `["id1","id2"]` (verbatim valid JSON literal — Array branch of `Interpolator::interpolate` `JsonBody` arm calls `value.to_string()` directly with NO `json_escape`). In `path_template` (UrlPath context) the same array produces `%5B%22id1%22%2C%22id2%22%5D` (compact JSON string then percent-encoded). These assertions are load-bearing in `test_BC_2_16_002_interpolator_*` suite; the context dispatch lives in `Interpolator::interpolate` (`prism-spec-engine/src/interpolation.rs`). Function-level behavioral anchor (TD-VSDD-091): the three interpolation entry points are `Interpolator::interpolate` (fetch-pipeline `path_template` and `body_template`), `Interpolator::interpolate_record_ids` (`${record_ids}` write-side), and `Interpolator::interpolate_write_params` (`${params.KEY}` write-side).
- Variable interpolation uses the syntax `${step_name.field}` where:
  - `step_name` is the `name` attribute of a prior step
  - `field` is a JSONPath-like dot-notation path into that step's response (e.g., `${query_ids.resources[*].id}` extracts all IDs from the array)
  - Array-valued variables trigger fan-out: the step is executed once per batch of values (batch size configurable per step, default 100)
- Each step's response is parsed according to:
  - `response_path`: a JSONPath expression pointing to the results array in the JSON response (e.g., `$.resources`, `$.data.items`)
  - `pagination_cursor_path` (if present): a JSONPath expression pointing to the pagination cursor in the response for automatic page iteration
  - `variables_produced`: a list of variable names that downstream steps can reference
- Pagination within a step follows the sensor spec's declared pagination config (cursor/offset/none), iterating until the API returns an empty page or the cursor is null
- **OffsetLimit Pagination Dispatch: POST-body vs GET-URL (DRIFT-D850-001, S-DEMO-CLAROTY-PAGINATION-001)** — When a step's `PaginationConfig` is `OffsetLimit`, the transport form of `offset` and `limit` parameters is determined by the step's HTTP method:
  - **GET (or method absent — defaults to GET):** `build_paged_url_impl` appends `?offset={offset}&limit={page_size}` to the request URL as query parameters. The body is unchanged (GET requests carry no body). This is the existing behavior for Cyberint (cursor-based), Armis, and CrowdStrike sensors that do not use OffsetLimit+POST.
  - **POST:** `build_paged_url_impl` returns the base URL unchanged — no `?offset=` or `&limit=` is appended to the URL. Instead, the request-issuing site (`issue_request_with_retry` → `build_request`) injects `offset` and `limit` as top-level integer keys into the POST request body. The injection merges into the step's interpolated `body_template` JSON object: the final body is `{ ...existing_body_template_keys, "offset": <u32>, "limit": <u32> }`. If `body_template` is absent or `{}`, the result is `{"offset": 0, "limit": page_size}` on the first page. Keys from `body_template` that conflict with `offset`/`limit` are overwritten by the pagination values. If `body_template` is present but not a valid JSON object (i.e., cannot be parsed as `serde_json::Map`), the pipeline returns `Err(SpecEngineError)` with `sensor_id` and `step_name` context; no partial result is returned. This behavior implements multi-page support for the Claroty xDome API (`devices`, `alerts`, `audit_logs` tables) where all read endpoints use `POST` and expect pagination params in the body — consistent with `crates/prism-dtu-claroty/src/types.rs` `GetAlertsBody`, `GetDevicesBody`, and `GetAuditLogBody` structs which declare `offset: Option<u32>` and `limit: Option<u32>` as body fields. Grounding: ADR-028 §D1 (TOML `method` field is the source of truth for HTTP method dispatch); Claroty xDome API POST-for-read pattern. Dispatch logic lives in `pipeline.rs`:`build_paged_url_impl` (URL side) and `build_request` (body injection side).
  - **Offset advancement is method-agnostic:** After each page, `offset += page_size` regardless of HTTP method. The loop termination condition (empty page: `page_record_count < page_size`) is likewise method-agnostic and must not be changed by the dispatch logic.
  - **First page:** `offset = 0`, `limit = page_size` on both GET and POST paths.
  - **Edge case — POST with empty body_template (`{}`):** Produces `{"offset": 0, "limit": page_size}` on first page. Correct behavior per AC-001 and EC-001 of S-DEMO-CLAROTY-PAGINATION-001.
  - **Edge case — GET with OffsetLimit (regression guard):** URL params appended as before. No body change (GET has no body). Existing Cyberint/Armis/CrowdStrike sensors must not regress. This postcondition is the contract anchor for Red Gate test `test_BC_2_16_002_pagination_get_method_continues_url_params` (AC-002 of S-DEMO-CLAROTY-PAGINATION-001).
- **PageNumber Pagination Dispatch: POST-body vs GET-URL (ADR-056, S-WAVE-A-CYBERINT-SPEC-001)** — When a step's `PaginationConfig` is `PageNumber`, the transport form of `page` and `size` parameters is determined by the step's HTTP method:
  - **POST:** `build_paged_url_impl` returns the base URL unchanged. `build_request` injects `"page"` and `"size"` as top-level integer keys into the POST request body, merged onto the interpolated `body_template`. The injected `page` value is the current 0-based loop index (`offset`) plus one (`offset + 1`); the first request injects `"page": 1`. The injected `size` value equals `page_size`. Merge semantics and error paths are identical to `OffsetLimit` POST dispatch: absent or empty `body_template` (`{}`) produces `{"page": 1, "size": page_size}` on the first request; a `body_template` that interpolates to a non-object JSON value returns `Err(SpecEngineError)` with `sensor_id` and `step_name` context (EC-002 path).
  - **GET (or method absent — defaults to GET):** `build_paged_url_impl` appends `?page={offset+1}&size={page_size}` to the URL as query parameters. The `?` vs `&` separator is determined by the existing `contains('?')` check, consistent with all other non-POST arms of `build_paged_url_impl`.
  - **Page counter:** The loop reuses the `offset: u32` state variable as a 0-based page index. The wire `page` value is always `offset + 1`. The first request emits `page = 1`.
  - **Advance rule:** `offset += 1`. Each page increments the page number by exactly 1. The advance is method-agnostic (applies regardless of POST vs GET). This is distinct from `OffsetLimit`'s `offset += page_size` and MUST NOT be confused with it.
  - **Termination:** `if page_record_count < page_size { break }` — identical to `OffsetLimit`.
  - **Activation gate:** `build_request` injects `page` and `size` only when `page_size > 0`. This guard serves the **engine-internal sentinel case**: `execute_step` (the single-request path, distinct from the pagination loop) passes `active_page_size = 0` to `build_request` to signal that no pagination injection is active. A TOML spec declaring `page_size = 0` is **rejected at spec-load time** by `validate_sensor_spec` §Category 4 with `SpecErrorCode::ESpec001` (§D3 spec-load layer; §D10 CE-2 validation obligation) — the value `0` cannot reach `build_request` from a validated spec. The `page_size > 0` guard in `build_request` is defense-in-depth for the engine-internal sentinel path only. Anchored: `S-WAVE-A-CYBERINT-SPEC-001` RG-017 (`validate_sensor_spec` `PageNumber { page_size: 0 }` spec-load rejection test).
  - **First page, empty `body_template`:** POST body becomes `{"page": 1, "size": page_size}`.
  - Grounding: ADR-056 §D3 (dispatch shape); `cyberint_alerts_openapi_06.20.2026.json` `GetAlertsRequest` schema (`page` field minimum 1; `size` field).
- The final step's response records are collected into an Arrow RecordBatch using the table's column definitions
- Rate limit hints from the `SensorSpec` are applied between API calls: inter-request delay = `1 / requests_per_second`, with burst allowance from `burst_size`
- **Adapter abstraction** — The auth-resolution mechanism is provided via a dyn-compatible `AuthProvider` trait (defined in `prism-spec-engine/src/auth_provider.rs`). `PipelineExecutor` accepts `&dyn AuthProvider`; the trait is object-safe (`Send + Sync` + manually-boxed Future return type per Rust stable RPITIT limitations). This enables sensor-spec-driven adapter dispatch at runtime, replacing compile-time-keyed `SensorAuth` enum dispatch.
- **Record truncation** — When the cumulative `PipelineResult.records.len()` would exceed the DI-019 cap of 10,000, execution truncates the final-step accumulator to exactly 10,000 records and sets `PipelineResult.truncated = true`. The truncation flag is the user-facing signal that data was lost; it does NOT propagate to the per-step `request_count`. The outer materialization-layer cap (in `prism-query/src/materialization.rs`) does NOT double-apply when the executor cap fires.
- **Request count semantics (v1.5)** — `PipelineResult.request_count` is the number of HTTP requests issued by the pipeline steps (NOT including `AuthProvider::acquire_token` calls, which use the AuthProvider's own transport). With the v1.5 eager-token semantic, a single-step single-page pipeline produces `request_count == 1` (not 2 as in v1.4, where a 401 probe request was required before the token was acquired).
- **Auth initial acquisition audit signal (v1.5, superseded by Structured Event Catalog v1.8)** — `PipelineExecutor::execute` and `PipelineExecutor::execute_step` each emit exactly one of three `tracing` events when `AuthProvider::acquire_token` is called eagerly at pipeline start. The field schema differs between the two entry points: `execute()` omits `step_name` (pipeline-level call site); `execute_step()` includes `step_name` (per-step call site). Full field-level enumeration and trigger conditions are in the **Structured Event Catalog (v1.8)** postcondition below. Token value is NEVER included in any event.
- **Auth refresh audit signal (superseded by Structured Event Catalog v1.8)** — When `AuthProvider::acquire_token` is invoked on a 401 retry (mid-pipeline token expiry), `issue_request_with_retry` emits one of four `auth_refresh_*` events (triggered, succeeded, failed, double_401). All four include `step_name` because they fire from within a step execution context. Full field-level enumeration is in the **Structured Event Catalog (v1.8)** postcondition below. Token value is NEVER included in any event. This satisfies VP-PLUGIN-005 assertion (d) (ADR-023 §E).
- **Partial-record discard on mid-pipeline HTTP failure** — When any fetch step's HTTP request fails with a non-401 non-200 status (e.g., 500, 503, network timeout, JSON parse error, page-cap exceeded, cursor non-advance), `PipelineExecutor::execute` returns `Err(SpecEngineError::HttpRequestFailed{...})`. The `PipelineResult` is NOT returned to the caller. ALL records accumulated from prior successfully-completed steps are discarded. This is the "all-or-nothing" semantic: callers must not assume partial data on Err return. Rationale: a partial PipelineResult could mislead downstream OCSF mappers into producing schema-mismatched rows; explicit Err propagation forces the caller to handle the failure mode. The 401-retry path is the exception (handled internally per the auth-refresh postcondition family).
- **Canonical Structured Event Catalog (v1.62)** — This catalog is the single source of truth for every `tracing::*!(event_type=…)` site within the `prism-spec-engine` crate (including `PipelineExecutor`, plugin runtime emissions via `PluginRuntime` and `host_functions::host_http_request`, and `HttpLookupSource` infusion HTTP enrichment) AND `prism-bin` boot-step plugin-load emissions (step 7.5 per ADR-023 §C4) AND `prism-bin` boot step 7/8/9/10/11 completion events (S-3.02-FOLLOWUP-RUNTIME, S-5.01-FOLLOWUP-MCP-BOOT) AND `prism-query` plugin write-tool registration (per ADR-026 D7) AND `prism-query` push-down time-window analysis (`extract_time_window_from_ast`, S-DEMO-QUERY-PUSHDOWN-001 EC-003) AND `prism-query` hot-reload overlay rebuild emissions (`rebuild_resolved_spec_map`, ADR-042 — reload.overlay_rebuild_failed, reload.overlay_rebuilt) AND `prism-ocsf` OCSF normalization emissions (SpecDrivenMapper field mapping, WASM dispatch, timestamp parse, construction-time empty config, deprecated class alias) AND `prism-mcp` MCP tool audit emissions (mcp.tool.called, mcp.tool.rejected, mcp.server.shutdown.*) AND `prism-mcp` schema-enumeration tool emissions (schema_enumeration.started, schema_enumeration.rejected, schema_enumeration.success — BC-2.10.012 `prism_describe`) AND `prism-bin` boot-local write audit stub emissions (write.intent.recorded, write.outcome.recorded) AND `prism-bin` step 9 alias-store boot emissions (boot.step9.alias_store_loaded, boot.step9.alias_store_load_failed, boot.step9.alias_store_empty) AND `prism-bin` `RunningServer::wait_for_shutdown` shutdown-outcome emissions (boot.shutdown.timeout, boot.shutdown.runtime_error, boot.shutdown.transport_init_error, boot.shutdown.task_panic) AND `prism-bin` boot step 9A adapter-registry population emission (boot.step9a.adapter_registry_populated, S-DEMO-001) AND `prism-query` plan-time column gate rejection (column_not_found.rejected, BC-2.11.016, S-DEMO-PRISMQL-ONBOARDING-001-B) AND `prism-spec-engine` ENRICH-1 source_path extraction failure (column_source_path_extraction_failed, S-DEMO-ENRICH-1 — two emission sites: `ColumnMapper::map_record` in `column_mapping.rs` and `build_column_array::extract_raw` in `spec_driven_adapter.rs`, both emitting identical schema). **Events and crates outside this catalog's scope:** `prism-security` and `prism-credentials` own their own structured event catalogs governed by their respective BCs — `capability_check` emissions from `prism-security/src/flag_audit.rs` are governed by BC-2.04.013; `credential_access` emissions from `prism-credentials/src/audit.rs` are governed by BC-2.03.010. New `event_type` sites in those crates MUST be registered in those BCs, not here. Additionally, `boot.audit.initialized` (boot step 6, `prism-bin`) is governed at event granularity by BC-2.05.012 §`boot.audit.initialized` Sentinel Event Schema — that BC provides the field schema and the load-bearing test (`bc_2_05_012_audit_init.rs`); note that `prism-bin` is otherwise in scope for this catalog (boot steps 7/8/9/10/11), so this exclusion applies at event granularity for step-6 only, not the entire crate. New `event_type` sites for boot step 6 audit initialization MUST be registered in BC-2.05.012, not here. PG-LP11-001 requires that new `event_type` sites added to ANY of the in-scope crates above MUST be enumerated here as a BC amendment in the same commit as the implementation. The catalog currently contains 90 structured events. Each event sets `event_type` as a structured field. All events are emitted via `tracing` macros; no event includes a token value. (Path B adjudication, fix-burst-8 stage 1: BC-2.16.002's prior narrow scope — "PipelineExecutor and helpers / pipeline.rs" — was a historical artifact from when only PipelineExecutor emitted structured events. PG-LP11-001 always intended a universal catalog; Path B aligns the scope statement to that de-facto role, avoiding new BC IDs (Path A cost) and catalog scatter across 4 BCs (Path C cost). Scope extended in PLUGIN-MIGRATION-001-C pass-1 fix-burst to include prism-ocsf normalization emissions. Scope extended in S-DEMO-ENRICHMENT-PIVOT-002 to include HttpLookupSource infusion emissions. Scope extended in S-DEMO-PRISMQL-ONBOARDING-001-A D-1262 to include prism-mcp schema_enumeration emissions from `prism_describe` tool. Scope extended in S-DEMO-PRISMQL-ONBOARDING-001-A D-1270 to include prism-query hot-reload overlay rebuild emissions from `rebuild_resolved_spec_map`, ADR-042. Scope extended in S-DEMO-ENRICH-1 to include `prism-spec-engine` and `prism-bin` ENRICH-1 source_path extraction failure emissions from `column_mapping.rs` and `spec_driven_adapter.rs`. Scope extended in ENRICH-4-B to include `prism-query` pipe-to-SQL lowering and planning-error emissions (`pipe.sql_lowering`, `pipe.sql_planning_error`) from `execute_against_session` in `crates/prism-query/src/materialization.rs`. Scope extended in S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 to include `prism-query` filter-mode SQL lowering and planning-error emissions (`filter.sql_lowering`, `filter.sql_planning_error`) from `execute_against_session` in `crates/prism-query/src/materialization.rs`. Scope extended in S-DEMO-ENRICHMENT-TYPED-OUTPUT-001 to include `prism-query` infusion UDF coercion-failure emissions (`infusion.coercion_failed`) from `InfusionAsyncUdf::coerce_to_typed` in `crates/prism-query/src/infusion_udf.rs`. Scope extended in S-PRISMQL-CASE-INSENSITIVE-001 to include `prism-ocsf` OCSF enum-label unrecognized-value warning emissions (`ocsf.enum_label_unrecognized`) from `OcsfNormalizer::normalize_with_mappers` in `crates/prism-ocsf/src/normalizer.rs`. Scope extended in S-PRISMQL-CASE-INSENSITIVE-001 architectural adjudication 2026-07-07 to additionally include `prism-bin` spec-driven adapter enum-label normalization emissions (`ocsf.enum_label_unrecognized`) from `build_column_array` in `crates/prism-bin/src/spec_driven_adapter.rs` (PRIMARY production insertion point — the spec-driven adapter materialization path through which DataFusion receives and queries sensor data; row 91 now lists both PRIMARY and SECONDARY emission sites; catalog count unchanged at 91). Scope extended in DEFECT-CSDEVICES-EMPTY-PIPELINE-001 to include `prism-query` SQL-mode DataFusion planning-error emission (`sql.sql_planning_error`) from `execute_against_session_with_registry` in `crates/prism-query/src/materialization.rs` (F-CSD-P20-010). Catalog count 91→92. Catalog count 92→90 (FB84: removed orphan rows `boot.type_spec_read_failed` and `boot.type_spec_parse_failed` — `build_type_spec_map_for_overlay` was refactored away; replacement propagates `Err(BootError::ConfigInvalid)` providing audit trail without catalog overhead per D-765 precedent (SAP-1 rule 5). Event-granularity exclusion added for `boot.audit.initialized` step-6 — governed by BC-2.05.012 §`boot.audit.initialized` Sentinel Event Schema, avoiding dual governance. Catalog label `(v1.61)` → `(v1.62)`.)

| event_type | level | function | fields (beyond event_type) | trigger condition |
|---|---|---|---|---|
| `auth_initial_acquired` | info | `PipelineExecutor::execute` | `sensor_id`, `client_id` | `acquire_token` returns `Ok(tok)` where `tok` is non-empty; eager call at pipeline start before the steps loop |
| `auth_initial_acquired_empty` | debug | `PipelineExecutor::execute` | `sensor_id`, `client_id` | `acquire_token` returns `Ok(tok)` where `tok` is empty string; typically `NullAuthProvider` (test-only) or a buggy production provider; pipeline continues with empty credential |
| `auth_initial_failed` | error | `PipelineExecutor::execute` | `sensor_id`, `client_id`, `detail` | `acquire_token` returns `Err`; pipeline aborts immediately, no fetch steps attempted |
| `auth_initial_acquired` | info | `PipelineExecutor::execute_step` | `sensor_id`, `client_id`, `step_name` | Same Ok-non-empty outcome as execute() variant; includes `step_name` because execute_step is a per-step entry point (field schema differs from the execute() emission) |
| `auth_initial_acquired_empty` | debug | `PipelineExecutor::execute_step` | `sensor_id`, `client_id`, `step_name` | Same Ok-empty outcome as execute() variant; includes `step_name` |
| `auth_initial_failed` | error | `PipelineExecutor::execute_step` | `sensor_id`, `client_id`, `step_name`, `detail` | Same Err outcome as execute() variant; includes `step_name` |
| `auth_refresh_triggered` | warn | `issue_request_with_retry` (called by both execute and execute_step) | `sensor_id`, `client_id`, `step_name` | HTTP 401 received on first attempt; refresh path entered; `acquire_token` about to be called again |
| `auth_refresh_succeeded` | info | `issue_request_with_retry` (called by both execute and execute_step) | `sensor_id`, `client_id`, `step_name` | `acquire_token` on refresh path returns `Ok`; fresh token acquired; retry will proceed |
| `auth_refresh_failed` | error | `issue_request_with_retry` (called by both execute and execute_step) | `sensor_id`, `client_id`, `step_name`, `detail` | `acquire_token` on refresh path returns `Err`; pipeline aborts |
| `auth_refresh_double_401` | error | `issue_request_with_retry` (called by both execute and execute_step) | `sensor_id`, `client_id`, `step_name` | Retry after refresh also returns 401; pipeline aborts with `SpecEngineError::AuthRefreshFailed`. NOTE: this event is only emitted for refreshable auth types (e.g., `Oauth2ClientCredentials`). `CookieRoundtrip` sensors emit `cookie_auth_401` instead (no retry). |
| `cookie_auth_401` | warn | `issue_request_with_retry` (called by both execute and execute_step) | `sensor_id`, `client_id`, `step_name` | HTTP 401 received on a `CookieRoundtrip` auth sensor. No retry attempted — the API key is static; calling `acquire_token()` again would re-read the same key, making retry provably futile (BC-2.01.017 EC-017-002). Pipeline aborts immediately with `SpecEngineError::CookieAuthFailed` (E-AUTH-004). Audit role: operational incident — the static cookie/API key is invalid, expired, or revoked; operator must update the credential in the keyring. Recurrence: one emission per pipeline where a `CookieRoundtrip` step receives HTTP 401. Retention: per organization audit policy. Registered: F-PR2-MED-001 (S-DTU-CYBERINT-AUTH-FIDELITY-001 PR-level fix-burst). |
| `pipeline_truncated` | warn | `PipelineExecutor::execute` (records accumulation loop) | `sensor_id`, `client_id`, `step_name`, `max_records`, `accumulated` | Cumulative `all_records.len()` reaches or exceeds the DI-019 cap of 10,000; records truncated to 10K and `PipelineResult.truncated` set true |
| `pagination_cursor_unsupported_type` | warn | `extract_cursor` (called from execute pagination loop) | `cursor_path`, `actual_type`, `cursor_preview` | Cursor value at `cursor_response_path` resolves to Array, Object, or Bool rather than String or Number; pagination treated as terminal |
| `fanout_invalid_source_type` | warn | `find_fan_out_array` (called from execute fan-out detection) | `step_name`, `var_name`, `actual_type` ("Object") | A template variable reference resolves to an Object-typed value; will be stringified into URL or body; likely a spec authoring bug |
| `fanout_ambiguous_multi_array` | warn | `find_fan_out_array` (called from execute fan-out detection) | `step_name`, `array_vars_count`, `first_var`, `other_vars` | Two or more array-valued variables are referenced in a step's templates; only the first array drives batching; semantics are ambiguous |
| `jsonpath_extraction_failed` | warn | `PipelineExecutor::execute` and `PipelineExecutor::execute_step` — JSON extraction call sites for a step's response_path | `sensor_id`, `step_name`, `path`, `detail` | `extract_at_path` fails for a step's `response_path` (e.g., path not found, bracket index out of bounds, malformed JSONPath syntax); `detail` carries the descriptive error string from `extract_at_path` |
| `jsonpath_size_cap_exceeded` | warn | `extract_with_tokens` — wildcard enumeration recursive descent | `path`, `max_size` | Nested wildcard extraction (e.g., `$.a[*].b[*]`) would exceed `MAX_JSONPATH_RESULT_SIZE` = 100,000 total elements before completion; extraction aborted and `Err` returned to caller |
| `plugin_load_unsigned` | warn | `PluginRuntime::load_all_plugins` | `plugin_path`, `plugin_hash` | Each successfully loaded plugin emits this event at WARN (v1.0 unsigned; audit-channel routing encoded by `event_type` field per ADR-023 §C4) |
| `plugin_load_disabled_via_envvar` | warn | `boot::plugin_load_step` (`prism-bin/src/boot.rs` step-7.5 function) | `env_var: "PRISM_DISABLE_PLUGIN_LOAD"` | `PRISM_DISABLE_PLUGIN_LOAD=1` detected at boot before plugin-load step; plugin loading administratively disabled; emitted before skip to preserve DI-004 audit completeness |
| `plugin_load_failed_manifest_no_allowed_urls` | error | `PluginRuntime::load_plugin` | `plugin_path`, `error: E-PLUGIN-013` | Plugin manifest missing required `allowed_urls` field; plugin rejected; remaining plugins continue loading |
| `plugin_load_failed_format_version_exceeded` | error | `PluginRuntime::load_plugin` | `plugin_path`, `format_version`, `max_supported` | Plugin `format_version` exceeds `CURRENT_SUPPORTED_VERSION`; plugin rejected; remaining plugins continue loading |
| `plugin_load_failed_manifest_name_missing` | error | `PluginRuntime::load_plugin` | `plugin_path`, `error: E-PLUGIN-015` | Manifest `name` field absent or empty string; plugin rejected at manifest gate before WIT compilation; remaining plugins continue loading (n-1 survivor) |
| `plugin_load_failed_manifest_version_malformed` | error | `PluginRuntime::load_plugin` | `plugin_path`, `version_value`, `error: E-PLUGIN-016` | Manifest `version` field absent or not valid semver; plugin rejected at manifest gate before WIT compilation; remaining plugins continue loading (n-1 survivor) |
| `plugin_load_failed_wit_invalid` | error | `PluginRuntime::load_plugin` | `plugin_path`, `missing_export`, `error: E-PLUGIN-001` | WIT validation failure — plugin component is missing one or more required WIT exports; plugin rejected; remaining plugins continue loading |
| `plugin_http_request_blocked` | warn | `host_http_request` (`prism-spec-engine/src/plugin/host_functions.rs`) | `plugin_id`, `url`, `reason: allowlist_mismatch` | Plugin attempted an outbound HTTP request to a URL not present in its manifest `allowed_urls` list; request blocked; plugin execution continues |
| `pipeline_max_requests_exceeded` | error | `PipelineExecutor` executor loop | `sensor_id`, `total_requests`, `max: MAX_REQUESTS_PER_PIPELINE` | Cumulative HTTP request count across all pipeline steps reaches `MAX_REQUESTS_PER_PIPELINE` (10,000); pipeline aborts; no further steps attempted |
| `plugin_directory_not_found` | info | `PluginRuntime::load_all_plugins` | `plugin_dir` | Plugin directory does not exist at the configured path; boot continues with zero plugins loaded (EC-D-001: non-error, expected on first-time or plugin-less deployments) |
| `plugin_load_failed_read_error` | error | `PluginRuntime::load_all_plugins` | `plugin_path`, `error` | Filesystem read of a `.prx` file failed (permissions, I/O error); plugin is skipped; remaining plugins continue loading (n-1 survivor) |
| `plugin_load_failed_compilation` | error | `PluginRuntime::load_all_plugins` | `plugin_path`, `error: E-PLUGIN-008`, `message` | WASM Component Model compilation of a `.prx` file failed (corrupt binary, bad magic, component wrapping failed); plugin is skipped; remaining plugins continue loading (n-1 survivor) |
| `plugin_load_failed_manifest_not_found` | error | `PluginRuntime::load_all_plugins` | `plugin_path`, `expected_manifest_path`, `error: E-PLUGIN-018` | Plugin `.prx` file found but no companion `.manifest.toml` exists at expected path; plugin rejected (n-1 survivor) |
| `plugin_load_failed_manifest_parse_error` | error | `PluginRuntime::load_all_plugins` | `plugin_path`, `error: E-PLUGIN-017`, `detail` | Companion `.manifest.toml` is present but fails TOML parse; plugin rejected (n-1 survivor); distinct from E-PLUGIN-015 which applies only when TOML parses but `name` field is absent |
| `plugin_load_failed_format_version_missing` | error | `PluginRuntime::load_all_plugins` | `plugin_path`, `supported`, `error: E-PLUGIN-019` | Manifest `format_version` field is absent entirely; plugin rejected (n-1 survivor); AC-5: absent `format_version` is a hard rejection, not silently treated as 0 |
| `plugin_log_level_unrecognized` | warn | `register_host_functions` → `host::log` callback (`host_functions.rs`) | `plugin_id`, `received_name` | Plugin sent a `log-level` enum variant name not recognized by the current host implementation (e.g., a future log-level added to a newer WIT IDL); host defaults to `LogLevel::Info` to preserve forward-compatibility. Emitted BEFORE the default so the downgrade is always observable in tracing. Not a trap — trapping on unrecognized future enum names would break forward-compat. Audit role: operational observability (not security). Recurrence: once per unrecognized log call from the plugin. |
| `write_tool_registration_after_boot` | warn | `register_write_tool` (`crates/prism-query/src/invalidation.rs`) | `plugin_name: String` (source: `entry.plugin_name` — `WriteToolInvalidationMap` struct field; set by `PluginRuntime` from plugin manifest `name` field at step 7.5 plugin-load per ADR-026 D7 v1.23), `tool_name: String` (source: `entry.tool_name` — `WriteToolInvalidationMap` struct field), `error: "E-PLUGIN-020"` (source: literal string constant) | `register_write_tool` called after query-engine init starts (step 8+, ADR-026 D7); the `AtomicBool` query-phase flag is set at step 8 start (first act of step 8, before QueryEngine construction proceeds), gating the write; registration rejected with `Err(SpecEngineError::WriteToolRegistrationAfterBoot)`. Audit role: forensic-trace (error event tied to E-PLUGIN-020; matches audit role of sibling WARN-level post-boot rejection events). Recurrence: one emission per post-boot registration attempt; not retried. Retention: per organization audit policy. |
| `plugin_registration_rolled_back` | error | `plugin_load_step_with_audit` step 7.6 (`crates/prism-bin/src/boot.rs`) | `plugin_name: String` (source: plugin manifest `name` field), `tool_name: String` (source: failing `WriteToolInvalidationMap.tool_name`), `registration_error: String` (source: `reg_err` Display), `rolled_back_tools: usize` (source: `deregister_write_tools_for_plugin` return value), `plugin_unregistered: bool` (source: `PluginRuntime::unregister_plugin` return value) | A plugin's write-tool registration failed (e.g., `DuplicateWriteToolRegistration`). All write tools previously registered for this plugin are removed from `DYNAMIC_WRITE_TOOLS` and the plugin is unregistered from `PluginRuntime` (fail-closed per BC-2.07.004 §write-then-read consistency — partial write-tool state would allow stale reads after writes). Emitted at ERROR severity per F-LP-IMPL-P4-002. Audit role: operational incident (plugin partially loaded then rolled back; operator action may be required to inspect duplicate tool_name in manifest). Recurrence: one emission per failed write-tool registration at boot step 7.6. Retention: per organization audit policy. |
| `timestamp.fallback_to_now` | warn | `PipelineExecutor` timestamp normalization (within sensor fetch pipeline, per ADR-028 v1.10 §D8-B Option A grammar extension) | `column: &str` (the `ColumnSpec` name where `timestamp_fallback_chain` exhausted) | `timestamp_fallback_chain` tried all configured fallback column references and all resolved to null/absent for the current record; `DateTime::now()` is used as the timestamp value. Audit role: Audit signal for timestamp normalization fallback — indicates either (a) production source data integrity issue (multiple timestamp fields null), or (b) excessive use which suggests the spec's `fallback_chain` doesn't match the actual data source's field availability. High frequency on a column is a signal worth investigating. Recurrence: one emission per record + column where exhaustion occurs; may be high-frequency on pathological data. Retention: per organization audit policy. Anchor: BC-2.16.013 v1.15 §Postconditions §1 Armis row; ADR-028 v1.10 §D8-B. Registered: FB-IMPL-1 2026-05-21. |
| `plugin_auth_provider_constructed` | info | `run_boot_sequence` step 7.5b via `validate_and_construct_auth_providers` (`crates/prism-bin/src/boot.rs`) | `sensor_id: String` (source: `sensor_spec` HashMap key — the sensor_id of the spec declaring `auth_plugin`), `plugin_id: String` (source: `auth_provider.plugin_id()` — the registered plugin ID from the PluginRuntime) | Boot step 7.5b successfully validated `auth_plugin` registry membership (via `validate_auth_plugin_fields`) and constructed `Arc<PluginAuthProvider>` for the sensor. Emitted once per sensor spec that declares `auth_plugin = "..."`. Consistent with sibling boot emissions: `boot.audit.initialized` (step 6), `plugin_load_disabled_via_envvar` (step 7.5 disabled path), `plugin_registration_rolled_back` (step 7.6 error path). Audit role: operational observability — confirms plugin auth wiring for each sensor before the MCP server starts (step 9 pre-traffic gate). Recurrence: one emission per successfully wired sensor at boot. Retention: per organization audit policy. F-LP3-LOW-001 closure (PLUGIN-MIGRATION-001-E FB-IMPL-3). |
| `plugin_auth_token_parse_error` | error | `emit_acquire_token_parse_error_and_fail` (`crates/prism-spec-engine/src/plugin/mod.rs`) — called from `dispatch_plugin_acquire_token` Component Model path when `kv_store.get(plugin_id, "token")` returns `None` after a successful `func.call` (host-observable symptom of guest `AuthError::ResponseParse`). UNCONDITIONAL — no `#[cfg(test)]` gate. The wasm32 guest runs in a sandboxed wasmtime instance with no tracing subscriber; only the HOST owns the tracing subscriber in production. | `plugin_id: &str` (Display via tracing `%plugin_id` field macro; source: `plugin_id` parameter of `dispatch_plugin_acquire_token` — registry key e.g. `"crowdstrike-oauth2"`), `error: literal string "acquire-token dispatch completed but no token was cached in KV store (guest AuthError::ResponseParse or missing kv_set call)"` (source: fixed string literal in `error!` call — no `%display` invocation; the string is passed directly as the `error` field value) | Plugin auth token JSON parse failed: the wasm32 guest's `acquire-token` function returned without caching a token in the KV store — the host-observable symptom of `AuthError::ResponseParse` (EC-002: invalid JSON body, EC-003: missing `access_token` field) or `AuthError::InvalidUTF8` (invalid UTF-8 in response body). Operator-investigable: indicates the OAuth2 token endpoint returned a malformed response; may indicate a misconfigured `token_endpoint`, a proxy/WAF rewriting the body, or a temporarily degraded upstream service. Audit role: error (operator action may be required). Recurrence: one emission per failed token acquisition where `func.call` succeeds but KV has no "token" key (no rate limiting). Return variant: `PluginError::AuthTokenNotCached` (E-PLUGIN-022 — NOT E-PLUGIN-008 CompilationFailed; runtime-behavioral failure distinct from compilation failure). Cross-reference: PLUGIN-MIGRATION-001-E EC-002, EC-003; F-LP7-MED-001 CORRECTION (PLUGIN-MIGRATION-001-E pass-7 CORRECTION burst 2026-05-23); F-LP8-MED-002 + F-LP8-MED-003 closure (PLUGIN-MIGRATION-001-E pass-8 FB-IMPL-7 2026-05-23). |
| `overlay.loaded` | info | `OverlayLoader::load_overlays` (`crates/prism-spec-engine/src/overlay.rs`) | `org_slug: &str`, `sensor_id: &str`, `instance_id: &str` | Per-org overlay TOML file successfully validated and merged onto TYPE spec to produce a `ResolvedSensorSpec`. Emitted once per overlay file per boot step 4 / config reload. Audit role: operational/traceability — confirms which per-org overlays were applied at boot. Recurrence: one emission per successfully-loaded overlay file; not emitted for absent `customers/` dir or empty org directories. Retention: per organization audit policy. BC-2.06.012 postcondition; S-CONFIG-MULTI-TENANT-OVERRIDE-001. |
| `boot.overlays_loaded` | info | `step4_load_sensor_specs_with_overlays` (`crates/prism-bin/src/boot.rs`) | `customers_dir: %display` (Display of `customers/` path), `overlay_count: usize` (number of successfully resolved overlays) | Boot step 4 completed overlay discovery; summary emission after all overlay files have been loaded. Emitted once per boot (or config reload) when the `customers/` directory is present and overlay loading succeeds. Audit role: operational/boot-traceability — confirms how many per-org overlays are active. Recurrence: once per boot. Retention: per organization audit policy. ADV-009 fix: S-CONFIG-MULTI-TENANT-OVERRIDE-001 PR #155 fix-burst. |
| `overlay.timeout_secs_ignored` | warn | `OverlayLoader::merge_overlay_onto_type_spec` (`crates/prism-spec-engine/src/overlay.rs`) | `sensor_id: %display` (Display of `type_spec.sensor_id`), `timeout_secs: u64` (the overlay-supplied timeout value) | An overlay TOML file declared a `timeout_secs` field that was accepted and stored in provenance but not yet wired to the HTTP client (deferred to S-CONFIG-MULTI-TENANT-OVERRIDE-002). Emitted once per `merge_overlay_onto_type_spec` call where `overlay.timeout_secs` is `Some`. Audit role: operational observability — makes the gap visible to operators so they know the timeout is not in effect. Recurrence: one emission per overlay merge where `timeout_secs` is set. Retention: per organization audit policy. SEC-PASS2-001 / SEC-PASS3-001 fix: S-CONFIG-MULTI-TENANT-OVERRIDE-001 PR #155 pass-3 fix-burst. |
| `ocsf.spec_column_absent_in_raw` | debug | `SpecDrivenMapper::map` (`crates/prism-ocsf/src/mappers/spec_driven.rs`) | `sensor_id: %display` (SpecDrivenMapper.sensor_id), `column: %display` (col.name), `ocsf_field: %display` (col.ocsf_field target) | A column declared in the sensor TOML spec with an `ocsf_field` annotation was absent from the raw vendor JSON record. Per HIGH-005 graceful degradation, `(col.name, Null)` is inserted into `extensions` so no spec-declared field is silently dropped. Audit role: operational observability — high frequency on a column signals schema drift between the sensor spec and the actual vendor data. Recurrence: one emission per absent spec-column per record; may be high-frequency on stale or partial vendor responses. Retention: per organization audit policy. PLUGIN-MIGRATION-001-C pass-1 fix-burst (F-LP1-HIGH-005). |
| `ocsf.wasm_dispatch_deferred` | warn | `SpecDrivenMapper::map` (`crates/prism-ocsf/src/mappers/spec_driven.rs`) | `sensor_id: %display` (SpecDrivenMapper.sensor_id), `column: %display` (col.name), `ocsf_field: %display` (col.ocsf_field target), `plugin_id: %display` (the "{sensor_id}-ocsf-transform" plugin ID) | A `ColumnType::Json` column has `ocsf_field` annotation and a matching WASM plugin IS registered in `PluginRuntime`, but the `call_ocsf_transform` WIT interface is not yet wired in this release (CRIT-002 fix). The raw column value is preserved in `extensions` (BC-2.02.007) rather than silently dropped. Operators see this warn to know which columns are deferred to WASM dispatch. Audit role: operational observability — signals pending WASM wiring for the named sensor/column pair. Recurrence: one emission per Json-column record where plugin is present but dispatch unavailable. Retention: per organization audit policy. PLUGIN-MIGRATION-001-C pass-1 fix-burst (F-LP1-CRIT-002). |
| `ocsf.timestamp_parse_failed` | warn | `SpecDrivenMapper::map` (`crates/prism-ocsf/src/mappers/spec_driven.rs`) | `sensor_id: %display` (SpecDrivenMapper.sensor_id), `field: %display` (col.name), `ocsf_field: %display` (col.ocsf_field target), `error: %display` (PrismError::OcsfTimestampParseError Display) | A `ColumnType::Datetime` column failed timestamp parsing (RFC3339, ISO 8601, or Unix integer). Per EC-004 graceful degradation, the raw value is preserved in `extensions` instead of aborting the entire record (CRIT-003 fix — replaced `?` propagation). Audit role: operational observability — indicates a schema mismatch between the sensor spec's `timestamp_formats` and the actual vendor data; may indicate a vendor format change. Recurrence: one emission per failed timestamp parse per record. Retention: per organization audit policy. PLUGIN-MIGRATION-001-C pass-1 fix-burst (F-LP1-CRIT-003). |
| `ocsf.spec_driven_mapper_empty_sensor_id` | warn | `SpecDrivenMapper::new` (`crates/prism-ocsf/src/mappers/spec_driven.rs`) | (no structured fields — emitted at construction time before sensor_id is known) | `SpecDrivenMapper` was constructed with a `ConfigSnapshot` containing no sensors (empty `sensor_specs` map). The `sensor_id` defaults to the empty string `""`, which will cause every subsequent `map()` call to return `OcsfNormalizationFailed` (sensor not found). This warn makes the misconfiguration visible to operators before any record processing occurs. Audit role: operational observability — indicates a boot-time or hot-reload race condition that produced an empty config snapshot. The condition is recoverable when the next hot-reload populates the snapshot. Recurrence: one emission per `SpecDrivenMapper::new()` call where `sensor_specs` is empty; typically zero emissions in production (sensors are always configured at boot). Retention: per organization audit policy. PLUGIN-MIGRATION-001-C pass-2 fix-burst (F-LP2-MED-001). |
| `ocsf.deprecated_class_alias` | warn | `EventClassSelector::select_by_class_name` (`crates/prism-ocsf/src/class_selector.rs`) | `class_name: %display` (the deprecated class-name string, e.g., `"security_finding"`), `resolved_class_uid: u32` (the class_uid the alias resolves to, e.g., `2004`) | `select_by_class_name` was called with a deprecated OCSF class-name alias (currently only `"security_finding"`, which resolves to 2004 per OCSF-CLASS-MIGRATION-001 Option A). The call succeeds and returns the canonical class_uid; this WARN is the operator notification that their sensor TOML `ocsf_class` value should be updated to `"detection_finding"`. Audit role: operator guidance — helps operators identify which sensor specs use the deprecated `ocsf_class = "security_finding"` value. Recurrence: one emission per `select_by_class_name` call with a deprecated alias; in the steady state after OCSF-CLASS-MIGRATION-001 migrates all production TOMLs, zero emissions expected from bundled specs; emissions from user-supplied external TOMLs remain possible. Retention: per organization audit policy. OCSF-CLASS-MIGRATION-001 Wave-5 Phase-A (BC-2.02.012 v1.4, BC-2.01.013 v1.10). |
| `ocsf.enum_label_unrecognized` | warn | TWO EMISSION SITES (architectural adjudication 2026-07-07): **(1) PRIMARY:** `build_column_array` in `crates/prism-bin/src/spec_driven_adapter.rs` — the spec-driven adapter materialization path (production); emitted when `OcsfEnumMap::normalize_enum_label(col_name, raw_value)` returns `None` for a String column with name in `["severity", "status", "activity_name", "disposition"]`. **(2) SECONDARY:** `OcsfNormalizer::normalize_with_mappers` (`crates/prism-ocsf/src/normalizer.rs`) — the DynamicMessage/protobuf normalization path; emitted within the enum-label normalization pass when a string enum-label field value has no case-insensitive match in the `OcsfEnumMap` caption set for that field. Both sites emit identical field schema. | `field_name: %display` (the OCSF string label field name — e.g., `"severity"`, `"status"`, `"activity_name"`, `"disposition"`; note: the string label field for OCSF activity is `"activity_name"`, NOT `"activity"` — `activity_id.sibling = "activity_name"` per OCSF v1.7.0 schema.json), `value: %display` (the as-received string value from the sensor that was not recognized in the canonical caption set — e.g., `"UNHANDLED"`, `"Unresolved"`; **control chars stripped via `prism_core::sanitize_for_log` (Unicode Cc category: C0 U+0000–U+001F, DEL U+007F, C1 U+0080–U+009F; plus U+2028 LINE SEPARATOR and U+2029 PARAGRAPH SEPARATOR) before truncation (CWE-117 / SEC-001 defense)**; **capped at 50 codepoints** before emission, SEC-002 defense against over-long vendor data leaking into log aggregation), `sensor_type: %display` (the sensor identifier string — e.g., `"armis"`, `"crowdstrike"`, `"claroty"`; **control chars stripped via `prism_core::sanitize_for_log` (Unicode Cc category: C0 U+0000–U+001F, DEL U+007F, C1 U+0080–U+009F; plus U+2028 LINE SEPARATOR and U+2029 PARAGRAPH SEPARATOR) before truncation (CWE-117 / SEC-001 defense)**; **capped at 50 codepoints** before emission, SEC-002 defense; symmetric with `value` field) | An OCSF string enum-label field carries a **non-empty** value with no case-insensitive match against the canonical OCSF caption set for that field in `OcsfEnumMap`. **Empty-string values do NOT trigger this emission — empty strings (`""`) bypass normalization entirely (representing unset/missing sensor fields, not vendor enum values) and pass through silently with no warn (see BC-2.02.013 §Postconditions, EC-02-028).** The value is left as-received (pass-through per RG-021 / BC-2.02.013 §Postconditions — PRIMARY path: left as-received in the Arrow `StringArray`; SECONDARY path: left as-received in the `DynamicMessage`); the containing record is never dropped. Callers using `IEQ`/`IIN` (BC-2.11.024) can still match unrecognized values case-insensitively. Audit role: operational observability — indicates a sensor emitting vendor-specific extension values or non-standard casing not covered by the OCSF caption set in `enum_map.rs` (e.g., Armis `severity='UNHANDLED'`, Claroty `status='Unresolved'`); high frequency on a field+sensor pair signals a spec amendment opportunity. Recurrence: one emission per unrecognized value per field per record; may be high-frequency on sensors with custom enum values. Retention: per organization audit policy. SECURITY: `value` contains the raw sensor enum-label string (short textual label — not a query value, not a credential); `sensor_type` contains the sensor identifier (structural metadata); no credential data, PII, or sensitive response content appears in any field. Traces to: BC-2.02.013 §Error Cases (unrecognized-value behavior); ADR-047 §D.3 (adapter-boundary normalization). SAP-1 obligation: PG-LP11-001 — PRIMARY site: implementer MUST add emission in `build_column_array` (`spec_driven_adapter.rs`) in the same commit as the spec-driven adapter normalization code (S-PRISMQL-CASE-INSENSITIVE-001); SECONDARY site: registered in v1.98 for `normalize_with_mappers` (`normalizer.rs`) — architectural adjudication 2026-07-07 confirms zero production callers on spec-driven query path. |
| `boot.step7.storage_validated` | info | `step7_init_storage` (`crates/prism-bin/src/boot.rs`) | (no structured fields beyond event_type) | Boot step 7 completed successfully: the RocksDB storage backend (opened in step 6) is validated as ready. Internal table registration via `register_internal_tables` is deferred to per-query `execute_inner` (each query creates its own ephemeral `SessionContext`); this emission confirms the step completed without error. Emitted once per boot on success. Audit role: operational boot-traceability — confirms step 7 reached completion before step 8 proceeds (BC-2.22.001 §Sequencing Invariant). Recurrence: once per boot. Retention: per organization audit policy. S-3.02-FOLLOWUP-RUNTIME (PG-LP11-001 catalog registration). |
| `boot.step8.query_engine_started` | info | `step8_init_query_engine` (`crates/prism-bin/src/boot.rs`) | (no structured fields beyond event_type) | Boot step 8 completed: the write-tool registration window is closed (via `mark_query_phase_started()`, ADR-026 §D7) and the query-engine phase is started. QueryEngine and WriteExecutor construction happens in boot step 9 (`step9_start_mcp_server`, implemented by the merged S-5.01-FOLLOWUP-MCP-BOOT story), which wires the full MCP server chassis with engine handles; per BC-2.22.001 AC-006 an empty spec catalog at step 9A emits `boot.step9a.adapter_registry_populated` with `sensor_count = 0` and boot continues by design — the once-promised fatal non-empty AdapterRegistry assertion was superseded (TD-S-PLUGIN-PREREQ-A-004 closed-as-superseded; ratification: `.factory/proposals/TD-S-PLUGIN-PREREQ-A-004-close-as-superseded-ratification.md`; the materialization `is_empty()` short-circuit is the accepted defense against silent empty results). Emitted once per boot after `mark_query_phase_started()` returns. Audit role: operational boot-traceability — confirms the write-tool registration window is permanently closed and the engine phase has begun (BC-2.22.001 §Step 8, ADR-022 §B). Recurrence: once per boot. Retention: per organization audit policy. S-3.02-FOLLOWUP-RUNTIME (PG-LP11-001 catalog registration). |
| `boot.step9.mcp_server_started` | info | `step9_start_mcp_server` (`crates/prism-bin/src/boot.rs`) | (no structured fields beyond event_type) | Boot step 9 completed: `PrismServer::with_deps` constructed and `serve_stdio` spawned as a background tokio task. Emitted once per boot after the background task is spawned (before it actually starts serving). Audit role: operational boot-traceability — confirms the MCP server chassis is wired and listening on stdio (BC-2.10.006, BC-2.22.001 §Step 9, ADR-022 §F). Recurrence: once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT (PG-LP11-001 catalog registration). |
| `mcp.tool.called` | info | `emit_tool_audit` (`crates/prism-mcp/src/server.rs`) — called by every tool handler method on `PrismServer` before domain logic | `tool_name: %display` (name of the MCP tool being invoked), `client_id: ?display` (Option — the client_id or clients from tool params, None if no client scope), `outcome: %display` (literal `"invoked"` on entry) | Every MCP tool handler emits this event as the audit entry for the tool call (BC-2.05.001 — one audit entry per tool invocation). Emitted after injection scan passes and before domain logic executes. MCP-02 (2026-06-10 review): `emit_tool_audit` is now async and, when an `AuditWriter` is wired, also writes a DURABLE companion record via `AuditWriter::write_tool_call` to the RocksDB `audit_buffer` CF. P5-02 (2026-06-10 review pass-5): durable-write failure handling follows the BC-2.05.001 TWO-CLASS contract via the prism-mcp `tool_classification_registry` (prism-audit `ToolClass`): WRITE-classified tools (`confirm_action`, `add_sensor_spec`, `create_alias`, `delete_alias`, `reload_config`) are FAIL-CLOSED — `emit_tool_audit` returns the `E-AUDIT-001` structured error and the handler aborts BEFORE any mutation or confirmation-token generation (BC-2.05.001 postcondition "Write operations fail-closed on audit failure" / DEC-014); all other (read-classified) tools are fail-open — the failure is logged with `audit_warning` semantics and the tool call proceeds (BC-2.05.001 postcondition "Read operations proceed on audit failure" / EC-05-002). PRL-P4-01 (2026-06-11): `reload_config` added to the WRITE-classified set — it calls `ConfigManager::store(candidate)` on the non-dry-run path, which is mutation-equivalent to `add_sensor_spec` (same ArcSwap swap site); BC-2.05.001 v1.4 adjudication. P4-03 (2026-06-10 review pass-4): on read-path durable-write failure `emit_tool_audit` returns the failure indicator and every success-envelope tool handler threads it into the response as `_meta.audit_warning: "audit emission failed"` (BC-2.05.001 EC-05-002). The tracing field schema is unchanged. Audit role: per-call security audit trail — every MCP tool invocation is traceable regardless of outcome. Recurrence: one emission per tool call. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT (PG-LP11-001 catalog registration); MCP-02 durable companion (2026-06-10 review Fix PR 3). |
| `mcp.tool.rejected` | warn | `PrismServer::scan_inputs_audited` (`crates/prism-mcp/src/server.rs`) — called by every tool handler method before domain logic | `tool_name: %display` (name of the MCP tool whose input was rejected), `reason_class: "prompt_injection"` (literal — the only rejection class emitted by this site), `flag_count: usize` (number of `SafetyFlag`s raised by the injection scanner), `categories: ?debug` (deduplicated, sorted `PatternCategory` variant names, e.g. `["PromptInjection"]`), `fields: ?debug` (deduplicated, sorted input field NAMES that triggered flags) | Emitted when the BC-2.09.001 pre-domain-logic injection scan rejects a tool's input (MCP-03, 2026-06-10 review). SECURITY: carries the rejection CLASSIFICATION only — raw input values and matched-pattern descriptions are deliberately excluded so injected content never re-enters a log or audit channel that an AI agent may later read. A durable companion record (outcome `"rejected_injection"`) is written via `AuditWriter::write_tool_call` (MCP-02 mechanism; not fail-closed per BC-2.05.001 EC-05-002). Audit role: security incident signal — prompt-injection attempts are traceable per tool. Recurrence: one emission per rejected tool call. Retention: per organization audit policy. Registered: MCP-03 fix-burst (2026-06-10 review package, Fix PR 3). |
| `mcp.server.shutdown.initiated` | info | `PrismServer::serve_stdio` (`crates/prism-mcp/src/server.rs`) — select! signal branch | `signal: %display` (either `"SIGINT"` or `"SIGTERM"`) | SIGINT or SIGTERM received during stdio serving; graceful shutdown initiated with 5-second drain window (BC-2.10.010). Emitted immediately when the signal branch fires in the select!. Audit role: operational boot-lifecycle — confirms the shutdown trigger and which signal caused it. Recurrence: at most once per serve_stdio invocation. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT (PG-LP11-001 catalog registration). |
| `mcp.server.shutdown.complete` | info | `PrismServer::serve_stdio_with_shutdown` (`crates/prism-mcp/src/server.rs`) — two emission sites: (a) natural_close arm of select!, (b) after clean signal drain | `path: %display` (literal `"natural_close"` on stdin EOF path; `"signal_drain"` after successful SIGINT/SIGTERM drain) | The MCP server has completed its graceful shutdown. Emitted from two code paths differentiated by `path`: `"natural_close"` when the rmcp background task exits naturally (stdin EOF / peer disconnect); `"signal_drain"` after SIGINT/SIGTERM triggers the 5-second drain window and all in-flight requests complete. OBS-2 fix (F-PASS5): `path` field disambiguates the two semantically distinct completion events that previously emitted with identical fields. Audit role: operational boot-lifecycle — confirms shutdown completed cleanly. Recurrence: at most once per serve_stdio invocation (exactly one of the two paths fires per invocation). Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-5 fix-burst (OBS-2 fix). |
| `mcp.server.shutdown.tasks_drained` | info | `PrismServer::serve_stdio` (`crates/prism-mcp/src/server.rs`) — after `service.close_with_timeout()` returns Ok(Some(_)) | (no structured fields beyond event_type) | In-flight MCP requests drained within the 5-second grace window after SIGINT/SIGTERM (BC-2.10.010 Step 2). Emitted only when `close_with_timeout` completes before the deadline. Audit role: operational boot-lifecycle — confirms graceful drain succeeded; all in-flight requests completed. Recurrence: at most once per serve_stdio invocation (only on signal-triggered shutdown, not natural transport close). Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst F-PASS4-CRIT-2 (BC-2.10.010 graceful shutdown implementation; replaces the paper-fix sleep(5)). |
| `mcp.server.shutdown.timeout` | warn | `PrismServer::serve_stdio_with_shutdown` (`crates/prism-mcp/src/server.rs`) — after `service.close_with_timeout()` returns `Ok(None)` (timeout elapsed) | `grace_secs: u64` (5 — the configured grace window) | 5-second grace window exceeded during SIGINT/SIGTERM shutdown (BC-2.10.010 timeout path, `Ok(None)` arm only). After this log entry, `serve_stdio_with_shutdown` returns `Err(RmcpError::TaskError(...))`. The `Err(join_err)` arm (background task panic) emits `mcp.server.shutdown.join_error` instead (F-PASS6-HIGH-3 fix). Audit role: operational incident — signals that in-flight requests were abandoned due to timeout; operator should investigate hung sensor API calls. Recurrence: at most once per serve_stdio invocation. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst F-PASS4-CRIT-2; HIGH-3 fix applied in pass-5 fix-burst; row amended in pass-6 fix-burst to split Ok(None) and Err arms. |
| `mcp.server.shutdown.force` | warn | `PrismServer::serve_stdio_with_shutdown` (`crates/prism-mcp/src/server.rs`) — second SIGINT during drain window (Unix only) | (no structured fields beyond event_type) | A second SIGINT (Ctrl-C) was received while the 5-second drain window was active (HIGH-4 / EC-10-019: double-SIGINT force-kill). The user explicitly requested immediate exit; after this log entry stdout is flushed and `process::exit(130)` is called (130 = 128 + SIGINT per Unix convention). This is the ONLY path in `serve_stdio_with_shutdown` that calls `process::exit` — the non-double-SIGINT timeout path returns `Err(RmcpError::TaskError(...))` instead (HIGH-3 fix). Audit role: operational incident — confirms force-kill requested by user; in-flight requests were not drained. Recurrence: at most once per serve_stdio invocation (only when two SIGINTs arrive within the 5-second drain window; non-Unix platforms never emit this event). Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-5 fix-burst (HIGH-4 fix). |
| `mcp.server.shutdown.join_error` | warn | `PrismServer::serve_stdio_with_shutdown` (`crates/prism-mcp/src/server.rs`) — two emission sites: (a) `Err(join_err)` arm of `close_with_timeout()` match in the signal-drain path; (b) `Err(join_err)` arm of `close_with_timeout()` match in the natural_close arm (bg task panicked before shutdown signal) | `error: %display` (the `tokio::task::JoinError` Display — panic message or cancellation reason) | An rmcp background task panicked or was cancelled unexpectedly. Fires on two paths: (a) during the SIGINT/SIGTERM drain window when `close_with_timeout` returns `Err(JoinError::Panic)`; (b) on the natural-close path when `is_transport_closed()` fires because the bg task dropped its tx clone during panic unwind — the natural_close arm now also calls `close_with_timeout` to surface the panic error (PR-163 deeper deflake fix: natural_close path incorrectly returned Ok(()) without checking JoinHandle state). Both paths return `Err(RmcpError::Runtime(join_err))` after this log entry. Semantically distinct from `mcp.server.shutdown.timeout`: a join_error means the task itself failed, not that the grace window expired. Audit role: operational incident — background task failure; operator should investigate potential panics in MCP request handling. Recurrence: at most once per serve_stdio invocation. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-6 fix-burst (F-PASS6-HIGH-3: corrected from mcp.server.shutdown.timeout); amended PR-163 deeper deflake to add natural-close path. |
| `write.intent.recorded` | info | `BootAuditWriter::write_intent` (`crates/prism-bin/src/boot.rs` boot-local struct; renamed from `TracingAuditWriter` in the MCP-02 fix — the struct now also writes durable MCP tool-call records via `write_tool_call`) | `intent_id: %display` (ulid::Ulid), `sensor: %display` (WritePlan.sensor field), `capability_path: %display` (String — derived from the plan with hyphen→underscore sanitization for `Allowed`; carried by the denied variant otherwise), `compile_time_enabled: bool`, `runtime_enabled: bool`, `result: %display` (literal `"permitted"` or `"denied"`) | Write intent recorded by the tracing audit writer stub (BC-2.05.001 tracing audit-coverage path). P5-03 (2026-06-10 review pass-5): the emission records the BC-2.05.009 capability-check fields (`capability_path`, `compile_time_enabled`, `runtime_enabled`, `result`) derived from the Phase 2 `CapabilityCheckResult` via `BootAuditWriter::capability_audit_fields` — `Allowed` → `(true, true, "permitted")`; `DeniedCompileTime` → `(false, false, "denied")` (runtime tier never reached; deny-by-default); `DeniedRuntime` → `(true, false, "denied")`. Emitted inside `write_intent` before returning the intent ID. The intent record itself is tracing-only; durable audit coverage for the invoking MCP tool call is the per-call record written by `BootAuditWriter::write_tool_call` to the RocksDB `audit_buffer` CF (MCP-02). That durable per-call mechanism IS the production audit mechanism — no Tower-layer AuditEmitter integration exists or is planned (P1-04 adjudication, 2026-06-10 review cycle). Audit role: write-pipeline security audit trail — every write intent is traceable. Recurrence: one emission per write intent call. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT (PG-LP11-001 catalog registration). |
| `write.outcome.recorded` | info | `BootAuditWriter::write_outcome` (`crates/prism-bin/src/boot.rs` boot-local struct; renamed from `TracingAuditWriter` in the MCP-02 fix) | `intent_id: %display` (ulid::Ulid), `succeeded: u32` (WriteResult.succeeded_count), `failed: u32` (WriteResult.failed_count) | Write outcome recorded by the tracing audit writer stub (BC-2.05.001 tracing audit-coverage path). Emitted inside `write_outcome`. Audit role: write-pipeline security audit trail — every write outcome is traceable alongside its corresponding intent. Recurrence: one emission per write outcome call. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT (PG-LP11-001 catalog registration). |
| `boot.step10.deferred` | warn | `step10_start_hot_reload` (`crates/prism-bin/src/boot.rs`) | (no structured fields beyond event_type) | Boot step 10 (HotReloadWatcher) is deferred to S-1.12-FOLLOWUP. Emitted once per boot on the `Ok(())` return path to inform operators that the server is operating in degraded mode (no filesystem change detection). Audit role: operational boot-traceability — confirms step 10 ran but without the watcher; hot-reload via filesystem events is unavailable. Recurrence: once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-3 fix-burst F-PASS3-MED-3 (PG-LP11-001 catalog registration; replaces `todo!()` which would have panicked). |
| `boot.step11.deferred` | warn | `step11_install_signal_handlers` (`crates/prism-bin/src/boot.rs`) | (no structured fields beyond event_type) | Boot step 11 (SIGHUP config-reload handler) is deferred to S-1.12-FOLLOWUP which provides the HotReloadWatcher needed by the reload path. SIGTERM/SIGINT are handled by `PrismServer::serve_stdio` (BC-2.10.010). Emitted once per boot on the `Ok(())` return path to inform operators that SIGHUP-triggered config reload is unavailable. Audit role: operational boot-traceability — confirms step 11 ran but without SIGHUP wiring. Recurrence: once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-3 fix-burst F-PASS3-MED-3 (PG-LP11-001 catalog registration; replaces `todo!()` which would have panicked). |
| `boot.step9.alias_store_loaded` | info | `step9_start_mcp_server` (`crates/prism-bin/src/boot.rs`) | `path: %display` (alias_file PathBuf Display) | An existing `aliases.toml` file was found in the config directory and successfully loaded into the AliasStore (CRIT-4 fix — alias_store wired into PrismServer::with_deps). Emitted once per boot when `alias_file.exists()` is true and `AliasStore::load` succeeds. Audit role: operational boot-traceability — confirms alias store loaded from persistent state on startup. Recurrence: once per boot when aliases.toml exists and is valid. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst (PG-LP11-001 catalog registration). |
| `boot.step9.alias_store_load_failed` | warn | `step9_start_mcp_server` (`crates/prism-bin/src/boot.rs`) | `path: %display` (alias_file PathBuf Display), `error: %display` (load error Display) | An existing `aliases.toml` was found but failed to parse (invalid TOML, schema mismatch). Boot continues with an empty alias store — alias definitions are lost for this session. Emitted once per boot when `alias_file.exists()` is true but `AliasStore::load` returns Err. Audit role: operational incident — aliases not loaded; operator should inspect aliases.toml for syntax errors. Recurrence: at most once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst (PG-LP11-001 catalog registration). |
| `boot.step9.alias_store_empty` | info | `step9_start_mcp_server` (`crates/prism-bin/src/boot.rs`) | `path: %display` (alias_file PathBuf Display — the expected location for the file) | No `aliases.toml` was found in the config directory; boot proceeds with an empty alias store. Emitted once per boot when `alias_file.exists()` is false. Audit role: operational boot-traceability — confirms alias store initialized empty (expected on first boot). Recurrence: once per boot when aliases.toml is absent. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst (PG-LP11-001 catalog registration). |
| `boot.shutdown.timeout` | warn | `RunningServer::wait_for_shutdown` (`crates/prism-bin/src/boot.rs`) — `Ok(Err(rmcp::RmcpError::TaskError(_)))` arm | (no structured fields beyond event_type) | The MCP server's graceful-drain timeout was exceeded (BC-2.10.010) — `RmcpError::TaskError` returned by `serve_stdio`. Returns `EXIT_GENERIC_ERROR` (exit 1). Audit role: boot-step-9 shutdown outcome traceability — confirms drain timeout occurred; operator should investigate hung in-flight requests. Recurrence: at most once per boot (single serve_stdio task). Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-10 fix-burst F-PASS10-HIGH-1 (PG-LP11-001 catalog registration). |
| `boot.shutdown.runtime_error` | error | `RunningServer::wait_for_shutdown` (`crates/prism-bin/src/boot.rs`) — `Ok(Err(rmcp::RmcpError::Runtime(_)))` arm | (no structured fields beyond event_type) | The MCP server's background task exited with an rmcp runtime error (e.g., internal rmcp protocol state error). Returns `EXIT_INTERNAL_ERROR` (exit 4). Audit role: boot-step-9 shutdown outcome traceability — signals an unexpected MCP runtime failure; operator should investigate. Recurrence: at most once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-10 fix-burst F-PASS10-HIGH-1 (PG-LP11-001 catalog registration). |
| `boot.shutdown.transport_init_error` | warn | `RunningServer::wait_for_shutdown` (`crates/prism-bin/src/boot.rs`) — `Ok(Err(e))` catch-all arm (non-TaskError, non-Runtime rmcp errors) | `error: %display` (the `rmcp::RmcpError` Display — transport or init failure reason) | The MCP server transport failed to initialize (e.g., stdin already closed before `serve()` completes — no MCP client ever connected). Functionally equivalent to a clean client-never-connected case. Returns `EXIT_SUCCESS` (exit 0) because no session was active. Emitted at WARN so the condition is observable in operator logs. Audit role: boot-step-9 shutdown outcome traceability — distinguishes normal never-connected-exit from genuine MCP client disconnects. Recurrence: at most once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-10 fix-burst F-PASS10-HIGH-1 (PG-LP11-001 catalog registration). |
| `boot.shutdown.task_panic` | error | `RunningServer::wait_for_shutdown` (`crates/prism-bin/src/boot.rs`) — `Err(_join_err)` arm (tokio `JoinError` wrapping a panic payload) | (no structured fields beyond event_type) | The MCP server background task panicked — the `tokio::task::JoinHandle` resolved to `Err(JoinError)`. The panic hook fires before this point, emitting additional context. Returns `EXIT_INTERNAL_ERROR` (exit 4). Audit role: boot-step-9 shutdown outcome traceability — confirms task panic occurred; `mcp.server.shutdown.join_error` fires inside `serve_with_transport_and_shutdown` (on either the shutdown-drain or natural-close path) when the bg task panics, then `serve_stdio` propagates `Err(RmcpError::Runtime)`, then this event fires in `wait_for_shutdown`'s `Err(_join_err)` arm at the boot layer. Recurrence: at most once per boot. Retention: per organization audit policy. S-5.01-FOLLOWUP-MCP-BOOT pass-10 fix-burst F-PASS10-HIGH-1 (PG-LP11-001 catalog registration); amended PR-163 deeper deflake to update join_error path description. |
| `boot.step9a.adapter_registry_populated` | info | `step9a_populate_adapter_registry` (`crates/prism-bin/src/spec_driven_adapter.rs`) — two emission sites: (a) early-return path when `resolved_spec_map` is empty (AC-006 — empty spec catalog); (b) normal-path after the adapter registration loop completes | `sensor_count: u64` (source: `registered_count as u64` on normal path; `0u64` literal on early-return path — number of `SpecDrivenSensorAdapter` instances successfully registered in `AdapterRegistry`), `org_count: u64` (source: `orgs_with_adapters.len() as u64` on normal path; `0u64` literal on early-return path — number of distinct `OrgId` values that had at least one adapter registered) | Boot step 9A completed: `AdapterRegistry` has been populated with `SpecDrivenSensorAdapter` instances derived from the resolved sensor spec map. Closes GAP-002-A — enables `fan_out()` in `prism-query` to resolve `(org_id, sensor_id) → Arc<dyn SensorAdapter>` and return live Arrow data from the DTU clones. Emitted on both the empty-catalog fast-path (`sensor_count=0`, `org_count=0`, `resolved_spec_map` was empty, AC-006) and the normal path (after the full registration loop). The three `warn!` diagnostics within the loop body (`org_slug_not_found`, `plugin_auth_provider_missing`, `unsupported_auth_type`) intentionally OMIT `event_type=` per SAP-1 discipline — they are internal operational diagnostics, not auditable boot-lifecycle events. Audit role: boot/operational diagnostic — records adapter registry population state at boot, enabling operators to verify the count of wired sensors and orgs before MCP server traffic begins (precedes step 9). Recurrence: once per boot (step 9A runs exactly once in the ADR-022 §B sequencing table; hot-reload is not yet wired to this step as of S-DEMO-001 v1.3). Retention: per organization audit policy. Story: S-DEMO-001 (BC-2.22.001 boot orchestration, BC-2.01.013 adapter construction). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation, story S-DEMO-001). |
| `push_down.inverted_time_range` | warn | `extract_time_window_from_ast` (`crates/prism-query/src/pushdown.rs`); SS-11 Query Execution | `event_type` (string literal `"push_down.inverted_time_range"`), `start_time: %display` (string — the resolved lower-bound datetime, ISO8601 format, from the WHERE predicate), `end_time: %display` (string — the resolved upper-bound datetime, ISO8601 format, from the WHERE predicate) | The PrismQL query's WHERE time predicate yields `start_time > end_time` (inverted time window). Both bounds are passed through to the sensor unchanged; the sensor will receive the inverted window and will return empty or anomalous results depending on its own predicate semantics. Audit role: diagnostic / query-anomaly signal — NON-audit; this is an observability event, not a security-audit event. It surfaces a likely authoring error in the query (e.g., swapped bounds, timezone confusion) or an incorrect computation of one of the bounds. Operators / query authors should inspect the query's WHERE clause. Recurrence: at most once per query where the extracted time window is inverted (fires inside `extract_time_window_from_ast` at the point where `start > end` is detected, before returning the `(start_time, end_time)` tuple to the caller). Retention: per organization audit policy. Cross-reference: S-DEMO-QUERY-PUSHDOWN-001 EC-003; BC-2.11.007 v1.8. SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation per project standing discipline). |
| `table_registry.rwlock_poisoned` | warn | Nine methods/labels in `TableRegistry` (`crates/prism-query/src/table_registry.rs`): four original read-path methods (`is_registered`, `registered_tables`, `sensor_for_table`, `registered_sensor_ids`) plus `columns_for_table` (fires on `columns_by_table` poison, returns empty Vec, fail-closed — M1 fix S-DEMO-FIDELITY-REMEDIATION-001) plus `check_availability_gate.dot_notation` (fires on `sensor_by_table` poison in the dot-notation rejection block within `check_availability_gate`, uses empty map for org filter, not an error — N2 fix S-DEMO-FIDELITY-REMEDIATION-001) plus `check_availability_gate` (fires on the `sensor_by_table` poison snapshot in the table-not-registered block, degrades to empty-map org filter, not an error — CR-001 fix-burst) plus `sensor_by_table_snapshot` (fires on poison, returns empty map, fail-closed — OBS-2 fix-burst) plus `column_type_for` (fires on `column_types_by_table` poison, returns `None`, fail-open — schema-aware datetime column-type lookup in `check_temporal_literals` degrades to column-type-unknown; no E-QUERY-041 fired for the affected column — S-PRISMQL-NATIVE-TEMPORAL-TYPING-001 fix-burst) | `event_type` (string literal `"table_registry.rwlock_poisoned"`), `method: %display` (string literal — name of the method or label that encountered the poison: `"is_registered"`, `"registered_tables"`, `"columns_for_table"`, `"sensor_for_table"`, `"sensor_by_table_snapshot"`, `"registered_sensor_ids"`, `"check_availability_gate.dot_notation"`, `"check_availability_gate"`, or `"column_type_for"`) | The `registered`, `sensor_by_table`, or `columns_by_table` RwLock inside `TableRegistry` is poisoned — another thread panicked while holding the write lock. The method returns its fail-closed default (`false` / empty `Vec` / `None` / empty HashMap) to prevent cascading failures; this WARN makes the abnormal condition observable to operators. For `columns_for_table` specifically: the `columns_by_table` poison causes the caller (`check_query_column_availability` single-tenant path) to receive an empty column list — column-gate checks degrade to fail-open (no columns visible means no columns blocked), consistent with the existing `registered_tables` fail-open pattern. For `check_availability_gate.dot_notation` specifically: fires in the dot-notation rejection block of `check_availability_gate` (the `sensor_by_table` snapshot used for the org-visible-tables filter in dot-name error reporting); falls back to empty map, org-filter signal degrades, dot-notation `TableNotAvailable` error is still returned but with empty available lists. For `check_availability_gate` specifically: the `sensor_by_table` poison causes the org-filter snapshot to fall back to an empty map, which means the gate constructs `TableNotAvailable` with empty `available_sensors`/`available_tables` (not an error, but a degraded org-filter signal). For `sensor_by_table_snapshot` specifically: the caller (explain.rs org-filter path) receives an empty map — tables in `ExplainResult.available_tables` will be filtered out (fail-closed; no over-disclosure). Audit role: operational incident signal — RwLock poison in a plan-time availability check indicates an upstream crash (likely in `register_sensor` or `deregister_sensor`) that should be investigated. The fail-closed defaults prevent the poison from causing incorrect query routing (queries against unconfigured sensors will fail with a different error rather than silently succeeding). Recurrence: one emission per poisoned-lock method call; in the steady state (no thread panics while holding the registry write lock) zero emissions expected. Retention: per organization audit policy. NB-1 (S-3.13 fix-burst): visibility gap — poison was previously silently fail-closed with no operator signal. CR-001 (S-3.13 CR-001 fix-burst): `check_availability_gate` added as 5th emitting method. OBS-2 (S-3.13 OBS-2 fix-burst): `sensor_by_table_snapshot` added as 6th emitting method. M1/N2 (S-DEMO-FIDELITY-REMEDIATION-001 fix-burst): `columns_for_table` added as 7th emitting method; `check_availability_gate.dot_notation` added as 8th label (within `check_availability_gate`). For `column_type_for` specifically: the `column_types_by_table` RwLock poison causes the caller (`check_temporal_literals` schema-aware datetime validation in `engine.rs`) to receive `None` — the column type is treated as unknown and no E-QUERY-041 is fired (fail-open: schema validation degrades gracefully rather than falsely rejecting comparisons on non-datetime columns). S-PRISMQL-NATIVE-TEMPORAL-TYPING-001 fix-burst: `column_type_for` added as 9th emitting method. SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation per project standing discipline). |
| `http_lookup_enrich_failed` | warn | `HttpLookupSource::enrich_single_async` and `HttpLookupSource::enrich_single` (both in `crates/prism-spec-engine/src/infusion/sources/http_lookup.rs`) — fired on any failure path that returns `None` (E-INFUSE-009 or E-INFUSE-010 conditions) | **ALWAYS present:** `infusion_id: %display` (the `spec_path` string, used as the infusion identity per ADR-040 §D8.7), `spec_path: %display` (the spec_path string), `error_code: &str` (the E-INFUSE error code for this sub-condition — one of `"E-INFUSE-009"` for HTTP/transport/parse failures or `"E-INFUSE-010"` for credential resolution failure). **Sub-condition-specific (present only on some emission sites):** `error_kind: %display` — classification of the transport/runtime failure kind returned by `classify_reqwest_error()` (e.g., `"connect"`, `"timeout"`, `"body_read"`); present on: runtime-build-error site (`error_kind = "runtime_build_error"`, literal string), HTTP `.send()` failure site, body-read failure site. `method: %display` — the unsupported HTTP method string; present only on the unsupported-method emission site (E-INFUSE-009 sub-condition). `status_code: u16` — HTTP status code; present only on the non-2xx status emission site (E-INFUSE-009 sub-condition). `credential_ref: %display` — the LOGICAL credential reference name from `[source.credential].ref` in the infusion spec (e.g., `"nvd.api_key"`); present only on the credential-resolution-failure site (E-INFUSE-010 sub-condition). AD-017 SECURITY: the env var NAME is excluded from `credential_ref` (only the logical ref name is logged); the credential VALUE is never present in any field; the full URL is never logged. `message: &str` — static description string; present only on the HTTP `.send()` failure site (value: `"HTTP call failed"`). | Emitted when `HttpLookupSource` fails to produce enrichment data. Covers seven emission sites across two call-path functions: (1) `enrich_single` — tokio runtime-build failure; (2–7) `enrich_single_async` — credential env-var not set (E-INFUSE-010), unsupported HTTP method (E-INFUSE-009), HTTP `.send()` transport error (E-INFUSE-009), non-2xx HTTP status (E-INFUSE-009), response body-read failure (E-INFUSE-009), JSON parse failure (E-INFUSE-009). All seven sites share `infusion_id`, `spec_path`, and `error_code`; sub-condition fields listed above appear at specific sites only. SECURITY: full URL and credential VALUES are NEVER included per AD-017 / CWE-209; `credential_ref` is the AD-017-safe logical name, not the env var name or credential value. Audit role: operational incident signal — enrichment data unavailable for a record; operator should investigate the configured endpoint, credential, or HTTP method. Recurrence: at most once per `enrich_single` / `enrich_single_async` call. Retention: per organization audit policy. Mandated by ADR-040 v2.0 §D8.7. SAP-1 obligation: PG-LP11-001. **v1.83 field-schema expansion (SAP-1 fix):** prior v1.82 catalog row listed only `infusion_id`, `spec_path`, `status_code` — missing `error_code`, `error_kind`, `credential_ref`, `method`, and `message`. Full field schema now matches the seven emission sites in `http_lookup.rs` per SAP-1 standing probe requirement. |
| `http_lookup_ssrf_rejected` | warn | `HttpLookupSource` construction / SSRF validation gate (`crates/prism-spec-engine/src/infusion/sources/http_lookup.rs`) — fired on `InfusionError::SsrfRejected` return path during registry-load / construction-time validation | `infusion_id: %display` (the spec_path string, used as the infusion identity), `spec_path: %display` (the spec_path string) | Emitted when the SSRF `base_url` validation gate rejects an `HttpLookupSource` during construction (registry-load time). The `base_url` resolved to a private/loopback/link-local address or was otherwise rejected by the SSRF allowlist check (ADR-040 §D8.5 — SSRF validation IN-SCOPE for PIVOT-002 per §Scope Boundary). SECURITY: the resolved IP address MUST NOT appear in this event (AD-017 / CWE-209). Audit role: security incident signal — SSRF attempt or misconfigured enrichment endpoint detected at load time; operator must correct the `base_url` in the infusion spec. Recurrence: once per rejected `HttpLookupSource` construction at registry-load. Retention: per organization audit policy. Mandated by ADR-040 v2.0 §D8.7. SAP-1 obligation: PG-LP11-001. **v1.82 addition: this event_type was absent from the v1.81 catalog (PIVOT-002 implementer registered three dotted-name rows but omitted `http_lookup_ssrf_rejected`). Added here per ADR-040 §D8.7. Implementer must add a `tracing::warn!` site in the SSRF validation gate with `event_type = "http_lookup_ssrf_rejected"`.** |
| `plugin_enrich_json_parse_error` | error | `PluginRuntime::enrich_single` Component Model path (`crates/prism-spec-engine/src/plugin/mod.rs`) — inside the `Val::String` arm of the `Option(Some(boxed_val))` branch | `plugin_id: %display` (the plugin registry key, e.g., `"threatintel-enrich"`), `error: %display` (the `serde_json::Error` Display — parse error description without the input JSON string) | `serde_json::from_str` failed to parse the string value returned by the plugin's `enrich-single` function. The Component Model ABI guarantees a `Val::String` was returned, but its content is not valid JSON. Returns `Err(PluginError::EnrichCallFailed)` (E-PLUGIN-023). SECURITY: the raw plugin-returned string is never logged (may contain untrusted content from the network). Audit role: plugin protocol error — indicates the plugin implementation's `enrich-single` function returned a non-JSON string; operator should update or debug the plugin. Recurrence: one emission per call where the string result is not valid JSON. Retention: per organization audit policy. S-DEMO-ENRICHMENT-PIVOT-002 (ADR-040 v2.0 WASM Plugin path). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation). |
| `plugin_enrich_unexpected_val` | error | `PluginRuntime::enrich_single` Component Model path (`crates/prism-spec-engine/src/plugin/mod.rs`) — two emission sites: (a) the `other =>` fallthrough arm inside `Option(Some(boxed_val))` (Val inside the Some is not Val::String); (b) the `Some(other) =>` fallthrough arm on the outer match (result is not `Val::Option`) | `plugin_id: %display` (the plugin registry key) | The Component Model result buffer contains an unexpected `Val` variant at one of two levels: (a) the `Some` inner value is not `Val::String` (e.g., `Val::S32`, `Val::Record`, etc.); or (b) the result element is not `Val::Option` at all (WIT contract violated). Returns `Err(PluginError::EnrichCallFailed)` (E-PLUGIN-023). Audit role: plugin protocol error — indicates a compiled plugin whose WIT ABI does not match the expected `option<string>` return type; operator should recompile the plugin against the current WIT interface. Recurrence: one emission per call where the result Val does not match the expected shape. Retention: per organization audit policy. S-DEMO-ENRICHMENT-PIVOT-002 (ADR-040 v2.0 WASM Plugin path). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation). |
| `schema_enumeration.started` | info | `prism_describe` tool handler (`crates/prism-mcp/src/tools/prism_describe.rs`) — emitted at the entry point of a valid `prism_describe` invocation, after `client_id` validation succeeds | `client_id: %display` (the `OrgSlug` identifying the requesting client organization) | Discovery initiation audit signal — emitted once per `prism_describe` call that passes input validation (valid `client_id` format per BC-2.10.012 DI-006). Fires before schema resolution begins. Audit role: discovery initiation — records that a client initiated a schema enumeration request. Recurrence: once per `prism_describe` call that reaches the schema-resolution path (i.e., after `client_id` accepted). Retention: per organization audit policy. Traces to: BC-2.10.012 (`prism_describe` tool, S-DEMO-PRISMQL-ONBOARDING-001-A). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation, D-1262). |
| `schema_enumeration.rejected` | warn | `prism_describe` tool handler (`crates/prism-mcp/src/tools/prism_describe.rs`) — emitted when `client_id` input fails validation (DI-006 invalid format) | `event_type: &str` (literal `"schema_enumeration.rejected"`) | Input rejection audit signal — the supplied `client_id` parameter did not pass format validation (DI-006: empty string, whitespace-only, or otherwise invalid `OrgSlug` format). The tool returns an error response without performing any schema lookup. Audit role: input rejection (DI-006) — records attempted schema enumeration with invalid client identity; useful for detecting misconfigured clients or injection attempts. Recurrence: once per `prism_describe` call where `client_id` fails DI-006 validation. Retention: per organization audit policy. Traces to: BC-2.10.012 (`prism_describe` tool, S-DEMO-PRISMQL-ONBOARDING-001-A). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation, D-1262). |
| `schema_enumeration.success` | info | `prism_describe` tool handler (`crates/prism-mcp/src/tools/prism_describe.rs`) — emitted after schema resolution completes successfully and the catalog response has been constructed | `client_id: %display` (the `OrgSlug` identifying the requesting client organization) | Successful schema catalog emission audit signal — emitted once per successful `prism_describe` call (i.e., `client_id` valid AND schema resolution returned a non-error result). Records that a client successfully retrieved the schema catalog. Audit role: successful schema catalog emission — confirms that a schema enumeration request completed and results were returned to the client. Recurrence: once per successful `prism_describe` call. Retention: per organization audit policy. Traces to: BC-2.10.012 (`prism_describe` tool, S-DEMO-PRISMQL-ONBOARDING-001-A). SAP-1 obligation: PG-LP11-001 (registered in same commit as implementation, D-1262). |
| `reload.overlay_rebuild_failed` | warn | `QueryEngine::rebuild_resolved_spec_map` (`crates/prism-query/src/engine.rs`) — emitted when `OverlayLoader::load_overlays` returns a non-empty `errors` vector during hot-reload | `error_count: usize` (number of validation errors returned by `OverlayLoader::load_overlays`) | Hot-reload overlay rebuild failure audit signal — emitted when the `resolved_spec_map` ArcSwap rebuild attempt encounters validation errors. Per DI-031 (non-fatal reload discipline), the prior `resolved_spec_map` is retained and the method returns `Err(SpecEngineError::ValidationFailed{errors})`. The caller (prism-mcp `reload_config_core`) is expected to log and continue. Audit role: reload-pipeline observability (ADR-042) — records that a hot-reload cycle failed to produce a new overlay map, enabling operators to detect and diagnose persistent reload failures. The `error_count` field provides the count of validation errors; the first error is propagated as `SpecEngineError::ValidationFailed`. Recurrence: once per `rebuild_resolved_spec_map` call where `OverlayLoader::load_overlays` returns at least one error. Retention: per organization audit policy. Traces to: ADR-042 (resolved_spec_map reload-aware ArcSwap), BC-2.10.013 (multi-tenant hot-reload notify). SAP-1 obligation: PG-LP11-001 (registered in D-1270 catalog sync burst, S-DEMO-PRISMQL-ONBOARDING-001-A). |
| `reload.overlay_rebuilt` | info | `QueryEngine::rebuild_resolved_spec_map` (`crates/prism-query/src/engine.rs`) — emitted after `OverlayLoader::load_overlays` succeeds and the new resolved map has been atomically swapped into the `ArcSwap` | `overlay_count: usize` (number of resolved org overlays in the newly-built map, i.e., `result.resolved.len()`) | Hot-reload overlay rebuild success audit signal — emitted once per successful `rebuild_resolved_spec_map` call (no validation errors, `ArcSwap::store` completed). Records that the `resolved_spec_map` has been atomically updated with a fresh overlay set. Audit role: reload-pipeline observability (ADR-042) — confirms that a hot-reload cycle successfully produced and installed a new overlay map; the `overlay_count` field lets operators verify the expected number of org overlays was rebuilt. Recurrence: once per `rebuild_resolved_spec_map` call that completes without errors. Retention: per organization audit policy. Traces to: ADR-042 (resolved_spec_map reload-aware ArcSwap), BC-2.10.013 (multi-tenant hot-reload notify). SAP-1 obligation: PG-LP11-001 (registered in D-1270 catalog sync burst, S-DEMO-PRISMQL-ONBOARDING-001-A). |
| `column_not_found.rejected` | warn | `check_column_availability` and `check_column_against_available_set` (`crates/prism-query/src/engine.rs`) — three emission sites, identical field schema: (a) single-tenant path (M1 fix, S-DEMO-FIDELITY-REMEDIATION-001): emitted in `check_column_availability` `if resolved_spec_map.is_none()` branch when `TableRegistry::columns_for_table` returns a non-empty column list that does not contain the referenced column; (b) multi-tenant path (original site, S-DEMO-PRISMQL-ONBOARDING-001-B): emitted in `check_column_availability` after org-scoped `resolved_spec_map` column lookup fails for the querying client; (c) binding-context path (DRIFT-IEQ-NONEXISTENT-COL-ERRPATH-001, FIX-IEQ-ERRPATH-001): emitted in `check_column_against_available_set`, called from `check_pipe_stage_columns` for pipe-mode positions 8–14, when the column is not present in the running binding-context `available` set at the failing stage | `column: %display` (the column name referenced in the query — e.g., `"sevrity"`; control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission), `table: %display` (the table name the column was looked up in — e.g., `"crowdstrike_alerts"`), `client_id: %display` (the `OrgSlug` of the client executing the query), `available_count: usize` (number of columns available in the table for this client: on the single-tenant path (a), `TableRegistry::columns_for_table(table_name).len()`; on the multi-tenant path (b), the count from org-scoped `resolved_spec_map`; on the binding-context path (c), `available.len()` where `available` is the current binding view at the failing stage — post-stats REPLACE = {aliases ∪ by_fields}; post-enrich union includes output_columns; reflects the derived-column view, NOT the raw `TableRegistry` schema for that stage; 0 if the binding context or table schema has no registered columns) | E-QUERY-038 plan-time column gate rejected a query because the referenced column does not exist in the table schema (paths a/b) or the derived-column binding context at the failing pipe stage (path c). The query is rejected before any sensor fetch is attempted. The `column` field contains only the column name (never a user-supplied query string fragment longer than the column identifier). All three emission sites use the identical field schema above. Audit role: query rejection / access-control trace — records that a query was rejected at plan time due to a schema mismatch; enables operators to diagnose typos, schema drift, or unauthorized column-access attempts. SECURITY: `column` field contains only the column identifier string (no PQL expression context), `table` contains only the table identifier. No query values or credential data appear in any field. Recurrence: once per query where the column gate rejects a column reference; in the steady state (well-formed queries) zero emissions expected per query. Retention: per organization audit policy. Traces to: BC-2.11.016 (E-QUERY-038 column gate), S-DEMO-PRISMQL-ONBOARDING-001-B AC-002, S-DEMO-FIDELITY-REMEDIATION-001 M1 fix, FIX-IEQ-ERRPATH-001 (third site). SAP-1 obligation: PG-LP11-001 (original site registered S-DEMO-PRISMQL-ONBOARDING-001-B; second site registered S-DEMO-FIDELITY-REMEDIATION-001 F-PHL3-MED-001 closure; third site registered FIX-IEQ-ERRPATH-001 ADV-FIX-P3-MED-001 closure). |
| `column_source_path_extraction_failed` | warn | Two emission sites, identical schema: (a) `ColumnMapper::map_record` (`crates/prism-spec-engine/src/column_mapping.rs`) — emitted in the `Err(e)` arm of `extract_at_path` when a column with `source_path = Some(path)` fails to extract from a raw record; the column is skipped and the record continues. (b) `build_column_array::extract_raw` (`crates/prism-bin/src/spec_driven_adapter.rs`) — emitted in the same `Err(e)` arm when `extract_at_path` fails during Arrow array construction; the cell becomes null. Both sites are guarded by `if let Some(ref path) = col.source_path` (ENRICH-1 §Design Decision 1 — only columns with an explicit `source_path` reach these paths; flat-key columns use the pre-ENRICH-1 `raw.get(&col.name)` path and never emit this event). | `column: %display` (the clean SQL column name from the TOML spec — e.g., `"iocs_value"`, `"behaviors_ioc_type"`), `source_path: %display` (the JSONPath declared in the TOML `[[tables.columns]]` `source_path` field — e.g., `"$.iocs[*].value"`, `"$.behaviors[*].ioc_type"`), `error: %display` (the `extract_at_path` error string describing why extraction failed — e.g., "path not found", "type mismatch at segment") | ENRICH-1 source_path extraction failed for a single column in a single record or Arrow cell. The failure is non-fatal: at site (a) the column is skipped for this record (the column will be absent from `MappingResult.mapped_fields` and `MappingResult.raw_extensions` for this record); at site (b) the cell becomes null in the Arrow column array. The containing record is never dropped — "records are NEVER dropped" invariant (BC-2.16.003). Audit role: operational diagnostic — enables operators to detect spec/wire mismatch (e.g., a TOML `source_path` declaring `$.iocs[*].value` that never resolves against live API records indicates the API shape differs from the TOML declaration; the `source_path` and `error` fields together pinpoint the mismatched JSONPath segment). In production with a correctly wired TOML spec and a live sensor, zero emissions expected per query. Recurrence: once per record per failing column. Under adversarial conditions (path absent from every record), emits once per record in the result set. Retention: per organization audit policy. SECURITY: `column` contains the TOML column name only (not a query value), `source_path` contains the JSONPath only (not sensor response data), `error` contains the extraction error string (not sensor response content). No credential data appears in any field. Traces to: S-DEMO-ENRICH-1 (ENRICH-1 source_path design), sensor-column-source-path-design.md §DD-1. SAP-1 obligation: PG-LP11-001 (registered in same burst as implementation, S-DEMO-ENRICH-1). |
| `pipe.sql_lowering` | debug | `execute_against_session` (`crates/prism-query/src/materialization.rs`) — emitted at TWO sites, both emitting identical field schema: **(a)** `Ast::Pipe(pipe)` arm immediately after `pipe_to_executable_sql` returns `Ok(sql)`, before `session_ctx.sql(&sql)` is called; **(b)** `Ast::SqlPipe(spq)` arm immediately after `sqlpipe_to_executable_sql` returns `Ok(sql)`, before `session_ctx.sql(&sql)` is called | `pipe_sql: %display` (the DataFusion SQL string produced by `pipe_to_executable_sql` — the lowered SQL representation of the PipeQuery AST; debug-level only; carries the lowered SQL including user-supplied WHERE-clause string literals, control-character-sanitized via `sanitize_for_log` at emission (CWE-117)), `event_type: "pipe.sql_lowering"` | ENRICH-4-B pipe-to-SQL lowering success diagnostic. Emitted once per `Ast::Pipe` or `Ast::SqlPipe` query that passes the pipe-to-SQL lowering step without error. The `pipe_sql` field carries the full lowered SQL string — useful for debugging pipe execution plans and verifying that the pipe AST was lowered as expected. Emitted at `debug` level (not `info`) because the SQL string may be verbose for complex pipe queries; operators can enable debug-level tracing on `prism_query::materialization` to capture it. Audit role: non-security diagnostic / operational debugging (NON-audit). This event is NOT a security-audit event; it records an internal query-execution step visible only in debug logging, not in the default info/warn/error production stream. SECURITY: `pipe_sql` carries the lowered SQL including user-supplied WHERE-clause string literals (not sensor response data, credentials, or PII); control characters are stripped at emission via `sanitize_for_log` (CWE-117). Operators should treat debug-level logs as internal diagnostic output. Recurrence: once per `Ast::Pipe` or `Ast::SqlPipe` query where pipe-to-SQL lowering succeeds. Retention: per organization audit policy (debug level; typically not retained in production log archives). Traces to: ENRICH-4-B (`Ast::Pipe` arm — original registration); S-PRISMQL-CASE-INSENSITIVE-001 (`Ast::SqlPipe` arm — same event_type reused per SAP-1). SAP-1 obligation: PG-LP11-001 (ENRICH-4-B registered `Ast::Pipe` arm; S-PRISMQL-CASE-INSENSITIVE-001 adversary pass-32 fix-burst registers `Ast::SqlPipe` arm). |
| `pipe.sql_planning_error` | error | `execute_against_session` (`crates/prism-query/src/materialization.rs`) — emitted at TWO sites, both emitting identical field schema: **(a)** `Err` arm of `session_ctx.sql(&sql).await` within the `Ast::Pipe(pipe)` arm, when DataFusion SQL planning fails on the lowered pipe SQL; **(b)** `Err` arm of `session_ctx.sql(&sql).await` within the `Ast::SqlPipe(spq)` arm, when DataFusion SQL planning fails on the lowered sqlpipe SQL | `error: %display` (the DataFusion error Display string — the raw planning error from DataFusion before client-facing redaction; present in server logs only), `pipe_sql: %display` (the DataFusion SQL string that triggered the planning error — the same value as in `pipe.sql_lowering` for this query), `event_type: "pipe.sql_planning_error"` | ENRICH-4-B pipe-to-SQL DataFusion planning error. Emitted when `session_ctx.sql(&sql)` returns `Err(e)` for a lowered pipe SQL string. The caller returns `PrismError::QueryExecutionFailed { detail: "pipe SQL planning error: <redacted; see server logs>" }` — the error detail visible to the MCP client is intentionally redacted (CWE-209 information exposure prevention). This event captures the FULL DataFusion error (in `error` field) and the failing SQL (in `pipe_sql` field) at the server log level so operators can diagnose planning failures without exposing internal SQL structure to clients. Audit role: error / operational diagnostic (not a security-audit event, but operator-actionable: indicates a pipe query that was accepted by the parser but rejected by DataFusion at planning time — may indicate a bug in `pipe_to_executable_sql` lowering logic, a schema mismatch between the pipe AST and registered MemTables, or a DataFusion version incompatibility). SECURITY: `error` contains the raw DataFusion error message (may include column names or operator names from the lowered SQL, NOT client credential data or sensor response content); `pipe_sql` carries the same lowered SQL as `pipe.sql_lowering`, including any user-supplied WHERE-clause string literals; control characters stripped via `sanitize_for_log` (CWE-117). Recurrence: once per `Ast::Pipe` or `Ast::SqlPipe` query where DataFusion SQL planning fails. Retention: per organization audit policy. Traces to: ENRICH-4-B (`Ast::Pipe` arm — original registration); S-PRISMQL-CASE-INSENSITIVE-001 (`Ast::SqlPipe` arm — same event_type reused per SAP-1). SAP-1 obligation: PG-LP11-001 (ENRICH-4-B registered `Ast::Pipe` arm; S-PRISMQL-CASE-INSENSITIVE-001 adversary pass-32 fix-burst registers `Ast::SqlPipe` arm). |
| `filter.sql_lowering` | debug | `execute_against_session` (`crates/prism-query/src/materialization.rs`) — emitted in the `Ast::Filter(filter)` arm immediately after the filter predicate is lowered to a `SELECT * FROM <table> WHERE <predicate>` SQL string, before `session_ctx.sql(&filter_sql)` is called | `filter_sql: %display` (the DataFusion SQL string produced by lowering the bare or source-qualified filter predicate — the lowered SQL representation of the FilterQuery AST; debug-level only, carries the lowered SQL including user-supplied WHERE-clause string literals, control-character-sanitized via `sanitize_for_log` at emission (CWE-117)), `event_type: "filter.sql_lowering"` | S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 filter-mode SQL lowering success diagnostic. Emitted once per `Ast::Filter` query that passes the predicate-to-SQL lowering step without error. The `filter_sql` field carries the full lowered SQL string — useful for debugging filter execution plans and verifying that the filter predicate was lowered as expected (e.g., `SELECT * FROM crowdstrike_detections WHERE severity = 'HIGH'`). Emitted at `debug` level (not `info`) because the SQL string is an internal execution detail; operators can enable debug-level tracing on `prism_query::materialization` to capture it. Audit role: non-security diagnostic / operational debugging (NON-audit). This event is NOT a security-audit event; it records an internal query-execution step visible only in debug logging, not in the default info/warn/error production stream. SECURITY: `filter_sql` carries the lowered SQL including user-supplied WHERE-clause string literals (not sensor response data, credentials, or PII); control characters are stripped at emission via `sanitize_for_log` (CWE-117). Operators should treat debug-level logs as internal diagnostic output. Recurrence: once per `Ast::Filter` query where the predicate-to-SQL lowering succeeds. Retention: per organization audit policy (debug level; typically not retained in production log archives). Traces to: S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 Area D (filter-mode SQL execution path, `execute_against_session` `Ast::Filter` arm), BC-2.11.023 §D4. SAP-1 obligation: PG-LP11-001 (registered in same spec burst as story delivery, S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001). |
| `filter.sql_planning_error` | error | `execute_against_session` (`crates/prism-query/src/materialization.rs`) — emitted in the `Err` arm of `session_ctx.sql(&filter_sql).await` within the `Ast::Filter(filter)` arm, when DataFusion SQL planning fails on the lowered filter SQL | `error: %display` (the DataFusion error Display string — the raw planning error from DataFusion before client-facing redaction; present in server logs only), `filter_sql: %display` (the DataFusion SQL string that triggered the planning error — the same value as in `filter.sql_lowering` for this query), `event_type: "filter.sql_planning_error"` | S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 filter-mode DataFusion planning error. Emitted when `session_ctx.sql(&filter_sql)` returns `Err(e)` for a lowered filter SQL string. The caller returns `PrismError::QueryExecutionFailed { detail: "filter SQL planning error: <redacted; see server logs>" }` — the error detail visible to the MCP client is intentionally redacted (CWE-209 information exposure prevention). This event captures the FULL DataFusion error (in `error` field) and the failing SQL (in `filter_sql` field) at the server log level so operators can diagnose planning failures without exposing internal SQL structure to clients. Audit role: error / operational diagnostic (not a security-audit event, but operator-actionable: indicates a filter query that was accepted by the parser but rejected by DataFusion at planning time — may indicate a predicate lowering bug, a schema mismatch between the filter AST and registered MemTables, or a DataFusion version incompatibility). SECURITY: `error` contains the raw DataFusion error message (may include column names or operator keywords from the lowered SQL, NOT client credential data or sensor response content); `filter_sql` carries the same lowered SQL as `filter.sql_lowering`, including any user-supplied WHERE-clause string literals; control characters stripped via `sanitize_for_log` (CWE-117). Recurrence: once per `Ast::Filter` query where DataFusion SQL planning fails. Retention: per organization audit policy. Traces to: S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 Area D (filter-mode SQL execution path, `execute_against_session` `Ast::Filter` arm), BC-2.11.023 §D4. SAP-1 obligation: PG-LP11-001 (registered in same spec burst as story delivery, S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001). |
| `sql.sql_planning_error` | error | `execute_against_session_with_registry` (`crates/prism-query/src/materialization.rs`) — emitted in the `Err` arm of `session_ctx.sql(&sql).await` within the `Ast::Sql(Select)` arm, when DataFusion SQL planning fails on the normalized SQL string | `error: %display` (the DataFusion error Display string — the raw planning error from DataFusion before client-facing redaction; present in server logs only), `sql: %display` (the DataFusion SQL string that triggered the planning error), `event_type: "sql.sql_planning_error"` | DEFECT-CSDEVICES-EMPTY-PIPELINE-001 SQL-mode DataFusion planning error. Emitted when `session_ctx.sql(&sql)` returns `Err(e)` for the normalized SQL string in the `Ast::Sql(Select)` arm. The caller returns `PrismError::QueryExecutionFailed { detail: "<redacted; see server logs>" }` — the error detail visible to the MCP client is intentionally redacted (CWE-209 information exposure prevention). This event captures the FULL DataFusion error (in `error` field) and the failing SQL (in `sql` field) at the server log level so operators can diagnose planning failures without exposing internal SQL structure to clients. Audit role: error / operational diagnostic (not a security-audit event, but operator-actionable: indicates a SQL-mode query accepted by the parser but rejected by DataFusion at planning time — may indicate a schema mismatch, a temporal literal issue, or a DataFusion version incompatibility). SECURITY: `error` contains the raw DataFusion error message (may include column names or operator names from the SQL, NOT client credential data or sensor response content); `sql` carries the normalized SQL string including any user-supplied WHERE-clause string literals; control characters stripped via `sanitize_for_log` (CWE-117). Recurrence: once per `Ast::Sql(Select)` query where DataFusion SQL planning fails. Retention: per organization audit policy. Traces to: DEFECT-CSDEVICES-EMPTY-PIPELINE-001 (F-CSD-P20-010). SAP-1 obligation: PG-LP11-001 (registered in same burst as implementation, DEFECT-CSDEVICES-EMPTY-PIPELINE-001). |
| `plugin.compilation_cache_init_skipped` | warn | `PluginRuntime` construction (`crates/prism-spec-engine/src/plugin/mod.rs`) | `error: %display` (the `wasmtime::Error` Display — cache initialization failure reason, e.g., filesystem permissions error, unsupported platform, or invalid cache directory path) | `wasmtime::Cache::new(cache_config)` returns `Err(e)` during `PluginRuntime` construction (S-PERF-GATE-008, ADR-049 D3). The wasmtime compilation cache is skipped (`config.cache(None)`) and `PluginRuntime` construction continues without caching. This is a **perf-degradation** (degradable) event — WASM plugins will recompile from scratch on each cold start rather than using the on-disk cache. Audit role: operator-diagnostic (NOT a security-audit event; no rotation/retention policy required — this is an observability signal enabling operators to diagnose why caching is unavailable, e.g., to correct directory permissions or path configuration). Recurrence: once per cold-start where `wasmtime::Cache::new` returns `Err`; zero emissions expected in normal operation when the cache directory is writable. Retention: per organization audit policy (diagnostic). Registered: S-PERF-GATE-008 / ADR-049 D3. SAP-1 obligation: PG-LP11-001 (registered in same spec burst as story delivery). |
| `infusion.coercion_failed` | warn | `InfusionAsyncUdf::coerce_to_typed` (`crates/prism-query/src/infusion_udf.rs`) — five emission sites within `coerce_to_typed`, all emitting an identical field schema: (1) JSON-list input to typed UDF (`value.starts_with('[')` with `output_type != DataType::Utf8`, ADR-051 D4 Scalar-Input rule — `declared_type` = `self.descriptor.output_type.as_str()` spec-vocabulary string, identical to sites 2–5); (2) Int64 parse failure (both direct `i64::parse` and JSON-number fallback exhausted — `declared_type = "integer"`); (3) Float64 parse failure (both direct `f64::parse` and JSON-number fallback exhausted — `declared_type = "float"`); (4) Boolean unrecognised value (case-insensitive match against `{true,1,yes,false,0,no}` exhausted — `declared_type = "boolean"`); (5) Datetime RFC-3339 parse failure (`parse_datetime_to_micros` returns `Err` — `declared_type = "datetime"`). | `field_name: %display` (the enrichment UDF field name, e.g., `"threat_score"` — the `InfusionUdfDescriptor::name` value associated with this UDF call; control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission), `infusion_id: %display` (the infusion spec ID from `InfusionUdfDescriptor::infusion_id`, e.g., `"cyberint_ioc_lookup"`; control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission), `declared_type: &str` (spec-vocabulary output_type string — `"integer"` / `"float"` / `"boolean"` / `"datetime"` — at ALL FIVE sites; `self.descriptor.output_type.as_str()` is used uniformly across all five emission sites in `InfusionAsyncUdf::coerce_to_typed` (`infusion_udf.rs`); NO Arrow DataType Debug form at any site; control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission), `truncated_value: %display` (the coercion-input string truncated to up to 50 chars via `value.chars().take(50).collect::<String>()`, then passed through `sanitize_for_log` which strips control chars in Unicode Cc category (C0 U+0000–U+001F, DEL U+007F, C1 U+0080–U+009F) and line/paragraph separators U+2028/U+2029 (CWE-117 / SEC-001) — the emitted value is NOT the raw input; AD-017 length guard additionally prevents over-long vendor data from leaking PII or credential fragments via log aggregation) | E-INFUSE-014: coercion failure — emitted when `coerce_to_typed` cannot coerce the enrichment-source string to the declared Arrow output type. The affected row produces NULL output; the containing record is NEVER dropped (BC-2.16.003 records-not-dropped invariant). Audit role: type-mismatch diagnostic — enables operators to detect schema mismatches between the enrichment plugin's output format and the infusion spec's declared `output_type`; high frequency on a field signals spec/wire misalignment (e.g., source returns a float string for an `integer`-typed field, or an RFC-3339 string that fails the parser). SECURITY: `truncated_value` is capped at 50 chars of enrichment-source output (vendor-supplied data, not credential material); the cap guards against incidental PII or secret leakage via over-long value logging (AD-017). `field_name` and `infusion_id` are structural spec identifiers — no sensor response content, PII, or credential data appears in any field. Recurrence: one emission per failing row per UDF call (NOT aggregated — each row whose coercion fails emits independently; no rate limiting). Under conditions where the source consistently returns un-coercible values, may be high-frequency (one emission per record). Retention: per organization audit policy. Traces to: E-INFUSE-014 (error-taxonomy.md §INFUSE), ADR-051 D2 (coercion branches for Int64/Float64/Boolean), ADR-052 D2 (datetime parse — `parse_datetime_to_micros` reuse), S-DEMO-ENRICHMENT-TYPED-OUTPUT-001 AC-012. SAP-1 obligation: PG-LP11-001 (registered in same spec burst as story delivery, S-DEMO-ENRICHMENT-TYPED-OUTPUT-001). |

## Variable Scope and Lifetime
- Variables produced by a step are available to all subsequent steps but not to prior steps
- Variables from the most recent execution of a step overwrite previous values (relevant when a step is re-executed due to fan-out)
- Query-time variables are available to all steps: `${query.client_id}`, `${query.filter.*}` (push-down filter values extracted by the query planner)

## Fan-Out Behavior
- When a variable interpolation resolves to an array, the step is executed in batches
- Batch size is configurable per step via `fan_out_batch_size` (default 100)
- Fan-out results are concatenated into a single result set for the step
- Fan-out respects rate limit hints — each batch counts as a separate request

## Invariants
- Steps execute sequentially; no parallel step execution within a single table fetch (simplifies variable scoping)
- The 10K materialization limit (DI-019) applies to the final collected records, not to intermediate step results
- No BC-specific invariants beyond DI-019 and rate-limit behavior defined above.

## Error Conditions
| Error | Condition | Behavior |
|-------|-----------|----------|
| `E-SPEC-010` | Variable interpolation failure at runtime (variable exists but field path does not match response structure) | `E-SPEC-010` with the step name, variable reference, and actual response structure hint |
| (sensor_error) | HTTP error on any step (non-401, non-200) | Pipeline aborts; ALL accumulated records discarded; `Err(HttpRequestFailed)` propagated to caller. NO partial `PipelineResult` returned. See partial-record-discard postcondition for rationale. |
| (no error) | Empty response from a non-final step | Subsequent steps receive empty arrays; produces zero results (not an error) |

## Edge Cases
| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| Fan-out batch of 250 IDs | 250 IDs; batch_size=100 | 3 executions: 100, 100, 50; all results concatenated |
| Empty non-final step | step 1 returns empty; step 2 needs step 1 output | Step 2 receives empty variable; produces zero records |
| Pagination | step has cursor pagination | Iterates pages until cursor=null or empty page |
| Rate limiting | spec declares 5 req/s | 200ms delay between requests; burst allowed |

## Canonical Test Vectors

See `.factory/specs/prd-supplements/test-vectors.md` for full canonical vectors.

| Scenario | Input | Expected Output |
|----------|-------|-----------------|
| Happy path — 2-step | step 1 fetches IDs; step 2 fetches details by ID | Final RecordBatch with detail records |
| Fan-out | step 2 depends on array from step 1 (250 items) | 3 batched requests; all results concatenated |
| HTTP error on step 1 | step 1 returns 500 | Pipeline aborts; sensor_error in response |
| Empty step 1 | step 1 returns zero records | Step 2 produces zero records; no error |
| Interpolation failure | step 2 references `${step1.missing_field}` | `E-SPEC-010` with step name and field path |

## Verification Properties

| VP ID | Description |
|-------|-------------|
| (none) | Fan-out batch concatenation requires HTTP mock integration; forward-reference scoping rejection is covered by VP-059 (BC-2.16.009 validation); no additional formal VP for runtime pipeline execution. |

## Traceability
| Field | Value |
|-------|-------|
| L2 Capability | CAP-029 |
| L2 Invariants | DI-019 |
| Related BCs | BC-2.16.001 (spec loading), BC-2.01.014 (exponential backoff on HTTP calls), BC-2.01.010 (partial failure) |
| Related ADRs | ADR-056 (PageNumber pagination grammar); ADR-028 (TOML method field as HTTP method source of truth) |
| Related Stories | S-WAVE-A-CYBERINT-SPEC-001 (implementing story for PageNumber wiring) |
| Priority | P0 |

## Changelog

| Version | Burst | Date | Author | Change |
|---------|-------|------|--------|--------|
| 2.13 | FB84 | 2026-07-28 | product-owner | **F-WASE-P68-MED-001 + MED-002 (BC-2.16.002 Catalog ↔ emission-site coherence).** MED-001 (orphan rows removed): deleted catalog rows for `boot.type_spec_read_failed` and `boot.type_spec_parse_failed` — both cited `build_type_spec_map_for_overlay` in `crates/prism-bin/src/boot.rs` as emission site; zero live emission sites exist for either event in the workspace. Root cause: `build_type_spec_map_for_overlay` was refactored away; comments in `boot.rs` confirm the replacement eliminates the double-parse and replaces the hard-abort, propagating `Err(BootError::ConfigInvalid(...))` instead. Error propagation provides audit trail without catalog overhead per SAP-1 rule 5 and D-765 precedent (removal of emission replaced by `?` propagation does not require a new catalog row). Catalog count 92→90. MED-002 (self-contradiction resolved): `plugin_auth_provider_constructed` row named `boot.audit.initialized` as a "sibling boot emission" while no catalog row and no exclusion clause existed for it. Applied option (b): added event-granularity exclusion to the "Events and crates outside this catalog's scope" section naming BC-2.05.012 §`boot.audit.initialized` Sentinel Event Schema as the governing contract — that BC provides the field schema and the load-bearing test (`bc_2_05_012_audit_init.rs`). Option (b) chosen over (a) because dual governance across two BCs is a higher defect risk than a clean exclusion; BC-2.05.012 already owns the event completely. Exclusion phrased at event granularity (not crate granularity) because `prism-bin` is broadly in scope for steps 7/8/9/10/11 — only step-6 audit initialization is excluded. Catalog label `(v1.61)` → `(v1.62)` per POL-30 Fork B (structural catalog change: two rows removed, exclusion clause extended). POL-29 9a: no named twin for BC-2.16.002 (BC-2.16.009 is a distinct capability, not a split-event twin); whole-file residual sweep performed for `boot.type_spec_read_failed`, `boot.type_spec_parse_failed`, `build_type_spec_map_for_overlay` — residual reference in v1.46 changelog row is historical record (append-only; not modified per TD-VSDD-091); 9b: exclusion clause extension is new content with no downstream verbatim copy target; 9c: two new process-governance MUST statements ("MUST be registered in BC-2.05.012") are authoring discipline enforced by SAP-1, consistent with existing governance MUSTs in the same exclusion clause for prism-security/prism-credentials — no story-anchor required for process-governance rules. Touched exactly one file: BC-2.16.002. |
| 2.12 | FB71 | 2026-07-27 | product-owner | F-CVA-HIGH-001: corrected PageNumber Pagination Dispatch activation gate bullet. Replaced false "skips injection" language with two-layer semantics verbatim from ADR-056 §D8 (copy-source for this row): (1) spec-load layer — `validate_sensor_spec` §Category 4 rejects `PageNumber { page_size: 0 }` with `SpecErrorCode::ESpec001`; value `0` cannot reach `build_request` from a validated spec; (2) runtime sentinel — `execute_step` (single-request path) passes `active_page_size = 0` as engine-internal sentinel; `page_size > 0` guard in `build_request` is defense-in-depth for this sentinel path only. Anchored: `S-WAVE-A-CYBERINT-SPEC-001` RG-017. POL-29 9a: no named twin for BC-2.16.002 (BC-2.16.009 is a distinct capability, not a split-event twin); 9b: BC-2.16.002 is the downstream copy target of ADR-056 §D8 — both swept in this burst (this is the copy-target fix pass); 9c: `S-WAVE-A-CYBERINT-SPEC-001` RG-017 is the load-bearing anchor for the corrected activation gate MUST. |
| 2.11 | FB53b / F-WASE-P64-CRIT-003 | 2026-07-26 | product-owner | **Added `PageNumber Pagination Dispatch: POST-body vs GET-URL (ADR-056, S-WAVE-A-CYBERINT-SPEC-001)` postcondition** as a sibling to the `OffsetLimit Pagination Dispatch` bullet. Closes the contract gap identified by adversary finding F-WASE-P64-CRIT-003: `PageNumber` had zero mentions in prior versions; this addition specifies the POST-body and GET-URL dispatch forms, the `offset + 1` page counter (first request emits `page = 1`), the `offset += 1` advance rule (distinct from `OffsetLimit`'s `offset += page_size`), the `page_record_count < page_size` termination condition, the activation gate (`page_size > 0`), and the empty-body-template first-request shape. Grounded in ADR-056 §D3 (dispatch shape) and the `cyberint_alerts_openapi_06.20.2026.json` `GetAlertsRequest` schema. Traceability section: added `Related ADRs` row (ADR-056, ADR-028) and `Related Stories` row (S-WAVE-A-CYBERINT-SPEC-001). No event-catalog row added (no new `event_type` site introduced). Frontmatter v2.10→v2.11; modified: 2026-07-26. |
| 2.10 | DEFECT-CSDEVICES-EMPTY-PIPELINE-001 / F-CSD-P22-005 SQL-field injection-safety overpromise correction | 2026-07-10 | product-owner | **Closes F-CSD-P22-005 (OBS) — five catalog rows for `filter.sql_lowering`, `filter.sql_planning_error`, `pipe.sql_lowering`, `pipe.sql_planning_error`, `sql.sql_planning_error` overpromised injection safety by claiming the SQL fields contain only "column names and operator keywords" / "structural query content only."** Lowered SQL embeds user-supplied WHERE-clause string literals verbatim (e.g., `WHERE severity = 'HIGH'` where `'HIGH'` is user input). The implementer (same burst, DEFECT-CSDEVICES-EMPTY-PIPELINE-001) applied `sanitize_for_log` at all five emission sites (`crates/prism-query/src/materialization.rs` lines 1259, 1406, 1413, 1471, 1478; `sanitize_for_log` imported at line 46). **Changes:** (1) `pipe.sql_lowering` fields column: "debug-level only, no-PII: contains column names and operator keywords from the query, not sensor data" → "debug-level only; carries the lowered SQL including user-supplied WHERE-clause string literals, control-character-sanitized via `sanitize_for_log` at emission (CWE-117)". (2) `pipe.sql_lowering` SECURITY sentence: "contains column names, operator keywords, and table identifiers from the user's PipeQuery (which are structural elements, not secret values); it does NOT contain sensor response data, credential values, or PII" → "carries the lowered SQL including user-supplied WHERE-clause string literals (not sensor response data, credentials, or PII); control characters are stripped at emission via `sanitize_for_log` (CWE-117)". (3) `pipe.sql_planning_error` SECURITY: "`pipe_sql` contains the same lowered SQL as `pipe.sql_lowering` (structural query content only)" → "`pipe_sql` carries the same lowered SQL as `pipe.sql_lowering`, including any user-supplied WHERE-clause string literals; control characters stripped via `sanitize_for_log` (CWE-117)". (4) `filter.sql_lowering` fields column: same correction pattern as (1). (5) `filter.sql_lowering` SECURITY: same correction as (2). (6) `filter.sql_planning_error` SECURITY: same correction as (3). (7) `sql.sql_planning_error` SECURITY: "`sql` contains the normalized SQL string (structural query content only)" → "`sql` carries the normalized SQL string including any user-supplied WHERE-clause string literals; control characters stripped via `sanitize_for_log` (CWE-117)". Catalog row count unchanged (92); no new event_type; catalog label bumped `(v1.60)` → `(v1.61)` per POL-30 Fork B (catalog-row description extension). Frontmatter v2.09→v2.10; modified: 2026-07-10. |
| 2.09 | DEFECT-CSDEVICES-EMPTY-PIPELINE-001-F-CSD-P20-010 | 2026-07-10 | product-owner | **F-CSD-P20-010 (SAP-1 / PG-LP11-001) closure: added new catalog row 92 `sql.sql_planning_error` ERROR emitted by `execute_against_session_with_registry` (`crates/prism-query/src/materialization.rs`) in the `Err` arm of `session_ctx.sql(&sql).await` within the `Ast::Sql(Select)` arm, when DataFusion SQL planning fails on the normalized SQL string.** Fields: `error: %display` (raw DataFusion error — server-side only; client receives `PrismError::QueryExecutionFailed { detail: "<redacted; see server logs>" }`, CWE-209 information exposure prevention), `sql: %display` (the normalized SQL string that triggered the planning error — structural query content only, no sensor response data or credentials). Audit role: error / operational diagnostic — indicates a SQL-mode query accepted by the parser but rejected by DataFusion at planning time; may indicate a schema mismatch, temporal literal issue, or DataFusion version incompatibility. Sibling of `pipe.sql_planning_error` (ENRICH-4-B) and `filter.sql_planning_error` (S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001). Scope statement extended to include `prism-query` SQL-mode DataFusion planning-error emission from `execute_against_session_with_registry` (F-CSD-P20-010). Catalog count 91→92; catalog bullet label `(v1.59)` → `(v1.60)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v2.08→v2.09; modified: 2026-07-10. |
| 2.08 | FIX-IEQ-ERRPATH-001-ADV-PR-P1-MED-001 | 2026-07-09 | product-owner | **ADV-PR-P1-MED-001 closure: `column_not_found.rejected` catalog row `column` field annotated with `sanitize_for_log` treatment (CWE-117 / SEC-001).** Commit 51f071ff (FIX-IEQ-ERRPATH-001) applied `sanitize_for_log` (strips Unicode Cc + U+2028/U+2029) to the `column` field at all THREE `column_not_found.rejected` emission sites; the catalog row's Fields column previously described `column: %display` as "the column name referenced in the query" with no sanitization annotation — the sibling row `infusion.coercion_failed` (row 91) shows the correct phrasing "control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission". **Change:** `column: %display` Fields description: `(the column name referenced in the query — e.g., "sevrity")` → `(the column name referenced in the query — e.g., "sevrity"; control-char-sanitized via \`sanitize_for_log\` (CWE-117 / SEC-001) before emission)`, mirroring the `infusion.coercion_failed` sibling phrasing exactly. No new event_type, no new rows, no field-schema structural change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). Companion: BC-2.11.016 v1.22 (§Postconditions injection-safety note for `column` MCP-facing payload, ADV-PR-P1-OBS-001); error-taxonomy.md v2.35 (E-QUERY-038 `{column}` placeholder injection-safety clause). Frontmatter v2.07→v2.08; modified: 2026-07-09. |
| 2.07 | FIX-IEQ-ERRPATH-001-ADV-FIX-P3-MED-001 | 2026-07-08 | product-owner | **ADV-FIX-P3-MED-001 closure: `column_not_found.rejected` catalog row updated from two to three emission sites.** The fix-PR FIX-IEQ-ERRPATH-001 added a THIRD emission site — `check_column_against_available_set` (`crates/prism-query/src/engine.rs`), called from `check_pipe_stage_columns` for pipe-mode positions 8–14 — emitting the same field schema (`column`, `table`, `client_id`, `available_count`). **Third site (DRIFT-IEQ-NONEXISTENT-COL-ERRPATH-001 traceback):** `available_count` on the binding-context path reflects `available.len()` where `available` is the current binding view at the failing stage — post-stats REPLACE = {aliases ∪ by_fields}; post-enrich union includes output_columns. This is the derived-column view, NOT the raw `TableRegistry` schema for that stage. **Changes to catalog row:** Trigger column: "two emission sites" → "three emission sites"; `check_column_availability` → `check_column_availability` and `check_column_against_available_set`; (c) binding-context path added with DRIFT-IEQ-NONEXISTENT-COL-ERRPATH-001 traceback. Fields column: `available_count` description expanded with path (c) semantics. Notes column: "Both emission sites" → "All three emission sites"; Traces-to extended with FIX-IEQ-ERRPATH-001; SAP-1 obligation extended with third site registration. No new event_type, no new row count change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). Companion: BC-2.11.016 v1.11 (ADV-FIX-P3-LOW-002, `available_columns` payload semantics clarification); error-taxonomy v2.25 (POL-25 sweep). Frontmatter v2.06 → v2.07; modified: 2026-07-08. |
| 2.06 | ADV-PR-P5-OBS-001-unicode-cc-scope-widening | 2026-07-08 | product-owner | **ADV-PR-P5-OBS-001 security-triage MEDIUM closure — `sanitize_for_log` control-char scope widened to Unicode Cc + U+2028/U+2029 (CWE-117; connectivity.rs sanitize_error parity).** Row 91 (`ocsf.enum_label_unrecognized`) `value` and `sensor_type` field descriptions: `(ASCII 0x00–0x1F + 0x7F)` → `(Unicode Cc category: C0 U+0000–U+001F, DEL U+007F, C1 U+0080–U+009F; plus U+2028 LINE SEPARATOR and U+2029 PARAGRAPH SEPARATOR)`. `infusion.coercion_failed` `truncated_value` field description: `strips ASCII control chars 0x00–0x1F and 0x7F` → `strips control chars in Unicode Cc category (C0 U+0000–U+001F, DEL U+007F, C1 U+0080–U+009F) and line/paragraph separators U+2028/U+2029`. No new event_type, no new rows, no field-schema structural change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). POL-29 sweep: no other `0x1F`/`ASCII control` scope citations tied to sanitize_for_log found in BC body (changelog rows excluded per TD-VSDD-091). Companion: error-taxonomy.md v2.19→v2.20 (E-INFUSE-013 + E-INFUSE-014 SEC-001 Rendering Notes updated with same widened scope). Frontmatter v2.05→v2.06; modified: 2026-07-08 (POL-27). |
| 2.05 | S-PRISMQL-CASE-INSENSITIVE-001-CR-004-SEC-001-sanitize-for-log | 2026-07-08 | product-owner | **CR-004 / SEC-001 (CWE-117) closure: catalog row 91 `ocsf.enum_label_unrecognized` field descriptions for `value` and `sensor_type` updated to document `sanitize_for_log` control-char stripping.** Both emission sites (PRIMARY: `build_column_array` in `spec_driven_adapter.rs`; SECONDARY: `normalize_with_mappers` in `normalizer.rs`) now apply `prism_core::sanitize_for_log` (strips ASCII control chars 0x00–0x1F and 0x7F, CWE-117 log-injection defense) BEFORE the 50-codepoint truncation cap (SEC-002). Row 91 `value` and `sensor_type` field descriptions amended to state "control chars stripped via `prism_core::sanitize_for_log` (ASCII 0x00–0x1F + 0x7F) before truncation (CWE-117 / SEC-001 defense)". No new event_type, no new rows, no field-schema structural change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). Frontmatter v2.04 → v2.05. |
| 2.04 | S-PRISMQL-CASE-INSENSITIVE-001-adversary-pass-32-F-P32-MED-001 | 2026-07-08 | product-owner | **LOCAL adversary pass-32 F-P32-MED-001 closure: `pipe.sql_lowering` and `pipe.sql_planning_error` catalog rows amended to enumerate BOTH emission sites.** Previously both rows described the emission site as ONLY the `Ast::Pipe(pipe)` arm (ENRICH-4-B original registration). S-PRISMQL-CASE-INSENSITIVE-001 added an `Ast::SqlPipe(spq)` arm in `execute_against_session` (`materialization.rs`) that reuses the same event_type values per SAP-1 (code comment: "SAP-1: reuse existing catalog event type"). Sibling rows (`ocsf.enum_label_unrecognized`, `column_not_found.rejected`, `column_source_path_extraction_failed`) all enumerate multiple emission sites explicitly — these two rows must follow the same convention. **Changes to `pipe.sql_lowering` row:** (1) Trigger column: changed from single-site description to "emitted at TWO sites, both emitting identical field schema: **(a)** `Ast::Pipe(pipe)` arm after `pipe_to_executable_sql`; **(b)** `Ast::SqlPipe(spq)` arm after `sqlpipe_to_executable_sql`". (2) First recurrence sentence: "`Ast::Pipe`" → "`Ast::Pipe` or `Ast::SqlPipe`". (3) `Recurrence:` field: updated to include `Ast::SqlPipe`. (4) `Traces to:` field: updated to cite both ENRICH-4-B (`Ast::Pipe`) and S-PRISMQL-CASE-INSENSITIVE-001 (`Ast::SqlPipe`). (5) SAP-1 obligation: updated to note both registration bursts. **Changes to `pipe.sql_planning_error` row:** identical amendment pattern — trigger column, recurrence, traces-to, and SAP-1 obligation updated to enumerate both `Ast::Pipe(pipe)` and `Ast::SqlPipe(spq)` arms. Both sites verified against S-PRISMQL-CASE-INSENSITIVE-001 worktree `materialization.rs` lines 1453-1456 (`Ast::SqlPipe` lowering debug) and 1459-1465 (`Ast::SqlPipe` planning error). Identical field schema: `pipe_sql: %display` + `event_type` for lowering; `error: %display` + `pipe_sql: %display` + `event_type` for planning error. No new event_type values; no row count change; no field-schema change. POL-30 Fork B: catalog-row description extension (no structural catalog change; catalog bullet label stays (v1.59); count unchanged 91). Frontmatter v2.03 → v2.04. |
| 2.03 | S-PRISMQL-CASE-INSENSITIVE-001-adversary-pass-16 | 2026-07-07 | product-owner | **LOCAL adversary pass-16 closures: F-P16-LOW-001 (`sensor_type` 50-codepoint cap) + F-P16-MED-002 (empty-string bypass trigger-condition clarification) in catalog row 91.** (1) **F-P16-LOW-001 (LOW):** `sensor_type` field description in row 91 lacked the 50-codepoint cap note. SEC-002 defense applies symmetrically to both `value` and `sensor_type` before emission. Change: `sensor_type: %display` description extended to state "capped at 50 codepoints before emission, SEC-002 defense; symmetric with `value` field". (2) **F-P16-MED-002 (MED — trigger-condition clarification):** Row 91 trigger-condition description started "An OCSF string enum-label field … carries a value with no case-insensitive match" without explicitly excluding empty strings. Empty-string values bypass normalization entirely at both insertion points and do NOT trigger this emission (they represent unset/missing sensor fields, not vendor enum values). Change: trigger-condition prepended with explicit empty-string exclusion: "An OCSF string enum-label field carries a **non-empty** value with no case-insensitive match … **Empty-string values do NOT trigger this emission …**" with cross-reference to BC-2.02.013 §Postconditions, EC-02-028. No new event_type, no new rows, no field-schema structural change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). Companion: BC-2.02.013 v1.5 → v1.6 (empty-string bypass §Postconditions bullet + EC-02-028 + test vector + `sensor_type` cap note). Frontmatter v2.02 → v2.03. |
| 2.02 | D-1584-POL-29-sweep | 2026-07-07 | product-owner | **v2.02 — row 91 live BC-2.02.013 cross-ref updated (v1.4 pin removed per TD-VSDD-091).** Catalog row 91 (`ocsf.enum_label_unrecognized`) carried two live cross-reference version pins to BC-2.02.013 v1.4: (1) pass-through behavior description ("pass-through per RG-021 / BC-2.02.013 v1.4 §Postconditions") and (2) Traces-to citation ("Traces to: BC-2.02.013 v1.4 §Error Cases"). Both updated to unversioned form ("BC-2.02.013 §Postconditions" / "BC-2.02.013 §Error Cases") — version pins are non-load-bearing on live cross-references per TD-VSDD-091. Historical changelog rows (v2.01, v2.00, v1.99, v1.98) retain their original version citations and are unchanged. Frontmatter v2.01 → v2.02. |
| 2.01 | S-PRISMQL-CASE-INSENSITIVE-001-adversary-pass-7-obs1-value-cap | 2026-07-07 | product-owner | **LOCAL adversary pass-7 OBS-1 closure: document 50-codepoint `value` cap (SEC-002) in catalog row 91 field schema.** Both `ocsf.enum_label_unrecognized` warn emission sites cap the `value` field at 50 codepoints before emitting (SEC-002 defense). Prior v2.00 catalog row 91 field schema listed `value: %display` with no cap — a future refactor could drop the cap without violating any written contract. Change: catalog row 91 `value` field description amended to state "capped at 50 codepoints before emission, SEC-002 defense against over-long vendor data leaking into log aggregation". No new event_type, no new rows, no field-schema change — description extension per POL-30 Fork B; catalog bullet label stays (v1.59); count unchanged (91). Companion: BC-2.02.013 v1.3 → v1.4 (same cap note added to §Postconditions warn-event field list and §Error Cases table). Frontmatter v2.00 → v2.01. |
| 2.00 | S-PRISMQL-CASE-INSENSITIVE-001-pass5-F-CRIT-002-catalog-row-91-amendment | 2026-07-07 | product-owner | **LOCAL adversary pass-5 F-CRIT-002 closure (architectural adjudication 2026-07-07): catalog row 91 `ocsf.enum_label_unrecognized` updated to list TWO emission sites.** Previously listed only SECONDARY site `OcsfNormalizer::normalize_with_mappers` (`crates/prism-ocsf/src/normalizer.rs`). Amended to list: **(1) PRIMARY (production):** `build_column_array` in `crates/prism-bin/src/spec_driven_adapter.rs` — the spec-driven adapter materialization path through which DataFusion receives and queries sensor data; emitted when `OcsfEnumMap::normalize_enum_label` returns `None` for a String column in the enum-label field set. **(2) SECONDARY (DynamicMessage/protobuf path, future):** `OcsfNormalizer::normalize_with_mappers` in `crates/prism-ocsf/src/normalizer.rs` — zero production callers on spec-driven query path; correct insertion point for future protobuf-export path. Both sites emit identical field schema. Row 91 "pass-through" description updated to distinguish PRIMARY (Arrow `StringArray` left as-received) and SECONDARY (`DynamicMessage` left as-received). Version references updated BC-2.02.013 v1.2 → v1.3 in two places. SAP-1 obligation updated to specify PRIMARY implementation obligation separately from SECONDARY. Scope statement extended to include PRIMARY emission site from `build_column_array` in `spec_driven_adapter.rs`. Catalog bullet label stays (v1.59) — description extension per POL-30 Fork B (no new rows, no event_type change, no field-schema change). Catalog count unchanged (91). Companion: BC-2.02.013 v1.2 → v1.3 (insertion-point amendment, PRIMARY/SECONDARY invariant addition, two-sites warn-event note). Frontmatter v1.99 → v2.00. |
| 1.99 | S-PRISMQL-CASE-INSENSITIVE-001-pass2-spec-fixes | 2026-07-06 | product-owner | **LOCAL adversary pass-2 field-name correction: catalog row 91 `ocsf.enum_label_unrecognized` — `field_name` example corrected from `"activity"` to `"activity_name"`.** OCSF v1.7.0 schema.json confirms `activity_id.sibling = "activity_name"` (OCSF sets `suppress_checks: ["sibling_convention"]` on `activity_id`, acknowledging this exception to the `{F}_id → {F}` pattern). A `normalize_enum_label` call keying on `"activity"` would fail to find captions (no such OCSF dictionary attribute); the correct key is `"activity_name"`. BC-2.02.013 version reference in this row updated v1.1 → v1.2 (companion BC-2.02.013 v1.2 carries the authoritative activity_name correction and status_id hazard note). Catalog count unchanged (91 events). No other rows affected. Frontmatter v1.98 → v1.99. |
| 1.98 | S-PRISMQL-CASE-INSENSITIVE-001-SAP1-ocsf-enum-label-unrecognized-catalog-row | 2026-07-06 | product-owner | **SAP-1 compliance (PG-LP11-001): added new catalog row 91 `ocsf.enum_label_unrecognized` WARN emitted by `OcsfNormalizer::normalize_with_mappers` (`crates/prism-ocsf/src/normalizer.rs`) when the enum-label normalization pass encounters a string value with no case-insensitive match in `OcsfEnumMap` for that field.** Fields: `field_name: %display` (OCSF string label field name — e.g., `"severity"`, `"status"`), `value: %display` (as-received unrecognized string — e.g., `"UNHANDLED"`), `sensor_type: %display` (sensor identifier — e.g., `"armis"`). Value is left as-received (pass-through per RG-021 / BC-2.02.013 v1.1); record never dropped; `IEQ`/`IIN` (BC-2.11.024) provides ergonomic fallback for callers. Scope statement extended to include `prism-ocsf` OCSF enum-label unrecognized-value warning emissions from `normalize_with_mappers` in `normalizer.rs`. Catalog count 90→91; catalog bullet label `(v1.58)` → `(v1.59)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v1.97→v1.98. Companion: BC-2.02.013 v1.0→v1.1 elaboration (F-CRIT-001, F-HIGH-001, F-HIGH-002, F-HIGH-003 closure). PG-LP11-001 discipline: PO-scope BC amendment precedes implementation; implementer must add this emission in the same commit as the `normalize_with_mappers` normalization code. |
| 1.97 | ADV-PR-P1-LOW-001-sanitize-for-log-field-descriptions | 2026-07-06 | product-owner | **ADV-PR-P1-LOW-001 doc-accuracy fix: `infusion.coercion_failed` catalog row — four field descriptions amended to accurately reflect CWE-117 / SEC-001 `sanitize_for_log` sanitization applied by `warn_coercion_failed` in `InfusionAsyncUdf::coerce_to_typed` (`infusion_udf.rs`).** (1) `truncated_value`: removed the word "raw"; description now states that the 50-char truncation (`value.chars().take(50).collect::<String>()`) is followed by `sanitize_for_log` (strips ASCII control chars 0x00–0x1F and 0x7F — truncate-then-sanitize order); notes the emitted value is NOT the raw input; cites CWE-117 / SEC-001. (2) `field_name`: added note that the value is control-char-sanitized via `sanitize_for_log` (CWE-117 / SEC-001) before emission. (3) `infusion_id`: same. (4) `declared_type`: same. No changes to event_type name, field names, level, audit role, recurrence policy, or the five-emission-sites count — all remain accurate. No volatile SHA or line-number pins introduced (TD-VSDD-091). Frontmatter v1.96→v1.97. Story S-DEMO-ENRICHMENT-TYPED-OUTPUT-001 pins this BC at v1.96; story-writer must propagate to v1.97 (POL-23). |
| 1.96 | ADV-P22-LOW-002-TD-VSDD-091-volatile-pin-fix | 2026-07-06 | product-owner | **TD-VSDD-091 volatile SHA→anchor fix (ADV-P22-LOW-002) + exhaustive sibling-sweep.** (1) Primary fix — `infusion.coercion_failed` catalog row, `declared_type` field description: replaced volatile commit SHA pin `89a09782` (implementer fix-burst reference, S-DEMO-ENRICHMENT-TYPED-OUTPUT-001 code HEAD) with durable behavioral anchor citing `InfusionAsyncUdf::coerce_to_typed` (`infusion_udf.rs`) and the behavioral fact of uniform `output_type.as_str()` emission across all five sites. (2) Sibling volatile pin — `boot.step9a.adapter_registry_populated` catalog row, SAP-1 obligation citation: replaced `commit 5805044d on branch feature/S-DEMO-001` with durable story anchor `story S-DEMO-001`. Both are non-changelog narrative spec content in the Canonical Structured Event Catalog — not in exempted classes (Red Gate tables, AC source-of-truth tables, §Changelog rows). SHA citations in changelog rows (v1.95, v1.94, v1.39, v1.9, v1.6, v1.5) are exempted per TD-VSDD-091 pass-report-changelog class. No `file.rs:NNN` line-number pins found outside changelog rows. Frontmatter v1.95→v1.96. |
| 1.95 | S-DEMO-ENRICHMENT-TYPED-OUTPUT-001-LOCAL-pass1-MED-002-catalog-reconciliation | 2026-07-06 | product-owner | **LOCAL adversary pass-1 MED-002 closure: `infusion.coercion_failed` catalog row corrected to remove Arrow DataType Debug deviation at site 1.** The v1.94 row introduced `declared_type` with a per-site split: spec-vocabulary strings (`"integer"`/`"float"`/`"boolean"`/`"datetime"`) for sites 2–5 but Arrow DataType Debug form via `format!("{output_type:?}")` (e.g., `"Int64"`) for site 1 (JSON-list path). The implementer fix-burst (code HEAD 89a09782) standardized all five `coerce_to_typed` emission sites to use `self.descriptor.output_type.as_str()` uniformly — the Arrow-Debug form at site 1 is GONE. **Trigger column change:** site (1) description reworded from "Arrow DataType Debug string via `format!("{output_type:?}")`" → "`self.descriptor.output_type.as_str()` spec-vocabulary string, identical to sites 2–5". **Fields column change:** `declared_type` description rewritten from per-site split to "spec-vocabulary output_type string — `"integer"` / `"float"` / `"boolean"` / `"datetime"` — at ALL FIVE sites; `self.descriptor.output_type.as_str()` used uniformly; NO Arrow DataType Debug form at any site". Sibling review: BC-2.19.001 confirmed no `declared_type` format citation requiring change (declared_type detail lives in this catalog row and error-taxonomy; BC-2.19.001 references E-INFUSE-014 without citing Arrow-Debug form). Companion: error-taxonomy v2.15→v2.16 (E-INFUSE-013 sub-condition 7 `{field}` slot made explicit; E-INFUSE-014 `{declared_type}` description strengthened). Frontmatter v1.94→v1.95. |
| 1.94 | S-DEMO-ENRICHMENT-TYPED-OUTPUT-001-SAP1-infusion-coercion-failed-catalog-row | 2026-07-05 | product-owner | **SAP-1 compliance (PG-LP11-001): added new catalog row `infusion.coercion_failed` WARN emitted by `InfusionAsyncUdf::coerce_to_typed` (`crates/prism-query/src/infusion_udf.rs`) at five emission sites — (1) JSON-list input to typed UDF (ADR-051 D4 Scalar-Input rule); (2) Int64 parse failure; (3) Float64 parse failure; (4) Boolean unrecognised value; (5) datetime RFC-3339 parse failure.** Fields (all five sites, identical schema): `field_name: %display` (the `InfusionUdfDescriptor::name`), `infusion_id: %display` (the `InfusionUdfDescriptor::infusion_id`), `declared_type: &str` (literal `"integer"`/`"float"`/`"boolean"`/`"datetime"` for sites 2–5; Arrow DataType Debug string via `format!("{output_type:?}")` for site 1), `truncated_value: %display` (first 50 chars, AD-017 guard). Semantics: E-INFUSE-014, row produces NULL (record never dropped per BC-2.16.003). Audit role: type-mismatch diagnostic. Recurrence: one per failing row per UDF call (not aggregated). Scope statement extended (parenthetical) to include `prism-query` infusion UDF coercion-failure emissions from `InfusionAsyncUdf::coerce_to_typed` in `infusion_udf.rs`. Catalog count 89→90; catalog bullet label `(v1.57)` → `(v1.58)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v1.93→v1.94. Code emission verified against `fd379ede` feature branch. |
| 1.93 | S-PRISMQL-NATIVE-TEMPORAL-TYPING-001-SAP1-column-type-for-catalog-row | 2026-07-04 | implementer | **SAP-1 compliance (PG-LP11-001): `table_registry.rwlock_poisoned` catalog row extended — `column_type_for` added as 9th emitting method (S-PRISMQL-NATIVE-TEMPORAL-TYPING-001 adversary pass-1 fix-burst).** `TableRegistry::column_type_for` (`crates/prism-query/src/table_registry.rs`) emits `method = "column_type_for"` when the `column_types_by_table` RwLock is poisoned — returns `None` (fail-open). The caller, `TemporalChecker::is_datetime_column` in `check_temporal_literals` (`engine.rs`), receives `None` and treats the column as type-unknown: no E-QUERY-041 is fired, allowing the ordering comparison to pass plan-time validation. Fail-open rationale: a poisoned registry cannot determine whether a column is datetime, so the conservative choice is to suppress the rejection (false reject is worse than false accept for a plan-time validator). No new `event_type` value introduced (same `"table_registry.rwlock_poisoned"` literal). Updated: "Eight methods/labels" → "Nine methods/labels"; `method: %display` closed-set expanded from 8 to 9 (adds `"column_type_for"`). This is a catalog-row description extension per POL-30 Fork B (no new rows, no event_type change); catalog bullet label stays `(v1.57)`. Frontmatter v1.92→v1.93. |
| 1.92 | S-PERF-GATE-008-SAP1-compilation-cache-init-skipped-catalog-row | 2026-07-01 | product-owner | **SAP-1 compliance (PG-LP11-001): added new catalog row `plugin.compilation_cache_init_skipped` WARN emitted by `PluginRuntime` construction (`crates/prism-spec-engine/src/plugin/mod.rs`) when `wasmtime::Cache::new(cache_config)` returns `Err(e)` during `PluginRuntime` init (S-PERF-GATE-008, ADR-049 D3).** Per ADR-049 D3, cache-init failure is DEGRADABLE (not fatal): construction continues with `config.cache(None)` after emitting this WARN. Field: `error: %display` (the `wasmtime::Error` Display — cache initialization failure reason). Audit role: operator-diagnostic (performance degradation signal — WASM plugins recompile from scratch on each cold start when the cache is unavailable; NOT a security-audit event; no rotation/retention policy required). Recurrence: once per cold-start where `wasmtime::Cache::new` fails; zero emissions expected in normal operation when the cache directory is writable. No scope statement extension needed — `PluginRuntime` construction is already within the catalog's declared scope ("plugin runtime emissions via `PluginRuntime`"). Catalog count 88→89; catalog bullet label `(v1.56)` → `(v1.57)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v1.91→v1.92. |
| 1.91 | S-DEMO-FIDELITY-REMEDIATION-001-F-PHL3-MED-001-rwlock-method-count-drift | 2026-06-28 | product-owner | **F-PHL3-MED-001 closure (SAP-1 / PG-LP11-001): two catalog row updates for S-DEMO-FIDELITY-REMEDIATION-001 delivery.** (1) **`table_registry.rwlock_poisoned` row:** method enumeration + count extended — no new `event_type` value introduced (same `"table_registry.rwlock_poisoned"` literal); only the row's description and `method: %display` closed-set enumeration updated. **M1 fix:** `TableRegistry::columns_for_table` (`crates/prism-query/src/table_registry.rs`) emits `method = "columns_for_table"` when the `columns_by_table` RwLock is poisoned — returns empty `Vec` (fail-closed, column-gate single-tenant path). **N2 fix:** the dot-notation rejection block within `check_availability_gate` emits `method = "check_availability_gate.dot_notation"` when the `sensor_by_table` RwLock is poisoned during org-visible-tables snapshot construction for the dot-name error report — falls back to empty map; dot-notation `TableNotAvailable` error still returned. Updated: "Six methods" → "Eight methods/labels"; closed-set expanded from 6 (`is_registered`, `registered_tables`, `sensor_for_table`, `registered_sensor_ids`, `check_availability_gate`, `sensor_by_table_snapshot`) to 8 (adds `columns_for_table`, `check_availability_gate.dot_notation`). (2) **`column_not_found.rejected` row:** trigger description updated to reflect two emission sites now exist within `check_column_availability` — the original multi-tenant path (S-DEMO-PRISMQL-ONBOARDING-001-B) plus the new single-tenant path added by M1 fix (`if resolved_spec_map.is_none()` branch using `TableRegistry::columns_for_table`). Both sites emit identical field schema (`column: %display`, `table: %display`, `client_id: %display`, `available_count: usize`). The `available_count` field description updated to distinguish the two sources. No new `event_type` value introduced; no row count change (stays 88). Both changes are catalog-row description extensions per POL-30 Fork B; catalog bullet label stays `(v1.56)`. Frontmatter v1.90→v1.91. |
| 1.90 | S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001-SAP1-filter-sql-catalog-rows | 2026-06-25 | product-owner | **SAP-1 compliance (PG-LP11-001): added 2 new catalog rows for filter-mode SQL execution emissions introduced by S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 Area D in `execute_against_session` `Ast::Filter` arm (`crates/prism-query/src/materialization.rs`).** `filter.sql_lowering` DEBUG (emitted after the bare/source-qualified filter predicate is lowered to `SELECT * FROM <table> WHERE <predicate>`, before DataFusion planning; fields: `filter_sql: %display` — the lowered SQL string; audit role: non-security diagnostic; recurrence: once per successful filter lowering; SECURITY: `filter_sql` contains table identifier, column names, and operator keywords only — no sensor response data or credentials). `filter.sql_planning_error` ERROR (emitted in the `Err` arm of `session_ctx.sql(&filter_sql).await` for the filter path; fields: `error: %display` — raw DataFusion planning error (server-side only; client receives redacted message per CWE-209), `filter_sql: %display` — the failing SQL; audit role: error/operational diagnostic; recurrence: once per filter query where DataFusion planning fails; SECURITY: fields contain structural SQL content only). Rows modeled on sibling `pipe.sql_lowering` / `pipe.sql_planning_error` (ENRICH-4-B). Scope statement extended to include `prism-query` filter-mode SQL lowering and planning-error emissions from `execute_against_session`. Catalog count 86→88; catalog label `(v1.55)` → `(v1.56)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Frontmatter v1.89→v1.90. |
| 1.89 | ENRICH-4-B-SAP1-pipe-sql-catalog-rows | 2026-06-23 | product-owner | **SAP-1 compliance (PG-LP11-001): added 2 new catalog rows for pipe-to-SQL execution emissions introduced by ENRICH-4-B in `execute_against_session` `Ast::Pipe` arm (`crates/prism-query/src/materialization.rs`).** `pipe.sql_lowering` DEBUG (emitted after `pipe_to_executable_sql` returns `Ok(sql)`, before DataFusion planning; fields: `pipe_sql: %display` — the lowered SQL string; audit role: non-security diagnostic; recurrence: once per successful pipe lowering; SECURITY: `pipe_sql` contains structural query elements only, no sensor data or credentials). `pipe.sql_planning_error` ERROR (emitted in the `Err` arm of `session_ctx.sql(&sql).await` for a pipe query; fields: `error: %display` — raw DataFusion planning error (server-side only; client receives redacted message per CWE-209), `pipe_sql: %display` — the failing SQL; audit role: error/operational diagnostic; recurrence: once per pipe query where DataFusion planning fails; SECURITY: fields contain structural SQL content only). Both emission sites verified against ENRICH-4-B worktree source at `materialization.rs` lines 899–913. Scope statement extended to include `prism-query` pipe-to-SQL lowering and planning-error emissions from `execute_against_session`. Catalog count 84→86; catalog label `(v1.54)` → `(v1.55)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Frontmatter v1.88→v1.89. |
| 1.88 | S-DEMO-ENRICH-1-SAP1-column-source-path-extraction-failed-catalog-row | 2026-06-23 | product-owner | **SAP-1 compliance (PG-LP11-001): added new catalog row `column_source_path_extraction_failed` WARN emitted by two ENRICH-1 sites — `ColumnMapper::map_record` (`crates/prism-spec-engine/src/column_mapping.rs`) and `build_column_array::extract_raw` (`crates/prism-bin/src/spec_driven_adapter.rs`) — on `extract_at_path` failure for a `source_path`-declared column.** Both sites emit identical schema: `column: %display` (TOML column name), `source_path: %display` (JSONPath from TOML spec), `error: %display` (extraction error string). Both sites verified against ENRICH-1 worktree source. Non-fatal: site (a) skips the column for the record; site (b) produces a null cell. Records are never dropped (BC-2.16.003 invariant). Scope statement extended to include ENRICH-1 `column_source_path_extraction_failed` emissions from `column_mapping.rs` and `spec_driven_adapter.rs`. Catalog count 83→84; catalog label `(v1.53)` → `(v1.54)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v1.87→v1.88. |
| 1.87 | S-DEMO-PRISMQL-ONBOARDING-001-B-SAP1-column-not-found-catalog-row | 2026-06-21 | implementer | **SAP-1 compliance (PG-LP11-001): added new catalog row `column_not_found.rejected` WARN emitted by `check_column_availability` (`crates/prism-query/src/engine.rs`) for E-QUERY-038 plan-time column gate rejections (S-DEMO-PRISMQL-ONBOARDING-001-B, BC-2.11.016).** Fields: `column: %display` (the column name referenced in the query, e.g. `"sevrity"`), `table: %display` (the table name the column was looked up in), `client_id: %display` (the `OrgSlug` of the querying client), `available_count: usize` (number of org-scoped columns available; 0 when `resolved_spec_map` is None). Audit role: query rejection / access-control trace. Recurrence: once per query where the column gate fires. SECURITY: `column` contains only the column identifier (never query value context), no credential data. Scope statement extended to include `prism-query` plan-time column gate rejection (`column_not_found.rejected`, BC-2.11.016). Catalog count 82→83; catalog bullet label `(v1.52)` → `(v1.53)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Frontmatter v1.86→v1.87. |
| 1.86 | D-1270-SAP1-reload-overlay-catalog-rows | 2026-06-20 | state-manager | **SAP-1 compliance (PG-LP11-001): added 2 new catalog rows for `rebuild_resolved_spec_map` hot-reload overlay emissions (S-DEMO-PRISMQL-ONBOARDING-001-A ADR-042 build, D-1270).** `reload.overlay_rebuild_failed` WARN (emitted when `OverlayLoader::load_overlays` returns validation errors during hot-reload; fields: `error_count: usize`; DI-031 non-fatal — prior map retained; audit role: reload-pipeline observability per ADR-042; recurrence: once per failing rebuild call). `reload.overlay_rebuilt` INFO (emitted on successful ArcSwap of the new overlay map; fields: `overlay_count: usize`; audit role: reload-pipeline observability per ADR-042 — confirms atomic map swap; recurrence: once per successful rebuild call). Both rows trace to ADR-042 (resolved_spec_map reload-aware ArcSwap) and BC-2.10.013 (multi-tenant hot-reload notify). Scope statement extended to include `prism-query` hot-reload overlay rebuild emissions from `rebuild_resolved_spec_map`. Catalog count 80→82; catalog bullet label `(v1.51)` → `(v1.52)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Frontmatter v1.85→v1.86. |
| 1.85 | D-1262-SAP1-schema-enumeration-catalog-rows | 2026-06-20 | state-manager | **SAP-1 compliance (PG-LP11-001): added 3 new catalog rows for `prism_describe` schema-enumeration tool emissions (S-DEMO-PRISMQL-ONBOARDING-001-A, D-1262).** `schema_enumeration.started` INFO (emitted at valid call entry; fields: `client_id`; audit role: discovery initiation; recurrence: once per successful-input call). `schema_enumeration.rejected` WARN (emitted on DI-006 invalid `client_id`; fields: `event_type`; audit role: input rejection; recurrence: once per rejected call). `schema_enumeration.success` INFO (emitted after schema resolution succeeds; fields: `client_id`; audit role: successful schema catalog emission; recurrence: once per successful call). All three traces to BC-2.10.012 (`prism_describe`). Scope statement extended to include prism-mcp schema-enumeration tool emissions. Catalog count 77→80; catalog bullet label `(v1.50)` → `(v1.51)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Frontmatter v1.84→v1.85. |
| 1.84 | PR195-FIND1-SpecEngineError-to-PluginError-correction | 2026-06-19 | product-owner | **Spec-accuracy correction: `SpecEngineError::EnrichCallFailed` → `PluginError::EnrichCallFailed` in two catalog rows (PR #195 PR-LEVEL pass FIND-1).** The `plugin_enrich_json_parse_error` and `plugin_enrich_unexpected_val` rows in the Canonical Structured Event Catalog both stated that `PluginRuntime::enrich_single` returns `Err(SpecEngineError::EnrichCallFailed)`. This is incorrect: `enrich_single` returns `Result<Option<Value>, PluginError>` (verified against `crates/prism-spec-engine/src/plugin/mod.rs` line 910); the failure variant lives on `PluginError`, not `SpecEngineError`. The correct type is `PluginError::EnrichCallFailed` (E-PLUGIN-023), which is also confirmed by `error-taxonomy.md` E-PLUGIN-023 entry and `ADR-040 §D5`. BC-2.16.002 was the lone artifact with the wrong type. `SpecEngineError::EnrichCallFailed` has never existed. The E-PLUGIN-023 error-code reference is correct and unchanged. No catalog-count change; no event_type change; no other content changes. Frontmatter v1.83→v1.84. |
| 1.83 | PIVOT-002-SAP1-http-lookup-field-schema-expansion | 2026-06-19 | product-owner | **SAP-1 fix: `http_lookup_enrich_failed` catalog row field schema expanded to match all 7 emission sites in `http_lookup.rs` (LOW finding from PIVOT-002 adversarial pass).** Prior v1.82 catalog row listed only three fields (`infusion_id`, `spec_path`, `status_code`) but the actual implementation emits up to 6 distinct fields across 7 call sites in `HttpLookupSource::enrich_single` and `enrich_single_async`. Missing fields added: `error_code: &str` (always present — `"E-INFUSE-009"` or `"E-INFUSE-010"` — distinguishes HTTP/transport/parse failures from credential failures); `error_kind: %display` (sub-condition-specific — present on runtime-build-error, `.send()` failure, and body-read failure sites, value from `classify_reqwest_error()` or literal `"runtime_build_error"`); `method: %display` (sub-condition-specific — present only on unsupported-method site); `credential_ref: %display` (sub-condition-specific — present only on E-INFUSE-010 credential-resolution site; AD-017 safe: logical ref name only, never env var name or credential value); `message: &str` (sub-condition-specific — present only on `.send()` failure site, value `"HTTP call failed"`). The field schema distinguishes which fields are ALWAYS present vs sub-condition-specific. AD-017 `credential_ref` note added: it is the LOGICAL ref name from `[source.credential].ref`, not the env var name, not the credential value — safe to log per AD-017. **POL-30 Fork B: this is a catalog-row description extension with no new event_type values and no row count change (77 → 77). Catalog bullet label stays `(v1.50)`.** Frontmatter v1.82→v1.83. |
| 1.82 | PIVOT-002-ADR040-correction-2026-06-17 | 2026-06-17 | product-owner | **ADR-040 §D8.7 source-of-truth correction: replaced PIVOT-002 dotted-name rows with ADR-mandated names.** PIVOT-002 implementer registered three `infusion.http_lookup.*` rows (`infusion.http_lookup.started`, `infusion.http_lookup.failed`, `infusion.http_lookup.succeeded`) but ADR-040 v2.0 §D8.7 mandates ONLY TWO event_type values: `http_lookup_enrich_failed` (fired on `InfusionError::HttpLookupFailed` — the general HTTP/parse failure path) and `http_lookup_ssrf_rejected` (fired on `InfusionError::SsrfRejected` — SSRF gate rejection at construction time). ADR wins per CLAUDE.md Source-of-Truth Precedence §2. Changes: (1) Removed three dotted-name rows: `infusion.http_lookup.started`, `infusion.http_lookup.failed`, `infusion.http_lookup.succeeded`. (2) Added two ADR-mandated rows: `http_lookup_enrich_failed` (WARN, `infusion_id`, `spec_path`, `status_code` conditional) and `http_lookup_ssrf_rejected` (WARN, `infusion_id`, `spec_path`). (3) Catalog count: 78 → 77 (net -1: removed 3, added 2). (4) Catalog bullet version label: `(v1.49)` → `(v1.50)` per POL-30 Fork B (structural change — 3 rows removed, 2 rows added). **Implementer work-orders in `http_lookup.rs`**: (a) rename `tracing::warn!` on failure path from `event_type = "infusion.http_lookup.failed"` → `event_type = "http_lookup_enrich_failed"`; (b) remove `tracing::debug!` sites for `infusion.http_lookup.started` and `infusion.http_lookup.succeeded` — no ADR mandate for those events; (c) add `tracing::warn!` on SSRF rejection path with `event_type = "http_lookup_ssrf_rejected"`, fields `infusion_id` and `spec_path` only (NEVER include resolved IP address). Frontmatter v1.81→v1.82. |
| 1.81 | PIVOT-002-out-of-band-reconciliation-2026-06-17 | 2026-06-17 | implementer | **SAP-1/PG-LP11-001 dual-path infusion catalog expansion (PIVOT-002).** Added 5 new event_type rows for `HttpLookupSource` and WASM plugin enrichment paths: `infusion.http_lookup.started` (debug), `infusion.http_lookup.failed` (warn), `infusion.http_lookup.succeeded` (debug), `plugin_enrich_json_parse_error` (error), `plugin_enrich_unexpected_val` (error). Catalog count 73→78; catalog bullet label `(v1.48)` → `(v1.49)`. Scope statement extended to include `HttpLookupSource` infusion HTTP enrichment. **NOTE: the three `infusion.http_lookup.*` rows were subsequently corrected to ADR-mandated names in v1.82** (ADR-040 §D8.7 source-of-truth precedence). The `plugin_enrich_json_parse_error` and `plugin_enrich_unexpected_val` rows (WASM plugin path) are correct and retained unchanged. Originally committed out-of-band by PIVOT-002 implementer (commit 3e327e99, unpushed); reconstructed here as a changelog tombstone per state-manager out-of-band reconciliation burst D-1215. |
| 1.80 | S-3.13 OBS-1/OBS-2 fix-burst | 2026-06-16 | implementer | **Catalog row `table_registry.rwlock_poisoned` updated: `sensor_by_table_snapshot` added as 6th emitting method (OBS-2 closure, S-3.13 OBS-1/OBS-2 fix-burst).** The new `pub(crate) sensor_by_table_snapshot()` method in `TableRegistry` takes a single RwLock acquisition to return an owned clone of the `sensor_by_table` map. `explain.rs` (org-filter path, ~line 1166) now calls this once instead of calling `sensor_for_table()` N times (one per table). On RwLock poison: `sensor_by_table_snapshot` returns empty map (fail-closed) and emits `table_registry.rwlock_poisoned` WARN — same poison behavior as `check_availability_gate` (the single-snapshot pattern this reuses). No new `event_type` introduced; reuses the existing catalog row and `event_type = "table_registry.rwlock_poisoned"` value. Updated catalog row: "Five methods" → "Six methods"; added `sensor_by_table_snapshot` to the method list and the `method: %display` field values. This is a catalog-row description extension per POL-30 Fork B (no new rows, no event_type change); catalog bullet label stays `(v1.48)`. Companion change OBS-1: SEC-001 test assertions in `crates/prism-query/src/tests/table_registry_tests.rs` hardened from `String::contains()` substring matching on the joined `available_sensors`/`available_tables` strings to split-on-`", "`-and-assert-exact-membership — prevents false-pass/fail when a future sensor id is a substring of another. The `did_you_mean` assertion also hardened to `assert_ne!(suggestion, " Did you mean: 'crowdstrike_alerts'?")` instead of `!suggestion.contains("crowdstrike_alerts")`. No BC content changes for OBS-1 (test robustness only). Frontmatter v1.79→v1.80. PG-LP11-001 discipline: implementer-scope BC amendment in the same burst as the code changes (branch feature/S-3.13). |
| 1.79 | S-3.13 CR-001 fix-burst | 2026-06-16 | implementer | **Catalog row `table_registry.rwlock_poisoned` updated: `check_availability_gate` added as 5th emitting method (CR-001 closure, S-3.13 fix-burst).** The `check_availability_gate` method emits `table_registry.rwlock_poisoned` WARN when the `sensor_by_table` RwLock is poisoned during org-filter snapshot construction; it degrades to an empty-map fallback (not an error) and returns `TableNotAvailable` with empty available lists. Updated catalog row: "Four read-path methods" → "Five methods"; added `check_availability_gate` to the method list and the `method: %display` field values; updated the Audit role description to document the check_availability_gate-specific degraded-org-filter behavior. No new event_type introduced (same `table_registry.rwlock_poisoned` value). This is a catalog-row description extension per POL-30 Fork B (no new rows, no event_type change); catalog bullet label stays `(v1.48)`. Frontmatter v1.77→v1.79 (also applies the missed v1.78 frontmatter bump from the S-3.13 NB-1/SEC-002 fix-burst changelog entry). PG-LP11-001 discipline: implementer-scope BC amendment in the same burst as the code changes (branch feature/S-3.13). |
| 1.78 | S-3.13 NB-1/SEC-002 fix-burst | 2026-06-16 | implementer | **New catalog row `table_registry.rwlock_poisoned` WARN (NB-1 closure, S-3.13 fix-burst).** Added row 73 `table_registry.rwlock_poisoned` WARN emitted by four `TableRegistry` read-path methods (`is_registered`, `registered_tables`, `sensor_for_table`, `registered_sensor_ids`) in `crates/prism-query/src/table_registry.rs` when the `registered` or `sensor_by_table` RwLock is poisoned (another thread panicked while holding the write lock). Fields: `event_type` (literal), `method: %display` (method name). Fail-closed behavior preserved; WARN added for operator visibility (NB-1). Scope statement extended to include `prism-query` `TableRegistry` RwLock poison emissions. Catalog count 72→73; catalog bullet label advanced `(v1.47)` → `(v1.48)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Companion: `DID_YOU_MEAN_MAX_NAME_BYTES = 128` cap added to `did_you_mean` (SEC-002, CWE-407); no new event_type for SEC-002 (it is a pure input-validation guard with no tracing emission). PG-LP11-001 discipline: implementer-scope BC amendment in the same commit as the code changes (branch feature/S-3.13). |
| 1.77 | PRL-P4-01 adjudication burst | 2026-06-11 | product-owner | **Row `mcp.tool.called` (row 145): `reload_config` added to WRITE-classified tool set (PRL-P4-01 MEDIUM finding closure).** `reload_config` calls `ConfigManager::store(candidate)` on the non-dry-run path — the same ArcSwap mutation path as `add_sensor_spec` — and is therefore mutation-equivalent under BC-2.05.001's no-unaudited-writes invariant. WriteTool set expanded from four to five: `confirm_action`, `add_sensor_spec`, `create_alias`, `delete_alias`, `reload_config`. No new `event_type` added or removed; catalog count stays 72; catalog bullet version label unchanged (v1.47 — this is a row-narrative-only update to the existing `mcp.tool.called` trigger description per POL-30 Fork B: no structural catalog change). Sibling: BC-2.05.001 v1.4 (write tool enumeration + invariant + edge case updated). |
| 1.76 | MCP-P8-OBS-01 PO-half — sibling-catalog cross-reference (2026-06-11 product-owner burst) | 2026-06-11 | product-owner | **Added sibling-catalog cross-reference note to the Canonical Structured Event Catalog (v1.47) scope statement** (MCP-P8-OBS-01 PO-half). The scope statement enumerated in-scope crates (prism-spec-engine, prism-bin, prism-query, prism-ocsf, prism-mcp) but did not explicitly document that `prism-security` and `prism-credentials` are out-of-scope by design, with their own governing BCs. Added explicit sentence: `capability_check` emissions from `prism-security/src/flag_audit.rs` are governed by BC-2.04.013; `credential_access` emissions from `prism-credentials/src/audit.rs` are governed by BC-2.03.010. This closes the process-gap finding (MCP-P8-OBS-01) — SAP-1 wording in CLAUDE.md is a human-edit-only scope (not touched). No event_type added or removed; catalog count stays 72; catalog bullet version label unchanged (v1.47 per POL-30 Fork B — scope-statement prose addition with no catalog-content structural change). Bumped v1.75→v1.76. |
| 1.75 | MCP cascade pass-5 P5-02/P5-03 + P5-01 anchor half (2026-06-10 review-cycle implementer fix-burst) | 2026-06-11 | implementer | **Row `mcp.tool.called`: BC-2.05.001 two-class audit-failure contract registered (P5-02). Row `write.intent.recorded`: BC-2.05.009 capability fields added to the field schema (P5-03). Rows `write.intent.recorded` / `write.outcome.recorded`: mis-anchored "BC-2.05.009 tracing path" → "BC-2.05.001 tracing audit-coverage path" (P5-01).** P5-02: `emit_tool_audit` now consults the prism-mcp `tool_classification_registry` (prism-audit `ToolClass`); WRITE-classified tools (`confirm_action`, `add_sensor_spec`, `create_alias`, `delete_alias`) abort with the `E-AUDIT-001` structured error BEFORE any mutation or confirmation-token generation on durable-audit failure (BC-2.05.001 postcondition / DEC-014); read-classified tools keep EC-05-002 fail-open `audit_warning` semantics — row 145 narrative updated; tracing field schema unchanged. P5-03: `BootAuditWriter::write_intent` no longer drops the Phase 2 `CapabilityCheckResult` — the emission now records `capability_path`, `compile_time_enabled`, `runtime_enabled`, `result` via `BootAuditWriter::capability_audit_fields` (derivation table in the row description); field schema EXTENDED (4 new fields). P5-01 anchor half: the fail-closed/audit-coverage description anchors in rows 153/154 belong to BC-2.05.001 (one audit entry per tool invocation, fail-closed for writes), not BC-2.05.009 (feature-flag evaluation audit); the BC-2.05.009 cite in row 153 now refers only to the capability fields it actually owns. No event_type added or removed; catalog count stays 72; catalog bullet label advanced `(v1.46)` → `(v1.47)` per POL-30 Fork B — a field-schema extension of an existing row is a catalog-content structural change (unlike the v1.72/v1.74 description-only precedents). PG-LP11-001 discipline: implementer-scope BC amendment in the same burst as the code fixes (code lands via branch fix/review-2026-06-10-mcp-boot commits ab2ab0ce / 5863dbc7 / 7c1c2a5e). |
| 1.74 | MCP cascade pass-4 P4-01/P4-02/P4-03 spec half (2026-06-10 review-cycle implementer fix-burst) | 2026-06-10 | implementer | **Rows `mcp.tool.called` / `mcp.tool.rejected`: audit-semantics citations corrected + P4-03 response annotation registered.** P4-01: both rows mis-cited BC-2.05.008 EC-05-013 (which covers empty `capability_checks` arrays on read ops) as the authority for "read-path audit is not fail-closed" — corrected to BC-2.05.001 postcondition "Read operations proceed on audit failure" / EC-05-002; the same mis-cite in the v1.71 changelog narrative was corrected in-place with an explicit correction marker. P4-02: the `mcp.tool.called` row anchored tool-call audit to BC-2.05.009 (feature-flag-evaluation audit) — corrected to BC-2.05.001 (one audit entry per tool invocation); companion code sweep renames `test_BC_2_05_009_write_tool_call_*` → `test_BC_2_05_001_write_tool_call_*` and corrects the emit_tool_audit/BootAuditWriter/AuditWriter docs on branch fix/review-2026-06-10-mcp-boot. P4-03: row 145 narrative extended — `emit_tool_audit` now returns the durable-write failure indicator and success-envelope handlers thread it into `_meta.audit_warning: "audit emission failed"` per BC-2.05.001 EC-05-002 (spec-wins adjudication; previously the envelope had no such field). No event_type added or removed; catalog count stays 72; catalog bullet label stays (v1.46) per POL-30 Fork B (description-text corrections are not catalog-content structural changes). PG-LP11-001 discipline: implementer-scope BC amendment in the same burst as the code fix. |
| 1.73 | MCP cascade pass-2 P2-06 spec half (2026-06-10 review-cycle implementer fix-burst) | 2026-06-10 | implementer | **Row `boot.step8.query_engine_started`: stale future-tense S-5.01-FOLLOWUP-MCP-BOOT deferral retired (P2-06).** The sentence "QueryEngine and WriteExecutor construction is deferred to S-5.01-FOLLOWUP-MCP-BOOT" cited a merged story in future tense; rewritten to present tense (construction happens in boot step 9, `step9_start_mcp_server`, implemented by the merged story). Added the superseded-assertion closure: the once-promised fatal non-empty AdapterRegistry assertion was superseded — TD-S-PLUGIN-PREREQ-A-004 closed-as-superseded by BC-2.22.001 AC-006 (empty spec catalog at step 9A emits `boot.step9a.adapter_registry_populated` with `sensor_count = 0` and boot continues BY DESIGN; architect ratification at `.factory/proposals/TD-S-PLUGIN-PREREQ-A-004-close-as-superseded-ratification.md`); the materialization `is_empty()` short-circuit is the accepted defense against silent empty results. Companion code change: `boot.rs` `step8_init_query_engine` doc comment + inline comment + log message text rewritten likewise (same burst, branch fix/review-2026-06-10-mcp-boot). event_type, level, function anchor, field schema, audit role, recurrence, and retention unchanged. No catalog-count change; POL-30 Fork B catalog bullet label UNCHANGED (description-only correction, per v1.67/v1.68/v1.72 precedent). Bumped v1.72→v1.73. |
| 1.72 | MCP cascade pass-1 P1-04 spec half (2026-06-10 review-cycle PO micro-burst) | 2026-06-10 | product-owner | **Row `write.intent.recorded`: false S-2.04 deferral anchor retired (P1-04).** The sentence "Full AuditEmitter integration via Tower layer requires S-2.04" was a false forward anchor — S-2.04 merged 2026-04-25 without delivering a Tower-layer AuditEmitter, and the adjudicated decision (P1-04, 2026-06-10 review cycle) is that NO Tower-layer story is planned: the durable per-call `AuditWriter::write_tool_call` record written by `BootAuditWriter` to the RocksDB `audit_buffer` CF (MCP-02, this cycle) IS the production audit mechanism. Row description rewritten to current behavior only: intent record is tracing-only; durable per-tool-call audit coverage via `BootAuditWriter::write_tool_call` → `audit_buffer` CF; no aspirational future-work claim. event_type, level, function anchor, field schema, audit role, recurrence, and retention unchanged. No catalog-count change; no event_type change; POL-30 Fork B catalog bullet label `(v1.46)` UNCHANGED (description-only correction, per v1.67/v1.68 precedent). Bumped v1.71→v1.72. |
| 1.71 | review-2026-06-10-fix-pr-3 (MCP-02/MCP-03) | 2026-06-10 | implementer | SAP-1 compliance (PG-LP11-001): added new catalog row `mcp.tool.rejected` WARN emitted by `PrismServer::scan_inputs_audited` (`prism-mcp/src/server.rs`) when the BC-2.09.001 pre-domain-logic injection scan rejects a tool's input (MCP-03, 2026-06-10 review Fix PR 3); fields: `tool_name`, `reason_class` (literal `"prompt_injection"`), `flag_count`, `categories`, `fields` — classification ONLY, raw injected content and matched-pattern descriptions deliberately excluded. Extended `mcp.tool.called` row: `emit_tool_audit` is now async and writes a durable companion record via `AuditWriter::write_tool_call` to the RocksDB `audit_buffer` CF (MCP-02; not fail-closed per BC-2.05.001 EC-05-002 — citation corrected in-place by the v1.74 P4-01 sweep; the original v1.71 narrative mis-cited BC-2.05.008 EC-05-013); tracing field schema unchanged. Sibling sweep (TD-VSDD-060): `write.intent.recorded` / `write.outcome.recorded` function anchors updated `TracingAuditWriter` → `BootAuditWriter` (boot-local struct renamed in the MCP-02 fix; now holds the step-6 RocksDB backend for durable tool-call writes). Catalog count 71→72; catalog bullet label `(v1.45)` → `(v1.46)` per POL-30 Fork B; scope statement prism-mcp parenthetical extended with mcp.tool.rejected. PG-LP11-001 discipline: implementer-scope BC amendment in the same burst as the tracing emission (code lands via Fix PR 3 branch fix/review-2026-06-10-mcp-boot). |
| 1.70 | DRIFT-D850-001-postcondition | 2026-06-08 | product-owner | Add **OffsetLimit Pagination Dispatch: POST-body vs GET-URL** postcondition (DRIFT-D850-001). Closes BC gap flagged in S-DEMO-CLAROTY-PAGINATION-001 §BC Gap. Specifies that for `PaginationConfig::OffsetLimit` steps: (1) GET/absent-method steps append `?offset=N&limit=M` to the URL (existing behavior preserved); (2) POST steps return the base URL unchanged and inject `offset`/`limit` as top-level integer keys into the JSON request body, merged onto the interpolated `body_template`. Grounded in `pipeline.rs` `build_paged_url_impl` (URL side) and `build_request` (body injection site), and in `crates/prism-dtu-claroty/src/types.rs` `GetAlertsBody`/`GetDevicesBody`/`GetAuditLogBody` struct field declarations (`offset: Option<u32>`, `limit: Option<u32>`). Postcondition anchors AC-001, AC-002, AC-004, AC-005, AC-006 of S-DEMO-CLAROTY-PAGINATION-001 and its four Red Gate tests. No new error code or invariant IDs introduced (existing `SpecEngineError` variants cover body-parse-failure path). Version bump: v1.69 → v1.70. |
| 1.69 | ADV-P173-P03-MED-001-changelog-integrity-fix | 2026-06-05 | product-owner | POLICY 32 changelog-integrity hygiene (ADV-P173-P03-MED-001) — full-column monotonic sweep: corrected all non-monotonic, duplicate, and missing version defects across the entire §Changelog column. **Cluster 1 (1.62–1.65)**: Three defects: (1) **Non-monotonic**: a v1.65 row appeared BELOW a v1.64 row. (2) **Duplicate**: two rows both labeled v1.64 — the "re-cascade interpolation clarification" row and the "DEFINITIVE CORRECTION" row. (3) **Missing 1.63**: the DEFINITIVE CORRECTION row's narrative references "v1.62 AND v1.63 amendments" confirming 1.63 existed but was not recorded as such. Reconstruction: the "re-cascade interpolation clarification" row is correctly v1.63 (mislabeled v1.64); the "DEFINITIVE CORRECTION" row is correctly v1.64; the `ocsf.deprecated_class_alias` row is correctly v1.65. Fix: (a) renumbered second v1.64 ("re-cascade") → v1.63; (b) reordered rows to strict descending: v1.65, v1.64, v1.63. **Cluster 2 (1.41/1.42 inversion)**: v1.41 row appeared BEFORE v1.42 row — violates POLICY 32 newest-first ordering. Both rows are dated 2026-05-23 (same burst date); v1.42 is the CORRECTION burst for the v1.41 defect and is therefore logically newer. Fix: reordered to strict descending: v1.42, v1.41. No row content changed in either cluster (POLICY 1 append-only). No catalog-content changes; no event_type changes; no body-text changes beyond this changelog and frontmatter. |
| 1.68 | OBS-1-stale-story-version-pin-fix | 2026-06-05 | product-owner | OBS-1 closure (PR #173 PR-LEVEL adversary pass 1): removed stale story-version pin `v2.2` from two live cross-reference sites that cited `S-DEMO-QUERY-PUSHDOWN-001 v2.2 EC-003`. Story is now at v2.5; EC-003 content is accurate and unchanged. Per TD-VSDD-091, the cleaner fix is version-agnostic citation — both sites now read `S-DEMO-QUERY-PUSHDOWN-001 EC-003` (no version pin) to prevent future staleness. Sites updated: (1) §Postconditions Canonical Structured Event Catalog scope statement (`prism-query` push-down time-window analysis clause, line 83); (2) catalog row 71 `push_down.inverted_time_range` Cross-reference field (line 157). Historical changelog row v1.66 is immutable per POLICY 1 (append-only) and left unchanged — it is a historical record of what was true at authoring time. No catalog-content changes; no event_type changes; POL-30 Fork B catalog bullet label `(v1.45)` UNCHANGED. |
| 1.67 | ADV-P04-LOW-002-prose-fix | 2026-06-05 | product-owner | ADV-P04-LOW-002 closure: corrected prose inaccuracy in the `push_down.inverted_time_range` WARN catalog row (row 71) Recurrence description. The phrase "before returning the FetchContext to the caller" was wrong — `extract_time_window_from_ast` returns a `(Option<String>, Option<String>)` tuple, not a `FetchContext`. Corrected to "before returning the `(start_time, end_time)` tuple to the caller". All other fields (event_type, field schema, level, function, audit role, recurrence semantics) are unchanged. No catalog-count change; no event_type change. POL-30 Fork B catalog bullet label UNCHANGED (description-only correction, not a new row or structural change). |
| 1.66 | S-DEMO-QUERY-PUSHDOWN-001-v2-SAP-1-catalog-row | 2026-06-05 | product-owner | SAP-1 compliance (PG-LP11-001): added new catalog row 71 `push_down.inverted_time_range` WARN emitted by `extract_time_window_from_ast` (`crates/prism-query/src/pushdown.rs`) when a PrismQL WHERE time predicate yields `start_time > end_time`. Fields: `event_type` (string literal), `start_time: %display` (ISO8601 string — requested lower bound), `end_time: %display` (ISO8601 string — requested upper bound). Audit role: diagnostic/query-anomaly signal (NON-audit). Recurrence: at most once per query with inverted time window. Cross-reference: S-DEMO-QUERY-PUSHDOWN-001 v2.2 EC-003; BC-2.11.007 v1.8. Scope statement extended to include `prism-query` push-down time-window analysis (`extract_time_window_from_ast`). Catalog count 70→71; catalog bullet label advanced `(v1.44)` → `(v1.45)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). PG-LP11-001 discipline: PO-scope BC amendment (spec burst precedes implementation). |
| 1.65 | Wave-5-Phase-A-PO-burst | 2026-06-03 | product-owner | SAP-1 compliance (PG-LP11-001): added new catalog row 70 `ocsf.deprecated_class_alias` WARN emitted by `EventClassSelector::select_by_class_name` (`crates/prism-ocsf/src/class_selector.rs`) when called with a deprecated OCSF class-name alias (e.g., `"security_finding"`). Fields: `class_name: %display`, `resolved_class_uid: u32`. Per BC-2.02.012 v1.4 Option A (OCSF-CLASS-MIGRATION-001), `"security_finding"` resolves to 2004 (not 2001) with this WARN emission. Scope statement extended to include "deprecated class alias" in the `prism-ocsf` normalization scope description. Catalog count 69→70; catalog bullet label advanced `(v1.43)` → `(v1.44)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). PG-LP11-001 discipline: PO-scope BC amendment (spec burst precedes implementation). |
| 1.64 | S-DEMO-002 definitive body_template Array/Object contract correction | 2026-06-03 | product-owner | **DEFINITIVE CORRECTION** — the v1.62 AND v1.63 amendments both mischaracterized `Interpolator::interpolate`'s `JsonBody` arm for `Array`/`Object` values. Verified against the authoritative `Interpolator::interpolate` `JsonBody` match arm in `prism-spec-engine/src/interpolation.rs` (orchestrator-confirmed at HEAD `3860b970`): the `JsonBody` arm has **three distinct branches** — (1) `Array | Object` → `value.to_string()` = **VERBATIM valid JSON literal** (no `json_escape`); (2) `String` scalar → `json_escape`; (3) `Number / Bool / Null` → `value_to_string` (verbatim literal). The v1.62/v1.63 claim of "NO special branch — all types flow through `value_to_string` → `json_escape`" was incorrect. Corrected: array `["id1","id2"]` in `body_template` (JsonBody context) → `["id1","id2"]` (valid JSON literal), NOT `[\"id1\",\"id2\"]`. This is what enables `body_template = '{"ids": ${query_detection_ids.resources}}'` to produce valid JSON `{"ids":["id1","id2"]}`. Test vector in Verification bullet corrected accordingly. Behavioral anchors cite function-level behavior per TD-VSDD-091 (not line numbers). |
| 1.63 | S-DEMO-002 re-cascade interpolation clarification | 2026-06-03 | product-owner | Clarifies the three distinct interpolation entry points to prevent recurrence of the v1.62 investigation confusion. (1) `Interpolator::interpolate` (used for fetch-pipeline `path_template` and `body_template`): confirmed NO special verbatim branch for Array/Object in the `JsonBody` arm — all types flow through `value_to_string` → `json_escape` uniformly. Arrays produce `[\"id1\",\"id2\"]` (NOT a valid JSON literal). The v1.62 description of this path was correct and is preserved. (2) `Interpolator::interpolate_record_ids` (`${record_ids}` write-side only): `JsonBody` arm calls `serde_json::to_string(record_ids)` directly — NO `json_escape` post-processing — producing a verbatim compact JSON array (valid literal). This is the ONLY path where arrays are inserted verbatim in JsonBody context. It is NOT available in fetch-pipeline steps. (3) `Interpolator::interpolate_write_params` (`${params.KEY}` write-side only): operates on pre-stringified `String` values; applies `json_escape` or `percent_encode` identically to the main path; no Array/Object type can appear. Root cause of investigation: orchestrator confused `interpolate_record_ids` verbatim behavior (write-side, `${record_ids}`) with `Interpolator::interpolate` fetch-pipeline behavior. Re-read of `interpolation.rs` lines 158-163, 289-296, 310-334, 341-388 confirmed v1.62 was accurate. Added explicit disambiguation text and three-function behavioral anchor table per TD-VSDD-091. No catalog-content changes; no event_type changes; POL-30 Fork B catalog bullet label `(v1.43)` UNCHANGED. |
| 1.62 | S-DEMO-002 re-cascade spec-drift fix (ADV-SDEMO002-P01-MED-001) | 2026-06-03 | product-owner | Closes ADV-SDEMO002-P01-MED-001 (spec-drift: code outran spec on interpolation escaping). (1) Corrected §56 Postconditions: removed incorrect claim that `body_template` is "interpolated identically to `path_template`" — replaced with accurate statement that `body_template` uses `InterpolationContext::JsonBody` via `Interpolator::interpolate`, which differs from `path_template`'s `UrlPath` context. (2) Added new **Interpolation Escaping Contract** postcondition enumerating the full context-dispatch contract: `UrlPath` (path_template) applies `Interpolator::percent_encode` over RFC 3986 unreserved charset; `JsonBody` (body_template) applies `Interpolator::json_escape` (escapes `"`, `\`, control chars). Verified: string scalars with inner `"` produce `\"` in JsonBody and `%22` in UrlPath; numbers/bools/null pass through trivially in both contexts; arrays serialize via `serde_json::Value::to_string()` then have inner `"` chars escaped in JsonBody (result is NOT a bare JSON literal — spec authors must not embed array variables expecting verbatim JSON). Behavioral anchor: `Interpolator::interpolate` → `value_to_string` → context dispatch (lines 158–163 `prism-spec-engine/src/interpolation.rs`; TD-VSDD-091: function/behavior anchor, not line number). Sibling-sweep (POL-25): BC-2.16.009 §65 references `path_template`/`body_template` variable resolution (not escaping) — no amendment needed; BC-2.16.013 references `serde_json::Value` for timestamp parsing only — no escaping claim present, no amendment needed. |
| 1.61 | S-DEMO-001 SAP-1 catalog fix-in-scope | 2026-05-31 | product-owner | SAP-1 compliance (PG-LP11-001): added new catalog row 69 `boot.step9a.adapter_registry_populated` INFO emitted by `step9a_populate_adapter_registry` (`crates/prism-bin/src/spec_driven_adapter.rs`). Fields: `sensor_count: u64` (count of `SpecDrivenSensorAdapter` instances registered in `AdapterRegistry`), `org_count: u64` (count of distinct `OrgId` values with at least one registered adapter). Two emission sites: (a) early-return when `resolved_spec_map` is empty (AC-006, `sensor_count=0`, `org_count=0`); (b) normal path after the full registration loop. Confirmed: the three `warn!` diagnostics within the loop body (org_slug_not_found, plugin_auth_provider_missing, unsupported_auth_type) correctly omit `event_type=` — they are internal operational diagnostics, not auditable boot-lifecycle events; no catalog rows needed for them. Scope statement extended to include `prism-bin` boot step 9A adapter-registry population emission. Catalog count 68→69; catalog bullet label advanced `(v1.42)` → `(v1.43)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Traceability: S-DEMO-001 (BC-2.22.001, BC-2.01.013). Fix-in-scope per CLAUDE.md Canonical Principle Rule 4 (production-grade default). |
| 1.60 | S-DTU-CYBERINT-AUTH-FIDELITY-001 PR-level fix-burst (F-PR2-MED-001) | 2026-05-30 | implementer | SAP-1 compliance (PG-LP11-001): added new catalog row `cookie_auth_401` WARN emitted by `issue_request_with_retry` when a `CookieRoundtrip` sensor receives HTTP 401. Fields: `sensor_id`, `client_id`, `step_name`. No retry — static API key cannot be refreshed; pipeline aborts with `SpecEngineError::CookieAuthFailed` (E-AUTH-004, BC-2.01.017 EC-017-002). Also amended `auth_refresh_double_401` row to note that `CookieRoundtrip` sensors emit `cookie_auth_401` instead. Catalog count 67→68; catalog bullet label advanced `(v1.41)` → `(v1.42)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as implementation. |
| 1.59 | PR-163 deeper deflake fix-burst | 2026-05-29 | implementer | SAP-1 compliance (PG-LP11-001): production code fix for natural-close path swallowing bg-task panic. (1) Row 64 (`mcp.server.shutdown.join_error`) AMENDED: emission site updated to two sites — (a) `Err(join_err)` arm of `close_with_timeout` in signal-drain path (existing); (b) NEW: `Err(join_err)` arm of `close_with_timeout` in the `natural_close_fut` arm when `is_transport_closed()` fires because bg task panicked. Description updated: the natural-close arm now calls `close_with_timeout` and propagates the JoinError rather than returning `Ok(())` unconditionally (PR-163 deeper deflake — original race was natural_close_fut winning the select before shutdown_tx, masking the panic). (2) Row 68 (`boot.shutdown.task_panic`) AMENDED: description updated to clarify that `mcp.server.shutdown.join_error` now fires on both signal-drain and natural-close paths (removed outdated "distinct from... fires from inside the shutdown drain window" qualifier). No catalog-count change (existing rows amended, none added). Catalog bullet label advanced `(v1.40)` → `(v1.41)` per POL-30 Fork B canonical rule (catalog-content structural change — row precondition changed). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as production code change. |
| 1.58 | S-5.01-FOLLOWUP-MCP-BOOT pass-11 fix-burst | 2026-05-28 | implementer | F-PASS11-MED-1 closure: deleted orphan catalog row `boot.step9.mcp_server_error` (formerly row 50). This event_type was registered in v1.51 as an emission from the `step9_start_mcp_server` background task when `serve_stdio` returns Err. The actual shutdown error handling was subsequently moved entirely to `RunningServer::wait_for_shutdown` (rows 65–68 added in v1.57). Code-grep confirms no `event_type = "boot.step9.mcp_server_error"` emission exists in the codebase; the row was an orphan superseded by the `wait_for_shutdown` event chain. Catalog count 68→67; catalog bullet label advanced `(v1.39)` → `(v1.40)` per POL-30 Fork B canonical rule (row deletion = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment. |
| 1.57 | S-5.01-FOLLOWUP-MCP-BOOT pass-10 fix-burst | 2026-05-28 | implementer | SAP-1 compliance (PG-LP11-001): F-PASS10-HIGH-1 fix — added 4 new structured event catalog rows (rows 65–68) for `RunningServer::wait_for_shutdown` shutdown-outcome emissions in `crates/prism-bin/src/boot.rs`. Row 65: `boot.shutdown.timeout` WARN in `Ok(Err(RmcpError::TaskError(_)))` arm — drain timeout exceeded; exit 1. Row 66: `boot.shutdown.runtime_error` ERROR in `Ok(Err(RmcpError::Runtime(_)))` arm — rmcp runtime error; exit 4. Row 67: `boot.shutdown.transport_init_error` WARN in `Ok(Err(e))` catch-all arm — transport init failure (stdin closed before serve); field `error: %display`; exit 0. Row 68: `boot.shutdown.task_panic` ERROR in `Err(_join_err)` arm — background task panicked (JoinError); exit 4. Scope statement extended to include `RunningServer::wait_for_shutdown` shutdown-outcome emissions. Catalog count 64→68; catalog bullet label advanced `(v1.38)` → `(v1.39)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as fix. |
| 1.56 | S-5.01-FOLLOWUP-MCP-BOOT pass-6 fix-burst | 2026-05-28 | implementer | SAP-1 compliance (PG-LP11-001): F-PASS6-HIGH-3 fix — corrected wrong event_type on `Err(join_err)` arm of `serve_stdio_with_shutdown`. (1) Row 64 ADDED: `mcp.server.shutdown.join_error` WARN in `PrismServer::serve_stdio_with_shutdown` — background task panic/cancellation during drain window; field `error: %display`; returns `Err(RmcpError::Runtime(join_err))`. (2) Row `mcp.server.shutdown.timeout` AMENDED: updated location to `Ok(None)` arm only (no longer covers `Err` arm); removed `optionally error: %display` field (now on join_error row instead); added reference to join_error sibling row. The previous row described both Ok(None) and Err arms — that was incorrect and caused the adversary to flag it as misclassified. Catalog count 63→64; catalog bullet label advanced `(v1.37)` → `(v1.38)` per POL-30 Fork B canonical rule (new row + field changes = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emission fix. |
| 1.55 | S-5.01-FOLLOWUP-MCP-BOOT pass-5 fix-burst | 2026-05-28 | implementer | SAP-1 compliance (PG-LP11-001): 3 catalog amendments for F-PASS5 fixes. (1) Row 63 ADDED: `mcp.server.shutdown.force` WARN in `PrismServer::serve_stdio_with_shutdown` — second SIGINT received during drain window; `process::exit(130)` follows (HIGH-4 / EC-10-019). No structured fields. (2) Row 62 AMENDED (`mcp.server.shutdown.timeout`): updated to reflect HIGH-3 fix — `process::exit(1)` replaced with `return Err(RmcpError::TaskError(...))` so Drop impls (tracing, RocksDB) run before caller exits. (3) Row 60 AMENDED (`mcp.server.shutdown.complete`): added `path: %display` field (values `"natural_close"` or `"signal_drain"`) to disambiguate two emission sites (OBS-2 fix). Catalog count 62→63; catalog bullet label advanced `(v1.36)` → `(v1.37)` per POL-30 Fork B canonical rule (new row + field changes = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. |
| 1.54 | S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst-2 | 2026-05-28 | implementer | SAP-1 compliance (PG-LP11-001): added 2 new structured event catalog rows (rows 61–62) for BC-2.10.010 graceful shutdown path emissions. Row 61: `mcp.server.shutdown.tasks_drained` INFO in `PrismServer::serve_stdio` — in-flight requests drained within the 5-second grace window after SIGINT/SIGTERM; no structured fields. Row 62: `mcp.server.shutdown.timeout` WARN in `PrismServer::serve_stdio` — 5-second grace window exceeded; `std::process::exit(1)` follows; fields: `grace_secs: u64`, optionally `error: %display`. Catalog count 60→62; catalog bullet label advanced `(v1.35)` → `(v1.36)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). F-PASS4-CRIT-2 closure: replaces the paper-fix `tokio::time::sleep(5)` with the real BC-2.10.010 six-step graceful shutdown sequence using `service.close_with_timeout(5s)`. PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. |
| 1.53 | S-5.01-FOLLOWUP-MCP-BOOT pass-4 fix-burst | 2026-05-27 | implementer | SAP-1 compliance (PG-LP11-001): added 3 new structured event catalog rows (rows 58–60) for boot step 9 alias-store loading emissions. Row 58: `boot.step9.alias_store_loaded` INFO in `step9_start_mcp_server` — `aliases.toml` found and loaded; field: `path: %display`. Row 59: `boot.step9.alias_store_load_failed` WARN in `step9_start_mcp_server` — `aliases.toml` found but failed to parse; fields: `path: %display`, `error: %display`. Row 60: `boot.step9.alias_store_empty` INFO in `step9_start_mcp_server` — no `aliases.toml` found; boot proceeds with empty alias store; field: `path: %display`. Scope statement extended to include prism-bin step 9 alias-store boot emissions. Catalog count 57→60; catalog bullet label advanced `(v1.34)` → `(v1.35)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. F-PASS3-CRIT-4 closure (alias_store wired into PrismServer::with_deps for alias tool handlers). |
| 1.52 | S-5.01-FOLLOWUP-MCP-BOOT pass-3 fix-burst | 2026-05-27 | implementer | SAP-1 compliance (PG-LP11-001): added 2 new structured event catalog rows (rows 56–57) for deferred boot steps 10 and 11. Row 56: `boot.step10.deferred` WARN in `step10_start_hot_reload` — HotReloadWatcher deferred to S-1.12-FOLLOWUP; step returns Ok(()) without watcher. Row 57: `boot.step11.deferred` WARN in `step11_install_signal_handlers` — SIGHUP config-reload handler deferred to S-1.12-FOLLOWUP; step returns Ok(()) with SIGTERM/SIGINT handled by serve_stdio. Both rows replace `todo!()` calls that would have caused `prism start` to panic (F-PASS3-MED-3 closure). Catalog count 55→57; catalog bullet label advanced `(v1.33)` → `(v1.34)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). |
| 1.51 | S-5.01-FOLLOWUP-MCP-BOOT | 2026-05-27 | implementer | SAP-1 compliance (PG-LP11-001): added 7 new structured event catalog rows (rows 49–55) for MCP server boot and tool audit emissions. Row 49: `boot.step9.mcp_server_started` INFO in `step9_start_mcp_server` — PrismServer spawned on stdio transport. Row 50: `boot.step9.mcp_server_error` ERROR in `step9_start_mcp_server` background task — serve_stdio exited with error. Row 51: `mcp.tool.called` INFO in `emit_tool_audit` (`prism-mcp/src/server.rs`) — per-tool invocation audit entry (BC-2.05.009); fields: `tool_name`, `client_id`, `outcome`. Row 52: `mcp.server.shutdown.initiated` INFO in `PrismServer::serve_stdio` — SIGINT/SIGTERM received; field: `signal`. Row 53: `mcp.server.shutdown.complete` INFO in `PrismServer::serve_stdio` — graceful shutdown complete. Row 54: `write.intent.recorded` INFO in `TracingAuditWriter::write_intent` (boot-local struct) — write intent audit stub; fields: `intent_id`, `sensor`. Row 55: `write.outcome.recorded` INFO in `TracingAuditWriter::write_outcome` (boot-local struct) — write outcome audit stub; fields: `intent_id`, `succeeded`, `failed`. Catalog count 48→55; catalog bullet label advanced `(v1.32)` → `(v1.33)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Scope statement extended to include prism-mcp MCP tool audit emissions and prism-bin boot-local write audit stub emissions. PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. |
| 1.50 | S-3.02-FOLLOWUP-RUNTIME | 2026-05-27 | implementer | SAP-1 compliance (PG-LP11-001): added 2 new structured event catalog rows (rows 47–48) for boot step 7/8 completion events. Row 47: `boot.step7.storage_validated` INFO event in `step7_init_storage` (`crates/prism-bin/src/boot.rs`) — emitted when step 7 completes (RocksDB backend validated, internal table registration deferred to per-query execute_inner). Row 48: `boot.step8.query_engine_started` INFO event in `step8_init_query_engine` (`crates/prism-bin/src/boot.rs`) — emitted after `mark_query_phase_started()` closes the write-tool registration window (ADR-026 §D7); QueryEngine/WriteExecutor wiring deferred to S-5.01-FOLLOWUP-MCP-BOOT. Catalog count 46→48; catalog bullet label advanced `(v1.31)` → `(v1.32)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). Scope statement extended to include boot step 7/8 completion events. PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. |
| 1.49 | PLUGIN-MIGRATION-001-C pass-2 fix-burst | 2026-05-27 | implementer | SAP-1 compliance (F-LP2-MED-001): added row 46 `ocsf.spec_driven_mapper_empty_sensor_id` WARN event emitted by `SpecDrivenMapper::new` when the `ConfigSnapshot` has no sensors. Emission site: `SpecDrivenMapper::new()` (`crates/prism-ocsf/src/mappers/spec_driven.rs`) in the `unwrap_or_else` branch that produces empty sensor_id. No structured fields (emitted before sensor_id is known). Trigger: construction-time config snapshot is empty. Catalog count 45→46; catalog bullet label advanced `(v1.30)` → `(v1.31)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). Scope statement extended to include construction-time empty config in the `prism-ocsf` normalization scope description. PG-LP11-001 discipline: implementer-scope BC amendment in same commit burst as tracing emission. |
| 1.48 | PLUGIN-MIGRATION-001-C pass-1 fix-burst | 2026-05-27 | implementer | SAP-1 compliance (F-LP1-HIGH-004): added 3 new structured event catalog rows (rows 43–45) for `prism-ocsf` OCSF normalization emissions. Row 43: `ocsf.spec_column_absent_in_raw` DEBUG in `SpecDrivenMapper::map` — spec-declared column absent from raw; Null inserted in extensions (HIGH-005). Row 44: `ocsf.wasm_dispatch_deferred` WARN in `SpecDrivenMapper::map` — WASM plugin present but `call_ocsf_transform` not yet wired; raw value preserved in extensions (CRIT-002). Row 45: `ocsf.timestamp_parse_failed` WARN in `SpecDrivenMapper::map` — timestamp parse failed; raw value preserved in extensions (CRIT-003). Scope statement extended to include `prism-ocsf` OCSF normalization emissions (Path B pattern). Catalog count 42→45; catalog bullet label advanced `(v1.29)` → `(v1.30)` per POL-30 Fork B canonical rule (new rows = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emissions. |
| 1.47 | S-CONFIG-MULTI-TENANT-OVERRIDE-001 PR #155 pass-3 fix-burst | 2026-05-25 | implementer | SAP-1 compliance (SEC-PASS2-001 / SEC-PASS3-001): added `overlay.timeout_secs_ignored` WARN event as row 42 to Canonical Structured Event Catalog. Function: `OverlayLoader::merge_overlay_onto_type_spec` (`crates/prism-spec-engine/src/overlay.rs`). Fields: `sensor_id: %display`, `timeout_secs: u64`. Trigger: overlay TOML sets `timeout_secs` which is stored in provenance but not yet wired to HTTP client. Audit role: operational observability — makes the wiring gap visible. Deferred to S-CONFIG-MULTI-TENANT-OVERRIDE-002. Catalog count 41→42; catalog bullet label advanced `(v1.28)` → `(v1.29)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emission. |
| 1.46 | S-CONFIG-MULTI-TENANT-OVERRIDE-001 PR #155 fix-burst | 2026-05-25 | implementer | SAP-1 compliance (ADV-009 + PRR-005): added 3 new structured event catalog rows (rows 39–41). Row 39: `boot.overlays_loaded` INFO event in `step4_load_sensor_specs_with_overlays` (`prism-bin/src/boot.rs`) — boot summary emission for per-org overlay loading (ADV-009 fix). Row 40: `boot.type_spec_read_failed` ERROR event in `build_type_spec_map_for_overlay` — I/O failure reading TYPE spec at boot overlay map construction (PRR-005 fix, Standing Rule 3 §2). Row 41: `boot.type_spec_parse_failed` ERROR event in `build_type_spec_map_for_overlay` — parse failure for TYPE spec at boot overlay map construction (PRR-005 fix). Catalog count 38→41; catalog bullet label advanced `(v1.27)` → `(v1.28)` per POL-30 Fork B canonical rule (new rows = catalog-content structural changes). |
| 1.45 | PLUGIN-MIGRATION-001-E fix-burst | 2026-05-24 | implementer | SAP-1 compliance: renamed row 37 event_type `plugin.auth_token_parse_error` → `plugin_auth_token_parse_error` (dot → underscore). Aligns BC catalog with production implementation in `emit_acquire_token_parse_error_and_fail` (`crates/prism-spec-engine/src/plugin/mod.rs`) which uses underscore naming per BC-2.16.002 event_type naming convention (all other rows use underscores). No catalog-count change; no POL-30 Fork B bullet-label change (existing catalog-content unchanged — event_type rename corrects a typo in the BC text, not a new row). |
| 1.44 | S-CONFIG-MULTI-TENANT-OVERRIDE-001 | 2026-05-23 | implementer | SAP-1 compliance: added `overlay.loaded` INFO event as row 38 to Canonical Structured Event Catalog. Function: `OverlayLoader::load_overlays` (`crates/prism-spec-engine/src/overlay.rs`). Fields: `org_slug: &str`, `sensor_id: &str`, `instance_id: &str`. Trigger: per-org overlay TOML file successfully validated and merged onto TYPE spec. Audit role: operational/traceability. Recurrence: one per overlay file per boot/reload. BC-2.06.012 postcondition requirement. Catalog count 37→38; catalog bullet label advanced `(v1.26)` → `(v1.27)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). PG-LP11-001 discipline: implementer-scope BC amendment in same commit as tracing emission. |
| 1.43 | FB-IMPL-7 (PLUGIN-MIGRATION-001-E) | 2026-05-23 | implementer | F-LP8-MED-002 + F-LP8-MED-003 closure (PLUGIN-MIGRATION-001-E pass-8): (1) Row 37 fields cell — removed contradictory `%display` annotation on `error` field: the `error!` macro receives a string literal directly (not a Display-formatted type); changed to `error: literal string "acquire-token dispatch completed but no token was cached in KV store (guest AuthError::ResponseParse or missing kv_set call)"`. Also updated `plugin_id` field annotation from `plugin_id: %str` to `plugin_id: &str (Display via tracing %plugin_id macro)` for consistency with sibling rows (F-LP8-MED-003 closure). (2) Row 37 description cell — added `Return variant: PluginError::AuthTokenNotCached (E-PLUGIN-022 — NOT E-PLUGIN-008 CompilationFailed)` to document that `emit_acquire_token_parse_error_and_fail` now returns the semantically correct variant (F-LP8-MED-002 closure). F-LP8-MED-004 note: deleted duplicate EC-002 test `test_acquire_token_EC_002_returns_response_parse_no_token_cached` (orphaned by FB-IMPL-6 CORRECTION signature revert); canonical EC-002 test is `test_acquire_token_EC_002_invalid_json_returns_response_parse`. No new catalog rows, no event_type changes; POL-30 Fork B catalog bullet label `(v1.25)` UNCHANGED. BC-INDEX v5.45→v5.46. |
| 1.42 | FB-IMPL-6-CORRECTION (PLUGIN-MIGRATION-001-E CORRECTION burst) | 2026-05-23 | implementer | F-LP7-MED-001 CORRECTION (paper-fix resolved): orchestrator structural-coverage axis review detected that the v1.41 emission was in a `#[cfg(test)]-gated guest helper` (`emit_auth_token_parse_error` in `crowdstrike-oauth2/src/lib.rs`) with a `#[cfg(not(test))]` no-op stub — the production wasm32 build NEVER emitted the event. Root cause: the wasm32 guest runs in a sandboxed wasmtime instance with no tracing subscriber; only the host owns the tracing subscriber. Correction: (1) Removed `emit_auth_token_parse_error` helper and no-op stub from guest; (2) Removed `plugin_id: &str` parameter from guest `acquire_token` and `get_token` (was only used by the now-removed helper); (3) Updated WIT entrypoints to not pass `plugin_name()`; (4) Updated all test call sites; (5) Replaced guest capturing-subscriber test with `test_acquire_token_EC_002_returns_response_parse_no_token_cached` (assert ResponseParse + no KV token — no tracing assertions); (6) Added `emit_acquire_token_parse_error_and_fail` `pub(crate)` host function in `crates/prism-spec-engine/src/plugin/mod.rs` — UNCONDITIONAL `error!` emission, called from `dispatch_plugin_acquire_token` Component Model path when `kv_store.get(plugin_id, "token")` returns `None`; (7) Load-bearing unit test `test_F_LP7_MED_001_host_emit_acquire_token_parse_error_fires_unconditionally` in `prism-spec-engine plugin::tests` calls `emit_acquire_token_parse_error_and_fail` directly and asserts `plugin.auth_token_parse_error` fires; (8) `#[ignore]`'d integration test `test_F_LP7_MED_001_host_dispatch_acquire_token_component_model_path_emits_audit_event` for future un-ignore with pre-built `.prx` (S-PLUGIN-CI-001). Row 37 emission site updated from guest helper to host function. |
| 1.41 | FB-IMPL-6 (PLUGIN-MIGRATION-001-E) | 2026-05-23 | implementer | F-LP7-MED-001 closure (PLUGIN-MIGRATION-001-E pass-7): added `plugin.auth_token_parse_error` ERROR event as row 37 to Canonical Structured Event Catalog. Function: `acquire_token` (`crates/prism-spec-engine/plugins/crowdstrike-oauth2/src/lib.rs`) via `emit_auth_token_parse_error` helper (gated `#[cfg(test)]` in native builds; no-op on wasm32). Fields: `plugin_id: %str` (call-site parameter: `plugin_name()` on wasm32, `"crowdstrike-oauth2"` in tests), `error: %display` (`AuthError::ResponseParse` Display — parse failure reason, never credential data). Trigger: any `AuthError::ResponseParse` branch in `acquire_token` (non-2xx status, invalid UTF-8 body, JSON parse failure, missing `access_token` field). Audit role: error. Recurrence: one per parse failure, no rate limiting. Cross-reference: PLUGIN-MIGRATION-001-E EC-002 + EC-003. Capturing-subscriber test `test_dispatch_plugin_acquire_token_response_parse_emits_audit_event` added in same commit. `acquire_token` + `get_token` signatures updated to accept `plugin_id: &str` parameter (TD-VSDD-060 sibling-sweep: WASM guest call sites pass `plugin_name()`, test call sites pass `"crowdstrike-oauth2"`, `get_token` threaded through). F-LP7-LOW-001 closure also in same commit: EC-003 test `test_acquire_token_EC_003_missing_access_token_returns_response_parse` added `kv_store.borrow().get("token") == None` assertion (sibling-symmetry with EC-001 + EC-002). Catalog count 36→37; catalog bullet label advanced `(v1.24)` → `(v1.25)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). SAP-1 standing probe compliance: implementer-scope BC amendment in same commit as tracing emission (PG-LP11-001). |
| 1.40 | FB-IMPL-3 (PLUGIN-MIGRATION-001-E) | 2026-05-22 | implementer | F-LP3-LOW-001 closure (PLUGIN-MIGRATION-001-E pass-3): added `plugin_auth_provider_constructed` INFO event as row 36 to Canonical Structured Event Catalog. Function: `run_boot_sequence` step 7.5b via `validate_and_construct_auth_providers` (`crates/prism-bin/src/boot.rs`). Fields: `sensor_id: String` (sensor_spec HashMap key), `plugin_id: String` (`auth_provider.plugin_id()` return). Trigger: `validate_and_construct_auth_providers` returns Ok — emitted once per successfully-wired plugin-auth sensor. Consistent with sibling boot emissions `boot.audit.initialized`, `plugin_load_disabled_via_envvar`, `plugin_registration_rolled_back`. Audit role: operational observability (pre-traffic confirmation of plugin auth wiring for each sensor). Catalog count 35→36; catalog bullet label advanced `(v1.23)` → `(v1.24)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). SAP-1 standing probe compliance: implementer-scope BC amendment in same commit as tracing emission (PG-LP11-001). |
| 1.39 | D-776-post-merge | 2026-05-22 | state-manager | POL-14 verification (no-op confirm): PR #153 (PLUGIN-MIGRATION-001-D) squash-merged to develop@3f2de889 at 2026-05-22T09:05:47Z; status already active — idempotent confirm per POL-14. |
| 1.38 | FB-IMPL-10 | 2026-05-21 | state-manager | F-LP11-MED-001 closure (6th POL-29 axis recurrence — 2nd-order propagation gap): row 112 Anchor cite-pin advanced `BC-2.16.013 v1.14` → `BC-2.16.013 v1.15` (FB-IMPL-9 bumped BC-2.16.013 frontmatter v1.14→v1.15 as side-effect of §Error Conditions cite sweep; BC-2.16.002 row 112 Anchor was not swept in that burst — 2nd-order propagation gap). No catalog-content structural change (no new rows, no event_type changes); POL-30 Fork B catalog bullet label `(v1.23)` UNCHANGED. BC-INDEX v5.40→v5.41. |
| 1.37 | FB-IMPL-5 PO | 2026-05-21 | product-owner | F-LP5-MED-001 closure: row 112 anchor cite-pin advanced `BC-2.16.013 v1.13` → `BC-2.16.013 v1.14` (BC-2.16.013 is now v1.14 per PO commit 4d934f28; the BC-2.16.002 catalog row was not swept in that burst — closing sibling-sweep gap per TD-VSDD-060 + POL-29 step 8f). No catalog-content structural change (no new rows, no event_type changes); POL-30 Fork B catalog bullet label `(v1.23)` UNCHANGED. BC-INDEX v5.38→v5.39. |
| 1.36 | FB-IMPL-1 | 2026-05-21 | product-owner | Add `timestamp.fallback_to_now` WARN event_type catalog row per ADR-028 v1.9 §D8-B Option A grammar contract (PLUGIN-MIGRATION-001-D). Architect bumped BC-2.16.013 v1.11→v1.12 specifying the tracing emission but did not propagate to BC-2.16.002 catalog in same burst — closing the routing gap pre-implementer dispatch to prevent adversarial pass-2 finding per CLAUDE.md §Conventions structured event catalog discipline. Catalog count 34→35; catalog bullet label advanced `(v1.22)` → `(v1.23)` per POL-30 Fork B canonical rule (new row = catalog-content structural change). |
| 1.35 | pass-11-spec-hygiene | 2026-05-18 | product-owner | frontmatter `deprecated/deprecated_by` concatenation defect fix (closes F-LP-IMPL-P11-HIGH-001; YAML-parse-breaking pre-existing defect). `deprecated: nulldeprecated_by: null` split into two separate lines `deprecated: ~` / `deprecated_by: ~` per sibling BC convention (BC-2.16.012). |
| 1.34 | pass-10-spec-hygiene | 2026-05-18 | product-owner | F-LP-IMPL-P10-SUG-001 closure (Option B adjudication): catalog bullet label advanced `(v1.21)` → `(v1.22)` at line 73 per POL-30 Fork B canonical rule. The v1.32 §Changelog row correctly recorded "intro updated v1.21→v1.22" — that description was the intended state; the actual bullet label at line 73 was not updated in the same v1.32 burst (split between implementing the row-34 content change and syncing the bullet-version label). Adding row 34 (`plugin_registration_rolled_back`) in v1.32 IS a catalog-content change, so the bullet MUST advance per Fork B. Sibling propagation per POL-29 step 8h/8i: BC-2.16.012 v1.28 (2 sites: line 84 §Postconditions + EC-016-012-005), error-taxonomy v1.38 (2 sites: E-PLUGIN-020 + E-PIPELINE-001 Canonical Structured Event Catalog bullet refs), S-PLUGIN-PREREQ-E story v1.49+ (5 sites: lines 219, 311, 359, 434, 435) updated in same burst. |
| 1.33 | FB-IMPL-4 | 2026-05-18 | state-manager | D-707 F-LP-IMPL-P5-003 closure: §Postconditions Canonical Structured Event Catalog intro count 33→34 sync (FB-IMPL-3 added row 34 `plugin_registration_rolled_back` but intro count was not updated in same burst; sibling-drift caught at pass-5). |
| 1.32 | FB-IMPL-3 | 2026-05-18 | implementer+state-manager | F-LP-IMPL-P4-002 Route A closure (implementer scope): added `plugin_registration_rolled_back` ERROR event as row 34 to Canonical Structured Event Catalog. Function: `plugin_load_step_with_audit` step 7.6 (`crates/prism-bin/src/boot.rs`). Fields: `plugin_name: String` (plugin manifest `name`), `tool_name: String` (failing `WriteToolInvalidationMap.tool_name`), `registration_error: String` (`reg_err` Display), `rolled_back_tools: usize` (`deregister_write_tools_for_plugin` return), `plugin_unregistered: bool` (`PluginRuntime::unregister_plugin` return). Trigger: write-tool registration failure; fail-closed rollback per BC-2.07.004 §write-then-read consistency. Audit role: operational incident. Catalog count 33→34; intro updated v1.21→v1.22. state-manager performed frontmatter sync (version + modified + timestamp) per routing table bookkeeping scope. |
| 1.31 | FB73 | 2026-05-17 | product-owner | F-LP85-HIGH-001 closure (PO scope): ADR-026 D7 pin v1.22→v1.23 propagation at BC-2.16.002 line 110 row 33 body cite (1 site). POL-30 Fork B preserved: catalog bullet label `(v1.21)` at line 74 UNCHANGED. Sibling files story v1.46 + BC-2.16.011 v1.10 + BC-2.16.012 v1.26 + VP-156 v0.18 + HS-003 v1.15 + error-taxonomy v1.38 swept in same burst. |
| 1.30 | FB70 | 2026-05-17 | product-owner | F-LP82-HIGH-001 closure (PO scope): line 110 row 33 body cite-pin `per ADR-026 D7 v1.21` → `per ADR-026 D7 v1.22` (single 5-char fix). **RETRACTS FB69 v1.29 row narrative** which incorrectly framed line 110 cite-pin as POL-30 Fork B frozen — Fork B freezes ONLY the catalog bullet-version-label at line 74 (`(v1.21)`, tracking catalog-content-version when event table content last changed); row-body ADR-pin citations are governed by POL-29 class (b) cite-pin propagation and MUST advance when ADR-026 bumps. FB55/FB56b/FB62 3-burst precedent (v1.17→v1.18, v1.18→v1.19, v1.19→v1.21 at this exact site) all correctly advanced under proper POL-30 scope. Line 74 catalog bullet label `(v1.21)` correctly preserved unchanged per Fork B canonical rule. |
| 1.29 | FB69 | 2026-05-17 | product-owner | F-LP81-HIGH-002 closure (PO scope): frontmatter version bump only (1 site — line 110 catalog bullet). POL-30 Fork B preserved: catalog bullet label `(v1.21)` UNCHANGED per POL-30 Fork B freeze. Body line 110 ADR-026 D7 v1.21 citation is the POL-30 Fork B frozen value and must not be incremented. Sibling files story v1.44 + BC-2.16.012 v1.25 + error-taxonomy v1.37 + VP-156 v0.17 + HS-003 v1.14 swept in same burst. |
| 1.28 | FB64 | 2026-05-17 | product-owner | F-LP76-HIGH-001 closure (PO scope): burst-label cell corrected FB74→FB62 in §Changelog row for v1.27. Original FB62 closure of F-LP74-HIGH-001 was labeled "FB74" derived from finding ID; canonical FB sequential counter was FB62 per state-manager records. POL-26 schema integrity + POL-29 cross-domain sibling consistency restored. POL-30 Fork B catalog bullet (v1.21) UNCHANGED. |
| 1.27 | FB62 | 2026-05-17 | product-owner | F-LP74-HIGH-001 closure (PO scope): ADR-026 D7 pin v1.19→v1.21 at line 110 catalog row 33 cite (1 site). POL-30 Fork B preserved — catalog bullet label `(v1.21)` UNCHANGED, frontmatter-only bump. |
| 1.26 | FB56b | 2026-05-17 | product-owner | F-LP68-HIGH-001 closure cascade (FB56b PO scope): ADR-026 D7 pin v1.18→v1.19 propagation at BC-2.16.002 Structured Event Catalog row line 110 (1 site; POL-30 Fork B catalog bullet `(v1.21)` UNCHANGED — frontmatter-only bump). POL-29 v1.17 step 8a FIRST APPLICATION cascade closure. |
| 1.25 | FB55 | 2026-05-17 | product-owner | F-LP67-HIGH-001 sibling-sweep closure (PO scope extension): ADR-026 D7 pin v1.17→v1.18 at Structured Event Catalog row for `write_tool_registration_after_boot` (line 110; 1 live-narrative site). Caught by FB55 PO workspace-grep sibling-sweep beyond original 5-file scope per correct-agent-pattern (CLAUDE.md PREREQ-B PG-LP11-001 precedent). POL-29 v1.16 step 3a (b) recurrence #18 within-burst closure extension. POL-30 Fork B: cite-pin correction in row body is NOT a catalog-content structural change (no new rows, no event_type changes, no count changes); catalog bullet label (v1.21) unchanged; BC frontmatter version bump alone applies. |
| 1.24 | FB50 | 2026-05-17 | architect | POL-23 sibling-sweep OBS-LP62-002 interpretation #2: catalog row 33 live-narrative ADR-026 D7 v1.10 pin bumped to v1.17 (current ADR-026 version per FB47 §Related ADRs row edit; D7 content unchanged since v1.16). |
| 1.23 | FB43 | 2026-05-16 | product-owner | F-LP54-HIGH-001 — v1.22 row narrative retroactively reframed under Fork B canonical rule (POL-30 established by FB42 D-662). The v1.22 closure correctly closed a legitimate FB37-introduced catalog-content-version sync gap (catalog row 33 was added at FB37 but bullet header (v1.20) was not bumped at that time; FB41 closed that legitimate gap by advancing bullet to (v1.21)). The v1.22 row's "syncing with frontmatter v1.21" framing was Fork-A-aligned; Fork B canonical rule (POL-30) instead recognizes that bullet-version-label tracks catalog-content-version INDEPENDENT of BC frontmatter. Under Fork B, FB41 fix remains correct (bullet (v1.21) reflects catalog state after FB37 row 33 addition), and post-FB41 state is internally consistent: bullet (v1.21) ↔ 8 cite-pin sites at (v1.21) ↔ frontmatter (v1.22) reflecting independent BC-narrative-cite-pin-update version-bump. The v1.22 row's "POL-30 codification candidate (BC frontmatter version bumps must include in-body canonical-anchor bullet label updates)" text is retired Fork-A phrasing; Fork B POL-30 instead canonicalizes independent versioning per SESSION-HANDOFF §POST-FB42-CLOSURE / SESSION-D644-TASKS §Fork B Canonical Rule. |
| 1.22 | FB41 | 2026-05-16 | product-owner | F-LP52-HIGH-001 §Postconditions Canonical Structured Event Catalog bullet header label advanced (v1.20)→(v1.21) syncing with frontmatter v1.21 + 8 PREREQ-E cite-pin sites. 9th manifestation of POL-23 within-FB sibling-sweep asymmetry closed; POL-30 codification candidate (BC frontmatter version bumps must include in-body canonical-anchor bullet label updates) for cycle-close. |
| 1.21 | FB37 | 2026-05-16 | product-owner | F-LP47-HIGH-001 row 33 trigger condition corrected from "after query engine init completes (step 8+)" to canonical "query-engine init starts (step 8+, ADR-026 D7); the AtomicBool query-phase flag is set at step 8 start (first act of step 8, before QueryEngine construction proceeds)"; semantic match with BC-2.16.012 EC-016-012-005 v1.16 + HS-PREREQ-E-003-05 v1.6. |
| 1.20 | prereq-e-fix-burst-14 | 2026-05-16 | product-owner | F-LP15-HIGH-001 — within-FB12 sibling-sweep asymmetry close (6th RECURRENCE of POL-23 class): §Postconditions Canonical Structured Event Catalog bullet header label advanced (v1.18) → (v1.19) matching frontmatter version (FB12 D-603 bumped frontmatter v1.18→v1.19 for row 33 source spec clarification but did not sync internal bullet label; downstream BC-2.16.012 + error-taxonomy cites of (v1.19) were phantom until this sync). Bump v1.19→v1.20 per POL-11 index-mutation-version-bump discipline + POL-23 sibling-sweep gate. |
| 1.19 | prereq-e-fix-burst-12 | 2026-05-16 | architect | F-LP13-HIGH-003 — Catalog row 33 source spec clarification: `plugin_name` field source = `entry.plugin_name` (`WriteToolInvalidationMap` struct field, set by `PluginRuntime` from plugin manifest `name` field per ADR-026 D7 v1.10); `tool_name` field source = `entry.tool_name` (`WriteToolInvalidationMap` struct field); `error` field source = literal string `"E-PLUGIN-020"`. Resolves spec-implementation coherence gap: row 33 mandated `plugin_name` field with no defined source; implementer could not satisfy the catalog contract without this provenance. Provenance anchored to ADR-026 D7 v1.10 Option A adjudication (struct extension). No catalog row count or event_type changes — source-spec clarification only. |
| 1.18 | prereq-e-fix-burst-11 | 2026-05-16 | product-owner | F-LP12-MED-001 — Add `write_tool_registration_after_boot` WARN event row to Canonical Structured Event Catalog (row 33). New emission site introduced by S-PLUGIN-PREREQ-E ADR-026 D7 + error-taxonomy E-PLUGIN-020 + HS-PREREQ-E-003-05. Source: `register_write_tool` in `crates/prism-query/src/invalidation.rs`. Fields: `plugin_name: String`, `tool_name: String`, `error: "E-PLUGIN-020"`. Audit role: forensic-trace. Recurrence: one emission per post-boot registration attempt; not retried. Catalog scope statement updated to include `prism-query` plugin write-tool registration (ADR-026 D7). Count 32→33; label v1.17→v1.18. Per PG-LP11-001 (codified PREREQ-B cascade) + CLAUDE.md Conventions §Structured event catalog discipline. Scope expansion to BC-2.16.002 per Canonical Principle Rule 4. |
| 1.17 | S-PLUGIN-PREREQ-D-fix-burst-impl-3 | 2026-05-14 | implementer | F-PASS3-HIGH-001 closure: add `plugin_log_level_unrecognized` catalog row (row 32). Emitted by `register_host_functions` `host::log` callback when plugin sends a WIT enum log-level name not recognized by the host (e.g., future log-level variant). Host safe-defaults to LogLevel::Info after emitting this event (forward-compat preservation). Fields: `plugin_id`, `received_name`. Audit role: operational observability. Catalog intro updated v1.16→v1.17; count 31→32. Closes F-PASS3-HIGH-001 (SOUL.md #4 observability; BC-2.16.002 PG-LP11-001 SOP). |
| 1.16 | S-PLUGIN-PREREQ-D-fix-burst-impl-2 | 2026-05-14 | implementer | F-PASS2-HIGH-001 closure: update Canonical Structured Event Catalog prose intro (v1.12→v1.16; count 25→31) per TD-VSDD-060 sibling-sweep gap. The intro line was last updated at v1.12 and missed 3 subsequent amendments (v1.13/v1.14/v1.15) that added 6 new catalog rows (25→31). BC version bumped to v1.16 to record this amendment. No catalog row content changes — metadata-only fix to the intro line. Closes F-PASS2-HIGH-001. |
| 1.15 | S-PLUGIN-PREREQ-D-fix-burst-impl-1 | 2026-05-14 | implementer | F-IMPL-LP1 fix-burst closures: (1) MED-001: add `message` field to `plugin_load_failed_compilation` row (emission site emits `message` but catalog row omitted it). (2) MED-002: rename `plugin_id` → `sensor_id` in `pipeline_max_requests_exceeded` catalog row and emission site (pipeline executor has sensor_id context, not plugin_id). (3) Add 3 new catalog rows: `plugin_load_failed_manifest_not_found` (E-PLUGIN-018, HIGH-005), `plugin_load_failed_manifest_parse_error` (E-PLUGIN-017, HIGH-003), `plugin_load_failed_format_version_missing` (E-PLUGIN-019, HIGH-006). Total catalog rows: 28 → 31. |
| 1.14 | S-PLUGIN-PREREQ-D-impl | 2026-05-14 | implementer | PG-LP11-001 closure: add 3 new Structured Event Catalog rows discovered during S-PLUGIN-PREREQ-D TDD implementation — `plugin_directory_not_found` (INFO, `PluginRuntime::load_all_plugins`, `plugin_dir`, EC-D-001 non-error path), `plugin_load_failed_read_error` (ERROR, same emitter, `plugin_path` + `error`, filesystem I/O failure on `.prx` read), `plugin_load_failed_compilation` (ERROR, same emitter, `plugin_path` + `error: E-PLUGIN-008`, WASM binary compilation failed). All 3 emit from `load_all_plugins` n-1 survivor loop. Total catalog rows: 25 → 28. Implements Standing Rule 3 §6: implementer amends BC-2.16.002 in same commit as emission site per PG-LP11-001 SOP codified in cycles/wave-4-operations/lessons.md Lesson 1. |
| 1.13 | fix-burst-37 | 2026-05-14 | state-manager | (D-541) F-LP40-MED-001 closure: frontmatter `modified` field updated from `null` to `2026-05-14` and `timestamp` updated from stale 2026-04-13T12:00:00 (original v1.0 cycle-1 authorship date) to 2026-05-14T00:00:00Z. Sibling-sweep gap from F-LP36-MED-001 / OBS-LP36-001 codification (fix-burst-34) — pattern was caught in BC-2.17.007 but not propagated to BC-2.16.002 despite 12 amendments through v1.12. Matches canonical pattern established in fix-burst-34 (BC-2.17.007 v1.2→v1.3 frontmatter sync). No body content changes — pure metadata sync per state-manager frontmatter-sync routing. |
| 1.12 | S-PLUGIN-PREREQ-D-fix-burst-17-stage-1A | 2026-05-13 | product-owner | F-LP18-MED-001 BC portion closure — add 2 new Structured Event Catalog rows for E-PLUGIN-015 and E-PLUGIN-016 manifest validation errors: `plugin_load_failed_manifest_name_missing` (ERROR, `PluginRuntime::load_plugin`, `plugin_path` + `error: E-PLUGIN-015`) and `plugin_load_failed_manifest_version_malformed` (ERROR, `PluginRuntime::load_plugin`, `plugin_path` + `version_value` + `error: E-PLUGIN-016`). AC-5 specifies 4 manifest error codes (E-PLUGIN-013/014/015/016); EC table covered all 4 after fix-burst-16; catalog was asymmetric with only E-PLUGIN-013/014 cataloged. This amendment restores symmetry. Total catalog rows: 23 → 25. Catalog label updated from "(v1.11)" to "(v1.12)". |
| 1.11 | S-PLUGIN-PREREQ-D-fix-burst-8-stage-1 | 2026-05-13 | product-owner | F-LP9-MEDIUM-001 closure — Path B adjudication: catalog-destination scope mismatch between BC-2.16.002 narrow scope ("PipelineExecutor and helpers / pipeline.rs only") and PG-LP11-001's universal-catalog architectural intent. 6 of 7 new story-listed event_type rows emit from outside pipeline.rs (PluginRuntime, host_functions, boot.rs step-7.5). Path B chosen: expand BC-2.16.002 scope to cover all prism-spec-engine emissions + prism-bin boot-step plugin-load emissions. Path A (new BC-2.17.008) rejected: unnecessary new ID per POL-1 append-only cost. Path C (scatter across 4 BCs) rejected: breaks PG-LP11-001 single-source-of-truth intent. Changes: (1) catalog header renamed from "Structured Event Catalog (v1.10)" to "Canonical Structured Event Catalog (v1.11)"; (2) scope statement rewritten to cover all prism-spec-engine + prism-bin plugin-load event_type sites; (3) 7 new rows added: plugin_load_unsigned, plugin_load_disabled_via_envvar, plugin_load_failed_manifest_no_allowed_urls, plugin_load_failed_format_version_exceeded, plugin_load_failed_wit_invalid, plugin_http_request_blocked, pipeline_max_requests_exceeded. Total catalog rows: 16 → 23. BC-2.22.001 delegation ("per BC-2.16.002") preserved unchanged — Path B makes BC-2.16.002 the correct authority; no material edit needed there. |
| 1.10 | S-PLUGIN-PREREQ-C-fix-burst-1 | 2026-05-12 | product-owner | Add 2 new Structured Event Catalog rows: jsonpath_extraction_failed (F-LP1-HIGH-001 — AC-2 bounds-check observability silence closure) and jsonpath_size_cap_exceeded (F-LP1-HIGH-007 — AC-2 nested-wildcard memory amplification protection). Total catalog rows: 14 → 16. Catalog label updated from "(v1.8)" to "(v1.10)". PG-LP11-001 SOP enforced. |
| 1.9 | S-PLUGIN-PREREQ-B-post-merge | 2026-05-12 | state-manager | Status draft→active per POL-14: anchor story S-PLUGIN-PREREQ-B merged via PR #143 at develop@ae7e26c8 (2026-05-12T06:58:48Z). 16 LOCAL adversary passes + 13 fix-bursts + PR-LEVEL 1/1 CLEAN + 34/34 CI + pr-reviewer APPROVE. No substantive content change — metadata-only promotion. |
| 1.8 | S-PLUGIN-PREREQ-B-fix-burst-11 | 2026-05-11 | product-owner | Add Structured Event Catalog enumerating all 14 event_type variants emitted by PipelineExecutor (auth_initial_* x 2 functions x 3 outcomes = 6; auth_refresh_* = 4; pipeline_truncated; pagination_cursor_unsupported_type; fanout_invalid_source_type; fanout_ambiguous_multi_array). Documents field-schema differences between execute() (no step_name on auth_initial_*) and execute_step() (includes step_name). Closes BC↔impl catalog drift surfaced by pass-11 (F-LP11-MED-001 + F-LP11-MED-002 + PG-LP11-001) and codifies the SOP that new event_type sites must be enumerated in BC. |
| 1.7 | S-PLUGIN-PREREQ-B-fix-burst-9 | 2026-05-11 | product-owner | Amend audit-signal postcondition row to enumerate THREE tracing events (was "one of two"): auth_initial_acquired (info, non-empty token), auth_initial_acquired_empty (debug, empty token), auth_initial_failed (error). Closes BC↔impl drift surfaced by pass-9 (F-LP9-MED-001). The third branch was added by fix-burst-7 (closing F-LP7-MED-001) and tested by fix-burst-8 (closing F-LP8-MED-001) but the BC text was never updated. |
| 1.6 | LOCAL-pass-7-fix | 2026-05-11 | product-owner | Clarify partial-record discard policy on mid-pipeline HTTP failure. Existing § Error Conditions row replaced with explicit "ALL accumulated records discarded" + new postcondition explaining all-or-nothing rationale. Closes F-LP7-MED-003 from LOCAL pass-7 adversary review at 8e9a92d0 (BC text ambiguity surfaced by partial-record test coverage gap). |
| 1.5 | LOCAL-pass-5-fix | 2026-05-11 | product-owner | Eager-token precondition lifecycle. Replace lazy-token-on-401 with eager-acquire-at-pipeline-start for non-Null AuthType. Closes F-LP5-LOW-003 from LOCAL pass-5 adversary review at d5a12e4a: prior lazy design polluted audit signal (auth_refresh_triggered fired on every legitimate execution) and doubled API quota per execution. Two new audit-log events (auth_initial_acquired/auth_initial_failed) augment the existing auth_refresh_* event family. request_count semantics now exclude AuthProvider transport. Status remains draft pending PREREQ-B merge — POL-14 promotes draft→active on merge. |
| 1.4 | LOCAL-pass-1-fix | 2026-05-11 | product-owner | Amend preconditions and postconditions to reflect AuthProvider abstraction introduced by S-PLUGIN-PREREQ-B. Lazy credential resolution replaces eager. New postconditions: AuthProvider trait dyn-safety; PipelineResult.truncated semantics; auth_refresh_triggered tracing event for VP-PLUGIN-005. Closes F-LP1-MED-001 from LOCAL pass-1 adversary review at b1b529fc. Status remains draft pending PREREQ-B merge — POL-14 promotes draft→active on merge. |
| 1.3 | pass-74-fix | 2026-04-20 | product-owner | Resolved (placeholder) row in ## Verification Properties per pass-74 VP-TBD decision matrix extension. |
| 1.2 | pass-73-fix | 2026-04-20 | state-manager | Deterministic changelog reorder: sorted all rows to descending version order (pass-73 bash script). |
| 1.1 | pre-build-sweep | 2026-04-20 | product-owner | Template-compliance sweep: added extracted_from/inputs/input-hash/traces_to frontmatter; added ## Description; added ## Invariants; added ## Error Conditions (from inline Error Handling); converted ## Traces → ## Traceability table; added ## Canonical Test Vectors; added ## Verification Properties; added ## Changelog. |
| 1.0 | cycle-1 | 2026-04-13 | product-owner | Initial draft (used ## Traces section) |
