# Ephemeral observability + dispatcher logs — do not commit
logs/events-*.jsonl
logs/capture-*.jsonl
logs/dispatcher-*.jsonl

# Sed backup leftovers — state-manager never commits these
*.bak
*.stage2bak

# Cargo/test runtime artifacts — not factory artifacts
regression-state.json

# Per-PR ephemeral delivery scratch (pr-description.md, pr-review.md) — never committed
# OCSF-CLASS-MIGRATION-001 (PR #174) and earlier were never committed; ephemeral pattern confirmed D-1047
code-delivery/
