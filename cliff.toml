# cliff.toml — git-cliff 2.14.1 changelog configuration for Prism
#
# Authority: ADR-063 D3 (categorization convention), D1 (version pin), D5 (release-prep integration)
#
# Breaking Changes section ordering: DEVIATION from ADR-063 v1.6 §D3 sketch.
# Intended ADR mechanism: numbered-group-name-prefix idiom — { breaking = true } parser routes
# breaking commits to "<!-- 0 -->Breaking Changes"; single group_by loop renders all groups.
# Actual behavior (git-cliff 2.14.1): { breaking = true } in commit_parsers does NOT override
# message-based parsers for group assignment — a feat!: commit gets group "<!-- 1 -->Added"
# regardless of whether the breaking parser is listed first or last. Verified empirically.
# Token-free workaround: the body template uses Tera filter(attribute="breaking", value=true)
# to separate breaking commits into a manual "### Breaking Changes" section rendered BEFORE
# the group_by loop. Non-breaking commits are processed by
# commits | filter(attribute="breaking", value=false) | group_by(attribute="group"),
# satisfying F-1 (the grouping loop for Added/Fixed/etc. uses group_by + striptags | trim).
#
# PR link injection: token-free via commit_preprocessors (DEVIATION from ADR-063 v1.6 §D3 sketch).
# Prism uses squash-merge, so every merged PR subject already contains (#NNN).
# The commit_preprocessors regex rewrites (#NNN) into a markdown link in commit.message before
# Tera rendering — no GITHUB_TOKEN or remote API call is needed.
# ADR-063 v1.6 §D3 sketch used commit.remote.pr_number / commit.remote.pr_url (populated via
# GITHUB_TOKEN + remote API), but those fields are EMPTY in a token-free dry-run, so the body
# template does NOT reference commit.remote.* fields. The [remote.github] block is retained for
# possible future remote-API use but the body template ignores it.
# BREAKING CHANGE footer values are surfaced inline via commit.breaking_description (ADR-063 v1.6 D3).
# No per-entry author @-handle attribution — Prism is a single-author project; uniform handles
# on every entry are noise, not signal (ADR-063 v1.6 D3).

[changelog]
header = ""
body = """
{% if version %}## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}## [Unreleased]
{% endif %}
{% set breaking_commits = commits | filter(attribute="breaking", value=true) %}
{% if breaking_commits | length > 0 %}
### Breaking Changes
{% for commit in breaking_commits %}
- **BREAKING** {{ commit.message }}{% if commit.breaking_description %} — {{ commit.breaking_description }}{% endif %}
{% endfor %}
{% endif %}
{% for group, group_commits in commits | filter(attribute="breaking", value=false) | group_by(attribute="group") %}
### {{ group | striptags | trim }}
{% for commit in group_commits %}
- {{ commit.message }}
{% endfor %}
{% endfor %}
"""
footer = ""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
commit_preprocessors = [
  # Rewrite "(#NNN)" in squash-merge commit subjects to markdown PR links.
  # Prism uses squash-merge; every merged PR subject already contains (#NNN).
  # This is the token-free PR-link mechanism — no GITHUB_TOKEN required.
  { pattern = "\\(#([0-9]+)\\)", replace = "([#${1}](https://github.com/drbothen/prism/pull/${1}))" },
]
commit_parsers = [
  { message = "^feat", group = "<!-- 1 -->Added" },
  { message = "^fix", group = "<!-- 2 -->Fixed" },
  { message = "^perf", group = "<!-- 3 -->Performance" },
  { message = "^refactor", group = "<!-- 4 -->Changed" },
  { message = "^security", group = "<!-- 5 -->Security" },
  { message = "^docs", skip = true },
  { message = "^ci", skip = true },
  { message = "^test", skip = true },
  { message = "^chore", skip = true },
  { message = "^style", skip = true },
  { message = "^build", skip = true },
  { message = "^revert", skip = true },
]
protect_breaking_commits = true
filter_commits = false
tag_pattern = "v[0-9].*"
skip_tags = ""
ignore_tags = ""
topo_order = false
sort_commits = "oldest"

[remote.github]
owner = "drbothen"
repo = "prism"
