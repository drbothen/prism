---
story_id: S-1.12
title: "prism-spec-engine: Hot Reload and Runtime Management"
wave: 1
level: "L4"
target_module: prism-spec-engine
subsystems: [SS-16]
priority: P1
depends_on: [S-1.11]
blocks: [S-3.13, S-5.07]
estimated_days: 2
points: 3
status: draft
document_type: story
version: "1.5"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "ac6b633"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-1"
phase: 2
behavioral_contracts: [BC-2.16.005, BC-2.16.006, BC-2.16.007, BC-2.16.008, BC-2.16.010]
verification_properties: [VP-032]
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.16.005, BC-2.16.006, BC-2.16.007, BC-2.16.008, BC-2.16.010]
anchor_capabilities: [CAP-029, CAP-030]
anchor_subsystem: ["SS-16"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.16.005-reload-config-tool.md
  - .factory/specs/behavioral-contracts/BC-2.16.006-arc-swap-config-access.md
  - .factory/specs/behavioral-contracts/BC-2.16.007-sensor-spec-hot-reload.md
  - .factory/specs/behavioral-contracts/BC-2.16.008-add-sensor-spec-tool.md
  - .factory/specs/behavioral-contracts/BC-2.16.010-list-sensor-specs-tool.md
  - .factory/specs/verification-properties/vp-032-hot-reload-atomicity.md
---

# S-1.12 — prism-spec-engine: Hot Reload and Runtime Management

## Narrative

As a Prism platform implementer, I want hot reload for sensor specs and
configuration in prism-spec-engine, so that operators can add, update, or remove
sensor specs at runtime without restarting the server — with arc-swap ensuring
lock-free config reads on the query hot path and in-flight queries completing
against the old config snapshot uninterrupted.

## Objective

Implement hot reload for sensor specs and config, plus runtime management tools.

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.16.005 | `reload_config` MCP Tool — Re-Read All Config Files, Validate, Atomic Swap, Notify |
| BC-2.16.006 | Arc-Swap Config Access on Hot Path — Lock-Free Reads for Query-Time Config Access |
| BC-2.16.007 | Sensor Spec Hot Reload — Add/Remove/Update Sensor Tables Without Restart |
| BC-2.16.008 | `add_sensor_spec` MCP Tool — Upload a New Sensor Spec at Runtime |
| BC-2.16.010 | `list_sensor_specs` MCP Tool — List Loaded Sensor Specs with Table Schemas and Status |

## Tasks

1. Implement `reload_config` logic: re-read all TOML files, validate, create new `ConfigSnapshot`, atomic swap via `arc-swap`, in-flight queries unaffected (BC-2.16.005)
2. Implement `ArcSwap`-based config access: `ConfigManager` wraps `ArcSwap<ConfigSnapshot>`, lock-free reads on query hot path (BC-2.16.006)
3. Implement sensor spec hot reload: detect add/remove/update of `.sensor.toml` files; add/remove SensorTableDescriptor entries and notify prism-query to update DataFusion tables without restart (BC-2.16.007)
4. Implement `add_sensor_spec` logic: accept TOML content, validate, write to spec directory, trigger reload (BC-2.16.008) — on write/cleanup failure return `E-SPEC-002` with path
5. Implement `list_sensor_specs` logic: return all loaded specs with table schemas, source count, and validation status (BC-2.16.010)
6. Write VP-032 proptest: if validation fails during reload, old config is retained (failed validation → no change)

## Acceptance Criteria

- **AC-1:** Given a modified `prism.toml`, When `reload_config` is called, Then the new config takes effect for subsequent queries while in-flight queries use the old config (BC-2.16.005)
- **AC-2:** Given concurrent query execution, When config is read, Then no lock contention occurs (arc-swap lock-free) (BC-2.16.006)
- **AC-3:** Given a new `.sensor.toml` dropped into the spec directory, When hot reload detects it, Then new tables appear in the query namespace (BC-2.16.007)
- **AC-4:** Given a valid TOML spec uploaded via `add_sensor_spec`, When it is processed, Then the spec is written and tables are registered (BC-2.16.008)
- **AC-5:** Given loaded specs, When `list_sensor_specs` is called, Then each spec's tables, source count, and status are returned (BC-2.16.010)
- **AC-6:** VP-032 proptest passes (failed validation retains old config)

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Hot-reload / filesystem watcher | `prism-spec-engine` (SS-16) | Mixed |
| arc-swap config publication | `prism-spec-engine` (SS-16) | Mixed |

`prism-spec-engine` implements hot-reload via the `notify` crate (AD-018): filesystem
events trigger debounced re-validation and atomic arc-swap publication of the new
`ConfigSnapshot`. Validation logic is pure; filesystem watching and arc-swap writes are
effectful. Per `architecture/module-decomposition.md`, all consumers of `ConfigManager`
(prism-sensors, prism-query, prism-operations, prism-mcp) read the new snapshot lock-free.

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `reload_config` called when new config fails validation | Old config retained unchanged — CI-002 invariant (BC-2.16.005; VP-032) |
| EC-002 | `add_sensor_spec` called with invalid TOML | Validation fails before write; no file written; `E-SPEC-002` returned if filesystem write fails (BC-2.16.008) |
| EC-003 | Hot reload detects `.sensor.toml` removal while a query is executing against that table | In-flight query completes against old registration; new queries see table absent (BC-2.16.007) |
| EC-004 | Concurrent `reload_config` calls (e.g., two MCP clients simultaneously) | Arc-swap ensures atomicity; one wins, other reads consistent snapshot |
| EC-005 | `list_sensor_specs` called when one spec is in `failed_validation` state | Returns all specs with per-spec `status` field including `failed_validation` (BC-2.16.010) |

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-spec-engine` (hot-reload) | Mixed | Pure validation logic; filesystem watcher and arc-swap write are effectful. Mixed per `architecture/purity-boundary-map.md`. |

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,800 |
| prism-spec-engine/src/config_manager.rs (ArcSwap<ConfigSnapshot>, lock-free reads) | ~900 |
| prism-spec-engine/src/hot_reload.rs (notify watcher, debounce, arc-swap publication) | ~1,200 |
| prism-spec-engine/src/reload_config.rs (reload_config MCP tool logic) | ~600 |
| prism-spec-engine/src/add_sensor_spec.rs (add_sensor_spec logic, E-SPEC-002) | ~600 |
| prism-spec-engine/src/list_sensor_specs.rs (list_sensor_specs logic) | ~400 |
| BC files (5 BCs) | ~2,500 |
| Proptest VP-032 | ~500 |
| Unit tests | ~800 |
| Total | ~9,300 |

Within the 30% context window budget (~40k tokens for a 128k-context agent).

## Previous Story Intelligence

S-1.11 (Spec Loading and Pipeline Execution) established `SpecParser`, `SensorSpec`,
`SensorTableDescriptor`, and the validation infrastructure. This story adds the
runtime management layer (hot reload, arc-swap, MCP management tools) on top.

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|---------------|----------------------|--------------------|
| S-1.11 | `SensorSpec` struct is the parsed representation of a `.sensor.toml` file | Validation returns structured errors with file line numbers | DataFusion table name conflicts must be detected at load time — not deferred to query time |
| S-1.11 | `SensorTableDescriptor` is the export struct for downstream prism-query consumption | OCSF field path validation is pure — no I/O | `ArcSwap<ConfigSnapshot>` — NOT `RwLock` — is the required config access pattern; in-flight queries hold `Arc` guards for the duration, freeing old snapshots when guards drop |

## Architecture Compliance Rules

| Rule | Source | Enforcement |
|------|--------|-------------|
| Config MUST be accessed via `ArcSwap<ConfigSnapshot>` — NOT `RwLock` | AD-018 (hot-reload atomicity); architecture/module-decomposition.md SS-16 | Code review: no `RwLock` on config in hot path; `ConfigManager` wraps `ArcSwap` |
| Failed validation during reload MUST retain the old config unchanged | BC-2.16.005 postcondition; VP-032 | Proptest VP-032 formally verifies old config retention on validation failure |
| `add_sensor_spec` MUST validate TOML before writing to disk | BC-2.16.008 precondition | Tests verify no file is written if validation fails |
| In-flight queries MUST complete against the old config snapshot | AD-018 invariant | Arc guard pattern: queries hold `Arc` for duration; old snapshot freed only when all guards drop |
| prism-spec-engine MUST NOT depend on DataFusion or Arrow (inherited from S-1.11) | architecture/module-decomposition.md SS-16 | Build-time enforcement via CI |

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| arc-swap | 1.x | ArcSwap<ConfigSnapshot> for lock-free config reads on query hot path |
| notify | 7.x | Cross-platform filesystem event watcher for .sensor.toml changes (AD-018) |
| toml | 0.8.x | TOML re-parsing on hot reload |
| serde | 1.x | ConfigSnapshot deserialization |
| proptest | 1.x | VP-032 proptest: failed validation retains old config unchanged |
| prism-core | workspace | PrismError, E-SPEC-002 (file write failure) |
| prism-spec-engine | workspace (self, from S-1.11) | SpecParser, SensorTableDescriptor, validation infrastructure |

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-spec-engine/src/config_manager.rs` | Create | ConfigManager wrapping ArcSwap<ConfigSnapshot>; lock-free load() on hot path |
| `crates/prism-spec-engine/src/hot_reload.rs` | Create | notify-based filesystem watcher with debounce; triggers re-validation and arc-swap |
| `crates/prism-spec-engine/src/reload_config.rs` | Create | reload_config MCP tool logic: re-read, validate, atomic swap, notify downstream |
| `crates/prism-spec-engine/src/add_sensor_spec.rs` | Create | add_sensor_spec logic: validate then write to spec dir, trigger reload; E-SPEC-002 on write failure |
| `crates/prism-spec-engine/src/list_sensor_specs.rs` | Create | list_sensor_specs logic: return all loaded specs with tables, source count, status |
| `crates/prism-spec-engine/src/lib.rs` | Modify | Export config_manager, hot_reload, reload_config, add_sensor_spec, list_sensor_specs |
| `crates/prism-spec-engine/tests/hot_reload_tests.rs` | Create | VP-032 proptest and integration tests for reload atomicity and failure retention |

## Verification Properties

| VP ID | Description |
|-------|-------------|
| VP-032 | Proptest: failed validation during reload retains old config unchanged |

## Dev Notes

- `ArcSwap<ConfigSnapshot>` provides epoch-based memory reclamation. In-flight queries hold an `Arc` guard for the duration of execution; the old snapshot is not freed until all guards are dropped. This is the correct model — do not use a `RwLock`.
- Hot reload file detection can use `notify` crate (cross-platform filesystem events) or poll on a configurable interval. The mechanism must be configurable; the default is filesystem events.
- `add_sensor_spec` must validate the TOML before writing to disk. If validation fails, no file is written and the error is returned immediately.
- `E-SPEC-002` must include the filesystem path where the write failed and the underlying OS error, to support operator debugging without requiring log access.
- `list_sensor_specs` response must include a `status` field per spec: `loaded`, `failed_validation`, `pending_reload`. This gives operators visibility into partial failure states.
- DataFusion table unregistration on spec removal must handle the case where queries are currently executing against the removed table — those queries should complete against the old registration; new queries should see the table as absent.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.5 | pass-92 | 2026-04-21 | story-writer | F92-006: anchor_capabilities [CAP-029] → [CAP-029, CAP-030] (4 of 5 BCs anchor CAP-030 Hot Configuration Reload; BC-2.16.008 now dual-anchor CAP-029+CAP-030 post-F92-001). |
| 1.4 | pass-64-fix | 2026-04-20 | story-writer | Filled [TODO: template scaffolding in Narrative/Token Budget/Previous Story Intelligence/Architecture Compliance Rules/Library & Framework Requirements/File Structure Requirements sections. Pre-build-sweep wave 2 over-claimed; this completes the remediation. |
| 1.3 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
