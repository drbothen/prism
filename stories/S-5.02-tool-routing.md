---
story_id: S-5.02
title: "prism-mcp: Tool Routing, Errors, and Client Scoping"
wave: 5
target_module: prism-mcp
crates_touched: [prism-mcp, prism-query, prism-security, prism-spec-engine]
# crates_touched note: prism-query/prism-security/prism-spec-engine are accessor-only "wiring not redesign" additions (ADR-022 §C) needed for tri-state list_capabilities enumeration (R4). prism-mcp is the primary implementation crate.
subsystems: [SS-10]
priority: P0
depends_on: [S-5.01-FOLLOWUP-MCP-BOOT]
depends_on_note: "Dependency pointer updated D-1163 2026-06-14: S-5.01 formal story row shows draft/not-started but scope was delivered by S-5.01-FOLLOWUP-MCP-BOOT (PR #163 develop@e898c3c9 2026-05-29). depends_on corrected to the actual graduation vehicle. Scope unchanged."
blocks: [S-5.03, S-5.08]
estimated_days: 3
points: 5
level: "L4"
status: merged
merged_pr: 191
merged_sha: bec894a2
merged_at: "2026-06-17"
document_type: story
version: "1.14"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "76729b7"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-5"
phase: 2
tdd_mode: strict
behavioral_contracts: [BC-2.10.004, BC-2.10.007, BC-2.10.011]
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.10.004, BC-2.10.007, BC-2.10.011]
anchor_capabilities: [CAP-005, CAP-009, CAP-034]
anchor_subsystem: ["SS-10"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.10.004-client-id-parameter-requirement.md
  - .factory/specs/behavioral-contracts/BC-2.10.007-structured-error-responses.md
  - .factory/specs/behavioral-contracts/BC-2.10.011-list-capabilities-meta-tool.md
---

# S-5.02 — prism-mcp: Tool Routing, Errors, and Client Scoping

## Narrative

As an MSSP analyst using Prism, I want every tool call to be scoped to a specific client
(or set of clients) and to receive structured, actionable error responses when something
goes wrong, so that I can reason about failures and route queries correctly in a
multi-tenant environment.

## Objective

Fix and extend the already-merged `validate_client_ids` free function, upgrade
`error_mapping.rs` to produce the ratified nested BC-2.10.007 structured content shape,
and replace the merged `list_capabilities` simple bool map with the ratified tri-state +
resolution-chain model (BC-2.10.011). This story bridges the gap between what shipped in
S-5.01-FOLLOWUP-MCP-BOOT and the locked PO contracts.

---

## Behavioral Contracts

| BC ID | Version | Title |
|-------|---------|-------|
| BC-2.10.004 | v2.8 | Client Scoping on Every Tool (Stateless Model) |
| BC-2.10.007 | v1.8 | Structured Error Responses |
| BC-2.10.011 | v1.6 | list_capabilities Meta-Tool |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,400 |
| `crates/prism-mcp/src/server.rs` (delta: validate_client_ids + list_capabilities) | ~3,500 |
| `crates/prism-mcp/src/error_mapping.rs` (delta: map_prism_error SensorRateLimited arm + to_error_data nested shape) | ~2,000 |
| BC files (3 BCs at v2.8/v1.8/v1.6) | ~2,500 |
| Test file `crates/prism-mcp/tests/tool_dispatch_tests.rs` (new BC-2.10.004/007/011 test block) | ~2,000 |
| Total | ~12,400 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

### R1 — Fix `validate_client_ids` message prefix (BC-2.10.004 v2.8)

**File:** `crates/prism-mcp/src/server.rs` (lines 1215–1231 — delta, do not rewrite the function)

The merged `validate_client_ids` emits an `rmcp::ErrorData` with message
`"Invalid client_id '{id}': must match [a-zA-Z0-9_-]{1,64} (BC-2.10.004)"` — it is
missing the `E-MCP-001: ` prefix required by BC-2.10.004 v2.8 Implementer Note §1.
Change the message format to:

```
"E-MCP-001: invalid client_id format: '{id}'"
```

Additionally, the `ErrorData` returned by `validate_client_ids` must be upgraded from the
flat `rmcp::model::ErrorData::new(...)` to produce the nested `structuredContent.error`
shape required by BC-2.10.007. Because `validate_client_ids` currently returns raw
`rmcp::model::ErrorData`, the MCP error response it produces has no `structuredContent`
key — only a `code` and `message`. To emit the full nested shape, `validate_client_ids`
must construct a `CallToolResult` with `isError: true` and the BC-2.10.007
`structuredContent.error` object (9 fields + `_meta.trust_level`). See R3 for the
shared error response builder.

Three cases per BC-2.10.004 v2.8 Error Cases table — all three MUST flow through
the structured shape:
- **(a) Missing/empty:** `client_id` absent or `""` → `E-MCP-001`, `original_params_valid: false`
- **(b) Malformed/invalid characters:** fails `[a-zA-Z0-9_-]{1,64}` → `E-MCP-001`, `original_params_valid: false`
- **(c) Well-formed but not registered:** passes format; unknown at runtime → `E-CFG-100` via `PrismError::ClientNotFound` → `error_mapping.rs` → existing `-32602` arm, `original_params_valid: true`

**CRITICAL — do NOT route (a)/(b) through `PrismError::InvalidClientId`.**
`PrismError::InvalidClientId` displays as `"E-AUTH-003: ..."` — a namespace collision
with the sensor-layer bearer-token rejection code. The `validate_client_ids` path must
stay on the `E-MCP-001` path, constructing the message string directly.

### R2 — Add `SensorRateLimited` explicit arm in `map_prism_error` (BC-2.10.007 v1.5)

**File:** `crates/prism-mcp/src/error_mapping.rs` (delta to the `PrismError::SensorRateLimited` match arm)

The existing arm in `map_prism_error` collapses `SensorRateLimited` into the generic
`SensorHttpError | SensorTimeout | SensorResponseParse | SensorRateLimited` group with
`(-32000, "Internal error; see audit log")`. This loses the `retry_after_ms` field.

Replace the multi-arm sensor group by splitting `SensorRateLimited` out:

```rust
// E-SENSOR-004: Sensor rate limited → -32000 Internal, retry_after_ms threaded out
PrismError::SensorRateLimited { retry_after_ms, .. } => (
    codes::INTERNAL_ERROR,
    format!("Internal error; see audit log"),  // redacted per AD-017
    // NOTE: retry_after_ms threaded to structured error builder; see to_error_data_with_retry
),
```

Because `map_prism_error` returns `(i32, String)`, the `retry_after_ms` value cannot be
carried through to `to_error_data` without changing the signature. The implementer MUST
add a companion function:

```rust
/// Like `to_error_data` but threads `retry_after_ms` through to the structured
/// BC-2.10.007 envelope when the error is `SensorRateLimited`.
/// Returns `(ErrorData, u64)` — `retry_after_ms` is always present on this variant.
pub fn to_error_data_with_retry(err: PrismError) -> (ErrorData, u64)
```

This function extracts `retry_after_ms: u64` from `SensorRateLimited` (the field is
required, never `Option`) before delegating to `map_prism_error`, and returns the
millisecond value alongside the `ErrorData` so the caller (the structured error
response builder) can convert it to seconds (`retry_after_ms / 1000`) for
`retry_after_seconds`.

**Note:** `retry_after_seconds: null` appears in the JSON wire shape only for
NON-`SensorRateLimited` error types (where the field is genuinely absent). For any
`SensorRateLimited` error, `retry_after_seconds` is always a non-null `u64` value.
The `null`-not-absent invariant (AC-006) is exercised by non-rate-limit error paths,
not by this variant.

### R3 — Upgrade error response shape to nested BC-2.10.007 envelope

**File:** `crates/prism-mcp/src/error_mapping.rs` (new function `build_structured_error_response`)

The BC-2.10.007 v1.5 wire shape is NESTED — not the flat 7-field object referenced in
the v1.5 story spec. The old story spec framing is superseded. The ratified shape is:

```json
{
  "isError": true,
  "content": [{"type": "text", "text": "ERROR: [{category}] - {message}. {suggestion}"}],
  "structuredContent": {
    "error": {
      "code":                  "E-MCP-001",
      "message":               "...",
      "category":              "validation",
      "retryable":             false,
      "retry_after_seconds":   null,
      "suggestion":            "...",
      "source":                "prism_mcp",
      "original_params_valid": false,
      "upstream_message":      null
    },
    "_meta": {"trust_level": "internal"}
  }
}
```

Add `build_structured_error_response(...)` to `error_mapping.rs` that constructs this
shape given the 9 `structuredContent.error` fields + the `content[].text` string.
All callers of `to_error_data` that produce user-visible MCP tool error responses MUST
route through this builder so the `_meta.trust_level: "internal"` field is always present.

`retry_after_seconds` MUST be `null` in JSON (not absent) for non-rate-limit error
variants where no retry hint exists. Type: `u64 | null`. For `SensorRateLimited`, the
value is always a non-null `u64`.

`upstream_message` MUST be `null` when the error originates in Prism (not a sensor).
For `SensorRateLimited`, the sensor error detail goes into `upstream_message` only —
never into `message` or `content[].text` per DI-006.

### R4 — Replace `list_capabilities` bool map with tri-state + resolution-chain (BC-2.10.011 v1.5)

**File:** `crates/prism-mcp/src/server.rs` (lines 3108–3197 — substantial delta to the `list_capabilities` handler body)

The merged handler produces `{client_id, client_registered, capabilities: Map<String, bool>, not_implemented, note}`. BC-2.10.011 v1.5 supersedes this with the tri-state + hierarchical capability-path model.

Required changes (per BC-2.10.011 v1.5 Implementer Gap §1–6):

1. **Tri-state per-capability:** Replace `capabilities: Map<String, serde_json::Value::Bool(live)>` with `capabilities: Map<String, CapabilityEntry>` where `CapabilityEntry` is a new struct with:
   - `status: CapabilityStatus` (enum: `"enabled"` | `"runtime_disabled"` | `"compile_time_disabled"`)
   - `resolution_chain: Vec<ResolutionStep>` (each step: `level: "compile_tier"|"runtime_tier"`, `result: "permit"|"allow"|"deny"`, `source: String`)

2. **Resolution logic:** For each capability path, call the SAME capability resolver used by the write pipeline (BC-2.04.004). If the capability resolver is not yet accessible from `PrismServer`, expose it from `WriteExecutor` or `FeatureFlagEvaluator`. A separate implementation is forbidden.

3. **Rename `not_implemented` → `not_registered_tools`:** Same constant (`NOT_YET_AVAILABLE_TOOLS`), renamed field.

4. **Cross-client summary mode** (`client_id: null`): Return per-client summary shape instead of the `"<all>"` placeholder string:
   ```json
   {"client_id": null, "clients": {"acme": {"client_registered": true, "enabled_count": 3, "runtime_disabled_count": 1, "compile_time_disabled_count": 2}}, "not_registered_tools": [...]}
   ```

5. **Remove `note` field:** The response is now self-documenting via `resolution_chain`.

6. **`client_registered`:** Keep — already correctly driven by `FeatureFlagEvaluator::client_exists()`.

**`ListCapabilitiesParams.client_id`** is already `Option<String>` in the merged code. No change needed to the params struct.

---

## Acceptance Criteria

**AC-001:** Given a `validate_client_ids` call with an empty string `""`, When the function
is invoked, Then it returns an error whose message starts with `"E-MCP-001: invalid client_id format:"`.
(traces to BC-2.10.004 v2.8 postcondition case (a))

**AC-002:** Given a `validate_client_ids` call with a client_id containing `/` or `..`
(e.g., `"acme/../../etc"`), When invoked, Then it returns `E-MCP-001` with
`original_params_valid: false` in the structured error envelope.
(traces to BC-2.10.004 v2.8 postcondition case (b))

**AC-003:** Given a tool call where `client_id` passes format validation but is not present
in the runtime capability registry, When `PrismError::ClientNotFound` is mapped via
`error_mapping.rs`, Then the response carries code `E-CFG-100`, `category: "configuration"`,
`retryable: false`, `original_params_valid: true`.
(traces to BC-2.10.004 v2.8 postcondition case (c))

**AC-004:** Given any MCP tool error response generated by the structured error builder,
When the response JSON is inspected, Then `structuredContent.error` contains exactly these
9 fields — `code`, `message`, `category`, `retryable`, `retry_after_seconds`, `suggestion`,
`source`, `original_params_valid`, `upstream_message` — and `structuredContent._meta.trust_level`
equals `"internal"`.
(traces to BC-2.10.007 v1.5 postcondition — wire shape)

**AC-005:** Given a `PrismError::SensorRateLimited { sensor: "crowdstrike".to_string(), retry_after_ms: 30_000 }`, When
mapped through `to_error_data_with_retry`, Then the structured error response has
`retryable: true` and `retry_after_seconds: 30` (= `retry_after_ms / 1000`).
(traces to BC-2.10.007 v1.8 postcondition — 429 wiring)

**AC-006:** Given a non-rate-limit error (e.g., `PrismError::SensorHttpError`) mapped
through `build_structured_error_response`, When the structured JSON is inspected, Then
`structuredContent.error.retry_after_seconds` is present in the JSON as `null` (not
absent from the object). Note: `SensorRateLimited` always carries a required `u64`
`retry_after_ms` — the `null` case only arises on non-rate-limit error variants where
no retry hint exists.
(traces to BC-2.10.007 v1.8 postcondition — null-not-absent invariant)

**AC-007:** Given a sensor error whose raw message contains a prompt-injection payload,
When the structured error response is constructed, Then the payload appears only in
`upstream_message` and is absent from `message` and `content[].text`.
(traces to BC-2.10.007 v1.5 invariant DI-006)

**AC-008:** Given `list_capabilities(client_id: "acme")` where a capability path has a
`[[write_endpoints]]` entry (compile tier permits) and the runtime prism.toml grants it,
When called, Then `capabilities["sensor.crowdstrike.containment"].status` is `"enabled"`
and `resolution_chain` has two steps (`compile_tier` → `permit` and `runtime_tier` → `allow`).
(traces to BC-2.10.011 v1.5 postcondition — single-client mode)

**AC-009:** Given `list_capabilities(client_id: "acme")` where a capability has no
`[[write_endpoints]]` entry in the sensor TOML (compile tier denies), When called, Then
`status` is `"compile_time_disabled"` and `resolution_chain` has one step
(`compile_tier` → `deny`).
(traces to BC-2.10.011 v1.5 postcondition — compile_time_disabled status)

**AC-010:** Given `list_capabilities(client_id: null)` (cross-client summary), When called,
Then the response shape is `{client_id: null, clients: {<id>: {client_registered, enabled_count, runtime_disabled_count, compile_time_disabled_count}}, not_registered_tools: [...]}`.
(traces to BC-2.10.011 v1.5 postcondition — cross-client summary mode)

**AC-011:** Given a `list_capabilities` response, When inspected, Then the field is named
`not_registered_tools` (not `not_implemented`), and it contains exactly the
`NOT_YET_AVAILABLE_TOOLS` constant's entries.
(traces to BC-2.10.011 v1.5 postcondition — field rename)

**AC-012 (HIGH-1 — auth token errors):** Given a tool call that fails due to
`PrismError::AuthTokenExpired` or `PrismError::AuthTokenInvalid`, When the structured
error response is constructed via `build_structured_error_response`, Then:
- `AuthTokenExpired` maps to code `E-AUTH-010`, `category: "authentication"`, `original_params_valid: true`
- `AuthTokenInvalid` maps to code `E-AUTH-011`, `category: "authentication"`, `original_params_valid: true`
(rationale: the token is the credential, not the query parameter — the original tool params
are structurally valid; the session credential is what expired/was revoked)
(traces to BC-2.10.007 v1.8 §Category decision rule — authentication category)

**AC-013 (HIGH-1 — identity-format errors):** Given a tool call that fails due to
`PrismError::InvalidOrgSlug`, `PrismError::InvalidAnalystId`, or `PrismError::InvalidClientId`
(identity-format validation failures), When the structured error response is constructed, Then:
- `InvalidOrgSlug` maps to code `E-AUTH-001`, `category: "authentication"`, `original_params_valid: false`
- `InvalidAnalystId` maps to code `E-AUTH-002`, `category: "authentication"`, `original_params_valid: false`
- `InvalidClientId` maps to code `E-AUTH-003`, `category: "authentication"`, `original_params_valid: false`
(rationale: identity-format failures mean the caller supplied a structurally invalid identity
token; the params are malformed, not simply rejected at runtime)
(traces to BC-2.10.007 v1.8 §Category decision rule — authentication category, identity-format subcategory)

**AC-014 (OBS-1 — cross-org sensor scoping denial):** Given a tool call that fails due to
`PrismError::SensorNotRegisteredForOrg` (E-QUERY-032), When the structured error response
is constructed, Then `category: "permission"` and `original_params_valid: true`.
(rationale: the params are structurally valid; the sensor exists but is not in scope for
this org — a cross-org isolation denial, not a malformed request)
(traces to BC-2.10.007 v1.8 §Category decision rule — permission category)

**AC-015 (OBS-2 — watchdog infrastructure failures):** Given a tool call that fails due
to a watchdog error (`PrismError::WatchdogKilled`, `PrismError::HeartbeatMissed`, or
`PrismError::RestartLimitExceeded`), When the structured error response is constructed,
Then `category: "internal"` and `original_params_valid: true`.
(rationale: watchdog failures are Prism infrastructure events; the original params were
valid — the subsystem failed independently of the request content)
(traces to BC-2.10.007 v1.8 §Category decision rule — internal category, watchdog subcategory)

---

## Red Gate Tests (BC-named, `tests/tool_dispatch_tests.rs`)

The following test names MUST be written as failing `todo!()` stubs BEFORE any
implementation is committed (TDD Red Gate per BC-8.30.001 strict mode):

```
// BC-2.10.004 — validate_client_ids message prefix
test_BC_2_10_004_empty_client_id_returns_e_mcp_001_prefix
test_BC_2_10_004_malformed_client_id_returns_e_mcp_001_prefix
test_BC_2_10_004_path_traversal_client_id_returns_e_mcp_001
test_BC_2_10_004_well_formed_unknown_client_id_maps_to_e_cfg_100

// BC-2.10.007 — structured error shape
test_BC_2_10_007_structured_error_has_nine_fields_and_meta_trust_level
test_BC_2_10_007_sensor_rate_limited_retry_after_seconds_ms_to_s_conversion
test_BC_2_10_007_no_retry_after_produces_null_not_absent
test_BC_2_10_007_upstream_message_isolation_from_prose_content

// BC-2.10.007 v1.8 — auth + permission + watchdog category mapping (HIGH-1/OBS-1/OBS-2)
test_HIGH_1_auth_token_expired_category_is_authentication
test_HIGH_1_auth_token_invalid_category_is_authentication
test_HIGH_1_invalid_org_slug_category_is_authentication
test_HIGH_1_invalid_analyst_id_category_is_authentication
test_HIGH_1_invalid_client_id_category_is_authentication
test_BC_2_10_007_sensor_not_registered_for_org_category_is_permission
test_BC_2_10_007_watchdog_killed_category_is_internal
test_BC_2_10_007_heartbeat_missed_maps_to_internal_category
test_BC_2_10_007_restart_limit_exceeded_maps_to_internal_category

// BC-2.10.011 — list_capabilities tri-state
test_BC_2_10_011_enabled_capability_has_two_resolution_steps
test_BC_2_10_011_compile_time_disabled_has_one_deny_step
test_BC_2_10_011_runtime_disabled_has_two_steps_deny_at_runtime_tier
test_BC_2_10_011_cross_client_null_returns_summary_shape
test_BC_2_10_011_not_registered_tools_field_not_not_implemented
```

---

## Architecture Mapping

| Component | Module | File | Pure/Effectful |
|-----------|--------|------|----------------|
| `validate_client_ids` free fn (format check) | `prism-mcp` (SS-10) | `src/server.rs` | Pure |
| `map_prism_error` + `to_error_data_with_retry` | `prism-mcp` (SS-10) | `src/error_mapping.rs` | Pure |
| `build_structured_error_response` builder | `prism-mcp` (SS-10) | `src/error_mapping.rs` | Pure |
| `list_capabilities` handler (tri-state) | `prism-mcp` (SS-10) | `src/server.rs` | Effectful (calls capability resolver) |
| Shared capability resolver | `prism-mcp` via `WriteExecutor` / `FeatureFlagEvaluator` | `prism-core` | Mixed |

Architecture section authority: `architecture/module-decomposition.md` (SS-10 boundary),
`architecture/dependency-graph.md` (prism-mcp → prism-core dependency).

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-mcp` error mapping | Pure | Deterministic variant → code mapping, no I/O. |
| `prism-mcp` `validate_client_ids` | Pure | Regex-equivalent string check, no I/O. |
| `prism-mcp` `list_capabilities` handler | Effectful | Calls `FeatureFlagEvaluator` and capability resolver; async MCP transport. |

---

## Verification Properties

No formal VPs for this story. Unit tests in `tool_dispatch_tests.rs` cover the
routing contract and structured error shape. Injection isolation (DI-006) is validated
by test `test_BC_2_10_007_upstream_message_isolation_from_prose_content`.

---

## Architecture Compliance Rules

1. **`validate_client_ids` must stay on the `E-MCP-001` path.** NEVER route cases (a)/(b)
   through `PrismError::InvalidClientId` — it displays as `E-AUTH-003`, a namespace
   collision with the sensor-layer bearer-token error.
2. **Nested structured error shape required (BC-2.10.007 v1.5).** The old flat 7-field
   story-spec framing is superseded. Every error response builder invocation must produce
   the 9-field `structuredContent.error` object + `_meta.trust_level: "internal"`.
3. **`retry_after_seconds` must be `null` (not absent) when not applicable.** Consistent
   client-side handling requires the field to always be present in the JSON.
4. **`list_capabilities` capability resolver must be the SAME instance used by the write
   pipeline (BC-2.04.004).** A separate implementation is a P1 finding under
   architecture compliance.
5. **`upstream_message` isolation.** Raw sensor error text must appear ONLY in
   `structuredContent.error.upstream_message`. It must never be interpolated into
   `message` or `content[].text` per DI-006.
6. **`#[non_exhaustive]` compliance.** Any new public struct (`CapabilityEntry`,
   `ResolutionStep`, `CapabilityStatus`) must carry `#[non_exhaustive]` per CLAUDE.md
   `#[non_exhaustive]` discipline.
7. **No `PrismError::InvalidClientId` in the MCP validate-path.** The symbol-keyed
   `new_unchecked_audit.rs` allowlist applies only to `OrgSlug::new_unchecked`.
8. **`"internal"` category for Prism-side infrastructure failures (BC-2.10.007 v1.8 F-4).**
   `PrismError::Internal`, `PrismError::Io`, and all `PrismError::Storage*` variants MUST
   map to `category: "internal"` — NOT `"upstream_error"`. `"upstream_error"` is reserved
   for genuine sensor/third-party boundary failures (`SensorHttpError`, `SensorTimeout`,
   `SensorResponseParse`, OCSF normalization variants). The `build_structured_error_response`
   builder (R3) must derive category from the variant match, not default every non-rate-limit
   path to `"upstream_error"`. Using `"upstream_error"` for Prism-internal I/O or storage
   failures is a semantic misclassification that misleads LLM-agent reasoning about fault domain.

### Forbidden Dependencies

`prism-mcp` must NOT gain a dependency on `prism-spec-engine` for capability resolution.
The capability resolver must be accessed via `prism-core`'s `FeatureFlagEvaluator` or
`WriteExecutor`. If that indirection requires an API addition to `prism-core`, add it
there — do not bypass the module boundary.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| rmcp | **1.7 (workspace)** | `CallToolResult`, `ErrorData`, `Parameters`, `#[tool_router]` |
| serde | 1.x (workspace) | Serialize `CapabilityEntry`, `ResolutionStep`, structured error |
| serde_json | 1.x (workspace) | JSON construction for nested error envelope |
| prism-core | workspace | `PrismError`, `FeatureFlagEvaluator`, capability resolver |

---

## File Structure Requirements

**This story touches existing merged files via DELTAS, not greenfield creates.
Do NOT create `middleware/client_id.rs`, `tools/meta.rs`, or `tests/routing.rs`.**

| File | Action | Delta Description |
|------|--------|-------------------|
| `crates/prism-mcp/src/server.rs` | Modify | (1) Fix `validate_client_ids` message to prefix `"E-MCP-001: "` and emit nested BC-2.10.007 error shape; (2) Replace `list_capabilities` handler body with tri-state + resolution-chain model; rename `not_implemented` → `not_registered_tools`; add cross-client summary shape for null `client_id` |
| `crates/prism-mcp/src/error_mapping.rs` | Modify | (1) Split `SensorRateLimited` out of the multi-arm sensor group into its own explicit arm; (2) Add `to_error_data_with_retry(err: PrismError) -> (ErrorData, u64)` companion function; (3) Add `build_structured_error_response(...)` that produces the 9-field + `_meta` nested shape |
| `crates/prism-mcp/tests/tool_dispatch_tests.rs` | Modify | Add new test block with the 13 `test_BC_2_10_004_*` / `test_BC_2_10_007_*` / `test_BC_2_10_011_*` Red Gate stubs (see Red Gate Tests section) |
| `crates/prism-query/src/write_dispatch.rs` | Modify | Add pub accessor for `NullAuditWriter` — accessor-only "wiring not redesign" (ADR-022 §C) needed for tri-state list_capabilities enumeration |
| `crates/prism-query/src/write_pipeline.rs` | Modify | Add `WriteExecutor::endpoint_registry()` pub accessor — accessor-only wiring for capability resolver exposure to prism-mcp |
| `crates/prism-security/src/feature_flag.rs` | Modify | Add `capability_paths_for_client` and `client_ids` pub accessors — accessor-only wiring for tri-state capability enumeration in list_capabilities |
| `crates/prism-spec-engine/src/write_endpoint.rs` | Modify | Add `all_capability_paths` pub accessor — accessor-only wiring exposing compile-tier capability paths for resolution-chain computation |

**No new files are needed.** The `ClientIdGuard` middleware abstraction was superseded by
the inline `validate_client_ids` free function in `server.rs` (present in merged code).
The `tools/meta.rs` for `list_capabilities` does not exist — the handler lives in
`server.rs` alongside all other tool handlers.

---

## Previous Story Intelligence

### S-5.01-FOLLOWUP-MCP-BOOT delivered (PR #163, develop@e898c3c9 2026-05-29)

- **`validate_client_ids` free fn already exists** in `server.rs` at lines 1215–1231.
  It validates `[a-zA-Z0-9_-]{1,64}` correctly but emits a raw `rmcp::model::ErrorData`
  without the `E-MCP-001: ` prefix and without the BC-2.10.007 nested structuredContent
  envelope. S-5.02's R1 task is a targeted fix to these two omissions — not a rewrite.

- **Param structs already carry `client_id`.** Every write-tool param struct includes
  `client_id: String`. Management tools that accept optional scoping use `client_id: Option<String>`.
  S-5.02 does NOT need to add `client_id` fields to existing structs.

- **Stubbed tools already return `-32003 not_yet_available`.** The `NOT_YET_AVAILABLE_TOOLS`
  constant and the `-32003` response are already wired. `list_capabilities` already reports
  this constant as `not_implemented`. R4 renames the field and expands the capability matrix.

- **`list_capabilities` handler already exists** at lines 3108–3197 with a bool map.
  R4 replaces the bool-map body with the tri-state + resolution-chain model. The params
  struct (`ListCapabilitiesParams.client_id: Option<String>`) is already correct.

- **`error_mapping.rs` `SensorRateLimited` arm** currently collapses into the generic
  sensor-error group. The implementer must split it out and add `to_error_data_with_retry`.

- **`ClientIdGuard` middleware abstraction was NOT built.** The middleware approach in the
  original story spec was superseded by the inline free-function pattern. Do not attempt to
  create a middleware module — it does not exist and is not needed.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `validate_client_ids` with `""` (empty string) | Returns `E-MCP-001` prefixed error; structured shape with `original_params_valid: false` |
| EC-002 | `validate_client_ids` with `"acme/../../etc"` | Returns `E-MCP-001` identifying the invalid entry; entire call rejected |
| EC-003 | `validate_client_ids` with a well-formed id that is not in the runtime registry | Handled downstream as `PrismError::ClientNotFound` → `E-CFG-100`, `original_params_valid: true` |
| EC-004 | `SensorRateLimited { retry_after_ms: 30_000 }` | Structured error has `retryable: true`, `retry_after_seconds: 30` |
| EC-005 | Non-rate-limit sensor error (e.g., `SensorHttpError`) — no retry hint | Structured error has `retry_after_seconds: null` (present but null, not absent). `SensorRateLimited` always provides a required `u64` ms value — the null case is exercised by non-rate-limit error variants only. |
| EC-006 | Sensor error message contains prompt injection payload | Payload in `upstream_message` only; absent from `message` and `content[].text` |
| EC-007 | `list_capabilities("acme")` where no write endpoints declared in TOML | All write capabilities show `compile_time_disabled`; single `compile_tier → deny` resolution step |
| EC-008 | `list_capabilities("acme")` where TOML declares endpoints but runtime config denies | `runtime_disabled`; two steps: `compile_tier → permit`, `runtime_tier → deny` |
| EC-009 | `list_capabilities(null)` cross-client summary | Returns `{client_id: null, clients: {...}, not_registered_tools: [...]}` shape |
| EC-010 | `list_capabilities` response inspected for field name | Field is `not_registered_tools` not `not_implemented` |
| EC-011 | Error response construction fails internally | Fallback to minimal error: `{"error": {"code": "E-MCP-999", "message": "Internal error during error formatting"}}` per BC-2.10.007 EC-10-012 |
| EC-012 | `PrismError::Internal { reason }` or `PrismError::Io(_)` passed to structured error builder | Structured error has `category: "internal"`, `retryable: false`, `upstream_message: null`, `source: "prism_mcp"` — NOT `"upstream_error"` (BC-2.10.007 v1.8 F-4) |
| EC-013 | Any `PrismError::Storage*` variant (e.g., `StorageWriteFailed`, `StorageReadFailed`) | Structured error has `category: "internal"`, `retryable: false` — storage failures are Prism-side; sensor was never reached (BC-2.10.007 v1.8 F-4) |
| EC-014 (HIGH-1) | `PrismError::AuthTokenExpired` passed to structured error builder | Code `E-AUTH-010`, `category: "authentication"`, `original_params_valid: true` — session credential expired, not a parameter format error |
| EC-015 (HIGH-1) | `PrismError::AuthTokenInvalid` passed to structured error builder | Code `E-AUTH-011`, `category: "authentication"`, `original_params_valid: true` — token revoked/invalid at runtime |
| EC-016 (HIGH-1) | `PrismError::InvalidOrgSlug` / `InvalidAnalystId` / `InvalidClientId` (identity-format) | Codes `E-AUTH-001` / `E-AUTH-002` / `E-AUTH-003` respectively; `category: "authentication"`, `original_params_valid: false` — malformed identity token supplied by caller |
| EC-017 (OBS-1) | `PrismError::SensorNotRegisteredForOrg` (E-QUERY-032) | `category: "permission"`, `original_params_valid: true` — cross-org isolation: sensor exists but not in this org's scope; params are structurally valid |
| EC-018 (OBS-2) | `PrismError::WatchdogKilled` / `HeartbeatMissed` / `RestartLimitExceeded` | `category: "internal"`, `original_params_valid: true` — watchdog infrastructure failures are Prism-side events; the original request params were valid |

---

## Dev Notes

- **E-AUTH code notes (HIGH-1 reflection):** `E-AUTH-010` (AuthTokenExpired) and `E-AUTH-011`
  (AuthTokenInvalid) are defined in error-taxonomy v2.26. The `E-AUTH-001` / `E-AUTH-002` /
  `E-AUTH-003` codes used by `InvalidOrgSlug` / `InvalidAnalystId` / `InvalidClientId` in
  S-5.02 are identity-validation codes at the MCP boundary; they are **correct and unaffected**
  by a pre-existing workspace-level collision: `prism-spec-engine` uses `E-AUTH-001` / `E-AUTH-002`
  for its OAuth2 credential variants (a distinct auth layer). That collision is a PRE-EXISTING
  workspace issue deferred to story `S-MAINT-EAUTH-COLLISION-001` for resolution. S-5.02's
  identity-validation pins are not changed.

- The `resolution_chain` in `list_capabilities` serves the same diagnostic function as
  the old `resolution_trace` concept in v1 story specs — one entry per resolution tier,
  showing what determined the outcome. Keep each step concise: `level + result + source`.
- Prompt injection defense (S-1.10 / DI-006) applies to sensor error messages in the
  `upstream_message` field. The `build_structured_error_response` builder must ensure
  the sensor text never leaks into `message` or `content[].text`.
- For the capability resolver integration: `FeatureFlagEvaluator` is already accessible
  from `PrismServer` via `self.write_executor` (see merged code line 3135
  `let ff = we.feature_flags()`). The tri-state evaluation must use `ff` for runtime-tier
  checks and `WriteEndpointRegistry` (or equivalent) for compile-tier checks.
- The test file follows the project convention: `tests/<bc-slug>_test.rs` for bc-specific
  suites, or additions to the existing `tests/tool_dispatch_tests.rs`. The 13 new Red
  Gate tests belong in `tool_dispatch_tests.rs` to keep BC-2.10.* tests colocated.

---

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.14 | error-taxonomy-v2.26-pin-propagation-2026-07-08 | 2026-07-08 | story-writer | **Reconciling pin round (pass-4 closures): error-taxonomy v1.84→v2.26. One live version-pin cite updated: §Dev Notes E-AUTH-010/011 definition cite. Historical changelog rows left unchanged per POL-29. AC semantics UNCHANGED. Also syncing frontmatter version 1.13→1.14 (no v1.13 changelog row was written for prior frontmatter bump; combined per POL-23).** |
| 1.12 | F-S502-PRL-LOW-2-red-gate-name-reconciliation | 2026-06-16 | story-writer | PR-LEVEL adversarial finding F-S502-PRL-LOW-2: reconciled 7 drifted Red Gate test names with actual shipped function names. (1) `test_BC_2_10_007_auth_token_expired_maps_to_e_auth_010_authentication_category` → `test_HIGH_1_auth_token_expired_category_is_authentication`. (2) `test_BC_2_10_007_auth_token_invalid_maps_to_e_auth_011_authentication_category` → `test_HIGH_1_auth_token_invalid_category_is_authentication`. (3) `test_BC_2_10_007_invalid_org_slug_maps_to_e_auth_001_original_params_valid_false` → `test_HIGH_1_invalid_org_slug_category_is_authentication`. (4) `test_BC_2_10_007_invalid_analyst_id_maps_to_e_auth_002_authentication_category` → `test_HIGH_1_invalid_analyst_id_category_is_authentication`. (5) `test_BC_2_10_007_invalid_client_id_maps_to_e_auth_003_authentication_category` → `test_HIGH_1_invalid_client_id_category_is_authentication`. (6) `test_BC_2_10_007_sensor_not_registered_for_org_maps_to_permission_category` → `test_BC_2_10_007_sensor_not_registered_for_org_category_is_permission`. (7) `test_BC_2_10_007_watchdog_killed_maps_to_internal_category_original_params_valid_true` → `test_BC_2_10_007_watchdog_killed_category_is_internal`. The remaining 2 v1.10-added names (`test_BC_2_10_007_heartbeat_missed_maps_to_internal_category`, `test_BC_2_10_007_restart_limit_exceeded_maps_to_internal_category`) already matched verbatim. Original 13 v1.6 Red Gate names were unaffected. Zero drift remaining. |
| 1.11 | F-S502-LOW-1-file-structure-sweep | 2026-06-16 | story-writer | TD-VSDD-060 sibling-sweep: corrected stale File Structure table cell for `error_mapping.rs` — companion function signature was `(ErrorData, Option<u64>)`, now `(ErrorData, u64)`, matching R2 task body (~line 147) and production code. The v1.7 changelog row records the original LOW-1 finding and is immutable historical record; no other live `Option<u64>` references to this function were found in the document. |
| 1.10 | HIGH-1-OBS-1-OBS-2-bc-2.10.007-v1.8-sync | 2026-06-16 | story-writer | Reflected implemented behavior from worktree @d0942825 and BC-2.10.007 v1.8 amendment. HIGH-1: added AC-012 (AuthTokenExpired→E-AUTH-010/authentication/original_params_valid:true; AuthTokenInvalid→E-AUTH-011/authentication/true) and AC-013 (InvalidOrgSlug→E-AUTH-001, InvalidAnalystId→E-AUTH-002, InvalidClientId→E-AUTH-003, category:authentication/original_params_valid:false — identity-format). OBS-1: added AC-014 (SensorNotRegisteredForOrg E-QUERY-032→category:permission/original_params_valid:true — cross-org isolation scoping denial). OBS-2: added AC-015 (WatchdogKilled/HeartbeatMissed/RestartLimitExceeded→category:internal/original_params_valid:true). Added EC-014–EC-018 covering the same cases. Added 9 Red Gate test stubs for the new ACs. Added E-AUTH cross-reference Dev Note documenting E-AUTH-010/011 in error-taxonomy v1.84, E-AUTH-001/002/003 identity-validation pins as correct, and pre-existing prism-spec-engine OAuth2 code collision deferred to S-MAINT-EAUTH-COLLISION-001. Advanced all live BC-2.10.007 version pins v1.7→v1.8 (§Behavioral Contracts table, Token Budget row, AC-005/006 traces, Arch Compliance Rule 8, EC-012/013). Historical §Changelog rows left immutable. |
| 1.9 | F-4-bc-2.10.007-v1.7-sync | 2026-06-16 | story-writer | F-4 closure reflection: added Architecture Compliance Rule 8 specifying that `PrismError::Internal`, `Io`, and all `Storage*` variants must map to `category: "internal"` (not `"upstream_error"`) per BC-2.10.007 v1.7 §Category decision rule. Added EC-012 (Internal/Io → internal category) and EC-013 (Storage* → internal category). Advanced BC-2.10.007 version pin v1.6→v1.7 in §Behavioral Contracts table, Token Budget BC files row (v2.8/v1.6/v1.6 → v2.8/v1.7/v1.6), and live AC trace comments (AC-005 and AC-006 traces updated). Historical §Changelog rows left immutable. |
| 1.8 | D-1181-at-merge-pin-sync | 2026-06-15 | state-manager | At-merge bookkeeping (D-1181): BC-2.10.011 version pin advanced v1.5→v1.6 in §Behavioral Contracts table and Token Budget BC files row (was "v2.8/v1.5/v1.5" → "v2.8/v1.6/v1.6"). BC-2.10.011 v1.6 self-contradiction fix (removed E-CFG-100/ClientNotFound from Error Cases; added EC-10-020 unknown-client-no-error; implementation semantics already aligned with v1.6 — no code change required). |
| 1.7 | S-5.02-LOCAL-pass-1-fix | 2026-06-14 | story-writer | HIGH-2: Added `crates_touched: [prism-mcp, prism-query, prism-security, prism-spec-engine]` frontmatter with inline note (accessor-only wiring, ADR-022 §C). Added four File Structure MODIFY rows for `prism-query/write_dispatch.rs` (NullAuditWriter), `prism-query/write_pipeline.rs` (endpoint_registry), `prism-security/feature_flag.rs` (capability_paths_for_client + client_ids), `prism-spec-engine/write_endpoint.rs` (all_capability_paths). LOW-1: Corrected R2 companion function signature from `Option<u64>` to `u64` (SensorRateLimited.retry_after_ms is required); updated AC-006 and EC-005 to reflect that the null-not-absent invariant is exercised by non-rate-limit error types, not SensorRateLimited; AC-005 updated to use struct syntax with required fields. BC-2.10.007 version in Behavioral Contracts table bumped v1.5→v1.6. |
| 1.6 | S-5.02-pre-TDD-finalize | 2026-06-14 | story-writer | Status ready. Applied PO-locked BC-2.10.004 v2.8 / BC-2.10.007 v1.5 / BC-2.10.011 v1.5. Rewrote Tasks as targeted deltas against merged code (server.rs + error_mapping.rs + tool_dispatch_tests.rs). Replaced middleware/meta.rs greenfield creates with delta-based file structure. Updated Previous Story Intelligence: validate_client_ids/list_capabilities already exist; ClientIdGuard middleware superseded. Corrected AC set to 11 ACs covering 3-case client_id taxonomy, 9-field nested error shape, null-not-absent retry_after_seconds, upstream_message isolation, and full tri-state list_capabilities. Added 13 concrete Red Gate test names. Fixed rmcp version 1.4→1.7 (workspace). Corrected TenantId→validate_client_ids path note. Points 3→5 (tri-state expansion is net-new work). Added tdd_mode: strict. |
| 1.5 | pass-pre-TDD-scan | 2026-06-14 | story-writer | Pre-TDD scan: noted BC divergences, rmcp version error, and list_capabilities scope expansion pending PO BC lock. |
| 1.4 | pass-60-fix | 2026-04-20 | story-writer | Fixed HIGH-001: resolved 3 stem-only BC input paths to full slugged filenames (BC-2.10.004-client-id-parameter-requirement, BC-2.10.007-structured-error-responses, BC-2.10.011-list-capabilities-meta-tool); renumbered duplicate 1.1 rows. |
| 1.3 | pass-59-fix | 2026-04-20 | story-writer | Fixed HIGH-002 (anchor_capabilities: CAP-010 → [CAP-005, CAP-009, CAP-034] per BC-INDEX union: BC-2.10.004→CAP-009, BC-2.10.007→CAP-034, BC-2.10.011→CAP-005). |
| 1.2 | Wave-5-patch | 2026-04-20 | story-writer | Pre-build sweep: added level, points, blocks, anchor_bcs/capabilities/subsystem, assumption_validations, risk_mitigations, inputs frontmatter; added ## Edge Cases section (synthesized from BC-2.10.004/007/011 boundary cases); renamed ## Library and Framework Requirements → ## Library & Framework Requirements; renamed ## Notes → ## Dev Notes. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
