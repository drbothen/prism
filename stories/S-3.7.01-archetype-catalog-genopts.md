---
document_type: story
level: "L3"
story_id: S-3.7.01
epic_id: "E-3.7"
version: "1.3"
status: merged
producer: story-writer
timestamp: "2026-04-27T00:00:00Z"
phase: 3.A
inputs:
  - .factory/specs/behavioral-contracts/BC-3.4.001.md
  - .factory/specs/behavioral-contracts/BC-3.4.003.md
  - .factory/specs/architecture/decisions/ADR-009-multi-tenant-data-generator.md
input-hash: "6ab6748"
traces_to: .factory/specs/prd.md
points: 5
depends_on: []
blocks: [S-3.7.02, S-3.7.03, S-3.7.04, S-3.7.05]
behavioral_contracts: [BC-3.4.001, BC-3.4.003]
verification_properties: [VP-108, VP-111, VP-115, VP-116, VP-117]
priority: "P0"
cycle: "v1.0.0-greenfield"
wave: 3
target_module: "prism-dtu-common"
subsystems: [SS-01]
estimated_days: 3
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-3.4.001, BC-3.4.003]
anchor_capabilities: [CAP-039]
anchor_subsystem: ["SS-01"]
tdd_mode: facade
title: "Archetype catalog + GenOpts API (prism-dtu-common generator module, D-056)"
# BC status: BC-3.4.001 and BC-3.4.003 authored and anchored
---

# S-3.7.01 — Archetype catalog + GenOpts API

## Narrative

As a generator implementer, I want a canonical `Archetype` enum, `GenOpts` struct,
and `default_page_size()` per-sensor function in `prism-dtu-common` behind the
`fixture-gen` feature gate, so that all four sensor generator stories (S-3.7.02
through S-3.7.05) can share a single source of truth for archetype semantics,
RNG initialisation, and pagination baselines.

## Track

Application Development

## Authority

ADR-009 is the authoritative design document for this story. Read it before implementing:
`.factory/specs/architecture/decisions/ADR-009-multi-tenant-data-generator.md`.

ADR-009 §2.1 defines the Hybrid Option C architecture — archetype catalog plus deterministic generator — that this story implements as the shared foundation in `prism-dtu-common`. ADR-009 §2.2 lists the exact 8 archetype variants (`HealthyOtEnvironment` through `DormantTenant`) with their semantic definitions. ADR-009 §2.3 specifies the `GenOpts` struct layout and `generate()` function signature. ADR-009 §2.4 defines the determinism contract including the exact `ChaCha20Rng::seed_from_u64(seed ^ org_id_hash)` formula; no deviation is permitted without an ADR-009 amendment. ADR-009 §2.5 specifies the org-tagged record ID format consumed by all four sensor generators. ADR-009 §2.8 mandates that the generator lives in `crates/prism-dtu-common/src/generator/` behind a `fixture-gen` feature gate and must not become a separate crate.

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-3.4.001 | Generator Determinism — Identical Inputs Produce Byte-Identical FixtureSet |
| BC-3.4.003 | Archetype Catalog Enumeration — 8 Archetypes with Defined Baselines |

Subsystem anchor justification: SS-01 (Sensor Adapters) owns `prism-dtu-common`
and the DTU generator crates per ARCH-INDEX Subsystem Registry. This story defines
the shared types that all generator crates consume. (Frontmatter was corrected
SS-06→SS-01 in v1.1, pass-27 M-27-001 / D-111/D-113; this prose synced in v1.2.)

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `Archetype` enum | `crates/prism-dtu-common/src/generator/archetype.rs` | pure-core |
| `GenOpts` struct | `crates/prism-dtu-common/src/generator/opts.rs` | pure-core |
| `seeded_rng` helper | `crates/prism-dtu-common/src/generator/rng.rs` | pure-core |
| `default_page_size()` | `crates/prism-dtu-common/src/generator/pagination.rs` | pure-core |
| `FixtureSet` / `Provenance` types | `crates/prism-dtu-common/src/generator/fixture.rs` | pure-core |

All modules are under `#[cfg(feature = "fixture-gen")]` (D-056).

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `generator/archetype.rs` | pure-core | Enum definition only; no I/O, no state |
| `generator/opts.rs` | pure-core | Struct definition + validation; no I/O |
| `generator/rng.rs` | pure-core | Deterministic RNG init from fixed inputs; no I/O |
| `generator/pagination.rs` | pure-core | Constant lookup; no I/O |
| `generator/fixture.rs` | pure-core | Struct definitions; no I/O |

---

## Acceptance Criteria

### AC-001 — `Archetype` enum defines exactly 8 variants (traces to BC-3.4.003 precondition 1)

`pub enum Archetype` is `#[non_exhaustive]` and declares exactly:
`HealthyOtEnvironment`, `CompromisedEndpoint`, `AuthOutage`, `LargeScale`,
`PaginationEdgeCases`, `SchemaDrift`, `HighChurn`, `DormantTenant`.
A companion `all_archetypes() -> &'static [Archetype]` function provides
exhaustive iteration for parameterised tests (BC-3.4.003 invariant 2).

### AC-002 — `GenOpts` struct matches ADR-009 §2.3 signature (traces to BC-3.4.001 precondition 2–5)

`pub struct GenOpts { pub seed: u64, pub scale: f64, pub time_anchor: DateTime<Utc>, pub overrides: serde_json::Value }`
with `GenOpts::default()` returning `{ seed: 42, scale: 1.0, time_anchor: prism_dtu_common::demo_time_anchor(), overrides: Value::Null }`.
The `time_anchor` default is the fixed demo-era constant `2026-01-01T00:00:00Z` (epoch
`1_767_225_600`), NOT wall-clock — corrected from the stale `DateTime::UNIX_EPOCH` claim
per BC-3.4.001 v0.9 precondition 4 (review-2026-06-10 P1-01; see Changelog v1.2).
`scale` is validated: must be positive and finite; `GenOpts::new()` returns
`Err(GenOptsError::InvalidScale)` if not satisfied (BC-3.4.001 precondition 3).

### AC-003 — RNG initialisation formula is exact (traces to BC-3.4.001 invariant 2)

`seeded_rng(seed: u64, org_id: &OrgId) -> ChaCha20Rng` computes
`org_id_hash = u64::from_le_bytes(org_id.as_bytes()[0..8])` and calls
`ChaCha20Rng::seed_from_u64(seed ^ org_id_hash)`.
No call to `rand::thread_rng()`, `SystemTime::now()`, or any non-deterministic
source exists anywhere in this module (BC-3.4.001 invariant 1).

### AC-004 — Override patch is JSON Merge Patch (RFC 7396) (traces to BC-3.4.001 postcondition 7)

`apply_overrides(base: serde_json::Value, patch: &serde_json::Value) -> serde_json::Value`
implements RFC 7396 merge semantics. A `null` patch value in the patch removes the key
from the base; a non-object patch replaces the entire base. Identical inputs always
produce identical output (determinism preserved through patching).

### AC-005 — `default_page_size()` per-sensor function provided (traces to BC-3.4.003 postcondition for PaginationEdgeCases)

`fn default_page_size(sensor: SensorType) -> usize` returns documented page-size constants:
values sourced from S-3.7.00 DERIVATION.md for Armis and CrowdStrike; from poller-bear and
poller-express SDK constants for Claroty and Cyberint. The function is used by
`PaginationEdgeCases` archetype to compute `page_size × 3` baseline count.

### AC-006 — `FixtureSet` and `Provenance` types defined (traces to BC-3.4.001 postcondition 1)

`pub struct FixtureSet { pub records: Vec<serde_json::Value>, pub cursors: Vec<String>, pub provenance: Provenance }`
`pub struct Provenance { pub org_id: OrgId, pub sensor_type: SensorType, pub archetype: Archetype, pub seed: u64, pub schema_valid: bool }`
`schema_valid` defaults to `true`; only `SchemaDrift` archetype sets it to `false`
(BC-3.4.002 postcondition for SchemaDrift).

### AC-007 — Feature gate: module absent from release builds (traces to BC-3.4.001 invariant 4)

All types in `crates/prism-dtu-common/src/generator/` are gated behind
`#[cfg(feature = "fixture-gen")]`. A `cargo build --release` of the workspace
without `--features fixture-gen` must not include any symbol from this module.
CI check: `grep -r "fixture_gen\|fixture-gen" target/release/` returns no hits.

## Tasks

1. Create `crates/prism-dtu-common/src/generator/` directory structure with
   `mod.rs`, `archetype.rs`, `opts.rs`, `rng.rs`, `pagination.rs`, `fixture.rs`.
2. Implement `Archetype` enum with `#[non_exhaustive]` and `all_archetypes()`.
3. Implement `GenOpts` with `Default`, `new()` validation, and `apply_overrides()`.
4. Implement `seeded_rng()` — XOR formula exactly matching BC-3.4.001 invariant 2.
   Add CI `grep` check for `thread_rng` in the module directory.
5. Implement `default_page_size(SensorType) -> usize` with documented values from S-3.7.00.
6. Implement `FixtureSet` and `Provenance` with correct field layout.
7. Add `#[cfg(feature = "fixture-gen")]` gates at module and crate level.
8. Write unit tests for: determinism (call twice, check equality), distinct seeds produce
   distinct output, `default_page_size` returns expected constants, RFC 7396 patch semantics.

## Previous Story Intelligence

S-3.7.00 derived `default_page_size` constants for Armis and CrowdStrike — read
`.references/schemas/armis/DERIVATION.md` and `.references/schemas/crowdstrike/DERIVATION.md`
before hardcoding values. Those files are the authoritative source.

## Architecture Compliance Rules

- Generator module lives in `crates/prism-dtu-common/src/generator/` — NOT a separate crate (D-056).
- Feature gate is `fixture-gen` — NOT `dtu` or `test`. Only activated in test/fixture builds.
- No production binary (`prism-mcp`, `prism-sensors`, etc.) may enable `fixture-gen` in its
  `Cargo.toml` feature list.
- `seeded_rng()` formula is locked: `seed ^ org_id_hash` — no deviation without ADR-009 amendment.

## Library & Framework Requirements

- `rand = "0.9"` with `ChaCha20Rng` (workspace pin — verify in `Cargo.toml`)
- `rand_chacha` — feature of `rand` or separate crate (verify workspace)
- `chrono = { features = ["serde"] }` (workspace pin)
- `serde_json` (workspace pin)
- `schemars` only if auto-generating schema from structs — optional

## File Structure Requirements

| Action | Path |
|--------|------|
| Create | `crates/prism-dtu-common/src/generator/mod.rs` |
| Create | `crates/prism-dtu-common/src/generator/archetype.rs` |
| Create | `crates/prism-dtu-common/src/generator/opts.rs` |
| Create | `crates/prism-dtu-common/src/generator/rng.rs` |
| Create | `crates/prism-dtu-common/src/generator/pagination.rs` |
| Create | `crates/prism-dtu-common/src/generator/fixture.rs` |
| Modify | `crates/prism-dtu-common/Cargo.toml` — add `fixture-gen` feature |
| Read | `.references/schemas/armis/DERIVATION.md` |
| Read | `.references/schemas/crowdstrike/DERIVATION.md` |

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,800 |
| BC files (2 BCs) | ~3,000 |
| ADR-009 relevant sections | ~2,000 |
| S-3.7.00 DERIVATION.md files | ~1,000 |
| Existing `prism-dtu-common/src/` for context | ~2,000 |
| New generator module files (write) | ~2,500 |
| Total | ~12,300 |

Within the 30% context window budget for a 128k-context agent.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `scale = 0.0` passed to `GenOpts::new()` | Returns `Err(GenOptsError::InvalidScale)` — zero is not positive |
| EC-002 | `scale = f64::INFINITY` | Returns `Err(GenOptsError::InvalidScale)` — not finite |
| EC-003 | `seed = u64::MAX`, `org_id` with all-zero first 8 bytes | `seed ^ 0 = seed`; valid; produces deterministic stream |
| EC-004 | `apply_overrides` with `null` patch value for a key | Key removed from base (RFC 7396) |
| EC-005 | `all_archetypes()` count | Returns exactly 8 elements; verified in unit test |

## Test Plan

- `test_determinism_same_inputs`: call `seeded_rng` twice with same args; verify identical stream
- `test_distinct_seeds`: different seeds → different first u64 from RNG
- `test_distinct_org_ids`: different org_ids, same seed → different first u64
- `test_genopts_default`: verify field values match spec
- `test_scale_validation`: `0.0`, `-1.0`, `inf`, `nan` → `Err`; `0.001`, `1.0`, `100.0` → `Ok`
- `test_default_page_size`: assert known constants per sensor type
- `test_rfc7396_patch`: null-value removal, object merge, scalar replacement
- `test_all_archetypes_count`: `all_archetypes().len() == 8`

## Risks / Open Questions

- `default_page_size` values for Claroty and Cyberint must be sourced from poller-bear /
  poller-express SDK constants — confirm before implementation.
- `rand_chacha` crate split from `rand` in recent versions — verify workspace pin.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.3 | Round-4 §Authority backfill | 2026-08-02 | story-writer | DRIFT-STORY-AUTHORITY-ABSENT-CORPUS-001 (D-2084): added §Authority citing ADR-009 §2.1, §2.2, §2.3, §2.4, §2.5, §2.8 |
| 1.2 | DTU cascade P5-01 sibling (architect-routed) | 2026-06-10 | story-writer | Merged-story annotation/sync (history append-only): AC-002 `GenOpts::default()` `time_anchor` claim corrected `DateTime::UNIX_EPOCH` → `prism_dtu_common::demo_time_anchor()` (`2026-01-01T00:00:00Z`, epoch `1_767_225_600` — fixed demo-era constant, NOT wall-clock) per BC-3.4.001 v0.9 precondition 4 (review-2026-06-10 P1-01). File-wide sweep: no other UNIX_EPOCH-default claims found. Sibling-sweep fix (TD-VSDD-060): §Behavioral Contracts subsystem-anchor prose SS-06 (Client Configuration) → SS-01 (Sensor Adapters), syncing to the frontmatter correction landed in v1.1 (pass-27 M-27-001) that never propagated to the body. ACs/traces/behavioral_contracts array otherwise UNCHANGED. Changelog section created retroactively per S-3.3.02 v1.1 precedent; v1.1/v1.0 rows reconstructed from factory-artifacts git history (9455332a). |
| 1.1 | wave-3-phase-3a-convergence-step-3-pass-27 | 2026-04-28 | story-writer (adversary fix-burst) | Frontmatter `subsystems: [SS-06]` → `[SS-01]` (M-27-001 sibling-fix from D-111/D-113). Body prose not synced at the time — corrected in v1.2. Status draft→merged landed separately (W3-FIX-G hygiene burst, no version bump). Row reconstructed retroactively in v1.2. |
| 1.0 | Phase 3 | 2026-04-27 | story-writer | Initial story creation. |
