---
story_id: S-1.02
title: "prism-core: Entity Types and State Machines"
level: "L4"
wave: 1
target_module: prism-core
subsystems: [SS-03, SS-07, SS-11, SS-12, SS-14]
priority: P0
depends_on: [S-1.01]
blocks: [S-1.06, S-2.03]
points: 3
estimated_days: 2
status: draft
document_type: story
version: "1.7"
producer: story-writer
timestamp: "2026-04-21T00:00:00Z"
input-hash: "1e29f9d"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-1"
phase: 2
behavioral_contracts: []
verification_properties: [VP-005, VP-006, VP-011, VP-029, VP-051, VP-055, VP-057]
assumption_validations: []
risk_mitigations: []
anchor_bcs: []
anchor_capabilities: []
anchor_subsystem: null
inputs:
  - ".factory/specs/prd.md"
  - ".factory/specs/verification-properties/vp-005-case-state-machine.md"
  - ".factory/specs/verification-properties/vp-006-case-state-no-self-transitions.md"
  - ".factory/specs/verification-properties/vp-011-credential-name-path-traversal.md"
  - ".factory/specs/verification-properties/vp-029-cursor-cap-200.md"
---

# S-1.02 — prism-core: Entity Types and State Machines

## Narrative

As a Prism platform implementer, I want all domain entity types and state machines
defined in prism-core, so that downstream crates (prism-operations, prism-mcp,
prism-audit) share a single authoritative definition of case status transitions,
alert severity levels, credential names, and cursor capacity — with formal proofs
that the constraints hold.

## Objective

Extend prism-core with domain entity types needed by the operations and MCP layers.
This story is purely additive to S-1.01; no existing code is modified. The state
machine correctness is verified by four Kani proofs (VP-005, VP-006, VP-011, VP-029).

---

## Behavioral Contracts

This story has no active SS-level BCs. These types are preconditions for BCs in
SS-14 (Case Management, BC-2.14.*) and SS-12 (Scheduler, BC-2.12.*).

| BC | Clause | Role in This Story |
|----|--------|--------------------|
| (none active) | -- | Types produced here are preconditions for downstream BCs |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,800 |
| New source files (entity types, state machines) | ~2,500 |
| VP-005, VP-006, VP-011, VP-029 Kani proofs | ~1,200 |
| Unit test files | ~1,000 |
| Total | ~6,500 |

Within the 30% context window budget.

---

## Tasks

1. Implement `CaseStatus` enum in `crates/prism-core/src/case.rs`:
   - Variants: `New`, `Acknowledged`, `Investigating`, `Resolved`, `Closed`
     (per BC-2.14.002 — security-ops terminology)
   - Implement `can_transition_to(&self, next: CaseStatus) -> bool` encoding exactly
     12 valid transitions (per BC-2.14.002):
     **Forward (linear):**
     - New → Acknowledged
     - Acknowledged → Investigating
     - Investigating → Resolved
     - Resolved → Closed
     **Skip-ahead:**
     - New → Investigating
     - New → Resolved
     - New → Closed
     - Acknowledged → Resolved
     - Acknowledged → Closed
     - Investigating → Closed
     **Reopen:**
     - Resolved → Investigating
     - Closed → Investigating
     (total: exactly 12 per VP-005)
   - Implement `valid_transitions(&self) -> &'static [CaseStatus]` for exhaustive
     listing (used by MCP tool for hint generation)
   - Derive: `Clone, Copy, Debug, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize`

2. Implement `DispositionCode` enum in `crates/prism-core/src/case.rs`:
   - Variants: `TruePositive`, `FalsePositive`, `Benign`, `Inconclusive`, `Duplicate`,
     `TestAlert`
   - Required when transitioning to `Resolved` (enforced in prism-operations, not here)
   - Derive same traits as CaseStatus

3. Implement `AlertSeverity` enum in `crates/prism-core/src/alert.rs`:
   - Variants: `Critical`, `High`, `Medium`, `Low`, `Informational`
   - Implement `as_ocsf_severity_id(&self) -> u32` mapping to OCSF severity_id values:
     Critical → 5, High → 4, Medium → 3, Low → 2, Informational → 1
   - Derive same traits as CaseStatus

4. Implement `TimelineEntryType` enum in `crates/prism-core/src/case.rs`:
   - Variants: `Note`, `StatusChange`, `AlertLink`, `EvidenceLink`, `OtImpact`
   - Derive same traits

5. Implement `CredentialName` newtype in `crates/prism-core/src/credentials.rs`:
   - Inner type: `Arc<str>`
   - Constructor: `CredentialName::new(s: &str) -> Result<Self, PrismError>`
   - Reject path traversal: any string containing `/`, `\`, `..`, or null bytes (`\0`)
   - Reject empty string
   - Max length: 128 characters
   - Derive: `Clone, Debug, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize`

6. Implement UUID v7 ID newtypes in `crates/prism-core/src/ids.rs`:
   - `ScheduleId(Uuid)`, `RuleId(Uuid)`, `CaseId(Uuid)`, `AlertId(Uuid)`
   - Each has a `::new() -> Self` constructor calling `Uuid::now_v7()`
   - Each has `::from_uuid(Uuid) -> Self` for deserialization
   - Derive: `Clone, Copy, Debug, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize`

7. Implement `CursorId` newtype in `crates/prism-core/src/cursor.rs`:
   - Inner type: `u64` (monotonically incrementing)
   - `CursorRegistry` struct tracking active cursors via `BTreeSet<CursorId>`
   - Constructor: `CursorRegistry::allocate(&mut self) -> Result<CursorId, PrismError>`
     - Rejects allocation when `active.len() >= 200`
     - Returns `Err(PrismError::CursorCapExceeded)` at limit
   - `CursorRegistry::release(&mut self, id: CursorId)` removes from active set
   - `CursorRegistry::active_count(&self) -> usize` for health reporting

8. Write VP-005 Kani proof in `crates/prism-core/src/proofs/case_status.rs`:
   - Enumerate all 25 `(from, to)` pairs (5 × 5); count how many return `true`
   - Assert the count equals exactly 12
   - Proof name: `proof_exactly_12_transitions`

9. Write VP-006 Kani proof in `crates/prism-core/src/proofs/case_status.rs`:
   - For every `CaseStatus` variant, assert `can_transition_to(same_variant) == false`
   - Proof name: `proof_no_self_transitions`

10. Write VP-011 Kani proof in `crates/prism-core/src/proofs/credential_name.rs`:
    - For strings containing `/`, assert `CredentialName::new()` returns `Err`
    - For strings containing `..`, assert `CredentialName::new()` returns `Err`
    - For strings containing `\0`, assert `CredentialName::new()` returns `Err`
    - Proof name: `proof_path_traversal_rejected`

11. Write VP-029 Kani proof in `crates/prism-core/src/proofs/cursor.rs`:
    - Allocate exactly 200 cursors — assert all succeed
    - Attempt 201st allocation — assert it returns `Err(CursorCapExceeded)`
    - Release one cursor; attempt allocation — assert it succeeds (cap enforces current
      count, not lifetime count)
    - Proof name: `proof_cursor_cap_200`

12. Write unit tests in `crates/prism-core/src/tests/`:
    - Test all 12 valid transitions and verify 13 invalid transitions are rejected
    - Test `AlertSeverity::as_ocsf_severity_id()` for all 5 variants
    - Test UUID v7 ID newtype creation and serde round-trip
    - Test `CursorRegistry` at boundary (199, 200, 201 allocations)

13. Write VP-051 Kani proof in `crates/prism-core/src/proofs/case_status_exhaustive.rs`:
    - Enumerate all 25 `(from_state, to_state)` pairs; for each assert `advance_case_state` returns Ok or the correct Err variant (E-CASE-004 for invalid, E-CASE-005 for self-transition)
    - Proof name: `proof_exhaustive_transition_table`
    - Traces to: BC-2.14.002 postconditions

14. Write VP-055 proptest in `crates/prism-core/src/proofs/storage_batch.rs`:
    - Prop 1 (atomicity): inject failure at position N < K in K-entry put_batch; assert zero entries readable after failure
    - Prop 2 (domain isolation): write key K to domain A; assert get(domain_B, K) == None
    - Uses MockStorageEngine; method: proptest
    - Traces to: BC-2.15.002 postconditions

15. Write VP-057 Kani proof in `crates/prism-storage/src/proofs/crash_recovery.rs`:
    - Symbolic `consecutive_crashes: u32` (bounded 0..=5); assert `advance_crash_counter` returns Denylist iff value+1 >= 3
    - Assert idempotence: same input produces same RecoveryAction
    - Proof name: `proof_denylist_threshold_three`
    - Traces to: BC-2.15.005 postconditions

---

## Acceptance Criteria

**AC-1:** Given `CaseStatus::New`, When `can_transition_to(CaseStatus::Acknowledged)` is
called, Then it returns `true` (forward linear transition).

**AC-2:** Given `CaseStatus::New`, When `can_transition_to(CaseStatus::Closed)` is
called, Then it returns `true` (skip-ahead transition per BC-2.14.002).

**AC-3:** Given `CaseStatus::Resolved`, When `can_transition_to(CaseStatus::Closed)` is
called without a `DispositionCode` being set (enforced downstream), Then the state
machine itself returns `true` (disposition enforcement is in prism-operations, not here).

**AC-4:** Given a `CredentialName` input of `"../../passwd"`, When `CredentialName::new()`
is called, Then it returns `Err(PrismError::InvalidCredentialName)`.

**AC-5:** Given a `CredentialName` input containing a null byte `"key\0value"`, When
`CredentialName::new()` is called, Then it returns `Err`.

**AC-6:** Given exactly 200 active cursors in `CursorRegistry`, When a 201st cursor is
requested via `allocate()`, Then it returns `Err(PrismError::CursorCapExceeded)`.

**AC-7:** Given 200 active cursors, When one is released and `allocate()` is called
again, Then it returns `Ok(CursorId)` (cap counts active cursors, not total allocated).

**AC-8:** Given `AlertSeverity::Critical`, When `as_ocsf_severity_id()` is called,
Then it returns `5`.

**AC-9:** VP-005 Kani proof passes: exactly 12 transitions are valid.

**AC-10:** VP-006 Kani proof passes: no self-transitions exist.

**AC-11:** VP-011 Kani proof passes: path traversal patterns are all rejected.

**AC-12:** VP-029 Kani proof passes: CursorId cap of 200 is enforced.

**AC-13:** VP-051 Kani proof passes: all 25 (from, to) pairs produce the correct Ok/Err outcome — exactly 12 Ok and 13 Err.
(traces to BC-2.14.002)

**AC-14:** VP-055 proptest passes: put_batch atomicity holds (failed batch leaves zero readable entries) and domain isolation holds (cross-domain key lookup returns None).
(traces to BC-2.15.002)

**AC-15:** VP-057 Kani proof passes: `advance_crash_counter` returns Denylist{86400s} if and only if `consecutive_crashes + 1 >= 3`, and Warn otherwise.
(traces to BC-2.15.005)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Entity types and state machines | `prism-core` | Pure |

This story extends `prism-core` (Layer 0) with domain entity types — `SensorId`,
`ScheduleId`, `AlertId`, `CaseId`, `DetectionRuleId`, `CredentialId`, and associated state
machine enums (`AlertStatus`, `CaseStatus`). These types are consumed by SS-03 (Credential
Management / `prism-credentials`), SS-11 (Query Execution / `prism-query`), SS-12
(Scheduler / `prism-operations`), and SS-14 (Alert & Case Management / `prism-operations`)
per `architecture/module-decomposition.md` § Crate Responsibilities.

Additionally, `CursorId` and `CursorRegistry` (also delivered by this story) enforce the
200-cursor cap policy owned by SS-07 (Adapter Pagination). The cap is enforced at the
type allocation boundary in `prism-core` (`CursorRegistry::allocate()`) to prevent
unbounded cursor allocation across all pagination consumers. SS-07 is the semantic owner
of the cap policy; S-1.02/prism-core is the enforcement vehicle. This cross-subsystem
contribution is the basis for SS-07's inclusion in this story's subsystems list.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-core` | Pure | Pure domain type definitions; no I/O, no external dependencies. Classified pure-core per `architecture/purity-boundary-map.md`. |

---

## Verification Properties

| VP | Type | Description | Passing Condition |
|----|------|-------------|-------------------|
| VP-005 | Kani proof | Exactly 12 valid CaseStatus transitions | Count of true pairs = 12 |
| VP-006 | Kani proof | No CaseStatus self-transitions | All 5 self-transition checks return false |
| VP-011 | Kani proof | CredentialName rejects path traversal | All traversal patterns return Err |
| VP-029 | Kani proof | CursorId enforces 200-cap | 200 succeed, 201st returns Err |
| VP-051 | Kani proof | Case State Machine — Exhaustive 5×5 Transition Table: per-pair accept/reject for all 25 combinations | Ok for 12 valid pairs; Err(E-CASE-004 or E-CASE-005) for all 13 invalid pairs |
| VP-055 | proptest | StorageEngine put_batch Atomicity and Domain Isolation over MockStorageEngine | Failed mid-batch write: zero entries readable; cross-domain: write to A not visible in B |
| VP-057 | Kani proof | Crash Recovery — Denylist at Three Consecutive Crashes: threshold is exactly 3 | Returns Denylist{86400s} iff consecutive_crashes+1 >= 3; Warn otherwise |

---

## Architecture Compliance Rules

- All entity types MUST be `Send + Sync` (async runtime requirement)
- State machine logic MUST live in `prism-core` — not in prism-operations or prism-mcp
  (single source of truth for transition rules)
- ID newtypes MUST use UUID v7 (time-ordered, not random v4) for sorted iteration
  in RocksDB
- `CursorRegistry` MUST be a plain struct (not async) — the async wrapper lives in
  prism-query (S-3.05)
- `CredentialName` validation MUST reject all four path traversal patterns: `/`, `\`,
  `..`, null byte

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| uuid | 1.x (feature: v7) | UUID v7 generation for ID newtypes |
| serde | 1.x | Serialize/Deserialize for all entity types |
| kani | (dev, cfg-gated) | VP-005, VP-006, VP-011, VP-029 proofs |

All other dependencies inherited from S-1.01 (thiserror, tracing).

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-core/src/case.rs` | Create | CaseStatus, DispositionCode, TimelineEntryType |
| `crates/prism-core/src/alert.rs` | Create | AlertSeverity enum |
| `crates/prism-core/src/credentials.rs` | Create | CredentialName newtype |
| `crates/prism-core/src/ids.rs` | Create | ScheduleId, RuleId, CaseId, AlertId newtypes |
| `crates/prism-core/src/cursor.rs` | Create | CursorId + CursorRegistry |
| `crates/prism-core/src/proofs/case_status.rs` | Create | VP-005, VP-006 Kani proofs |
| `crates/prism-core/src/proofs/credential_name.rs` | Create | VP-011 Kani proof |
| `crates/prism-core/src/proofs/cursor.rs` | Create | VP-029 Kani proof |
| `crates/prism-core/src/lib.rs` | Modify | Add new module declarations and re-exports |
| `crates/prism-core/src/proofs/case_status_exhaustive.rs` | Create | VP-051 Kani proof — exhaustive 5×5 transition table |
| `crates/prism-core/src/proofs/storage_batch.rs` | Create | VP-055 proptest — put_batch atomicity and domain isolation |
| `crates/prism-storage/src/proofs/crash_recovery.rs` | Create | VP-057 Kani proof — denylist at three consecutive crashes |

---

## Previous Story Intelligence

S-1.01 established: `TenantId`, `PrismError`, `StorageDomain`, `ColumnOptions`,
`ConfigSnapshot`, `TracingConfig`. Errors added in this story (`InvalidCredentialName`,
`CursorCapExceeded`) must be added as variants to the existing `PrismError` enum in
`error.rs`. Do not create a second error type.

---

## Edge Cases

| Edge Case | Handling |
|-----------|----------|
| CaseStatus: self-transition (e.g., `New → New`) | `can_transition_to()` returns `false` — VP-006 proves no self-transitions exist |
| CaseStatus: backward transition not in the 12 (e.g., `Acknowledged → New`) | Returns `false`; VP-005 proves exactly 12 valid transitions |
| CredentialName: empty string | `CredentialName::new("")` returns `Err(PrismError::InvalidCredentialName)` |
| CredentialName: path traversal (`../../passwd`) | `Err` — any of `/`, `\`, `..`, `\0` triggers rejection (AC-4, VP-011) |
| CredentialName: length exactly 128 | `Ok` — boundary valid |
| CredentialName: length 129 | `Err` — exceeds max |
| CursorRegistry: at exactly 200 active cursors, allocate 201st | `Err(PrismError::CursorCapExceeded)` — VP-029 proves this boundary |
| CursorRegistry: release then re-allocate | `Ok` — cap counts active cursors, not lifetime cursors (AC-7) |
| AlertSeverity: unknown integer mapping | Not applicable — enum is exhaustive; all 5 variants have known `severity_id` values |
| UUID v7 ID collision | Statistically impossible (time-ordered v7); no de-dup logic required at this layer |

---

## Dev Notes

- The exact set of 12 valid transitions must be documented in a const array adjacent
  to `can_transition_to()` so the Kani proof and the runtime function share the same
  source of truth. Do not duplicate the transition list.
- UUID v7 requires the `uuid` crate with feature flag `v7`. Confirm this is in
  `Cargo.toml` for `prism-core` before implementing `ScheduleId::new()`.
- `CursorRegistry` is intentionally not async-safe at this layer. The query engine
  (S-3.05) wraps it in `Arc<Mutex<CursorRegistry>>` for concurrent access.
- `DispositionCode` is defined here but enforcement logic (requiring it on `Resolved`
  transitions) lives in prism-operations (S-4.06). This story only defines the type.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.7 | pass-88 | 2026-04-21 | story-writer | F88-001: corrected Task 15 path `crates/prism-persistence/...` → `crates/prism-storage/src/proofs/crash_recovery.rs` (VP-057 module is prism-storage per VP-INDEX v1.10). F88-006: added File Structure Requirements rows for VP-051, VP-055, VP-057 proof files. |
| 1.6 | pass-87-F87-003 | 2026-04-21 | story-writer | F87-003: body-propagated VP-051, VP-055, VP-057 — added Verification Properties table rows, Tasks 13–15, and AC-13/AC-14/AC-15. |
| 1.5 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.4 | remediation-step5 | 2026-04-20 | story-writer | BLK-003: corrected VP filename slugs in `inputs:` list (vp-005-case-state-machine.md, vp-006-case-state-no-self-transitions.md replace stale slugs). Reset `input-hash` to `[pending-recompute]`. |
| 1.3 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; renamed ## Notes to ## Dev Notes; renamed section heading to ## Library & Framework Requirements. |
| 1.2 | post-convergence | 2026-04-19 | architect | Added SS-07 to subsystems list reflecting cross-subsystem contribution: Cursor type enforces SS-07 pagination cap policy at the prism-core allocation boundary. Added Architecture Mapping narrative paragraph explaining joint ownership. Closes P3P41-A-OBS-001 via Option B. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
