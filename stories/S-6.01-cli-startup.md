---
story_id: S-6.01
title: "prism-bin: CLI, Startup, and Initialization"
wave: 6
target_module: prism-bin
subsystems: [SS-06, SS-10]
priority: P0
depends_on: [S-5.01, S-5.05, S-2.01]
estimated_days: 2
status: draft
document_type: story
version: "1.6"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "8fd2b1f"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-6"
phase: 2
behavioral_contracts: [BC-2.10.001, BC-2.10.006, BC-2.10.010, BC-2.06.006]
verification_properties: []
level: "L4"
points: 3
blocks: [S-6.02, S-6.03, S-6.04, S-6.05]
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.10.001, BC-2.10.006, BC-2.10.010, BC-2.06.006]
anchor_capabilities: [CAP-009, CAP-034]
anchor_subsystem: ["SS-06", "SS-10"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.10.001-server-handler-implementation.md
  - .factory/specs/behavioral-contracts/BC-2.10.006-stdio-transport.md
  - .factory/specs/behavioral-contracts/BC-2.10.010-graceful-shutdown.md
  - .factory/specs/behavioral-contracts/BC-2.06.006-dry-run-validation-mode.md
---

# S-6.01 — prism-bin: CLI, Startup, and Initialization

## Narrative

As an MSSP operator installing Prism, I want a single binary with clear CLI commands
(`serve`, `init`, `health`, `--dry-run`) and a robust startup sequence that validates
configuration and initializes all subsystems before accepting MCP traffic, so that
misconfiguration or database issues are caught immediately with clear error messages
rather than manifesting as runtime failures mid-query.

## Objective

Implement the `prism` binary crate with clap-based CLI argument parsing, the full
ordered startup sequence, signal handling for graceful shutdown, and tracing subscriber
configuration. This is the integration point that wires all prism-* crates into a
working binary.

---

## Behavioral Contracts

This story has no subsystem-level BCs of its own — it wires together BCs already
covered by S-5.01 through S-5.05, S-2.01, and S-1.06. Compliance is verified through
end-to-end behavior in S-6.02.

| BC ID | Title |
|-------|-------|
| BC-2.10.001 | rmcp ServerHandler Implementation |
| BC-2.10.006 | Stdio Transport |
| BC-2.10.010 | Graceful Shutdown on SIGTERM/SIGINT |
| BC-2.06.006 | --dry-run Flag Validates Config and Prints Redacted Summary |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,000 |
| crates/prism-bin/src/main.rs | ~2,500 |
| crates/prism-bin/src/commands/serve.rs | ~2,000 |
| crates/prism-bin/src/commands/init.rs | ~1,000 |
| crates/prism-bin/src/commands/health.rs | ~800 |
| crates/prism-bin/src/startup.rs | ~2,000 |
| Cargo.toml | ~400 |
| Total | ~10,700 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Create `crates/prism-bin/Cargo.toml`:
   - Declare binary target `prism`
   - Dependencies: `clap` (4.x, derive feature), `tokio` (full), `tracing`,
     `tracing-subscriber`, `prism-mcp`, `prism-core`, `prism-storage`

2. Implement clap CLI in `crates/prism-bin/src/main.rs`:
   - Top-level: `prism [OPTIONS] <COMMAND>`
   - Global options: `--config <PATH>` (override config file path), `--dry-run`
   - Commands: `serve`, `init`, `health`, `register`, `install-secops-factory`
   - Each command dispatches to a handler in `src/commands/`
   - The `Commands` enum MUST enumerate ALL
     subcommands including those implemented in later stories. Add the following
     variants at enum definition time, each dispatching to a placeholder handler that
     prints "not yet implemented" and exits 1 until the owning story replaces it:
     - `Logs(LogsArgs)` — placeholder for S-5.08 (future story, logs subcommand)
     - `Credential(CredentialArgs)` — placeholder replaced by S-6.04
     - `MigrateStorage(MigrateStorageArgs)` — placeholder replaced by S-6.05
     - Scope delta: ~200 lines — enum variants + placeholder dispatch functions only.
       No business logic belongs here.
     - The owning story (S-6.04 for `credential`, S-6.05 for `migrate-storage`) replaces
       the placeholder dispatch WITHOUT modifying the enum — it only fills in the handler
       implementation. The enum variant declarations made here are permanent.
     - `LogsArgs`, `CredentialArgs`, `MigrateStorageArgs` are stub structs with no
       fields at this stage; the owning stories flesh out the arg definitions.

3. Implement `prism serve` in `crates/prism-bin/src/commands/serve.rs`:
   - Call `startup::run(config_path, dry_run)` which executes the ordered startup
     sequence
   - On dry-run: print redacted config summary and exit 0/1 per validation result
   - On success: hand off to MCP server stdio loop (blocks until shutdown)

4. Implement ordered startup sequence in `crates/prism-bin/src/startup.rs`:
   a. Parse CLI args (already done by clap before this function is called)
   b. Load config: `config::loader::load(config_path)` → `PrismConfig`
   c. Validate config: `config::validation::validate(&config)` → collect errors, exit 1
      if any
   d. Initialize tracing subscriber: stderr writer, configured log level per subsystem
   e. Open RocksDB: `StorageEngine::open(state_dir)` — if schema version mismatch, print
      migration instructions and exit 3
   f. Probe keyring: verify the OS keychain is accessible, log warning if not
   g. Load sensor specs: `SpecEngine::load_all(&config)` — populate spec registry
   g2. Start event pollers: `AdapterRegistry::start_pollers(&config, &storage, cancel_token)`
       — spawn background EventPoller tasks for all event_stream tables (S-2.08)
   h. Load detection rules: `RuleEngine::load_all(&storage)` — warm rule cache
   i. Initialize scheduler: `Scheduler::start(&config, &storage)` — restore persisted
      schedules and resume any that fired while server was down
   j. Build `PrismContext`: assemble all initialized subsystems into shared context
   k. Register tools: `PrismServer::new(context)` — applies feature-flag gating
   l. Start stdio transport: `rmcp::stdio(server).await` — blocks here

5. Implement `prism init` in `crates/prism-bin/src/commands/init.rs`:
   - Check for existing `prism.toml` in current directory; abort with message if present
   - Write a template `prism.toml` with commented-out examples for common sensor types
   - Print "Created prism.toml — edit to add your clients and credentials"

6. Implement `prism health` in `crates/prism-bin/src/commands/health.rs`:
   - Load config (same as serve, but skip storage/scheduler init)
   - Instantiate a minimal `PrismContext` (no RocksDB, no scheduler)
   - Call `SensorHealthChecker::check_all(&config)` for all configured sensors
   - Print per-sensor status table to stdout: sensor | client | status | latency_ms
   - Exit 0 if all sensors healthy or partial, exit 1 if all sensors down

7. Configure tracing subscriber in `crates/prism-bin/src/startup.rs`:
   - Default: INFO level, text format
   - `--log-json` flag: switch to JSON format (for log aggregators)
   - Per-subsystem overrides via env: `RUST_LOG=prism_query_engine=debug,prism_mcp=info`
   - All output to stderr (BC-2.10.006 stdio reservation)

8. Implement signal handling in `crates/prism-bin/src/commands/serve.rs`:
   - Install handlers using `tokio::signal` before starting the stdio transport
   - On SIGTERM or Ctrl-C: call `PrismContext::shutdown()` which flushes audit buffer
     and closes RocksDB
   - Hard timeout: if shutdown takes > 5 seconds, force exit with code 1

---

## Acceptance Criteria

**AC-1:** Given `prism serve` with a valid `prism.toml`, When started, Then the binary
initializes all subsystems in order and the MCP server responds to `initialize` on stdio.

**AC-2:** Given `prism init` run in a directory without a `prism.toml`, When executed,
Then a `prism.toml` is created containing commented examples for CrowdStrike, Claroty,
Cyberint, and Armis sensors.

**AC-3:** Given `prism health` with configured sensors, When executed, Then a per-sensor
status table is printed to stdout with columns: sensor, client, status, latency_ms.

**AC-4:** Given a RocksDB database with a schema version older than the current binary's
expected version, When `prism serve` starts, Then the startup sequence prints a migration
instruction and exits with code 3.

**AC-5:** Given SIGTERM delivered to a running `prism serve`, When received, Then the
server completes graceful shutdown within 5 seconds and the RocksDB LOCK file is released.

**AC-6:** Given `prism serve --dry-run` with a config containing 2 validation errors,
When executed, Then both errors are printed with their TOML paths and suggestions, and
the process exits with code 1.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| CLI startup and signal handling | `prism-bin` | Effectful |
| MCP server initialization | `prism-mcp` (SS-10) | Effectful |

`prism-bin` implements `main()` — argument parsing (clap), signal handler registration
(SIGTERM/SIGINT), storage initialization, config loading, and `PrismServer` startup.
All startup operations are effectful (process I/O, filesystem reads, tokio runtime
launch). Per `architecture/module-decomposition.md`, `prism-bin` depends on `prism-mcp`,
`prism-storage`, and `prism-spec-engine`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-bin` (startup) | Effectful | Process init, signal registration, runtime launch. Effectful-shell per `architecture/purity-boundary-map.md`. |
| `prism-mcp` (server init) | Effectful | MCP stdio transport binding. Effectful-shell. |

---

## Verification Properties

No formal VPs for this story. The startup sequence is validated through the smoke tests
in S-6.02.

---

## Architecture Compliance Rules

- `prism-bin` MUST be a thin integration crate — no business logic belongs here; all
  logic lives in `prism-mcp`, `prism-storage`, etc.
- The startup sequence order (config → validate → storage → specs → rules → scheduler →
  context → tools → transport) is MANDATORY — deviating from this order risks using
  uninitialized subsystems
- `prism health` MUST NOT open RocksDB — it is a network connectivity check only and
  should not require a state directory to exist
- Tracing output MUST go to stderr — even `prism init` success messages should be
  written to stderr or stdout-as-user-facing-text, never mixed with MCP protocol data
- On exit code 3 (schema mismatch), the binary MUST print the expected and actual
  schema versions to help operators identify which migration to run

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| clap | 4.x (derive feature) | CLI argument parsing, subcommands |
| tokio | 1.x (full features) | Async runtime, signal handling |
| tracing | 0.1.x | Structured logging |
| tracing-subscriber | 0.3.x | Subscriber setup, RUST_LOG support, stderr writer |
| prism-mcp | workspace | PrismServer, PrismContext, config loading |
| prism-core | workspace | Shared types |
| prism-storage | workspace | StorageEngine, schema version check |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-bin/Cargo.toml` | Create | Binary crate manifest |
| `crates/prism-bin/src/main.rs` | Create | CLI entrypoint, clap setup |
| `crates/prism-bin/src/startup.rs` | Create | Ordered startup sequence |
| `crates/prism-bin/src/commands/mod.rs` | Create | Command module declarations |
| `crates/prism-bin/src/commands/serve.rs` | Create | prism serve command |
| `crates/prism-bin/src/commands/init.rs` | Create | prism init command |
| `crates/prism-bin/src/commands/health.rs` | Create | prism health command |
| `crates/prism-bin/templates/prism.toml.tmpl` | Create | Template config for prism init |

---

## Previous Story Intelligence

- S-5.01 (Bootstrap): `PrismServer::new(context)` is the entry point for server
  initialization. The startup sequence ends by calling this.
- S-5.05 (Config Loading): `config::loader::load()` and `config::validation::validate()`
  are the first two steps in the startup sequence. Error handling follows their contracts.
- S-2.01 (RocksDB Init): `StorageEngine::open()` performs the schema version check
  and returns `Err(PrismError::SchemaMigrationRequired)` on mismatch. Catch this and
  exit with code 3.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | RocksDB schema version mismatch on `prism serve` | Prints migration instructions with expected/actual versions; exits with code 3 |
| EC-002 | Config has 2+ validation errors on `prism serve --dry-run` | All errors printed with TOML paths; exits with code 1 |
| EC-003 | SIGTERM delivered to running server | Graceful shutdown within 5 seconds; RocksDB LOCK released; exit code 0 |
| EC-004 | `prism init` run in directory that already has `prism.toml` | Aborts with message; existing file not overwritten |
| EC-005 | `prism health` run without state directory present | Succeeds (no RocksDB opened); only network connectivity checked |
| EC-006 | Shutdown takes longer than 5 seconds | Force exit with code 1 |

---

## Dev Notes

- The template `prism.toml` created by `prism init` is the primary onboarding surface
  for new operators. Make it readable: include comment blocks explaining each section,
  example values with realistic-looking (but clearly fake) credentials, and a link to
  documentation.
- The startup sequence is intentionally fail-fast up through config validation, then
  transitions to structured error reporting for storage and subsystem issues. Operators
  need config problems caught before any resource allocation happens.
- `prism health` is designed to be runnable without a state directory, making it useful
  in CI/CD pipelines to validate credential and network connectivity before deploying.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.6 | pass-60-fix | 2026-04-20 | story-writer | Fixed MED-003: populated subsystems: [SS-06, SS-10] to match anchor_subsystem: ["SS-06", "SS-10"]. MED-001: renumbered duplicate Changelog version rows for monotonicity. |
| 1.4 | pass-59-fix | 2026-04-20 | story-writer | Fixed HIGH-001 (inputs: missing specs/ prefix + stem-only BC paths → full slugged paths) and HIGH-002 (anchor_capabilities: [CAP-006, CAP-010] → [CAP-009, CAP-034] per BC-INDEX union: BC-2.10.001/006/010→CAP-034, BC-2.06.006→CAP-009). |
| 1.3 | step5-remediation | 2026-04-20 | story-writer | IMP-001-A: Added S-6.04 and S-6.05 to blocks (CLI startup must complete before config validation commands and sensor status commands are available). Closes IMP-001-A bidirectionality gap for S-6.01→S-6.04 and S-6.01→S-6.05 edges. |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
