---
story_id: S-4.04
title: "prism-operations: Detection Evaluation (Single/Correlation/Sequence)"
wave: 4
target_module: prism-operations
subsystems: [SS-13]
priority: P0
depends_on: [S-4.03]
blocks: [S-4.05]
estimated_days: 3
points: 5
level: "L4"
status: draft
document_type: story
version: "1.1"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: null
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-4"
phase: 2
behavioral_contracts: [BC-2.13.002, BC-2.13.003, BC-2.13.004, BC-2.13.012, BC-2.13.013]
verification_properties: [VP-027]
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.13.002, BC-2.13.003, BC-2.13.004, BC-2.13.012, BC-2.13.013]
anchor_capabilities: [CAP-017]
anchor_subsystem: ["SS-13"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.13.002.md
  - .factory/specs/behavioral-contracts/BC-2.13.003.md
  - .factory/specs/behavioral-contracts/BC-2.13.004.md
  - .factory/specs/behavioral-contracts/BC-2.13.012.md
  - .factory/specs/behavioral-contracts/BC-2.13.013.md
  - .factory/specs/verification-properties/VP-027.md
---

# S-4.04 — prism-operations: Detection Evaluation (Single/Correlation/Sequence)

## Narrative

As a Prism detection engine, I want to evaluate detection rules against incoming query
results using single-event, correlation, and sequence detection modes, so that analyst-
defined rules automatically fire alerts when suspicious patterns emerge across live sensor
data without requiring manual review of every query result.

## Objective

Implement the three detection evaluation modes (single-event predicate per record,
threshold-over-sliding-window correlation with reset-after-fire, and ordered multi-event
sequence matching with timeout), detection state persistence in RocksDB, alert
deduplication per match mode, and proper resource management ensuring `SessionContext` is
dropped on all error and panic paths.

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.13.002 | Single-Event Detection — Evaluate Rule Predicate Against Each Differential Record |
| BC-2.13.003 | Correlation Detection — Threshold Over Sliding Window with Group-By, Reset-After-Fire |
| BC-2.13.004 | Sequence Detection — Ordered Multi-Event Pattern Matching Within Time Window |
| BC-2.13.012 | Detection State Persistence — RocksDB Domain for Correlation Windows, Sequence State, Alert History |
| BC-2.13.013 | Alert Deduplication — Per-Match-Mode Dedup Keys Prevent Duplicate Alerts |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,500 |
| evaluator.rs (DetectionEvaluator orchestrator, events MemTable setup) | ~2,000 |
| single.rs (single-event predicate evaluation) | ~1,200 |
| correlation.rs (sliding window, group_by, reset-after-fire, group key cap) | ~2,500 |
| sequence.rs (ordered pattern matching, partial match tracker, timeout) | ~2,500 |
| state.rs (RocksDB detection_state CF, length-prefix key encoding) | ~2,000 |
| dedup.rs (per-mode dedup key computation, dedup check) | ~1,200 |
| tests/eval_tests.rs | ~2,500 |
| props/vp027_dedup_keys.rs (proptest) | ~600 |
| Total | ~17,000 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Implement `DetectionEvaluator` in
   `crates/prism-operations/src/detection/evaluator.rs`:
   - Entry point: `evaluate(added_records: &[Row], rules: &[CompiledRule]) -> Vec<Alert>`
   - Input: `added_records` from S-4.02 `DiffResult.added` — removed records are NOT
     evaluated by the detection engine
   - For each rule: register `added_records` as the `events` DataFusion MemTable, execute
     the compiled SQL via the shared `SessionContext`, collect matches
   - `SessionContext` lifecycle: wrap in `scopeguard::defer!` to guarantee drop on all
     exit paths including panics

2. Implement single-event evaluation in
   `crates/prism-operations/src/detection/single.rs` (BC-2.13.002):
   - For each `Row` in `added_records`: evaluate the compiled WHERE clause against that
     single row
   - Efficient path: use DataFusion's `RecordBatch` API to evaluate predicates in batch
     rather than row-by-row
   - On match: call dedup check, then call alert generator (S-4.05)

3. Implement correlation evaluation in
   `crates/prism-operations/src/detection/correlation.rs` (BC-2.13.003):
   - Sliding window: window boundaries are anchored to `event_time` field of the incoming
     records, NOT to `now()`. This ensures replay and backtest correctness.
   - `group_by` key support: records are grouped by the specified field before threshold
     counting. Each group key tracks its own counter independently.
   - Per-rule group key cap: default 10,000 group keys per rule. When the cap is reached,
     new group keys are silently dropped (not queued). Log a warning with rule ID.
   - Reset-after-fire: once a correlation rule fires for a group key, that key's counter
     resets to 0. The window does not reset — only the count.
   - State persistence: correlation window state stored in RocksDB `detection_state` CF
     under keys prefixed `\x00` (group state)

4. Implement sequence evaluation in
   `crates/prism-operations/src/detection/sequence.rs` (BC-2.13.004):
   - Ordered multi-event pattern: `[event_A, event_B, event_C]` where each event is a
     predicate that must match in order
   - Partial match tracking: maintain a `SequenceTracker` per (rule, entity_id) pair that
     records which steps have been satisfied
   - Timeout: partial matches older than `sequence.timeout` (required field in rule
     definition) are evicted from state
   - Sequence fires when all steps are satisfied for the same entity within the timeout
     window
   - State persistence: sequence trackers stored in RocksDB `detection_state` CF under
     keys prefixed `\x00` (group state, same prefix as correlation)

5. Create the `detection_state` RocksDB column family in
   `crates/prism-operations/src/detection/state.rs` (BC-2.13.012):
   - CF name: `"detection_state"`
   - Key encoding with length-prefix byte:
     - `\x00{rule_id}:{group_key}` — correlation window and sequence tracker state
     - `\x01{rule_id}:{group_key}` — rate limit counters
     - `\x02{rule_id}:{dedup_key}` — alert deduplication entries
   - Rate limit counter writes MUST be performed under a `Mutex` to prevent concurrent
     increment races (CI-004 compliant)
   - Eviction policy:
     - `\x00` (group state): evict stale entries on spec reload (7-day default)
     - `\x01` (rate_limit): NOT evicted by age — they expire naturally as they are
       rewritten each hour
     - `\x02` (dedup): NOT evicted by age — they persist for the dedup window duration

6. Implement alert deduplication in
   `crates/prism-operations/src/detection/dedup.rs` (BC-2.13.013):
   - `Single` mode dedup key: `blake3(rule_id + record_hash)` — a given record that
     already fired a rule won't fire again within the dedup window
   - `Correlation` mode dedup key: `blake3(rule_id + group_key + window_start_epoch)`
     — same group + window combination fires at most once
   - `Sequence` mode dedup key: `blake3(rule_id + entity_id + sequence_completion_epoch)`
     — same entity completing the same pattern fires at most once per completion
   - Dedup check: look up `\x02{rule_id}:{dedup_key}` in `detection_state` CF; if present
     and not expired, suppress alert; if absent, write entry with TTL and proceed
   - Dedup window: configurable per rule via `dedup_window` field (default: `interval`
     of the parent schedule, or 1 hour if no parent schedule)

7. Write VP-027 proptest in `crates/prism-operations/props/vp027_dedup_keys.rs`:
   - Property: for any two distinct (rule, record) pairs, their single-mode dedup keys
     are different. For equal pairs, keys are identical.
   - Also verify: single, correlation, and sequence modes produce structurally distinct
     keys (different key prefixes or formats) so cross-mode collisions are impossible.

---

## Acceptance Criteria

**AC-1:** Given a single-event rule `severity_id >= 4`, When a record with
`severity_id = 5` is in `added_records`, Then an alert fires for that record.
(BC-2.13.002)

**AC-2:** Given a single-event rule `severity_id >= 4`, When a record with
`severity_id = 3` is in `added_records`, Then no alert fires. (BC-2.13.002)

**AC-3:** Given a correlation rule `count >= 5 within 10m group_by src_ip`, When 5
records with the same `src_ip` arrive within 8 minutes, Then an alert fires and the
counter for that `src_ip` resets to 0. (BC-2.13.003)

**AC-4:** Given a correlation rule with a group key cap of 10,000, When the 10,001st
distinct group key arrives, Then it is silently dropped (no alert, no error) and a
warning is logged. (BC-2.13.003)

**AC-5:** Given a sequence rule `[login_failure, login_failure, login_success]` with a
2-minute timeout, When three matching events arrive in order within 90 seconds for the
same `user_id`, Then the sequence fires. (BC-2.13.004)

**AC-6:** Given a sequence rule with a 2-minute timeout, When the third event arrives
3 minutes after the first, Then the sequence does NOT fire (timeout exceeded) and the
partial match tracker is evicted. (BC-2.13.004)

**AC-7:** Given detection state at epoch N persisted to RocksDB, When Prism restarts,
Then correlation windows and sequence trackers are recovered and evaluation continues
without resetting counters. (BC-2.13.012)

**AC-8:** Given a single-event alert that already fired for record R, When R appears
again in `added_records` within the dedup window, Then no duplicate alert fires.
(BC-2.13.013)

**AC-9:** VP-027 proptest passes — dedup keys are distinct for distinct inputs and
identical for equal inputs; cross-mode collisions do not occur.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Detection rule evaluation engine | `prism-operations` (SS-13) | Mixed |
| Detection state persistence | `prism-storage` (SS-15) | Effectful |

`prism-operations` implements the detection evaluation loop — on each scheduler tick, runs
compiled detection SQL against differential results batches. Rule match/no-match logic is
pure; RocksDB state reads/writes (`detection_state` CF) and query execution calls are
effectful. Per `architecture/module-decomposition.md`, `DetectionEngine` is called by
`Scheduler` and depends on `QueryEngine` and `StorageBackend`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-operations` (detection eval) | Mixed | Pure condition evaluation logic; effectful for RocksDB state and query dispatch. Mixed per `architecture/purity-boundary-map.md`. |
| `prism-storage` (detection_state CF) | Effectful | RocksDB reads/writes. Effectful-shell. |

---

## Verification Properties

| VP ID | Type | Property |
|-------|------|----------|
| VP-027 | Proptest | Dedup key is correct per match mode: distinct inputs → distinct keys; same inputs → same key; no cross-mode collisions |

---

## Architecture Compliance Rules

- Detection evaluation receives `added_records` ONLY from the diff engine. Removed
  records are never evaluated — a disappearing row is not a detection event.
- `SessionContext` MUST be wrapped in `scopeguard::defer!` before any fallible or async
  operation. This is the CI guarantee against context leaks (VP-036 validates this).
- Detection evaluation reuses the per-schedule `Arc<SessionContext>` returned by
  `QueryEngine::execute_scheduled()` (S-3.02) — it does NOT create a separate pool
  AND does NOT use a global shared pool. Each scheduled query gets its own ephemeral
  200MB `GreedyMemoryPool`; detection operates within that budget.
- VP-036 (SessionContext drop on error) is an integration test owned by S-6.07
  (prism-dtu-crowdstrike). S-4.04 implements detection evaluation with proper session
  cleanup; S-6.07's DTU provides the test vehicle via its FailureLayer fault injection.
- Correlation window timestamps MUST be anchored to `event_time` (record field), not
  `Instant::now()`. Using wall clock time breaks replay correctness and makes tests
  non-deterministic.
- Rate limit counter writes MUST be under `Mutex` (CI-004). Do not use atomic integers
  for rate limit state — the RocksDB write + in-memory counter combination requires
  mutual exclusion.
- The `detection_state` CF key prefix byte (`\x00`, `\x01`, `\x02`) MUST be the FIRST
  byte of every key to enable efficient prefix scans for eviction and state recovery.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| datafusion | 53 | MemTable registration, SQL execution for detection |
| scopeguard | 1.x | SessionContext drop guarantee on all exit paths |
| blake3 | 1.x | Dedup key hashing |
| rocksdb | 0.24 | detection_state CF, length-prefix key encoding |
| proptest | 1.x | VP-027 property-based test |
| prism-core | workspace | PrismError, Alert types, SeverityId |
| prism-storage | workspace | RocksDB CF helpers |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-operations/src/detection/evaluator.rs` | Create | DetectionEvaluator orchestrator, MemTable setup |
| `crates/prism-operations/src/detection/single.rs` | Create | Single-event predicate evaluation |
| `crates/prism-operations/src/detection/correlation.rs` | Create | Correlation detection, sliding window, group key cap |
| `crates/prism-operations/src/detection/sequence.rs` | Create | Sequence detection, partial match tracker |
| `crates/prism-operations/src/detection/state.rs` | Create | detection_state CF, length-prefix key helpers |
| `crates/prism-operations/src/detection/dedup.rs` | Create | Per-mode dedup key computation and check |
| `crates/prism-operations/src/detection/tests/eval_tests.rs` | Create | Evaluation tests for all three modes |
| `crates/prism-operations/props/vp027_dedup_keys.rs` | Create | VP-027 proptest |

---

## Previous Story Intelligence

S-4.03 established the detection rule types (`DetectionRule`, `CompiledRule`,
`RuleCondition`), the compiler, UDFs, and the `detection_rules` RocksDB CF. This story
consumes those and adds the runtime evaluation engine and `detection_state` CF.

The `GreedyMemoryPool` sharing pattern from S-3.02 must be understood before
implementing the evaluator. The evaluator should accept an `Arc<SessionContext>` from the
query engine rather than creating a new one, to stay within the shared memory budget.

S-4.02's `DiffResult.added` is the input to this evaluator. The integration between
S-4.02 (diff engine) and S-4.04 (detection evaluator) happens in the schedule executor
(S-4.01): after `compute_diff`, the added records are passed to
`DetectionEvaluator::evaluate`.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Single-event rule evaluates a record where `event_time` field is null | Rule predicate evaluated normally against other fields; null `event_time` does not crash the evaluator |
| EC-002 | Correlation rule group key cap (10,000) reached; 10,001st distinct key arrives | Silently dropped; WARN log emitted with rule ID; no alert, no error |
| EC-003 | Sequence rule times out before all steps satisfied | Partial match tracker evicted from `detection_state` CF; no alert fires |
| EC-004 | Same record appears in `added_records` for two concurrent rule evaluations | Each rule evaluates the record independently; dedup key prevents duplicate alerts from the same (rule, record) pair |
| EC-005 | Dedup key lookup finds existing entry within dedup window | Alert suppressed; no duplicate written to `alerts` CF; no broadcast |
| EC-006 | `SessionContext` panics during rule SQL execution | `scopeguard::defer!` ensures `SessionContext` is dropped on all exit paths including panics |
| EC-007 | RocksDB write for `detection_state` CF fails mid-evaluation | Error propagated; no partial state written; rate limit counter not incremented |
| EC-008 | `removed_records` passed to `DetectionEvaluator::evaluate` | Evaluator ignores removed records; only `added_records` are evaluated (by design) |

## Dev Notes

- Correlation sliding windows use `event_time` anchoring. This means the window boundary
  for a record is `[record.event_time - window_duration, record.event_time]`. Records
  older than the window are excluded from the count even if they arrived recently. This
  enables correct replay but requires that `event_time` is populated in all records.
- The 10,000 group key cap per rule is the default. Rules with high-cardinality group
  keys (e.g., grouping by user_id in a large enterprise) may need this cap raised.
  Expose it as a per-rule configuration field in the `CorrelationSpec`.
- Sequence matching is inherently stateful and grows with the number of entities being
  tracked. The timeout eviction is critical to bounding state size. Ensure the eviction
  runs on every tick, not just when new events arrive for a given entity.
- The `\x01` (rate_limit) keys in `detection_state` are written by S-4.05's alert
  generation, not by this story. This story creates the CF and defines the key encoding
  scheme. S-4.05 writes to `\x01` keys.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.1 | Wave-5-patch | 2026-04-20 | story-writer | Pre-build sweep: added level, points, blocks, anchor_bcs/capabilities/subsystem, assumption_validations, risk_mitigations, inputs frontmatter; added ## Edge Cases section (synthesized from BC-2.13.002/003/004/012/013 boundary cases); renamed ## Library and Framework Requirements → ## Library & Framework Requirements; renamed ## Notes → ## Dev Notes. |
