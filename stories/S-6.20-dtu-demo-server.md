---
story_id: S-6.20
title: "prism-dtu-demo-server: Unified Multi-Clone Demo Harness"
wave: 1
level: "harness"
target_module: prism-dtu-demo-server
subsystems: [SS-18]
priority: P1
depends_on: [S-6.06, S-6.07, S-6.08, S-6.09, S-6.10, S-6.14, S-6.15]
blocks: []
estimated_days: 3
points: 5
status: draft
document_type: story
version: "1.0"
producer: story-writer
timestamp: "2026-04-22T00:00:00Z"
input-hash: ""
traces_to: ""
inputs:
  - .factory/stories/S-6.06-dtu-common.md
  - .factory/stories/S-6.09-dtu-cyberint.md
  - .factory/specs/architecture/decisions/ADR-002-l2-dtu-clone-template.md
  - .factory/specs/architecture/dtu-assessment.md
cycle: "v1.0.0-greenfield"
epic_id: "E-6"
phase: 2
behavioral_contracts: []
verification_properties: []
assumption_validations: []
risk_mitigations: [R-DEMO-001, R-DEMO-002]
anchor_bcs: []
anchor_capabilities: []
anchor_subsystem: SS-18
---

# S-6.20 — prism-dtu-demo-server: Unified Multi-Clone Demo Harness

## Narrative

As a Prism analyst, demo presenter, or CI system, I want a single command that starts
multiple DTU clones with configurable ports, fixture sets, and failure modes, so that
live demos and recorded walkthroughs work identically, and so production Prism can be
pointed at the harness via a matching config file.

## Objective

Implement `prism-dtu-demo-server` — a binary crate that boots multiple DTU clones in a
single process for both live demos (analyst-facing walkthroughs, stakeholder presentations,
sales demos) and recorded demos (VHS tapes, shell transcripts, CI regression artifacts).

Closes the following gaps identified in DTU design review:

- Only `S-6.09` ships a `demo_server` binary; no multi-clone launcher exists.
- Per-clone demo servers each hardcode a fixed port — no coordinated port table.
- Static fixture sets cannot be overridden at runtime without recompiling.
- All existing demo servers are plain HTTP only; TLS is required for stakeholder demos.

This crate orchestrates the seven merged DTU clones (S-6.07..S-6.10, S-6.14, S-6.15)
via their `BehavioralClone` trait interfaces. It does NOT duplicate any clone logic.

---

## Behavioral Contracts

No new product-level BCs — this is test/demo infrastructure only. Architecture anchor:
`dtu-assessment.md §4` (Delivery Model) and `dtu-assessment.md §6` (Integration with
Test Infrastructure).

| Reference | Role |
|-----------|------|
| `dtu-assessment.md §4` | Defines `BehavioralClone` as the uniform fleet management interface |
| `dtu-assessment.md §6` | Cross-cutting DTU integration rules: ephemeral ports, feature gating, no production deps |
| ADR-002 §6 | Required `/dtu/configure`, `/dtu/reset`, `/dtu/health` endpoints on every clone |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~3,500 |
| `prism-dtu-demo-server/Cargo.toml` | ~400 |
| `src/main.rs` (CLI entry point, Clap subcommands) | ~600 |
| `src/config.rs` (`DemoConfig` TOML schema) | ~500 |
| `src/harness.rs` (multi-clone boot + URL table) | ~800 |
| `src/tls.rs` (optional rcgen self-signed cert) | ~500 |
| `src/seed_endpoint.rs` (`POST /dtu/<clone>/seed`) | ~500 |
| `configs/demo.toml` (canonical config) | ~300 |
| `configs/prism-demo.toml` (Prism production preset) | ~300 |
| `crates/prism-dtu-common/src/clone.rs` (seed() trait extension) | ~200 |
| Total | ~7,600 |

Well within the 30% context window budget for a single implementation session.

---

## Tasks

1. Create `crates/prism-dtu-demo-server/Cargo.toml`:
   - `[[bin]] name = "prism-dtu-demo-server"`
   - `publish = false`, `description = "Unified multi-clone demo harness for Prism DTU clones"`
   - `[features] dtu = ["prism-dtu-crowdstrike/dtu", "prism-dtu-claroty/dtu", "prism-dtu-cyberint/dtu", "prism-dtu-armis/dtu", "prism-dtu-threatintel/dtu", "prism-dtu-nvd/dtu"]`
   - Optional feature `tls = ["rcgen", "rustls"]`
   - `[lints] workspace = true`
   - Dependencies: `prism-dtu-common` (path), `prism-dtu-crowdstrike` (path),
     `prism-dtu-claroty` (path), `prism-dtu-cyberint` (path), `prism-dtu-armis` (path),
     `prism-dtu-threatintel` (path), `prism-dtu-nvd` (path), `tokio` (1.x full),
     `axum` (0.7.x), `serde` (1.x derive), `toml` (0.8.x), `clap` (4.x derive),
     `tracing` (0.1.x), `tracing-subscriber` (0.3.x), `anyhow` (1.x)
   - Optional deps: `rcgen` (0.13.x), `rustls` (0.23.x)

2. Extend `prism-dtu-common` `BehavioralClone` trait with a `seed()` default method:
   ```rust
   async fn seed(&self, payload: serde_json::Value) -> anyhow::Result<()> {
       let _ = payload;
       Err(anyhow::anyhow!("UnsupportedOperation: seed() not implemented for this clone"))
   }
   ```
   This is an additive, backwards-compatible change. All existing clones inherit the
   default `UnsupportedOperation` response. Clones that support seeding override this
   method. Document the method in `prism-dtu-common/src/clone.rs` with a doc comment
   explaining demo-harness use.

3. Define `DemoConfig` TOML schema in `src/config.rs`:
   ```toml
   [clones.crowdstrike]
   enabled = true
   port = 17080          # 0 = ephemeral
   fixture_set = "default"
   initial_failure_mode = "none"
   seed = 42
   tls = false

   [clones.claroty]
   enabled = true
   port = 17081
   # ... same fields
   ```
   All fields have defaults; minimal config is `[clones.<name>] enabled = true`.

4. Implement `src/main.rs` with Clap subcommands:
   - `prism-dtu-demo-server start --config <path> [--tls] [--bind-any]`
     - Validates config; prints field-level errors on invalid TOML
     - Starts each enabled clone on its configured port (or ephemeral if `port = 0`)
     - Prints URL table to stdout:
       ```
       | Clone         | URL                       | Fixture | Failure |
       |---------------|---------------------------|---------|---------|
       | crowdstrike   | http://127.0.0.1:17080    | default | none    |
       | claroty       | http://127.0.0.1:17081    | default | none    |
       ```
     - Blocks on SIGINT/SIGTERM; logs graceful shutdown and stops all clones
   - `prism-dtu-demo-server stop` — sends SIGTERM to a backgrounded harness PID (reads
     `.prism-dtu-demo-server.pid`)
   - `prism-dtu-demo-server configure <clone> <json>` — convenience wrapper over
     `POST http://127.0.0.1:{port}/dtu/configure`

5. Implement `src/harness.rs` — `DemoHarness`:
   - Holds `Vec<Box<dyn BehavioralClone>>` with clone metadata (name, configured port)
   - `start_all()`: iterates; binds each clone; collects `(name, url, fixture, failure)` rows
   - `stop_all()`: calls `reset()` on each clone (signals shutdown via tokio task abort)
   - `print_url_table()`: formats and prints the URL table to stdout

6. Implement `POST /dtu/<clone>/seed` admin endpoint in `src/seed_endpoint.rs`:
   - Registered by the harness as a top-level axum route alongside clone servers
   - Calls `clone.seed(payload)` for the named clone
   - Returns 200 `{"status":"ok"}` on success, 404 if clone name not found,
     400 if clone returns `UnsupportedOperation`, 413 if payload > 1 MiB
   - Bind address for admin routes is `127.0.0.1` by default; only `--bind-any`
     flag permits non-loopback (same rule as admin endpoints in ADR-002 §6)

7. Implement optional TLS in `src/tls.rs` (only compiled with `features = ["tls"]`):
   - Uses `rcgen` to generate a self-signed certificate at startup (ECDSA-P256, 30-day validity)
   - Serves clone HTTPS via `rustls`
   - Prints cert fingerprint to stdout so analysts can add to trust store for demos
   - `main.rs` emits a clearly visible warning: `[DEMO-GRADE TLS] Self-signed cert. Not for production.`

8. Provide canonical configs:
   - `configs/demo.toml`: all 7 merged clones enabled on sequential ports
     (crowdstrike=17080, claroty=17081, cyberint=17082, armis=17083,
     threatintel=17084, nvd=17085; one port per clone, stable across recordings)
   - `configs/prism-demo.toml`: Prism production config preset pointing sensors at
     the demo server URLs (so `prism start --config configs/prism-demo.toml` routes
     all queries through the harness)

9. Provide `crates/prism-dtu-demo-server/README.md`:
   - Live demo example: `cargo run -p prism-dtu-demo-server --features dtu -- start --config configs/demo.toml`
   - Recorded demo example with VHS: how to use the URL table to parameterise VHS tape env vars
   - Seeding example: `curl -XPOST http://127.0.0.1:17080/dtu/crowdstrike/seed -d @fixture-override.json`
   - TLS example: `cargo run -p prism-dtu-demo-server --features dtu,tls -- start --config configs/demo.toml --tls`

---

## Acceptance Criteria

**AC-1:** Given a valid `demo.toml` enabling all 7 merged clones (`crowdstrike`, `claroty`,
`cyberint`, `armis`, `threatintel`, `nvd`), when `prism-dtu-demo-server start --config
demo.toml` runs, then all 7 clones bind their configured ports and a URL table is printed
to stdout with one row per enabled clone.

**AC-2:** Given the demo server is running, when an analyst runs
`curl http://127.0.0.1:17080/devices/queries/devices/v1 -H "Authorization: Bearer x"`,
then the response HTTP status and body structure match the S-6.07 CrowdStrike fixture
contract (cursor pagination, `resources` array).

**AC-3:** Given the demo server is running, when `POST /dtu/crowdstrike/seed` is called
with a valid fixture JSON payload (≤1 MiB), then subsequent queries to the CrowdStrike
clone reflect the new fixture data. If the clone returns `UnsupportedOperation`, then the
seed endpoint returns HTTP 400 `{"error": "UnsupportedOperation: ..."}`.

**AC-4:** Given the demo server is started with `--tls` and the `tls` feature enabled,
when a client connects over HTTPS using the auto-generated self-signed certificate, then
the TLS handshake succeeds and the request reaches the clone (verified via `curl --cacert
<cert> https://127.0.0.1:<port>/dtu/health`).

**AC-5:** Given the demo server is running, when SIGTERM is received (or Ctrl-C), then
all clone servers stop gracefully within 5 seconds and the process exits with code 0.

**AC-6:** Given the canonical `configs/prism-demo.toml`, when production Prism is
started with `prism start --config configs/prism-demo.toml`, then Prism sensor adapters
resolve to the demo server URLs and all sensor queries are routed through the harness
(verified by querying `prism://sensors/health` and seeing 7 clones reported healthy).

**AC-7:** Given a VHS tape uses the demo server as its backend with `seed = 42` (default),
when the tape is re-run without any `seed()` calls, then the clone output is byte-identical
across runs (deterministic seeding respected per `prism-dtu-common::seeded_rng`).

**AC-8:** Given `cargo build -p prism-dtu-demo-server` without `--features dtu`, then
the build succeeds but the `prism-dtu-demo-server` binary is not produced (or emits a
compile error referencing the `dtu` feature), enforcing that the harness is never compiled
into non-DTU builds. (Implementation: `#![cfg(feature = "dtu")]` on `main.rs`.)

**AC-9 (ADR-002 admin endpoint bind rule):** Given the demo server is started without
`--bind-any`, when a `POST /dtu/<clone>/seed` request arrives on a non-loopback interface,
then the server rejects the bind and logs an error pointing to `--bind-any`.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| `DemoHarness` | `crates/prism-dtu-demo-server/src/harness.rs` | Effectful (manages multiple async HTTP servers) |
| `DemoConfig` | `crates/prism-dtu-demo-server/src/config.rs` | Pure (TOML deserialization) |
| CLI entry point | `crates/prism-dtu-demo-server/src/main.rs` | Effectful (Tokio runtime, SIGTERM) |
| Seed endpoint | `crates/prism-dtu-demo-server/src/seed_endpoint.rs` | Effectful (HTTP handler, delegates to clone) |
| TLS support | `crates/prism-dtu-demo-server/src/tls.rs` | Effectful (cert generation, TLS listener) |
| `BehavioralClone::seed()` extension | `crates/prism-dtu-common/src/clone.rs` | Pure default method (returns Err) |

Subsystem anchor: SS-18 owns this story's scope because `prism-dtu-demo-server` is
test/demo infrastructure — a direct extension of the DTU fleet managed under SS-18
(Action Delivery + DTU infra) per the ARCH-INDEX Subsystem Registry. The harness
orchestrates clones that expose the DTU surface to the Prism sensor adapters (SS-01,
SS-08, SS-18, SS-19) without adding product-level BCs.

Reference architecture files: `architecture/module-decomposition.md` (COMP-DTU-005),
`architecture/decisions/ADR-002-l2-dtu-clone-template.md` (canonical L2 clone template),
`architecture/dtu-assessment.md §4` (delivery model).

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-dtu-demo-server` | Effectful | Manages multiple async HTTP server lifecycles, SIGTERM handling, TOML config I/O |
| `DemoConfig` | Pure | Stateless TOML deserialization; no I/O |
| `BehavioralClone::seed()` (default) | Pure | Default impl returns an Err value; no I/O |

---

## Architecture Compliance Rules

- MUST NOT compile into production binaries: `#![cfg(feature = "dtu")]` on `src/main.rs`
  (same gate used by `prism-dtu-cyberint/src/bin/demo_server.rs` — see S-6.09 precedent).
- MUST respect ADR-002 L2 clone template: does NOT duplicate clone logic; only
  orchestrates existing clones through the `BehavioralClone` trait.
- Demo-only admin endpoints (`POST /dtu/<clone>/seed` and TLS cert generation) MUST be
  loopback-only by default; reject bind addresses outside `127.0.0.1`/`::1` unless
  `--bind-any` flag is explicitly passed (mirrors ADR-002 §6 admin endpoint rule).
- MUST NOT depend on production Prism crates (`prism-sensors`, `prism-query`,
  `prism-operations`, `prism-mcp`, `prism-spec-engine`, `prism-audit`). Adding any of
  these to `Cargo.toml` MUST fail via cargo-deny rule.
- Canonical port assignment in `demo.toml` (17080–17085) MUST be stable across
  recordings to ensure VHS tape byte-identical replay (AC-7).
- The `BehavioralClone::seed()` extension in `prism-dtu-common` is additive and
  backwards-compatible: existing clones inherit the default `UnsupportedOperation` return.
  No existing clone implementation requires modification.
- `configs/demo.toml` and `configs/prism-demo.toml` MUST be committed to the repository
  under `configs/`; they are not generated at runtime.

**Forbidden dependencies:** `prism-dtu-demo-server` MUST NOT depend on `prism-sensors`,
`prism-query`, `prism-operations`, `prism-mcp`, `prism-spec-engine`, or `prism-audit`.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| prism-dtu-common | path dep | BehavioralClone trait + seed() extension |
| prism-dtu-crowdstrike | path dep | L4 clone (S-6.07) |
| prism-dtu-claroty | path dep | L4 clone (S-6.08) |
| prism-dtu-cyberint | path dep | L2 clone (S-6.09) |
| prism-dtu-armis | path dep | L2 clone (S-6.10) |
| prism-dtu-threatintel | path dep | L2 clone (S-6.14) |
| prism-dtu-nvd | path dep | L2 clone (S-6.15) |
| tokio | 1.x (full) | Async runtime, SIGTERM handling |
| axum | 0.7.x | Seed endpoint HTTP handler |
| clap | 4.x (derive) | CLI subcommand parsing |
| serde | 1.x (derive) | Config deserialization |
| toml | 0.8.x | TOML config file parsing |
| tracing | 0.1.x | Structured logging |
| tracing-subscriber | 0.3.x | Log output formatting |
| anyhow | 1.x | Error propagation |
| rcgen | 0.13.x (optional, `tls`) | Self-signed certificate generation |
| rustls | 0.23.x (optional, `tls`) | TLS listener |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-dtu-demo-server/Cargo.toml` | Create | Binary crate manifest |
| `crates/prism-dtu-demo-server/src/main.rs` | Create | CLI entry point + Clap subcommands |
| `crates/prism-dtu-demo-server/src/config.rs` | Create | DemoConfig TOML schema |
| `crates/prism-dtu-demo-server/src/harness.rs` | Create | DemoHarness multi-clone boot + URL table |
| `crates/prism-dtu-demo-server/src/seed_endpoint.rs` | Create | POST /dtu/<clone>/seed admin endpoint |
| `crates/prism-dtu-demo-server/src/tls.rs` | Create | Optional TLS support (feature-gated) |
| `crates/prism-dtu-demo-server/README.md` | Create | Demo usage documentation |
| `configs/demo.toml` | Create | Canonical demo config (all 7 merged clones) |
| `configs/prism-demo.toml` | Create | Prism production preset pointing at demo server |
| `crates/prism-dtu-common/src/clone.rs` | Modify | Add seed() default method to BehavioralClone |
| `Cargo.toml` (workspace) | Modify | Add prism-dtu-demo-server to workspace members |

---

## Previous Story Intelligence

- S-6.09 (prism-dtu-cyberint): Established the single-clone demo server pattern in
  `src/bin/demo_server.rs`. This story generalizes that pattern to a multi-clone harness.
  The S-6.09 `demo_server.rs` binary (20 lines: `CyberintClone::new()` → `start()` →
  `println!("READY http://{addr}")` → park on `ctrl_c()`) is the direct precedent.
  S-6.20 supersedes it; the S-6.09 bin can remain for backwards compat or be deprecated.
- S-6.06 (prism-dtu-common): Provides `BehavioralClone` trait. The `seed()` default
  method extension in this story is additive — no breaking change to S-6.06's contract.
- ADR-002: All admin endpoints must follow loopback-only rule (§6). S-6.20 enforces this
  for the seed endpoint and the TLS cert admin surface.
- S-6.07..S-6.10, S-6.14, S-6.15: All merged. Their `BehavioralClone` implementations
  are the direct dependencies. S-6.20 does not re-implement any of their logic.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Port conflict: configured port already bound by another process | Print `[ERROR] Port 17080 already in use (EADDRINUSE). Choose a different port in demo.toml.` and exit 1 |
| EC-002 | Invalid TOML config | Validate before starting any clones; print field-level errors (e.g. `[clones.crowdstrike.port]: invalid type — expected integer, got string`); exit 1 |
| EC-003 | Clone crate not compiled in (cfg gate miss) | Emit a `[WARN] prism-dtu-<name> not compiled — skipping. Enable with --features dtu` and skip; do not abort harness boot |
| EC-004 | Seed payload too large (>1 MiB) | Reject with HTTP 413 `{"error": "payload too large — max 1 MiB"}` |
| EC-005 | Simultaneous `POST /dtu/configure` calls on different clones | Each clone holds its own `Arc<State>` with independent locks — no cross-clone contention; both succeed concurrently |
| EC-006 | `prism-dtu-demo-server start` called without `--features dtu` (binary absent) | Cargo does not produce the binary; the user sees a cargo error. `main.rs` `#![cfg(feature = "dtu")]` enforces this at compile time |
| EC-007 | `--tls` flag without `tls` feature compiled in | Print `[ERROR] TLS support not compiled. Rebuild with --features dtu,tls.` and exit 1 |
| EC-008 | SIGTERM received while a clone is mid-request | Tokio graceful shutdown: in-flight requests complete within 5s timeout, then force-abort |

---

## Risks

| Risk ID | Description | Mitigation |
|---------|-------------|-----------|
| R-DEMO-001 | Demo server accidentally bound to non-loopback in a stakeholder environment — exposes internal fixture data on a network interface | Default bind to `127.0.0.1`/`::1`; non-loopback bind requires explicit `--bind-any` flag; log a prominent `[SECURITY WARNING]` when `--bind-any` is active |
| R-DEMO-002 | Dynamic fixture seeding via `POST /dtu/<clone>/seed` breaks VHS tape determinism | Log every seed call with timestamp and payload hash to stderr; provide `POST /dtu/<clone>/reset` endpoint to restore fixture state; document seed + reset in README |

---

## Dependencies

Explicit `depends_on` per frontmatter:
- S-6.06 (prism-dtu-common) — `BehavioralClone` trait base; `seed()` extension lands here
- S-6.07 (prism-dtu-crowdstrike) — L4 clone; must be merged before harness can import it
- S-6.08 (prism-dtu-claroty) — L4 clone; must be merged
- S-6.09 (prism-dtu-cyberint) — L2 clone; must be merged
- S-6.10 (prism-dtu-armis) — L2 clone; must be merged
- S-6.14 (prism-dtu-threatintel) — L2 clone; must be merged
- S-6.15 (prism-dtu-nvd) — L2 clone; must be merged

S-6.07..S-6.10 are all merged (wave-1 DTU slice complete 2026-04-22). S-6.14..S-6.15 are
merged (wave-0c complete 2026-04-22). All `depends_on` entries are satisfied; S-6.20 can
be dispatched immediately.

Blocks: none. S-6.20 does not gate any product stories.

**Wave placement rationale:** Wave 1 is correct. All dependencies are Wave 0 or Wave 1
(and all are already merged). S-6.20 blocks nothing in Wave 2+. Placing it in Wave 1
allows demo infrastructure to be available before Wave 2 product stories begin
implementation — useful for stakeholder demos during the Wave 2 build window.

**Dependency anchor justifications:**
- S-6.20 depends on S-6.06 because `BehavioralClone::seed()` is added to `prism-dtu-common`;
  S-6.06 must be merged and the trait stable before S-6.20 extends it.
- S-6.20 depends on S-6.07..S-6.10, S-6.14, S-6.15 because the harness `Cargo.toml`
  lists them as path dependencies; their crates must exist and compile before S-6.20
  can build against them.
- S-6.20 blocks nothing because it is additive infrastructure; no product story requires
  the unified harness to be present for its own build or test.

---

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.0 | S-6.20-draft | 2026-04-22 | story-writer | Initial story creation. |
