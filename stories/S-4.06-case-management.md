---
story_id: S-4.06
title: "prism-operations: Case Management"
wave: 4
target_module: prism-operations
subsystems: [SS-14]
priority: P0
depends_on: [S-4.05, S-2.01]
blocks: [S-4.07, S-4.08]
estimated_days: 3
points: 5
level: "L4"
status: draft
document_type: story
version: "1.5"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "8e43eb2"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-4"
phase: 2
behavioral_contracts: [BC-2.14.001, BC-2.14.002, BC-2.14.003, BC-2.14.004, BC-2.14.005, BC-2.14.006, BC-2.14.007, BC-2.14.009, BC-2.14.013]
verification_properties: [VP-052, VP-053, VP-054, VP-060]
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.14.001, BC-2.14.002, BC-2.14.003, BC-2.14.004, BC-2.14.005, BC-2.14.006, BC-2.14.007, BC-2.14.009, BC-2.14.013]
anchor_capabilities: [CAP-022]
anchor_subsystem: ["SS-14"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.14.001-create-case-tool.md
  - .factory/specs/behavioral-contracts/BC-2.14.002-case-state-transitions.md
  - .factory/specs/behavioral-contracts/BC-2.14.003-update-case-tool.md
  - .factory/specs/behavioral-contracts/BC-2.14.004-list-cases-tool.md
  - .factory/specs/behavioral-contracts/BC-2.14.005-get-case-tool.md
  - .factory/specs/behavioral-contracts/BC-2.14.006-disposition-assignment.md
  - .factory/specs/behavioral-contracts/BC-2.14.007-timeline-annotations.md
  - .factory/specs/behavioral-contracts/BC-2.14.009-case-persistence.md
  - .factory/specs/behavioral-contracts/BC-2.14.013-auto-case-creation.md
---

# S-4.06 — prism-operations: Case Management

## Narrative

As a Prism operations platform, I want a full case management subsystem with create,
transition, annotate, list, and retrieve operations, so that security analysts can
organize related alerts into cases, track investigation progress through a defined state
machine, and build a structured audit trail of all case activity.

## Objective

Implement case management: `create_case` from alert IDs, a 5-state case state machine
with 12 valid transitions, `update_case` for transitions/dispositions/annotations,
`list_cases` with filtering, `get_case` for full detail, disposition enforcement on
Resolved transitions, append-only timeline with 5 annotation types, and RocksDB
persistence for full restart recovery.

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.14.001 | `create_case` MCP Tool — Create Case from One or More Alerts |
| BC-2.14.002 | Case State Transitions — 5-State Machine with 12 Valid Transitions |
| BC-2.14.003 | `update_case` MCP Tool — Transition State, Set Disposition, Add Annotation |
| BC-2.14.004 | `list_cases` MCP Tool — Filter by Status, Client, Severity, Assignee |
| BC-2.14.005 | `get_case` MCP Tool — Full Case Detail with Timeline and Linked Alerts |
| BC-2.14.006 | Disposition Assignment — Required on Resolved Transition |
| BC-2.14.007 | Timeline Annotations — 5 Types: Note, StatusChange, AlertLink, EvidenceLink, OtImpact |
| BC-2.14.009 | Case Persistence — RocksDB Domain for Case State, Timeline, Disposition, Metrics |
| BC-2.14.013 | Auto-Case-Creation from High-Severity Detection Rules |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,800 |
| case.rs (Case, CaseStatus state machine, DispositionCode, TimelineEntry types) | ~2,500 |
| case/dedup.rs (decide_dedup_action pure fn, DedupDecision enum) | ~800 |
| create_case.rs (create_case tool handler) | ~1,500 |
| update_case.rs (update_case tool handler, transition + annotation) | ~2,000 |
| list_cases.rs (list_cases with filters and pagination) | ~1,500 |
| get_case.rs (get_case full detail) | ~1,200 |
| timeline.rs (append-only timeline, 5 annotation types) | ~1,500 |
| persistence.rs (RocksDB cases CF helpers) | ~1,500 |
| tests/case_tests.rs | ~2,500 |
| Total | ~17,800 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Define case types in `crates/prism-operations/src/cases/case.rs`:
   - `Case { id: CaseId, title: String, status: CaseStatus, severity: SeverityId,
     client_id: ClientId, alert_ids: Vec<AlertId>, disposition: Option<DispositionCode>,
     timeline: Vec<TimelineEntry>, created_at: Timestamp, updated_at: Timestamp,
     resolved_at: Option<Timestamp>, closed_at: Option<Timestamp>,
     created_by: AnalystId }`
   - `CaseId`: UUID v7 (time-sortable)
   - `CaseStatus`: uses prism-core's 5-state enum — `New`, `Acknowledged`, `Investigating`,
     `Resolved`, `Closed` (per BC-2.14.002)
   - 12 valid transitions from prism-core's `CaseStatus::can_transition_to()` (S-1.02):
     Forward: New→Acknowledged, Acknowledged→Investigating, Investigating→Resolved, Resolved→Closed
     Skip-ahead: New→Investigating, New→Resolved, New→Closed, Acknowledged→Resolved, Acknowledged→Closed, Investigating→Closed
     Reopen: Resolved→Investigating, Closed→Investigating
   - All other transitions return E-CASE-004 (invalid transition)
   - `DispositionCode`: uses prism-core's enum — `TruePositive`, `FalsePositive`, `Benign`,
     `Inconclusive`, `Duplicate`, `TestAlert` (per S-1.02)
   - `TimelineEntry { id: Uuid, entry_type: TimelineEntryType, content: String,
     from_status: Option<CaseStatus>, to_status: Option<CaseStatus>,
     created_by: AnalystId, created_at: Timestamp }`
   - `from_status`/`to_status`: populated only for `StatusChange` entries, `None` for others
   - `TimelineEntryType`: enum — `Note`, `StatusChange`, `AlertLink`, `EvidenceLink`,
     `OtImpact`
   - For `StatusChange` entries: include structured `from_status: CaseStatus` and
     `to_status: CaseStatus` fields (in addition to human-readable content string).
     This enables reliable MTTR computation in S-4.07 without regex parsing.

2. Implement `create_case` MCP tool handler in
   `crates/prism-operations/src/cases/create_case.rs` (BC-2.14.001):
   - Parameters: `title`, `alert_ids: Vec<AlertId>` (1..=100), `severity: SeverityId`,
     `client_id: ClientId`
   - Validate: all `alert_ids` exist in the `alerts` CF; all alerts belong to the
     specified `client_id`
   - Create `Case` with status `New`, resolved_at: None, closed_at: None, no disposition, empty timeline
   - Auto-generate first timeline entry: `StatusChange` → `"Case created with {n} alerts"`
   - Persist to RocksDB `cases` CF
   - Return created `Case`

3. Implement `update_case` MCP tool handler in
   `crates/prism-operations/src/cases/update_case.rs` (BC-2.14.003):
   - Parameters: `case_id`, optional `status: CaseStatus`, optional
     `disposition: DispositionCode`, optional `annotation: AnnotationInput`
   - All parameters are optional; at least one must be provided (return E-CASE-007 if
     all are absent — distinct from E-CASE-005 which is reserved for self-transitions)
   - Transition: if `status` is provided, call `CaseStatus::can_transition_to()`;
     return E-CASE-004 on invalid transition, E-CASE-005 on self-transition (per BC-2.14.002)
   - Timestamp management (per BC-2.14.002):
     - On Resolved: set `resolved_at` to now (only if null — preserve first resolution time)
     - On Closed: set `closed_at` to now
     - On reopen (Resolved/Closed → Investigating): clear `closed_at`, preserve `resolved_at`
   - Alert cascade (per BC-2.14.002):
     - On Resolved: set `resolved_at` on all linked alerts via `alert_ids`
     - On reopen: clear linked alerts' `resolved_at`
   - Disposition: if `status == Resolved` and no disposition provided (current or new),
     return E-CASE-006 (disposition required, per BC-2.14.002)
   - Auto-generate `StatusChange` timeline entry when status changes
   - Persist updated `Case` to RocksDB in a single write

4. Implement `list_cases` MCP tool handler in
   `crates/prism-operations/src/cases/list_cases.rs` (BC-2.14.004):
   - Parameters: optional `status`, optional `client_id`, optional `severity`,
     optional `created_after: Timestamp`, optional `created_before: Timestamp`,
     optional `page_cursor`, `page_size` (default 20, max 100)
   - Scan `cases` CF with prefix `case:`, apply filters, return paginated results
   - Each result item: `id`, `title`, `status`, `severity`, `client_id`, `alert_count`,
     `created_at`, `updated_at`
   - Cursor-based pagination using `CaseId` (UUID v7) as cursor

5. Implement `get_case` MCP tool handler in
   `crates/prism-operations/src/cases/get_case.rs` (BC-2.14.005):
   - Parameters: `case_id`
   - Return full `Case` including: all fields, complete `timeline`, list of linked
     `Alert` summaries (id, title, severity, triggered_at), `disposition`
   - Return E-CASE-001 if case not found

6. Enforce disposition requirement in `update_case` (BC-2.14.006):
   - A transition to `Resolved` MUST have a `DispositionCode` set — either from the
     current case state or provided in the same `update_case` call
   - Return E-CASE-006 if the `Resolved` transition is attempted without a disposition

7. Implement timeline management in
   `crates/prism-operations/src/cases/timeline.rs` (BC-2.14.007):
   - Timeline is append-only. Entries are never deleted or modified.
   - `Note`: free-text annotation from analyst; max 10,000 characters
   - `StatusChange`: auto-generated on every status transition; content is
     `"{prev_status} → {new_status} by {analyst_id}"`
   - `AlertLink`: link to an additional alert not in the original create_case call;
     content is the alert ID
   - `EvidenceLink`: external evidence URL or file reference; content is the URL/path
   - `OtImpact`: OT/ICS-specific impact annotation; content describes the OT asset or
     process impacted
   - All entries: UUID v4 id, `AnalystId` from the calling session, `Timestamp` of
     creation

8. Implement case persistence helpers in
   `crates/prism-operations/src/cases/persistence.rs` (BC-2.14.009):
   - `save_case(case: &Case)`: serialize to bincode, write to RocksDB `cases` CF under
     key `case:{id}`
   - `load_case(id: CaseId) -> Option<Case>`: read and deserialize
   - `list_cases_paginated(filter, cursor, page_size) -> (Vec<CaseSummary>, Option<Cursor>)`:
     prefix scan with filter application
   - On startup: scan `cases` CF to warm in-memory index (status → Vec<CaseId>) for
     O(1) status-based filtering

9. Implement automatic case creation triggered by
   CRITICAL-severity rule fires (BC-2.14.013):
   - Subscribe to the alert broadcast channel (same `tokio::sync::broadcast::Receiver<Alert>`
     used by the action engine in S-4.08)
   - For each incoming `Alert`:
     - If `alert.severity_id == SeverityId::Critical` (severity_id = 5 in OCSF):
       - Check feature flag `FEATURE_AUTO_CASE_CREATION` (S-1.08) — skip if not enabled

   **9a. Pure dedup decision function** — implement in
   `crates/prism-operations/src/case/dedup.rs`:

   ```rust
   pub fn decide_dedup_action(
       existing_case: Option<CaseSummary>,
       new_alert: &Alert,
       window_secs: u64,
       now: TimestampSecs,
   ) -> DedupDecision {
       match existing_case {
           Some(c) if now - c.created_at < window_secs => DedupDecision::Link(c.id),
           _ => DedupDecision::Create,
       }
   }

   pub enum DedupDecision {
       Link(CaseId),
       Create,
   }
   ```

   Required properties: total (defined for all input combinations);
   deterministic (same inputs → same output); side-effect-free (no I/O,
   no clock access, no shared state). VP-060 verifies via Proptest.

   **9b. Effectful wrapper** — implement `CaseDedupRegistry::check_and_create()`:
   - Query RocksDB for existing case for `(rule_id, client_id)` — fetches the
     most recent matching case (if any).
   - Call `decide_dedup_action(existing_case, alert, window_secs, now)`.
   - Execute the decision in a single WriteBatch transaction:
     - On `Link(case_id)`: append to existing case's `alert_ids` and timeline.
     - On `Create`: call internal `create_case_internal(...)`:
       - `title`: `"AUTO: {rule_name} — {client_id}"` (truncated to 200 chars)
       - `alert_ids`: `[alert.id]`
       - `severity`: `SeverityId::Critical`
       - `client_id`: from alert
       - `created_by`: `AnalystId::system("auto_case_creation")`
       - After case creation: append initial `Note` timeline entry:
         `"Case auto-created from CRITICAL alert {alert_id} (rule: {rule_name})"`
   - Audit-log the decision outcome (`CaseAutoCreated` or `AlertLinkedToCase`
     — extends S-2.05's specialized audit events).
   - Auto case creation failures MUST NOT propagate — wrap in `tokio::task::spawn` and
     log errors without panicking. The alert broadcast channel must not be blocked.

   Config: `[auto_case]` section in `prism.toml` with:
   - `enabled: bool` (default false — feature flag gates it)
   - `severity_threshold: SeverityId` (default CRITICAL; may be configured to HIGH)
   - `dedup_window_secs: u64` (default 300)

---

## Acceptance Criteria

**AC-1:** Given 3 valid alert IDs for client "acme", When `create_case` is called, Then
a case is created with status `New`, 3 linked alerts, and an initial `StatusChange`
timeline entry. (BC-2.14.001)

**AC-2:** Given an alert ID that does not exist, When `create_case` is called with that
ID, Then E-CASE-008 (alert not found) is returned and no case is created. (BC-2.14.001)

**AC-3:** Given a case in `New` status, When `update_case(status: Acknowledged)` is
called, Then the transition succeeds, a `StatusChange` timeline entry is appended, and
the case status is `Acknowledged`. (BC-2.14.002, BC-2.14.003)

**AC-4:** Given a case in `Resolved` status, When `update_case(status: Acknowledged)` is
called, Then E-CASE-004 (invalid transition) is returned — backward transitions to
Acknowledged are not permitted, only reopen to Investigating. (BC-2.14.002)

**AC-5:** Given a case in `Investigating` status, When `update_case(status: Resolved)` is
called without a `disposition`, Then E-CASE-006 (disposition required) is returned.
(BC-2.14.006)

**AC-6:** Given a case in `Investigating` status, When `update_case(status: Resolved,
disposition: TruePositive)` is called, Then the transition succeeds, disposition is
set, and a `StatusChange` timeline entry is appended. (BC-2.14.006)

**AC-7:** Given 20 cases, When `list_cases(status: New, client_id: "acme")` is called,
Then only New cases for client "acme" are returned. (BC-2.14.004)

**AC-8:** Given a case with 3 timeline entries, When `get_case` is called, Then the full
case including all 3 timeline entries and linked alert summaries is returned. (BC-2.14.005)

**AC-9:** Given `update_case` with an `annotation` of type `"ot_impact"` and content
`"PLC unit 3 impacted"`, When processed, Then the timeline entry is appended with type
`OtImpact`. (BC-2.14.007)

**AC-10:** Given a case persisted to RocksDB, When Prism restarts, Then `get_case`
returns the full case including all timeline entries — complete recovery. (BC-2.14.009)

**AC-11:** Given `FEATURE_AUTO_CASE_CREATION` is enabled and a CRITICAL-severity alert
fires, When the alert is broadcast, Then a case is automatically created with title
`"AUTO: {rule_name} — {client_id}"`, the alert is linked, and a Note timeline entry is
appended. (BC-2.14.013)

**AC-12:** Given `FEATURE_AUTO_CASE_CREATION` is enabled and a second CRITICAL alert
from the same rule fires within 300s of the first, When the alert is broadcast, Then the
alert is linked to the EXISTING auto-created case (dedup window active — no second case
created). (BC-2.14.013)

**AC-12b:** Given the pure function `decide_dedup_action(existing_case, alert, window, now)`,
When called with various input combinations (existing_case = None / Some, time-window
boundary cases, etc.), Then the function returns `Link(case_id)` iff existing case is
present AND `now - case.created_at < window_secs`; returns `Create` otherwise.
The function MUST be total, deterministic, and side-effect-free. Verified by VP-060
proptest. (BC-2.14.013)

**AC-13:** Given `FEATURE_AUTO_CASE_CREATION` is NOT enabled, When a CRITICAL-severity
alert fires, Then no case is auto-created. (BC-2.14.013)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Case manager and lifecycle | `prism-operations` (SS-14) | Mixed |
| `decide_dedup_action()` pure fn | `prism-operations/case/dedup.rs` | Pure |
| `CaseDedupRegistry::check_and_create()` | `prism-operations/case/dedup.rs` | Effectful |
| Case persistence | `prism-storage` (SS-15) | Effectful |
| MCP case tools | `prism-mcp` (SS-10) | Effectful |

`prism-operations` implements `CaseManager` — aggregates alerts into cases, manages case
status transitions (`Open` → `InProgress` → `Resolved` → `Closed`), and exposes case
CRUD via MCP. State machine transition logic is pure; RocksDB persistence (`cases` CF,
AD-004), audit emission, and MCP tool dispatch are effectful. Per
`architecture/module-decomposition.md`, SS-14 (Alert & Case Management) is owned by
`prism-operations`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-operations` (case manager) | Mixed | Pure state machine transitions; effectful for RocksDB writes and audit calls. Mixed per `architecture/purity-boundary-map.md`. |
| `prism-operations/case/dedup.rs` — `decide_dedup_action()` | Pure | Total, deterministic, no I/O, no clock access, no shared state. VP-060 proptest verifies. |
| `prism-operations/case/dedup.rs` — `CaseDedupRegistry::check_and_create()` | Effectful | Reads RocksDB, writes WriteBatch, emits audit events. Effectful-shell wrapping the pure decision fn. |
| `prism-storage` (cases CF) | Effectful | RocksDB reads/writes. Effectful-shell. |
| `prism-mcp` (case tools) | Effectful | MCP transport I/O. Effectful-shell. |

---

## Verification Properties

| VP ID | Description | Anchor Story |
|-------|-------------|-------------|
| VP-060 | Proptest: `decide_dedup_action()` is total, deterministic, and returns `Link` iff `existing_case.is_some() && now - case.created_at < window_secs` | S-4.06 |

---

## Architecture Compliance Rules

- `CaseStatus::can_transition_to()` MUST be implemented as a pure function on the
  `CaseStatus` enum in `prism-core` (S-1.02 established the entity types). The 12
  valid transitions are a closed set — do not use a dynamic rule table.
- `decide_dedup_action()` MUST be a pure function: no RocksDB access, no `SystemTime`
  calls, no `tokio::spawn`, no shared state. Clock value is passed in as `now:
  TimestampSecs`. Violations are a build-time enforcement failure.
- Timeline is append-only. There is no `update_timeline_entry` or `delete_timeline_entry`
  operation. This is an audit trail, not a mutable notes field.
- Case persistence uses bincode serialization for consistency with other RocksDB domains (cases can have large
  timelines). Do not use JSON for case persistence in the `cases` CF.
- The in-memory status index (status → Vec<CaseId>) is a read optimization. It must be
  rebuilt from the `cases` CF on startup and updated on every `update_case` write.
  It is not the source of truth — RocksDB is.
- `update_case` MUST write the entire updated `Case` struct in a single RocksDB put.
  Do not use separate puts for status, disposition, and timeline — partial writes during
  a crash would leave the case in an inconsistent state.
- `CaseDedupRegistry::check_and_create()` MUST execute the dedup query + decision +
  write in a single WriteBatch transaction. Do not issue the RocksDB write before
  calling `decide_dedup_action()`.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| uuid | 1.x | CaseId (UUID v7), TimelineEntry id (UUID v4) |
| bincode | 2.x | binary serialization for case persistence (consistent with other CFs) |
| rocksdb | 0.24 | cases CF |
| proptest | 1.x | VP-060: property-based tests for decide_dedup_action() |
| prism-core | workspace | PrismError, CaseStatus (S-1.02), SeverityId, AlertId types |
| prism-storage | workspace | RocksDB CF helpers |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-operations/src/cases/mod.rs` | Create | Cases module root |
| `crates/prism-operations/src/cases/case.rs` | Create | Case, CaseStatus, DispositionCode, TimelineEntry types |
| `crates/prism-operations/src/cases/dedup.rs` | Create | decide_dedup_action() pure fn, DedupDecision enum, CaseDedupRegistry effectful wrapper |
| `crates/prism-operations/src/cases/create_case.rs` | Create | create_case MCP tool handler |
| `crates/prism-operations/src/cases/update_case.rs` | Create | update_case MCP tool handler |
| `crates/prism-operations/src/cases/list_cases.rs` | Create | list_cases with filtering and pagination |
| `crates/prism-operations/src/cases/get_case.rs` | Create | get_case full detail |
| `crates/prism-operations/src/cases/timeline.rs` | Create | Append-only timeline, 5 annotation types |
| `crates/prism-operations/src/cases/persistence.rs` | Create | RocksDB cases CF helpers, startup index warm |
| `crates/prism-operations/src/cases/tests.rs` | Create | Case management unit + integration tests |

---

## Previous Story Intelligence

S-4.05 introduced the `Alert` type, `AlertId`, and the `alerts` RocksDB CF. This story's
`create_case` validates that all provided `alert_ids` exist in that CF. Ensure the
`alerts` CF handle is accessible to the case management module — pass it as a dependency
injection parameter, not as a global static.

S-2.01 defines the RocksDB initialization and domain map. Register the `"cases"` CF in
the domain map before this story is implemented. The `cases` CF must be opened with a
comparator that preserves UUID v7 byte-order for time-sorted prefix scans.

S-1.02 defines `CaseStatus` and `CaseStatus::can_transition_to()`. Review that
implementation to confirm all 12 transitions are encoded before implementing
`update_case`. If S-1.02 does not yet encode the full 12-transition set, add them as
part of this story (amendments to S-1.02's types are in scope).

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `create_case` with an `alert_id` that does not exist in the `alerts` CF | Returns E-CASE-008; no case created |
| EC-002 | `create_case` with alerts belonging to different `client_id` values | Returns validation error; no case created (all alerts must belong to specified client) |
| EC-003 | `update_case(status: Resolved)` without disposition on a case that already has one set | Succeeds — existing disposition satisfies the requirement; no E-CASE-006 |
| EC-004 | `update_case` with no parameters provided (no status, disposition, or annotation) | Returns E-CASE-007 (at least one parameter required) |
| EC-005 | `update_case(status: Acknowledged)` on a `Resolved` case | Returns E-CASE-004 (invalid transition — backward to Acknowledged not permitted) |
| EC-006 | Reopen: `update_case(status: Investigating)` on a `Closed` case | Succeeds; `closed_at` cleared; `resolved_at` preserved (first resolution time retained) |
| EC-007 | Auto-case creation fails (e.g., RocksDB write error) | Error is logged; alert broadcast channel is NOT blocked; failure does not propagate |
| EC-008 | Second CRITICAL alert from same rule within 300s dedup window | Alert linked to existing auto-case; no new case created |
| EC-009 | `list_cases` with cursor pointing to a deleted case ID | Returns results after the cursor position using UUID v7 lexicographic order; no error |
| EC-010 | Timeline `Note` exceeding 10,000 characters | Rejected with structured validation error; timeline entry not appended |
| EC-011 | `decide_dedup_action` called with `now - case.created_at == window_secs` (boundary, equal) | Returns `Create` — window is exclusive (`< window_secs`); boundary is NOT within window |
| EC-012 | `decide_dedup_action` called with `existing_case = None` | Always returns `Create` regardless of `now` or `window_secs` values |

## Dev Notes

- The `ot_impact` annotation type is specific to the OT/ICS context of Prism's MSSP
  deployment (1898 & Co serves industrial clients). It allows analysts to record which
  OT assets (PLCs, RTUs, HMIs) were impacted by a security incident, which is critical
  for incident response in OT environments.
- If MSSP workflows need a "pending vendor" state (e.g., blocked on CrowdStrike support),
  model this as a timeline annotation of type `Note` on a case in `Investigating` status.
  The 5-state machine (New/Acknowledged/Investigating/Resolved/Closed) is intentionally
  minimal per BC-2.14.002.
- Cursor-based pagination using UUID v7 CaseId is correct because UUID v7 is
  time-sortable. A cursor value of `case_id_X` means "return cases with ID > X in
  lexicographic order," which equals "return cases created after case_id_X."
- The 12 valid transitions include two "reopen" paths: `Resolved → Investigating` and
  `Closed → Investigating` (per BC-2.14.002). Reopening always returns to Investigating,
  never to New or Acknowledged. On reopen, `closed_at` is cleared but `resolved_at` is
  preserved (first resolution time for accurate MTTR).
- The pure/effectful split in task 9 (dedup.rs) is the canonical pattern for all
  BC-2.14.013 logic: the decision is pure and proptest-verified (VP-060); only the
  RocksDB query and write are effectful. Do not collapse these into a single function.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.5 | pass-74-defer-close | 2026-04-20 | story-writer | Updated task 9 to mandate pure/effectful split for dedup logic; added AC-12b verifying pure decide_dedup_action(); added VP-060 to verification_properties. Closes BC-2.14.013 DEFER from pass-74 decision matrix. |
| 1.4 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.3 | pass-59-fix | 2026-04-20 | story-writer | Fixed HIGH-001 (inputs: stem-only BC paths → full slugged paths) and HIGH-002 (anchor_capabilities: CAP-018 → CAP-022 per BC-INDEX BC-2.14.001-013→CAP-022). |
| 1.2 | Wave-5-patch | 2026-04-20 | story-writer | Pre-build sweep: added level, points, blocks, anchor_bcs/capabilities/subsystem, assumption_validations, risk_mitigations, inputs frontmatter; added ## Edge Cases section (synthesized from BC-2.14.001/002/003/006/007/013 boundary cases); renamed ## Library and Framework Requirements → ## Library & Framework Requirements; renamed ## Notes → ## Dev Notes. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
