---
story_id: S-3.08
title: "prism-query: Hidden Columns"
wave: 3
target_module: prism-query
subsystems: [SS-11]
priority: P1
depends_on: [S-3.02]
blocks: []
estimated_days: 1
points: 2
status: draft
document_type: story
version: "1.5"
level: "L4"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "248b3b0"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-3"
phase: 2
behavioral_contracts: [BC-2.11.001, BC-2.11.005, BC-2.11.010, BC-2.11.012]
# BC status: osquery-inspired enhancement; no dedicated BCs authored yet.
# Anchored to closest active BCs: BC-2.11.001 (query tool — hidden cols affect result schema),
# BC-2.11.005 (ephemeral materialization — hidden cols injected at materialization),
# BC-2.11.010 (explain_query — hidden_columns field added to output),
# BC-2.11.012 (virtual fields — hidden cols use same injection point as _sensor/_client).
# PO to author dedicated BC-2.11.016+ for hidden column spec if formal coverage needed.
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.11.001, BC-2.11.005, BC-2.11.010, BC-2.11.012]
anchor_capabilities: [CAP-015]
anchor_subsystem: ["SS-11"]
inputs:
  - ".factory/specs/prd.md"
---

# S-3.08 — prism-query: Hidden Columns

## Narrative

As a Prism query engine, I want certain columns to be queryable in WHERE/GROUP BY
predicates but excluded from SELECT * results so that internal API metadata (raw
response JSON, request IDs, cost tracking fields) does not clutter analyst result
sets unless explicitly requested — matching the osquery hidden column pattern.

## Objective

Add a `hidden: bool` flag to column metadata in sensor and internal table descriptors.
Hidden columns are included in the DataFusion schema (queryable in predicates, grouping,
and explicit SELECT), but filtered out of SELECT * projections. Expose four default
hidden columns (`_raw_api_response`, `_query_cost_ms`, `_rate_limit_remaining`,
`_api_request_id`) and support custom hidden columns declared in sensor spec files.

---

## Behavioral Contracts

| BC ID | Title | Coverage |
|-------|-------|----------|
| BC-2.11.001 | `query` MCP Tool Accepts Scoping + PrismQL Query String | Hidden columns affect result schema returned by query tool |
| BC-2.11.005 | Ephemeral Materialization — Fan-Out, Normalize, Arrow RecordBatch, DataFusion MemTable | Hidden columns injected at same point as virtual fields in materialization |
| BC-2.11.010 | `explain_query` MCP Tool | Task 7 adds `hidden_columns` field to ExplainResult |
| BC-2.11.012 | Virtual Fields in Queries — `_sensor`, `_client`, `_source_table` | Hidden default columns use same injection point as virtual fields |

Note: No dedicated BC for the hidden-column pattern exists yet. These are the closest active BCs
whose behavior is extended by this story. PO authorship of a dedicated BC (e.g., BC-2.11.016)
for the hidden-column contract is recommended before S-3.08 transitions to `ready`.

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,800 |
| column_meta.rs (hidden flag, ColumnDescriptor extension) | ~800 |
| projection.rs (SELECT * filtering logic) | ~1,200 |
| default_hidden_columns.rs (4 built-in hidden column definitions) | ~600 |
| sensor spec schema changes (hidden field in [[sensor.tables.columns]]) | ~400 |
| tests/ (SELECT *, explicit SELECT, WHERE predicate, pipe fields stage) | ~1,500 |
| Total | ~6,300 |

Well within the 30% context window budget.

---

## Tasks

1. Extend `ColumnDescriptor` (or equivalent column metadata type) with `hidden: bool`:
   - Default: `false` (columns are visible by default)
   - Location: wherever `SensorTableDescriptor` and `InternalTableDescriptor` store
     per-column metadata — likely `prism-spec-engine` or `prism-query/src/schema.rs`

2. Extend sensor spec `[[sensor.tables.columns]]` schema:
   ```toml
   [[sensor.tables.columns]]
   name = "_raw_api_response"
   col_type = "Utf8"
   hidden = true                  # new field; default false
   ```
   Validate in `SpecParser`: `hidden` is optional boolean, default `false`.

3. Register hidden columns in DataFusion schema normally:
   - They appear in the Arrow schema for the MemTable
   - DataFusion can reference them in WHERE, GROUP BY, ORDER BY, JOIN ON clauses
   - This is NOT about omitting them from the schema — it is about omitting them
     from SELECT * expansion only

4. Implement SELECT * filtering in the query materialization/result path:
   - After DataFusion `collect()`, before returning results to the MCP layer:
     inspect the schema for columns with `hidden = true`
   - If the query used `SELECT *` (or filter/pipe mode which implicitly selects all):
     strip hidden columns from the returned `RecordBatch` using Arrow
     `RecordBatch::project()` with a column index list that excludes hidden columns
   - If the query explicitly named a hidden column (e.g., `SELECT _raw_api_response
     FROM crowdstrike_alerts`): return it — explicit beats implicit

5. Detecting "explicit vs implicit" SELECT:
   - The parsed `QueryPlan` already tracks whether the projection is `SelectAll` or
     `SelectColumns(Vec<String>)`. Use this flag in the result layer.
   - Filter mode and pipe mode without a `fields` stage → `SelectAll` → strip hidden
   - SQL `SELECT *` → `SelectAll` → strip hidden
   - SQL `SELECT col1, col2` → `SelectColumns` → include hidden columns if named
   - Pipe `| fields + _raw_api_response` → the `fields` stage produces an explicit
     column list → include hidden if named

6. Define default hidden columns injected automatically into every sensor table
   (in `default_hidden_columns.rs`):
   - `_raw_api_response: Utf8` — full API response JSON for the record (before
     OCSF normalization); injected during materialization alongside `_sensor` etc.
   - `_query_cost_ms: Float64` — per-record fetch time in ms (proportional share
     of the sensor API call latency; populated from `QueryMetrics` in S-3.09)
   - `_rate_limit_remaining: Int64` — rate limit remaining from the API response
     header at fetch time (e.g., `X-RateLimit-Remaining`); `NULL` if not provided
   - `_api_request_id: Utf8` — trace/request ID from API response headers
     (e.g., `X-Request-Id`, `X-Trace-Id`); `NULL` if not provided

7. Expose hidden column information in `explain_query` output:
   - Add `hidden_columns: Vec<String>` to the explain response
   - Example: `"hidden_columns": ["_raw_api_response", "_query_cost_ms", ...]`
   - Helps analysts discover that hidden columns exist without needing docs

---

## Behavioral Contract Linkage

| BC ID | ACs Covering |
|-------|-------------|
| BC-2.11.001 | AC-1, AC-2, AC-3 (hidden cols affect result schema returned by the query tool) |
| BC-2.11.005 | AC-3 (default hidden cols injected at materialization alongside _sensor/_client) |
| BC-2.11.010 | AC-6 (hidden_columns field in ExplainResult) |
| BC-2.11.012 | AC-1, AC-3 (hidden cols use same injection point as _sensor/_client/_source_table) |

---

## Acceptance Criteria

**AC-1:** Given a sensor table with column `_raw_api_response` marked `hidden = true`,
When `SELECT * FROM crowdstrike_alerts` executes, Then `_raw_api_response` is NOT
present in the result schema or any returned row.

**AC-2:** Given the same table, When `SELECT _raw_api_response, severity FROM
crowdstrike_alerts WHERE severity_id >= 4` executes, Then `_raw_api_response` IS
present in results — explicit SELECT includes hidden columns.

**AC-3:** Given a filter mode query `severity >= high`, When results are returned,
Then none of the four default hidden columns (`_raw_api_response`, `_query_cost_ms`,
`_rate_limit_remaining`, `_api_request_id`) appear in the result set.

**AC-4:** Given a WHERE clause referencing a hidden column (`WHERE _api_request_id =
'abc-123'`), When the query executes, Then the WHERE predicate evaluates correctly
against the hidden column, even though that column is not in SELECT * output.

**AC-5:** Given a pipe mode query `FROM crowdstrike_alerts | where severity_id >= 3`,
When results are returned, Then hidden columns are absent. When followed by
`| fields + _raw_api_response`, Then `_raw_api_response` is present.

**AC-6:** Given `explain_query(query: "SELECT * FROM crowdstrike_alerts")`, When the
response is returned, Then it includes `"hidden_columns": ["_raw_api_response",
"_query_cost_ms", "_rate_limit_remaining", "_api_request_id"]` (or the table-specific
hidden set).

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Hidden column filtering | `prism-query` (SS-11) | Pure |

`prism-query` implements hidden-column projection — columns tagged `hidden: true` in sensor
TOML specs are stripped from query results before MCP serialization. The filtering logic
is a pure Arrow batch transformation. Per `architecture/module-decomposition.md`, this
feature sits inside `prism-query`'s DataFusion post-processing pipeline, consuming column
metadata from `prism-spec-engine`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-query` (hidden columns) | Pure | Pure Arrow batch column projection; no I/O. Pure per `architecture/purity-boundary-map.md`. |

---

## Verification Properties

None assigned for this story.

---

## Architecture Compliance Rules

- Hidden columns MUST be included in the DataFusion Arrow schema — omitting them from
  the schema would break WHERE/GROUP BY references. The filtering is applied AFTER
  DataFusion execution, in the result serialization layer.
- The four default hidden columns (`_raw_api_response`, `_query_cost_ms`,
  `_rate_limit_remaining`, `_api_request_id`) MUST be injected at the same point
  as `_sensor`, `_client`, `_source_table` virtual fields — in `materialization.rs`
  (or equivalent), not in each sensor adapter.
- `_raw_api_response` content MUST be treated as untrusted external data and passed
  through `InjectionScanner::scan()` if it appears in any MCP response field that
  the AI agent reads — consistent with the 4-layer injection defense.
- Do NOT use DataFusion column metadata or Arrow field metadata to store the hidden
  flag — DataFusion may not preserve arbitrary field metadata through projections.
  Maintain the hidden flag in Prism's own `ColumnDescriptor` and apply the filter
  in Prism's result layer.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| prism-core | workspace | PrismError, column descriptor types |
| prism-query | workspace | QueryPlan, SelectAll/SelectColumns discrimination |
| arrow | `"58"` (transitive via datafusion) | RecordBatch::project() for column filtering |
| prism-security | workspace | InjectionScanner for _raw_api_response content |
| serde_json | 1.x | _raw_api_response value serialization |
| tracing | `"0.1.44"` | Debug logging for projection decisions |

**Workspace MSRV:** Rust 1.85 (driven by arrow/datafusion/rocksdb/proptest).

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-query/src/column_meta.rs` | Create | ColumnDescriptor with hidden flag |
| `crates/prism-query/src/projection.rs` | Create | SELECT * filtering, hidden column exclusion |
| `crates/prism-query/src/default_hidden_columns.rs` | Create | 4 built-in hidden column definitions |
| `crates/prism-query/src/materialization.rs` | Modify | Inject 4 default hidden columns per RecordBatch |
| `crates/prism-spec-engine/src/spec/column_spec.rs` | Modify | Add hidden boolean field to ColumnSpec |
| `crates/prism-query/src/explain.rs` | Modify | Add hidden_columns to explain output |
| `crates/prism-query/src/tests/hidden_columns_tests.rs` | Create | SELECT *, explicit SELECT, WHERE, pipe tests |

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-11-070 | `SELECT *` with hidden columns present | Hidden columns absent from result schema and all returned rows |
| EC-11-071 | Explicit `SELECT _raw_api_response, severity FROM ...` | `_raw_api_response` included — explicit beats implicit |
| EC-11-072 | `WHERE _api_request_id = 'abc'` with `SELECT *` | WHERE evaluates correctly against hidden column; column absent from results |
| EC-11-073 | Pipe mode `| fields + _raw_api_response` | `_raw_api_response` present in results; other hidden columns absent |
| EC-11-074 | `_query_cost_ms` before S-3.09 is implemented | Column present in schema with `NULL` value; no error |

---

## Previous Story Intelligence

S-3.02 implements `materialization.rs` and the virtual field injection path for
`_sensor`, `_client`, `_source_table`. The four default hidden columns use the same
injection point — coordinate to ensure a single, consistent injection pass rather
than scattered per-field additions.

The `SelectAll` vs `SelectColumns` discrimination in the `QueryPlan` is established
by S-3.01 (parser) and consumed by S-3.02 (execution). The hidden column filtering
in this story is a new consumer of that same flag — read the plan struct carefully
to avoid re-implementing the projection logic.

---

## Dev Notes

- The osquery hidden column pattern (columns queryable in WHERE but absent from
  SELECT *) is the direct model. osquery uses this for columns like `pid_with_namespace`
  that are expensive to compute and only needed in specific queries.
- `_raw_api_response` is the highest-value hidden column for power users: it lets
  analysts inspect the raw sensor API JSON without extracting every field into OCSF.
  Keeping it hidden prevents it from bloating every result set.
- `_query_cost_ms` depends on `QueryMetrics` from S-3.09. If S-3.09 is not yet
  implemented, this column should be `NULL` rather than blocking this story.
- Hidden columns in sensor spec files are opt-in per column. There is no mechanism
  to hide a column that another column definition made visible — the spec is the
  source of truth.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.5 | W3-spec-remediation | 2026-05-04 | story-writer | BC anchor backfill: behavioral_contracts/anchor_bcs set to [BC-2.11.001, BC-2.11.005, BC-2.11.010, BC-2.11.012]. BC table and Behavioral Contract Linkage section added. anchor_capabilities [CAP-015], anchor_subsystem [SS-11]. Version pins: arrow "58", tracing "0.1.44". MSRV Rust 1.85. |
| 1.4 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.3 | remediation-step5 | 2026-04-20 | story-writer | BLK-005: converted `inputs:` from dict format to YAML string-list (prd.md only). Reset `input-hash` to `[pending-recompute]`. |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
