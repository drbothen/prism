---
story_id: S-6.11
title: "prism-dtu-slack: DTU for Slack Webhook API — L2 (stateful)"
wave: 2
level: "L4"
target_module: prism-dtu-slack
subsystems: [SS-18]
priority: P0
depends_on: [S-6.06]
blocks: [S-4.08, S-5.06]
estimated_days: 2
points: 3
status: draft
assumption_validations: []
risk_mitigations: [R-DTU-006]
anchor_bcs: []
anchor_capabilities: []
anchor_subsystem: null
document_type: story
version: "1.7"
producer: story-writer
timestamp: "2026-04-20T00:00:00Z"
input-hash: "8e43eb2"
inputs:
  - .factory/specs/prd.md
  - .factory/specs/architecture/dtu-assessment.md
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-6"
phase: 2
behavioral_contracts: []
verification_properties: []
---

# S-6.11 — prism-dtu-slack: DTU for Slack Webhook API — L2 (stateful)

## Narrative

As a Prism integration-test author, I want a behavioral clone of the Slack Incoming
Webhook API at fidelity level L2 (stateful), so that integration tests for Slack action
delivery can validate Block Kit payload shape, `message_ts` deduplication tracking,
and 429 rate-limit retry behavior without hitting the live Slack API.

## Objective

Implement `prism-dtu-slack` — an L2 stateful behavioral clone of the Slack webhook
endpoint. The clone validates received Block Kit payload structure, returns a `message_ts`
in successful responses, captures all received payloads for test assertion, and can inject
rate-limit (429) and error (400, 500) responses. No SMTP or syslog testing is included
here — those use `mailpit` and `SyslogReceiver` from `prism-dtu-common` respectively.
See `dtu-assessment.md §3.5.1` for full scope matrix.

---

## Behavioral Contracts

No new product-level BCs — this is test infrastructure. Architecture anchor:
`dtu-assessment.md §3.5.1` (Slack scope matrix, Block Kit validation requirement,
rate-limit behavior, payload capture test API).

| Reference | Role |
|-----------|------|
| `dtu-assessment.md §3.5.1` | Canonical endpoint, Block Kit validation, error simulation |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,000 |
| `prism-dtu-slack/Cargo.toml` | ~250 |
| `src/lib.rs` + `SlackClone` struct | ~500 |
| `src/state.rs` (payload capture + rate-limit counter) | ~400 |
| `src/routes/webhook.rs` | ~700 |
| `fixtures/` (2 JSON files) | ~600 |
| `tests/fidelity.rs` | ~350 |
| BC files (0 BCs) | ~0 |
| Total | ~4,800 |

Well within the 30% context window budget.

---

## Tasks

1. Create `crates/prism-dtu-slack/Cargo.toml`:
   - Dependencies: `prism-dtu-common` (path dep), `axum` (0.7.x), `tokio` (1.x full),
     `tower` (0.4.x), `serde` (1.x derive), `serde_json` (1.x), `http` (1.x),
     `tracing` (0.1.x)
   - `[features] dtu = []`

2. Create `src/lib.rs` — `SlackClone` struct implementing `BehavioralClone`:
   - `start()`: binds `127.0.0.1:0`, spawns axum router
   - `reset()`: clears payload capture store and rate-limit counter
   - `received_payloads(&self) -> Vec<serde_json::Value>`: test API for assertions

3. Create `src/state.rs` — `SlackState`:
   - `received_payloads: Mutex<Vec<Value>>` — captures each Block Kit payload received
   - `request_count: AtomicU32` — for rate-limit threshold tracking

4. Create `src/routes/webhook.rs`:
   - `POST /services/{token}` (path param `token` is ignored — any path segment accepted):
     - Validates payload is a JSON object with either `blocks` or `text` key present
     - If missing both: return 400 with body `"invalid_payload"` (matches real Slack behavior)
     - If valid: capture payload in `received_payloads`; return 200 JSON:
       `{"ok": true, "message_ts": "1234567890.123456"}` — `message_ts` is a fake stable
       string (not random) so integration tests can assert on it
     - Unknown top-level fields: rejected per `dtu-assessment.md §3.5.1`
       (return 400 `"unknown_field"` for non-spec top-level keys)

5. Rate-limit behavior:
   - Track `request_count` with `AtomicU32`
   - When `FailureMode::RateLimit { after_n_requests }` is configured:
     - After threshold: return 429 with body `"ratelimited"` and header `Retry-After: 30`
   - Reset counter on `POST /dtu/reset`

6. Create `POST /dtu/configure` and `POST /dtu/reset`:
   - `configure`: accepts `{"rate_limit_after": N}` or `{"fail_with": 500}`
   - `reset`: clears `received_payloads` and `request_count`; returns 200

7. Create `GET /dtu/received-payloads` test API:
   - Returns `{"payloads": [...]}` — list of all Block Kit payloads received since last reset
   - Used by integration tests to assert payload shape

8. Create fixture files:
   - `fixtures/valid-block-kit.json`: a sample valid Block Kit payload with `blocks` array
     containing a `section` block with `text` field — used as the reference shape for
     fidelity validation
   - `fixtures/block-kit-schema.json`: minimal Block Kit schema: allowed top-level fields
     (`blocks`, `text`, `username`, `icon_emoji`, `icon_url`, `attachments`) — used by
     the DTU's payload validator

9. Create `tests/fidelity.rs`:
   - `#[tokio::test] fn slack_dtu_fidelity()`: starts `SlackClone`, POSTs the valid
     Block Kit fixture, asserts HTTP 200 with `ok: true` and `message_ts` present

---

## Acceptance Criteria

**AC-1:** Given `POST /services/{token}` with a valid Block Kit payload containing
`blocks`, Then the response is HTTP 200 `{"ok": true, "message_ts": "1234567890.123456"}`.

**AC-2:** Given `POST /services/{token}` with a payload missing both `blocks` and `text`,
Then the response is HTTP 400 with body `"invalid_payload"` — exercising Prism's
`E-ACTION-004` error path.

**AC-3:** Given `POST /services/{token}` with a payload containing an unknown top-level
field `"unknown_key": "value"`, Then the response is HTTP 400 with body `"unknown_field"`.

**AC-4:** Given `FailureMode::RateLimit { after_n_requests: 3, retry_after_secs: 30 }`,
When the 4th POST arrives, Then the response is HTTP 429 with `Retry-After: 30` and body
`"ratelimited"` — exercising Prism's action retry logic.

**AC-5:** Given 3 successful webhook deliveries, When `GET /dtu/received-payloads` is
called, Then the response contains all 3 captured payloads in order.

**AC-6:** Given `reset()` is called, Then `received_payloads` is empty and the rate-limit
counter is reset to 0.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| SlackClone | `crates/prism-dtu-slack/src/lib.rs` | Effectful (HTTP server, state) |
| SlackState | `crates/prism-dtu-slack/src/state.rs` | Effectful (Mutex, AtomicU32) |
| Route handlers | `crates/prism-dtu-slack/src/routes/` | Effectful (HTTP I/O) |

Reference architecture files: `architecture/module-decomposition.md` (COMP-DTU-006),
`architecture/dependency-graph.md` (prism-dtu-slack → prism-dtu-common).

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-dtu-slack` | Effectful | Axum HTTP server with in-memory Mutex state (dev-dependency only). Effectful-shell per `architecture/purity-boundary-map.md`. |
| `prism-dtu-common` | Effectful | Shared tower middleware. Effectful-shell. |

---

## Architecture Compliance Rules

- DTU clone MUST match the Slack Incoming Webhook API contract exactly (same endpoint
  path pattern, same request/response shapes, same status codes) as documented in
  `dtu-assessment.md §3.5.1`.
- DTU clone MUST NOT introduce new behaviors not present in the real Slack Webhook API.
- DTU clone MUST be deterministic: `message_ts` MUST be a fixed stable string
  (`"1234567890.123456"`), not random — regression tests assert on this value.
- DTU clone MUST NOT require network access during test execution.
- This crate MUST be gated behind `#[cfg(any(test, feature = "dtu"))]`.
- Block Kit schema validation MUST reject unknown top-level fields per `fixtures/block-kit-schema.json`.
- See `.factory/specs/architecture/dtu-assessment.md` for cross-cutting DTU rules.
- **Forbidden dependencies:** `prism-dtu-slack` MUST NOT depend on `prism-sensors`,
  `prism-query`, `prism-operations`, `prism-mcp`, or `prism-spec-engine`. Adding any of
  these to `Cargo.toml` MUST fail the build via deny rule.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| prism-dtu-common | path dep | BehavioralClone, LatencyLayer, FailureLayer |
| axum | 0.7.x | In-process HTTP server |
| tokio | 1.x (full) | Async runtime |
| serde / serde_json | 1.x | JSON serialization + Block Kit validation |
| http | 1.x | StatusCode |

**Forbidden dependencies:** `prism-dtu-slack` MUST NOT depend on `prism-sensors`,
`prism-query`, `prism-operations`, `prism-mcp`, or `prism-spec-engine`.

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-dtu-slack/Cargo.toml` | Create | Crate manifest |
| `crates/prism-dtu-slack/src/lib.rs` | Create | SlackClone + BehavioralClone impl |
| `crates/prism-dtu-slack/src/state.rs` | Create | Payload capture + rate-limit state |
| `crates/prism-dtu-slack/src/routes/webhook.rs` | Create | POST webhook handler |
| `crates/prism-dtu-slack/fixtures/valid-block-kit.json` | Create | Reference Block Kit payload |
| `crates/prism-dtu-slack/fixtures/block-kit-schema.json` | Create | Allowed top-level fields |
| `crates/prism-dtu-slack/tests/fidelity.rs` | Create | Fidelity validator test |

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Empty JSON object `{}` POSTed as payload | HTTP 400 `"invalid_payload"` (missing both `blocks` and `text`) |
| EC-002 | 500 failure mode configured | Next POST returns HTTP 500 — tests retry exhaustion path |
| EC-003 | Very large Block Kit payload (>100KB) | DTU accepts it; no size limit enforced (Prism side enforces size) |
| EC-004 | `message_ts` used twice in deduplication | Integration test can assert that two deliveries produce the same `message_ts` value (stable fake value, not random) |
| EC-005 | `POST /dtu/reset` during active payload capture | `received_payloads` cleared atomically; in-flight request completes normally |

---

## Previous Story Intelligence

- S-6.06 (prism-dtu-common): Must be complete. Import `BehavioralClone`, `StubConfig`,
  `FailureLayer` from it. Note that `SyslogReceiver` and `WebhookReceiver` in common
  are for syslog/generic-webhook action testing — `prism-dtu-slack` covers Slack-specific
  Block Kit validation, which is distinct.

---

## Risks

| Risk ID | Description | Mitigation |
|---------|-------------|-----------|
| R-DTU-006 | Slack Block Kit schema drift — Slack periodically deprecates element types | Pin Block Kit schema version in `fixtures/block-kit-schema.json`; update when Slack announces deprecations |

---

## Dependencies

- S-6.06 (prism-dtu-common) — shared infrastructure; must complete first.
- Blocks: S-4.08 (action delivery), S-5.06 (action/infusion MCP tools).
  Consumer S-4.08 requires this DTU for integration tests of Slack action delivery
  (Block Kit payload validation, 429 retry behavior). Consumer S-5.06 requires this
  DTU for integration tests of the `fire_action` MCP tool against the Slack surface.

## Dev Notes

- The `message_ts` is intentionally a stable fake string (`"1234567890.123456"`), not
  randomly generated — regression tests assert on this exact value for deduplication testing.
- Block Kit schema validation uses `fixtures/block-kit-schema.json` as the allow-list;
  any unknown top-level key returns 400 `"unknown_field"` to match real Slack behavior.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.7 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.6 | remediation-pass59 | 2026-04-20 | story-writer | HIGH-003: replaced dtu-strategy.md with dtu-assessment.md in inputs (correct file). |
| 1.5 | step5-option2 | 2026-04-20 | story-writer | REVERSED Track A IMP-001-B removal: restored DTU → product blocks edges per user directive Option 2 (DTU-first: product stories need DTU clones as test fixture prerequisites). Wave reassigned 0 → 2 (action DTUs precede wave-4 S-4.08 and wave-5 S-5.06 consumers). |
| 1.4 | remediation-step5 | 2026-04-20 | story-writer | IMP-001-B: removed cross-graph `blocks` edges into product graph (S-4.08, S-5.06); DTU stories are test infrastructure, not schedule prerequisites — applied Step 5 validator's recommended default. |
| 1.3 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases (EC-005) + ## Architecture Compliance Rules; normalized ## Dev Notes and ## Library & Framework Requirements headings. |
| 1.2 | B-44 | 2026-04-19 | story-writer | Renamed trigger_action → fire_action in Dependencies section to match api-surface.md:164 canonical tool name. Closes P3P43-A-HIGH-001. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
