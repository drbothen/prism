---
story_id: S-2.08
title: "prism-sensors: Event Table Abstraction and Local Buffering"
wave: 2
target_module: prism-sensors
subsystems: [SS-01, SS-16]
priority: P0
depends_on: [S-2.06, S-2.01, S-1.11]
estimated_days: 3
status: draft
document_type: story
version: "1.7"
producer: product-owner
timestamp: "2026-04-26T00:00:00Z"
input-hash: "572c2a9"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-2"
phase: 2
behavioral_contracts: []
verification_properties: []
level: "L4"
inputs:
  - .factory/specs/prd.md
points: 5
blocks: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: []
anchor_capabilities: []
anchor_subsystem: null
---

# S-2.08 — prism-sensors: Event Table Abstraction and Local Buffering

## Narrative

As a Prism detection engine, I want sensor tables classified as either point-in-time
(live API fetch per query) or event-stream (locally buffered, queried from buffer) so
that scheduled detection queries against high-frequency event sources execute as fast
local RocksDB reads rather than live API round-trips — matching the osquery event
publisher/subscriber pattern.

## Objective

Distinguish the two table types in the sensor spec schema, implement a local RocksDB
`event_buffer` column family for event-stream tables, run a background polling task
per event-stream table that ingests and TTL-expires events, and route query execution
through the correct path (live API or buffer scan) transparently to all three PrismQL
modes.

---

## Behavioral Contracts

None yet — osquery-inspired enhancement without formal BCs at this stage.

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,500 |
| event_buffer.rs (RocksDB CF ops, timestamp-keyed writes/reads/TTL) | ~2,500 |
| poller.rs (background task per event-stream table, configurable interval) | ~2,000 |
| prism-core/table_type.rs (canonical TableType enum) | ~400 |
| prism-sensors/table_dispatch.rs (routing logic, imports prism_core::TableType) | ~800 |
| sensor spec schema changes (SensorSpec, TableSpec; imports prism_core::TableType) | ~800 |
| prism-query crate scaffold (Cargo.toml, lib.rs) | ~300 |
| prism-query/types.rs (SensorQueryDescriptor struct) | ~300 |
| materialization.rs (`inject_source_type` pure-data fn) | ~600 |
| tests/ (unit + integration against test RocksDB) | ~2,500 |
| Total | ~12,700 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Add `TableType` enum to `prism-core` as the single canonical definition:
   ```rust
   pub enum TableType {
       PointInTime,    // fetch on every query
       EventStream,    // buffer locally, query from buffer
   }
   ```
   Re-export from `prism-core::lib.rs`. Both `prism-sensors` and `prism-spec-engine`
   import `prism_core::TableType`; neither crate defines its own copy.

2. Extend `[[sensor.tables]]` in sensor spec schema:
   ```toml
   [[sensor.tables]]
   table_name = "process_events"
   type = "event_stream"           # new field; default = "point_in_time"
   poll_interval = "60s"           # how often to call sensor API to ingest events
   retention = "24h"               # TTL for buffered events; default 24h
   ```
   Validate in `SpecParser`: `poll_interval` and `retention` are only valid when
   `type = "event_stream"`. `poll_interval` minimum: 10s. `retention` maximum: 7d.

3. Use the existing `EventBuffer` variant from `StorageDomain` (defined in S-1.01, initialized in S-2.01) in `prism-storage`:
   - CF name: `"event_buffer"` — this becomes the 16th column family
   - Key format: `{sensor_id}/{table_name}/{client_id}/{timestamp_micros_be}/{ulid}`
   - Timestamp in big-endian bytes enables range scans in time order
   - Value: OCSF-normalized JSON blob per event record

4. Implement `EventBufferStore` in `crates/prism-sensors/src/event_buffer.rs`:
   - `write_events(sensor_id, table_name, client_id, records: Vec<NormalizedRecord>)
     -> Result<usize>`
   - `scan_events(sensor_id, table_name, client_id, since: SystemTime, until: SystemTime)
     -> Result<Vec<NormalizedRecord>>`
   - `evict_expired(sensor_id, table_name, retention: Duration) -> Result<u64>`
     — lazy eviction: called at read time before returning results; also called
     on schedule from the background poller after each ingest cycle

5. Implement background poller in `crates/prism-sensors/src/poller.rs`:
   - `EventPoller` struct: one instance per `(sensor_id, table_name, client_id)`
   - Spawned as a `tokio::task` by `AdapterRegistry::start_pollers()` at startup
   - Loop: sleep `poll_interval` → call sensor API (via existing `SensorAdapter::fetch`)
     → write to `EventBufferStore` → evict expired records → repeat
   - On API error: log WARN with sensor/table/client context, continue loop (do not crash)
   - On shutdown signal (`CancellationToken`): drain and exit cleanly
   - Configurable via `[query] event_poller_concurrency` (default 4 — max concurrent
     background pollers, shared semaphore)

6. Implement query routing in `crates/prism-sensors/src/fanout.rs`:
   - Before fan-out: check `TableSpec.table_type`
   - `PointInTime` → existing live API fetch path (unchanged)
   - `EventStream` → `EventBufferStore::scan_events()` for the query time range
   - Both paths return `Vec<RecordBatch>` to the query engine; routing is transparent
   - If `EventStream` table has no buffered data (cold start, poller not yet run):
     fall back to live API fetch once, log INFO "event_buffer cold-start fallback:
     {sensor}/{table}/{client}"

7. Create the `prism-query` crate scaffold:
   - `crates/prism-query/Cargo.toml` — workspace crate with initial deps: `prism-core`,
     `prism-storage`, `prism-spec-engine`, `serde`, `serde_json`. **No DataFusion. No Arrow.**
   - `crates/prism-query/src/lib.rs` — re-export `pub mod materialization`
   - Add `"crates/prism-query"` to `[workspace] members` in the root `Cargo.toml`

7a. Define `SensorQueryDescriptor` in `crates/prism-query/src/types.rs`:
   ```rust
   pub struct SensorQueryDescriptor {
       pub table_name: String,
       pub table_type: prism_core::TableType,
       pub rows_from_buffer: bool,
   }
   ```
   This is a prism-query–local type carrying only what `inject_source_type` needs:
   the table's delivery model and whether the current rows came from the buffer.
   It is distinct from `InternalTableDescriptor` (prism-core, S-2.03), which describes
   internal RocksDB tables (alerts, audit, aliases) and has no `table_type` field.

   Implement the pure-data `_source_type` injection function in
   `crates/prism-query/src/materialization.rs`:
   ```rust
   pub fn inject_source_type(
       rows: &mut Vec<serde_json::Value>,
       descriptor: &SensorQueryDescriptor,
   )
   ```
   - Sets `"_source_type"` key on each row map to `"buffered"` when
     `descriptor.table_type == TableType::EventStream && descriptor.rows_from_buffer`,
     or `"live"` otherwise (PointInTime table, or EventStream cold-start fallback).
   - Operates on `serde_json::Value` row maps only — **no DataFusion, no Arrow**.
   - S-3.02 will wire this function into the DataFusion `TableProvider` integration.

8. Update `get_diagnostics(subsystem: "fanout")` to report per-table event_buffer stats:
   - `event_buffer_size_bytes` per `(sensor, table, client)`
   - `last_poll_time` and `last_poll_record_count`
   - `poller_status`: running | error | cold_start

---

## Acceptance Criteria

**AC-1:** Given a sensor spec with `type = "event_stream"` and `poll_interval = "60s"`,
When `AdapterRegistry::start_pollers()` is called at startup, Then a background
`EventPoller` task is spawned for that table and calls the sensor API approximately
every 60 seconds.

**AC-2:** Given buffered events in `event_buffer` CF for `(crowdstrike, process_events,
acme)`, When a PrismQL query targets `crowdstrike_process_events`, Then results are
served from RocksDB (no live API call).

**AC-3:** Given a PointInTime table, When queried, Then the live API fetch path is taken
(unchanged behavior).

**AC-4:** Given events older than the table's `retention` period in the buffer, When
`evict_expired()` runs after an ingest cycle, Then those records are deleted from
RocksDB and do not appear in subsequent scans.

**AC-5:** Given `type = "event_stream"` with no prior poll (cold start), When a query
arrives, Then the engine falls back to a single live API fetch, writes results to the
buffer, and logs the cold-start fallback at INFO level.
**STATUS: DEFERRED to S-3.02 (sensor-adapter wiring).** S-2.08 ships only the
structural foundation (EventPoller construction, CancellationToken handling,
diagnostics). The actual cold-start fetch behavior is implemented when
SensorAdapter is wired in S-3.02. (W2-P1-A-002 fix)

**AC-6:** Given an event_stream poller that encounters an HTTP 429 from the sensor API,
When the error occurs, Then the poller logs WARN and continues the loop after the
configured retry backoff — it does NOT crash or stop the poller task.

**AC-7:** Given a sensor spec with `poll_interval = "5s"` (below 10s minimum), When
`SpecParser` validates the spec, Then validation fails with a descriptive error message
before the spec is loaded.

**AC-8:** Given a PrismQL query in any of the three modes (filter, SQL, pipe) against
an event_stream table, When the query executes, Then the routing is transparent —
the query result schema is identical regardless of which path (buffer scan vs. live
fetch) was taken.

**AC-9:** Given a `Vec<serde_json::Value>` row slice representing buffered EventStream
results, When `inject_source_type(&mut rows, &descriptor)` is called with a
`SensorQueryDescriptor` whose `table_type` is `TableType::EventStream` and
`rows_from_buffer` is `true`, Then every row in the slice has a `"_source_type"` key
with value `"buffered"` — verified as a pure unit test constructing a
`SensorQueryDescriptor` fixture, with no DataFusion or Arrow.

**AC-10:** Given the same `inject_source_type` function called with a
`SensorQueryDescriptor` whose `table_type` is `TableType::PointInTime`
(or `rows_from_buffer: false` for a cold-start fallback), Then every row has
`"_source_type": "live"` — verified as a pure unit test constructing a
`SensorQueryDescriptor` fixture, with no DataFusion or Arrow.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Event table DataFusion providers | `prism-sensors` (SS-01) + `prism-spec-engine` (SS-16) | Effectful |
| OCSF-normalized Arrow schema | `prism-ocsf` (SS-02) | Pure |

`prism-sensors` registers per-sensor event tables as DataFusion `TableProvider` implementations
that issue HTTP fetches on scan. `prism-spec-engine` drives the TOML-defined sensor spec
pipeline. `prism-ocsf` provides the Arrow schema for materialized OCSF records (pure).
Per `architecture/module-decomposition.md`, `prism-query` orchestrates these providers
during query fan-out (S-3.02).

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-sensors` (event tables) | Effectful | HTTP sensor fetches during DataFusion scan. Effectful-shell per `architecture/purity-boundary-map.md`. |
| `prism-spec-engine` (pipeline) | Mixed | Pure TOML spec logic; effectful for HTTP calls via pipeline executor. Mixed. |
| `prism-ocsf` (Arrow schema) | Pure | Pure schema construction; no I/O. Pure-core. |

---

## Verification Properties

None assigned for this story.

---

## Architecture Compliance Rules

- `event_buffer` is included in the 16-CF `StorageDomain` enum (S-1.01) and
  initialized by S-2.01. The existing CFs must not be renumbered.
- Timestamp keys in `event_buffer` MUST use big-endian byte encoding to enable
  lexicographic range scans in chronological order. Do NOT use little-endian.
- Background pollers share the same `reqwest::Client` and HTTP semaphore as query-time
  fetches — they are NOT exempt from the 200-connection global cap.
- `crates/prism-query/Cargo.toml` initial dependencies MUST be limited to: `prism-core`,
  `prism-storage`, `prism-spec-engine`, `serde`, `serde_json`. **No DataFusion. No Arrow.**
  S-3.02 adds `datafusion` + `arrow` deps when wiring the `TableProvider` integration.
  Any PR touching S-2.08 that introduces DataFusion or Arrow as a dep of `prism-query`
  is an architecture violation and must be rejected.
- The `_source_type` virtual field injection function (`inject_source_type`) is delivered
  as pure-data Rust in this story. When S-3.02 lands, it MUST consume this function and
  inject `_source_type` via the same virtual field injection path as `_sensor`, `_client`,
  and `_source_table` — not as a DataFusion UDF or a post-query transform.
- All three PrismQL modes MUST route through the same table-type dispatch logic.
  The parser and planner are unaffected; only the fetch phase differs.
- Event data in `event_buffer` is NOT audit-logged as a query result — it is
  infrastructure state. The audit log records the query execution, not the buffer
  internals.
- **[Defect 1 fix] `SensorQueryDescriptor` and `InternalTableDescriptor` are distinct
  types serving different concerns and MUST NOT be merged or aliased.**
  `InternalTableDescriptor` (defined in `prism-core`, introduced in S-2.03) describes
  internal RocksDB tables (alerts, audit, aliases) — it carries `table_name`, `domain`,
  `requires_audit_read`, and `rocksdb_backed`; it has no `table_type` field and no sensor
  context. `SensorQueryDescriptor` (defined in `prism-query/src/types.rs`, this story)
  describes sensor query routing for `_source_type` injection — it carries `table_name`,
  `table_type: prism_core::TableType`, and `rows_from_buffer: bool`. The two types are
  passed to completely different consumers; conflating them is an architecture violation.
- **[Defect 2 fix] `TableType` enum has one canonical home: `crates/prism-core/src/table_type.rs`.**
  Both `prism-sensors` and `prism-spec-engine` import `prism_core::TableType`; neither
  crate defines its own copy. `crates/prism-sensors/Cargo.toml` deps are unchanged
  (prism-core was already a dep); `crates/prism-spec-engine/Cargo.toml` deps are
  unchanged (prism-core was already a dep). Any PR that defines a local `TableType` in
  prism-sensors or prism-spec-engine is an architecture violation and must be rejected.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| prism-core | workspace | PrismError, TenantId, StorageDomain, TableType |
| prism-storage | workspace | RocksDB CF access, EventBufferStore |
| prism-sensors | workspace | SensorAdapter::fetch for poller ingest |
| prism-spec-engine | workspace | SensorSpec, TableSpec, SpecParser (prism-query dep) |
| rocksdb | 0.24 | event_buffer CF reads/writes/deletes |
| tokio | 1.x | Background poller tasks, CancellationToken |
| tokio-util | 0.7.x | CancellationToken for graceful shutdown |
| ulid | 0.x | ULID suffix for key uniqueness within same microsecond |
| serde_json | 1.x | Serialization of NormalizedRecord to CF value |
| tracing | 0.1.x | Structured logging per poller task |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-core/src/table_type.rs` | Create | Canonical `TableType` enum (`PointInTime`, `EventStream`) + `as_str` / `Display`; re-exported from `prism-core::lib.rs` |
| `crates/prism-sensors/src/table_dispatch.rs` | Create | Query-routing logic (`route_table_query`, `TableTypeRouteDecision`); imports `prism_core::TableType` — does NOT redefine the enum |
| `crates/prism-sensors/src/event_buffer.rs` | Create | EventBufferStore (RocksDB CF ops) |
| `crates/prism-sensors/src/poller.rs` | Create | EventPoller background task |
| `crates/prism-sensors/src/fanout.rs` | Modify | Add table-type dispatch before fan-out |
| `crates/prism-sensors/src/lib.rs` | Modify | Re-export new modules |
| `crates/prism-core/src/storage.rs` | Verify | `StorageDomain::EventBuffer` already defined here (S-1.01); confirm `"event_buffer"` CF name matches key-format in Task 3 |
| `crates/prism-spec-engine/src/spec_parser.rs` | Modify | Add `type`, `poll_interval`, `retention` fields to `TableSpec` struct; imports `prism_core::TableType` (does not redefine it) |
| `Cargo.toml` | Modify | Add `"crates/prism-query"` to `[workspace] members` |
| `crates/prism-query/Cargo.toml` | Create | prism-query crate manifest; deps: prism-core, prism-storage, prism-spec-engine, serde, serde_json. No DataFusion. No Arrow. |
| `crates/prism-query/src/lib.rs` | Create | Public re-exports: `pub mod materialization; pub mod types` |
| `crates/prism-query/src/types.rs` | Create | `SensorQueryDescriptor` struct (table_name, table_type: prism_core::TableType, rows_from_buffer) |
| `crates/prism-query/src/materialization.rs` | Create | `inject_source_type(rows: &mut Vec<serde_json::Value>, descriptor: &SensorQueryDescriptor)` pure-data fn; no DataFusion; no Arrow |
| `crates/prism-sensors/src/tests/event_buffer_tests.rs` | Create | Unit tests for CF ops and TTL |
| `crates/prism-sensors/src/tests/poller_tests.rs` | Create | Integration tests for poller loop |

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Event stream table queried before any poll has run (cold start) | Falls back to single live API fetch; results written to buffer; logs INFO "event_buffer cold-start fallback: {sensor}/{table}/{client}" |
| EC-002 | `poll_interval = "5s"` (below 10s minimum) in sensor spec | `SpecParser` rejects with descriptive error before spec is loaded |
| EC-003 | EventPoller encounters HTTP 429 from sensor API | Logs WARN; continues loop after retry backoff; does NOT crash or stop the poller task |
| EC-004 | Events older than `retention` period remain in `event_buffer` CF | `evict_expired()` deletes them after ingest cycle; deleted records do not appear in subsequent scans |
| EC-005 | Sensor spec with `type = "event_stream"` is hot-reloaded with changed `poll_interval` | Old EventPoller cancelled via CancellationToken; new poller spawned with updated interval atomically |

---

## Previous Story Intelligence

S-2.06 establishes `SensorAdapter::fetch()` — the poller reuses this method verbatim.
S-2.01 creates the RocksDB instance and its column families — the `event_buffer` CF is
added here. Coordinate with S-2.01 implementer: if S-2.01 is already implemented, add
the CF via a migration; if concurrent, add to the initial CF list directly.

S-1.11 provides `SensorSpec` and `TableSpec` types — the new `type`, `poll_interval`,
and `retention` fields extend `TableSpec`.

The poller's graceful shutdown relies on `tokio-util::CancellationToken`. The same
token used for the MCP server shutdown should be passed into `start_pollers()` so
pollers stop cleanly when the process exits.

---

## Dev Notes

- **Scope clarification — prism-query crate is created in this story:** This story
  creates the `prism-query` crate scaffolding (Cargo.toml, lib.rs,
  src/materialization.rs) and lands the pure-data `inject_source_type` virtual field
  injection function. S-3.02 (query materialization) extends `prism-query` with the
  DataFusion `TableProvider` integration that consumes this injection function.
  **`prism-query` in S-2.08 must NOT depend on DataFusion or Arrow** — those are
  S-3.02's deps. The injection function operates on `serde_json::Value` row maps,
  consistent with the convention used by `prism_storage::audit_buffer` and other
  prism-core data flows.

- **Hot-reload poller lifecycle:** When a sensor spec is hot-reloaded (S-1.12/S-3.13),
  event pollers must be started/stopped accordingly:
  - Spec added with `type = "event_stream"`: spawn new EventPoller task
  - Spec removed: cancel the corresponding EventPoller via CancellationToken
  - Spec modified (poll_interval changed): cancel old poller, spawn new one
  This hooks into the same config-swap notification used by S-3.13 (dynamic table
  availability). Both table registration and poller lifecycle must update atomically.
- The osquery event publisher/subscriber pattern is the direct inspiration here:
  osquery's event publishers run continuously and write to a local SQLite backing store;
  subscribers (tables) expose that buffer as queryable tables. Prism's equivalent uses
  RocksDB instead of SQLite, which gives us better write throughput and TTL semantics.
- Detection engine benefits are significant: a 5-minute schedule checking process events
  goes from ~500ms live API round-trip to ~5ms local RocksDB range scan. This reduces
  detection latency and sensor API pressure simultaneously.
- `poll_interval` is a best-effort scheduling target — actual intervals may be slightly
  longer under load. This is acceptable for detection workloads (5-minute schedules
  tolerate ±5s jitter).
- Cold-start behavior (fall-back to live API on first query) means there is no hard
  dependency on the poller having run before queries can succeed. This is especially
  important for the first query after a restart.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.7 | w2-p1-adversary-fix | 2026-04-26 | product-owner | v1.7 reconciliation: Pass-1 adversary (W2-P1-A-002) found AC-5 misrepresented as PASS in evidence-report when implementation is structural-only (EventPoller construction, CancellationToken handling, diagnostics). AC-5 reclassified DEFERRED-to-S-3.02 alongside AC-6. The actual cold-start fetch behavior (SensorAdapter wiring) is deferred to S-3.02. No BC changes; no product code changes. |
| 1.6 | stub-review-defect-reconciliation | 2026-04-26 | product-owner | Defect 1: replaced `InternalTableDescriptor` with `SensorQueryDescriptor` throughout (Task 7a, AC-9, AC-10, File Structure, Architecture Compliance Rules, Library table); added `crates/prism-query/src/types.rs` Create row for SensorQueryDescriptor; added Architecture Compliance Rule distinguishing SensorQueryDescriptor (prism-query, sensor routing) from InternalTableDescriptor (prism-core, RocksDB internal tables). Defect 2: moved TableType canonical home to `crates/prism-core/src/table_type.rs`; replaced prism-sensors `table_type.rs` Create row with `table_dispatch.rs` Create row (imports prism_core::TableType); updated prism-spec-engine `spec_parser.rs` Modify row to import rather than redefine TableType; added Architecture Compliance Rule for single canonical TableType in prism-core. |
| 1.5 | revert-prism-query-deferral | 2026-04-26 | product-owner | Reverted v1.4 deferral of prism-query/materialization.rs: re-added prism-query crate scaffold (Cargo.toml, lib.rs, materialization.rs) to File Structure Requirements; added root Cargo.toml workspace member entry; replaced deferral Dev Note with scope-clarification note (prism-query created here, DataFusion wiring in S-3.02); added Architecture Compliance Rule for no DataFusion/Arrow in S-2.08 prism-query; added Task 7a for inject_source_type pure-data fn; added AC-9 and AC-10 for _source_type injection; updated Token Budget; kept v1.4 path corrections (prism-core/storage.rs, spec_parser.rs). |
| 1.4 | worktree-baseline-check | 2026-04-26 | product-owner | Cross-crate scope defect fix: removed prism-query materialization.rs row (crate does not exist until S-3.02); corrected prism-storage/domains.rs → prism-core/storage.rs (EventBuffer already defined there); corrected prism-spec-engine/spec/table_spec.rs → spec_parser.rs (TableSpec lives there); deferred _source_type injection to S-3.02 with explicit Dev Note; updated Task 7, Token Budget, and ACs accordingly. |
| 1.3 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
