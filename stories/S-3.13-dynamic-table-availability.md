---
story_id: S-3.13
title: "prism-query: Dynamic Table Availability"
wave: 3
target_module: prism-query
subsystems: [SS-11]
priority: P1
depends_on: [S-3.02, S-1.12]
blocks: []
estimated_days: 1
points: 2
status: draft
document_type: story
version: "1.7"
level: "L4"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "248b3b0"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-3"
phase: 2
behavioral_contracts: [BC-2.16.007, BC-2.16.001, BC-2.11.001]
# BC status: osquery-inspired enhancement; no dedicated dynamic-availability BC authored yet.
# BC-2.16.007: sensor spec hot reload — dynamic table registration is the query-engine side of hot reload.
# BC-2.16.001: sensor spec file loading — TableRegistry populated from loaded sensor specs.
# BC-2.11.001: query tool — E-QUERY-001 with suggestions returned for unregistered tables.
# PO to author dedicated BC if formal coverage needed.
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.16.007, BC-2.16.001, BC-2.11.001]
anchor_capabilities: [CAP-015, CAP-029, CAP-030]
anchor_subsystem: ["SS-11", "SS-16"]
inputs:
  - ".factory/specs/prd.md"
---

# S-3.13 — prism-query: Dynamic Table Availability

## Narrative

As a Prism analyst, I want the query engine to only expose tables for sensors that
are currently configured and loaded so that my query workspace reflects reality —
unloaded sensors produce helpful errors with suggestions, not cryptic DataFusion
schema failures, and adding or removing a sensor spec file immediately changes
which tables are available.

## Objective

Implement dynamic DataFusion table registration driven by the live `ConfigSnapshot`.
When a sensor spec is loaded, its tables are registered in the DataFusion schema.
When a spec is unloaded or removed, its tables are deregistered. Queries against
unregistered tables return `E-QUERY-001` with a `did you mean` suggestion listing
configured sensors. Hot reload adds and removes tables atomically within the
config swap. Per osquery's dynamic table availability pattern.

---

## Behavioral Contracts

| BC ID | Title | Coverage |
|-------|-------|----------|
| BC-2.16.007 | Sensor Spec Hot Reload — Add/Remove/Update Sensor Tables Without Restart | TableRegistry register/deregister on hot-reload is the query-engine implementation of this BC |
| BC-2.16.001 | Sensor Spec File Loading — Parse TOML, Validate Schema, Register Tables | TableRegistry populated from sensor specs loaded at startup |
| BC-2.11.001 | `query` MCP Tool Accepts Scoping + PrismQL Query String | E-QUERY-001 with suggestions returned when query targets unregistered table |

Note: No dedicated BC for dynamic table availability exists yet. PO authorship recommended.

## Behavioral Contract Linkage

| BC ID | ACs Covering |
|-------|-------------|
| BC-2.16.007 | AC-4, AC-5 (hot-reload add/remove → register/deregister; MCP notification sent) |
| BC-2.16.001 | AC-1 (is_registered() reflects loaded specs) |
| BC-2.11.001 | AC-2, AC-8 (E-QUERY-001 with sensor suggestions for all three query modes) |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,800 |
| table_registry.rs (dynamic registration, sensor lifecycle) | ~2,000 |
| error_handling.rs (E-QUERY-001 with sensor suggestions) | ~800 |
| hot_reload_integration.rs (config swap → table delta) | ~1,200 |
| mcp_resources.rs (prism://config/clients reflects loaded sensors) | ~600 |
| tests/ (register, deregister, hot-reload, suggestion, in-flight) | ~1,500 |
| Total | ~7,900 |

Well within the 30% context window budget.

---

## Tasks

1. Implement `TableRegistry` in `crates/prism-query/src/table_registry.rs`:
   ```rust
   pub struct TableRegistry {
       registered: Arc<RwLock<HashSet<String>>>,  // table names currently available
       schema_by_table: Arc<RwLock<HashMap<String, SchemaRef>>>,
   }
   impl TableRegistry {
       pub fn register_sensor(&self, spec: &SensorSpec) -> Result<()>;
       pub fn deregister_sensor(&self, sensor_id: &str) -> Result<()>;
       pub fn is_registered(&self, table_name: &str) -> bool;
       pub fn registered_tables(&self) -> Vec<String>;
       pub fn sensor_for_table(&self, table_name: &str) -> Option<String>;
   }
   ```
   - `register_sensor()`: for each `[[sensor.tables]]` entry in the spec, add
     `{sensor_id}_{table_name}` to `registered` and store its Arrow schema
   - `deregister_sensor()`: remove all tables whose names start with `{sensor_id}_`
   - Thread-safe: `Arc<RwLock>` on both collections; reads are non-blocking in normal
     operation

2. Initialize `TableRegistry` on `QueryEngine` construction:
   - Load initial set from the `ConfigSnapshot` at startup
   - For each sensor spec in the snapshot: call `register_sensor()`
   - Store `TableRegistry` as `Arc<TableRegistry>` in `QueryEngine`

3. Integrate `TableRegistry` with the hot-reload path (S-1.12):
   - After a successful config swap, compute the delta:
     - New sensors in `new_config` not in `old_config`: call `register_sensor()`
     - Sensors in `old_config` not in `new_config`: call `deregister_sensor()`
     - Sensors in both: check if spec changed; if so, deregister + re-register
   - Send MCP `notifications/tools/list_changed` if the registered table set changed
   - In-flight queries that started before deregistration: they hold an
     `Arc<ConfigSnapshot>` reference (CI-007); their `SensorAdapter` calls proceed
     against the old spec. Only new queries see the updated table set.

4. Validate table references against `TableRegistry` during query planning in
   `planner.rs`:
   - After parsing, for each `source_ref` in the `QueryPlan`: check
     `table_registry.is_registered(source_ref)`
   - If not registered: return `Err(PrismError::QueryError(E-QUERY-001 { ... }))`
     BEFORE the fan-out phase (fail fast)
   - Error message format:
     ```
     Table 'armis_alerts' is not available. Sensor 'armis' is not configured.
     Available sensors: [crowdstrike, claroty, cyberint]
     Available tables: [crowdstrike_alerts, crowdstrike_hosts, claroty_devices, ...]
     ```
   - Use edit-distance comparison (Levenshtein ≤ 3) between the requested table name
     and all registered tables to produce a "Did you mean: crowdstrike_alerts?" hint

5. Update `explain_query` to list only registered tables:
   - `"available_tables"` in the explain response MUST reflect
     `table_registry.registered_tables()` — not a hardcoded list
   - Call `registered_tables()` at explain time (not cached in the plan)

6. Update `prism://config/clients` MCP resource to reflect only loaded sensors:
   - The resource handler reads `table_registry.registered_tables()` grouped by
     sensor_id to build the sensor list
   - On hot-reload that changes the table set, the `notifications/resources/list_changed`
     MCP notification causes the host to re-read the resource

7. Update `prism://diagnostics/summary` to include unavailable sensor warnings:
   - If a query was attempted against an unregistered table in the last hour:
     include a summary entry `"unregistered_table_queries": [{"table": "armis_alerts",
     "count": 3, "last_attempted": "10:05:00"}]`

---

## Acceptance Criteria

**AC-1:** Given `armis.sensor.toml` is loaded and `crowdstrike.sensor.toml` is NOT
loaded, When `table_registry.is_registered("crowdstrike_alerts")` is called, Then
it returns `false`. When `table_registry.is_registered("armis_alerts")` is called,
Then it returns `true`.

**AC-2:** Given a query `SELECT * FROM crowdstrike_alerts` when CrowdStrike is not
configured, When the query is planned, Then it returns `E-QUERY-001` with the message
"Sensor 'crowdstrike' is not configured. Available sensors: [armis, claroty]" —
before any fan-out occurs.

**AC-3:** Given a user types `SELECT * FROM crowdstrike_alert` (missing 's'), When
the error is returned, Then it includes `"did_you_mean": "crowdstrike_alerts"`.

**AC-4:** Given `armis.sensor.toml` is added via hot-reload (file drop or
`add_sensor_spec` MCP call), When the config swap completes, Then
`table_registry.is_registered("armis_alerts")` returns `true` and a
`notifications/resources/list_changed` MCP notification is sent.

**AC-5:** Given `claroty.sensor.toml` is removed via hot-reload, When the config
swap completes, Then `table_registry.is_registered("claroty_devices")` returns
`false`. In-flight queries that started before the removal complete normally.

**AC-6:** Given `explain_query` is called, When the response is returned, Then
`"available_tables"` lists only the currently registered tables — not a static list.

**AC-7:** Given `prism://config/clients` MCP resource is read, When only CrowdStrike and
Claroty are configured, Then the resource lists exactly those two sensors and their
tables — not Armis or Cyberint.

**AC-8:** Given a SQL query in SQL mode, filter mode, and pipe mode each reference
an unregistered table, When each is planned, Then all three return `E-QUERY-001` —
the validation is mode-agnostic, applied at the plan level.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Dynamic table availability / catalog | `prism-query` (SS-11) | Mixed |
| Spec-driven table registration | `prism-spec-engine` (SS-16) | Mixed |

`prism-query` implements a dynamic DataFusion catalog that reflects currently-configured
sensors as available tables — tables appear/disappear as sensor specs are added or removed
via hot-reload (S-1.12). The catalog query is pure; arc-swap reads and catalog mutation
are effectful. Per `architecture/module-decomposition.md`, `prism-spec-engine` drives
catalog updates via `ConfigManager`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-query` (dynamic catalog) | Mixed | Pure catalog state logic; effectful for arc-swap reads and DataFusion catalog registration. Mixed per `architecture/purity-boundary-map.md`. |
| `prism-spec-engine` (config manager) | Mixed | Pure spec validation; effectful for arc-swap publication. Mixed. |

---

## Verification Properties

None assigned for this story.

---

## Architecture Compliance Rules

- Table availability check MUST happen at plan time (before fan-out), not at parse
  time. The parser produces a syntactically valid AST regardless of whether tables
  exist; the planner validates semantic correctness against the live registry.
- `TableRegistry` reads during query planning use `RwLock::read()` (non-exclusive) —
  they MUST NOT block query execution. `RwLock::write()` is only held during
  registration/deregistration which happen on the config-reload path, not the
  query path.
- The edit-distance suggestion (Levenshtein ≤ 3) MUST be computed at error time
  from `table_registry.registered_tables()` — not a static list. This ensures
  suggestions are always accurate even as sensors are added/removed.
- In-flight query isolation (CI-007): a query that started before a deregistration
  holds a reference to the old `ConfigSnapshot` and the old `AdapterRegistry`. It
  MUST NOT be terminated mid-execution. Only new queries see the updated table set.
- `notifications/resources/list_changed` is sent only when the table set actually
  changes — not on every hot-reload. Use set comparison before/after the swap.

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| prism-core | workspace | PrismError, E-QUERY-001 error variant |
| prism-query | workspace | QueryEngine, QueryPlan, planner.rs |
| prism-spec-engine | workspace | SensorSpec, hot-reload notification |
| datafusion | `"53.1"` | Table registration in SessionContext — **`register_table`/`deregister_table` concurrency semantics require TDD-time confirmation or use CatalogProvider approach** |
| std::collections | stdlib | HashSet, HashMap for registry |
| std::sync | stdlib | RwLock for thread-safe registry |
| edit-distance | 1.x | Levenshtein distance for table name suggestions |

**DataFusion Concurrency Note:** `SessionContext::register_table`/`deregister_table` thread-safety
in DataFusion 53.x is unconfirmed. If not concurrency-safe, use a `CatalogProvider`-based approach
(implement `CatalogProvider` + `SchemaProvider` backed by `TableRegistry`) rather than direct
`register_table` calls. TDD-gate: check DataFusion 53.1 `SessionContext` source before Task 1.

**Workspace MSRV:** Rust 1.85 (driven by arrow/datafusion/rocksdb/proptest).

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-query/src/table_registry.rs` | Create | TableRegistry with register/deregister/is_registered |
| `crates/prism-query/src/planner.rs` | Modify | Table availability validation + E-QUERY-001 with suggestions |
| `crates/prism-query/src/engine.rs` | Modify | Initialize TableRegistry, wire hot-reload delta |
| `crates/prism-query/src/explain.rs` | Modify | available_tables from live registry |
| `crates/prism-mcp/src/resources.rs` | Modify | prism://config/clients from registry, not static config |
| `crates/prism-query/src/tests/table_registry_tests.rs` | Create | Register, deregister, hot-reload, suggestion, in-flight tests |

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-11-120 | Query against unregistered table with typo (e.g. `crowdstrike_alert`) | `E-QUERY-001` with `did_you_mean: "crowdstrike_alerts"` (Levenshtein ≤ 3) |
| EC-11-121 | Hot-reload removes sensor while query is in-flight | In-flight query completes normally using old `ConfigSnapshot`; new queries see deregistered table |
| EC-11-122 | Hot-reload adds new sensor spec | New sensor's tables immediately registered; `notifications/resources/list_changed` sent |
| EC-11-123 | Hot-reload changes spec for existing sensor (schema update) | Sensor deregistered then re-registered atomically; MCP notification sent only if table set changed |
| EC-11-124 | `RwLock::write()` held during registration while query read is in-progress | Read waits; write completes; no deadlock; query resumes with updated registry |
| EC-11-125 | No sensors configured at startup | `table_registry.registered_tables()` returns empty list; queries return `E-QUERY-001` with empty available-sensor list |

---

## Previous Story Intelligence

S-3.02 implements `QueryEngine` and the query execution pipeline. `TableRegistry`
is a new field on `QueryEngine` — adding it requires updating the `QueryEngine::new()`
constructor, which was established in S-3.02.

S-1.12 implements hot reload and the `ConfigSnapshot` arc-swap. The delta computation
(new sensors, removed sensors) in this story hooks into the notification channel
established by S-1.12. Read S-1.12's reload notification pattern carefully — this
story must use the same callback/notification mechanism, not invent a separate one.

S-5.03 implements MCP resources. The `prism://config/clients` resource handler update in
this story modifies code established in S-5.03. If S-5.03 is already implemented,
update its handler; if not, note in the implementation that this story's handler
must be merged into S-5.03's implementation.

---

## Dev Notes

- The osquery pattern: osquery only registers tables for extensions that are currently
  running. If an extension crashes, its tables disappear from the schema. Prism's
  equivalent is sensor specs — if a spec is removed, its tables disappear from the
  DataFusion namespace.
- The error message with sensor suggestions is particularly important for AI-generated
  queries. When Claude generates a query against a sensor the analyst hasn't configured,
  the error message tells Claude exactly which sensors ARE available — enabling it to
  self-correct without human intervention.
- Edit-distance suggestions: compute Levenshtein distance between the requested name
  and all registered table names; return the top 1 match if distance ≤ 3. The
  `edit-distance` crate (pure Rust, no unsafe) is suitable for this. Avoid bringing
  in a full fuzzy-search library for this simple use case.
- Hot-reload table delta: the simplest correct implementation is deregister-all-for-sensor
  then re-register. This handles schema changes within a spec (e.g., new table added
  to an existing sensor) without needing fine-grained diff logic.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.7 | W3-spec-remediation | 2026-05-04 | story-writer | BC anchor backfill: behavioral_contracts [BC-2.16.007, BC-2.16.001, BC-2.11.001]; anchor_bcs, anchor_capabilities [CAP-015, CAP-029, CAP-030], anchor_subsystem [SS-11, SS-16] set. BC table and Behavioral Contract Linkage section added. Version pins: datafusion "53.1" (register_table concurrency TDD-gate noted). MSRV Rust 1.85. |
| 1.6 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.5 | remediation-step5 | 2026-04-20 | story-writer | BLK-005: converted `inputs:` from dict format to YAML string-list (prd.md only). Reset `input-hash` to `[pending-recompute]`. |
| 1.4 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.3 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.2 | B-49 | 2026-04-19 | story-writer | P3P48-A-HIGH-004: Replaced bare `prism://sensors` → `prism://config/sensors` at all 5 sites (Token Budget table, Task 6, AC-7, File Structure Requirements, Previous Story Intelligence). |
| 1.1 | B-49 | 2026-04-19 | story-writer | URI alignment follow-up — replaced `prism://config/sensors` (safe default) with `prism://config/clients` per architect's Track 1 decision (folded into existing Configuration State resource). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
