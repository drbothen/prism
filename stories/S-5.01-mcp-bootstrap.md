---
story_id: S-5.01
title: "prism-mcp: Server Bootstrap and Tool Registration"
wave: 5
target_module: prism-mcp
subsystems: [SS-10]
priority: P0
depends_on: [S-1.08, S-3.02, S-4.01]
blocks: [S-5.02, S-5.05, S-5.06, S-5.08, S-6.01]
estimated_days: 3
points: 5
level: "L4"
status: draft
document_type: story
version: "1.9"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "d35ec9c"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-5"
phase: 2
behavioral_contracts: [BC-2.04.014, BC-2.10.001, BC-2.10.002, BC-2.10.003, BC-2.10.005, BC-2.10.006, BC-2.10.010]
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.04.014, BC-2.10.001, BC-2.10.002, BC-2.10.003, BC-2.10.005, BC-2.10.006, BC-2.10.010]
anchor_capabilities: [CAP-005, CAP-009, CAP-015, CAP-034]
anchor_subsystem: ["SS-10"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.04.014-tools-list-changed-notification.md
  - .factory/specs/behavioral-contracts/BC-2.10.001-server-handler-implementation.md
  - .factory/specs/behavioral-contracts/BC-2.10.002-tool-registration-via-tool-router.md
  - .factory/specs/behavioral-contracts/BC-2.10.003-conditional-tool-registration.md
  - .factory/specs/behavioral-contracts/BC-2.10.005-notifications-tools-list-changed.md
  - .factory/specs/behavioral-contracts/BC-2.10.006-stdio-transport.md
  - .factory/specs/behavioral-contracts/BC-2.10.010-graceful-shutdown.md
---

# S-5.01 — prism-mcp: Server Bootstrap and Tool Registration

## Narrative

As a Claude Code user integrating Prism into an MSSP analyst workflow, I want the MCP
server to start cleanly, register all tools, and respond to MCP protocol messages over
stdio, so that analysts can immediately discover and invoke Prism capabilities without
any additional configuration.

## Objective

Implement the MCP server using rmcp 1.4 with full tool registration, stdio transport,
and graceful shutdown. This story establishes the MCP protocol surface that all
downstream stories (tool implementations) attach to.

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.04.014 | notifications/tools/list_changed on Config Reload or Server Startup |
| BC-2.10.001 | rmcp ServerHandler Implementation |
| BC-2.10.002 | Tool Registration via #[tool_router] |
| BC-2.10.003 | Conditional Tool Registration (Feature-Flag Gated) |
| BC-2.10.005 | notifications/tools/list_changed on Config Reload |
| BC-2.10.006 | Stdio Transport |
| BC-2.10.010 | Graceful Shutdown on SIGTERM/SIGINT |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,000 |
| prism-mcp/src/server.rs | ~3,000 |
| prism-mcp/src/tools/mod.rs (tool registration) | ~2,500 |
| prism-mcp/src/main.rs (transport + signal handling) | ~1,500 |
| BC files (7 BCs) | ~4,200 |
| Test files | ~2,000 |
| Cargo.toml | ~400 |
| Total | ~14,400 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Create `crates/prism-mcp/Cargo.toml` with dependencies: `rmcp = "1.4"`, `tokio`,
   `tracing`, `prism-core`, `prism-query`, `prism-operations`, `prism-spec-engine`.

2. Implement `PrismServer` struct in `crates/prism-mcp/src/server.rs`:
   - Implement `rmcp::ServerHandler` trait
   - Hold `Arc<PrismContext>` (shared state: config, RocksDB handle, scheduler, etc.)
   - Respond to `initialize` with server capabilities, protocol version, and tool list
   (BC-2.10.001)

3. Register all MCP tools declared in `architecture/api-surface.md` v1.4 Tool Registry
   via `#[tool_router]` macro in `crates/prism-mcp/src/tools/mod.rs`: (BC-2.10.002)
   - Always-visible tools (28 per v1.4): unconditional `tools/list` presence
   - Capability-gated tools (24 per v1.4): hidden behind feature flags per BC-2.04.005
     hidden-tools pattern
   - Full enumeration and behavioral contracts: see `architecture/api-surface.md` v1.4
     Tool Registry and BC-2.10.002 v2.3

4. Implement conditional tool registration: at startup, check compile-time feature flags
   (e.g., `crowdstrike-write`, `cyberint-write`) and runtime `FeatureFlag` state; omit
   write tools from the registered set when their flag is disabled (BC-2.10.003)

5. Implement stdio transport in `crates/prism-mcp/src/main.rs`:
   - Wire `rmcp::transport::stdio()` to `PrismServer`
   - Ensure all `tracing` output goes to stderr (configure subscriber with
     `with_writer(std::io::stderr)`)
   - stdout is reserved exclusively for MCP JSON-RPC framing (BC-2.10.006)

6. Implement graceful shutdown in `crates/prism-mcp/src/main.rs`:
   - Install `tokio::signal::ctrl_c()` and `tokio::signal::unix::signal(SIGTERM)` handlers
   - On signal: set shutdown flag, flush audit buffer, close RocksDB cleanly
   - Hard timeout: if shutdown takes > 5s, force exit with code 1 (BC-2.10.010)

7. Write integration tests in `crates/prism-mcp/tests/bootstrap.rs`:
   - Spawn `PrismServer` in-process with a mock `PrismContext`
   - Assert `tools/list` returns 28 always-visible entries by default; 52 entries when all feature flags enabled
   - Assert feature-flag-gated tools are absent when flag is off

---

## Acceptance Criteria

**AC-1:** Given `PrismServer` startup with a valid `PrismContext`, When an MCP
`initialize` message is received, Then the server responds with a capabilities object
that includes the protocol version and a non-empty tool list.
(traces to BC-2.10.001 postcondition 1)

**AC-2:** Given the default feature-flag configuration, When `tools/list` is called,
Then the response contains exactly 28 always-visible tools matching the
`architecture/api-surface.md` v1.4 Always-Visible table; When `all_write_capabilities_enabled`,
Then the response contains 52 entries matching the full Tool Registry.
(traces to BC-2.10.002 v2.3)

**AC-3:** Given the binary compiled without the `crowdstrike-write` feature flag, When
`tools/list` is requested, Then CrowdStrike write tools (e.g., `acknowledge_alert` write
variants) are absent from the list.
(traces to BC-2.10.003 postcondition 1)

**AC-4:** Given stdio transport is active, When a `query` tool call is processed, Then
the JSON-RPC response is written to stdout and all log lines are written to stderr, with
no log content appearing on stdout.
(traces to BC-2.10.006 invariant 1)

**AC-5:** Given a running `PrismServer`, When `SIGTERM` is delivered, Then the server
completes in-flight requests, flushes the audit buffer, closes RocksDB, and exits within
5 seconds.
(traces to BC-2.10.010 postcondition 1)

**AC-6:** Given a config reload adds or removes a feature-flag-enabled write tool, When
the MCP server re-evaluates capabilities (prism-security determines the tool set), Then
`notifications/tools/list_changed` JSON-RPC notification is sent exactly once to the
active client, and the notification is sent AFTER the new capability set is effective.
(traces to BC-2.04.014 postcondition 1)

**AC-7:** Given the MCP server handler receives a config reload signal (SIGHUP or
equivalent), When reload processing completes successfully, Then the server emits
`notifications/tools/list_changed` via stdio transport, and the notification conforms to
MCP protocol (jsonrpc: "2.0", method, no params).
(traces to BC-2.10.005 postcondition 1)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| MCP server bootstrap | `prism-mcp` (SS-10) | Effectful |
| Binary entry point | `prism-bin` | Effectful |

`prism-mcp` implements `PrismServer` (rmcp 1.4 `ServerHandler`, AD-005) — tool
registration via `#[tool_router]` macro, middleware stack setup, and stdio transport
binding. `prism-bin` contains `main()`, which initializes storage, loads config, and
starts the MCP server. Both are effectful (I/O, runtime startup). Per
`architecture/module-decomposition.md`, `prism-bin` depends on `prism-mcp` and
`prism-storage`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-mcp` (server bootstrap) | Effectful | rmcp transport binding, tool macro expansion, middleware I/O. Effectful-shell per `architecture/purity-boundary-map.md`. |
| `prism-bin` (entry point) | Effectful | Process startup, signal handling, runtime initialization. Effectful-shell. |

---

## Verification Properties

No formal VPs for this story. Integration tests serve as the primary validation
mechanism for server bootstrap behavior.

---

## Architecture Compliance Rules

- `prism-mcp` MUST NOT directly import `rocksdb` — all storage access goes via
  `prism-storage` through `PrismContext`
- `PrismServer` MUST be `Send + Sync` (rmcp requires it for async handler dispatch)
- stdout MUST be reserved exclusively for MCP JSON-RPC; any diagnostic write to stdout
  is a correctness bug (Claude Code reads stdout as protocol data)
- Feature flag checks at tool registration MUST use the same `FeatureFlag` resolver
  defined in S-1.08 — do not duplicate flag logic
- Tool handler functions MUST be individually testable in isolation from the server
  (accept `Arc<PrismContext>` directly, not `&self` on a server struct)

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| rmcp | 1.4 | MCP server protocol implementation |
| tokio | 1.x (full features) | Async runtime, signal handling |
| tracing | 0.1.x | Structured logging to stderr |
| tracing-subscriber | 0.3.x | Subscriber setup with stderr writer |
| serde_json | 1.x | JSON serialization for tool inputs/outputs |
| prism-core | workspace | Shared types, PrismError |
| prism-query | workspace | Query execution |
| prism-operations | workspace | Schedule management |
| prism-spec-engine | workspace | Sensor spec management |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-mcp/Cargo.toml` | Create | Crate manifest with rmcp and runtime deps |
| `crates/prism-mcp/src/main.rs` | Create | stdio transport setup, signal handling, startup |
| `crates/prism-mcp/src/server.rs` | Create | PrismServer struct + ServerHandler impl |
| `crates/prism-mcp/src/context.rs` | Create | PrismContext: shared state container |
| `crates/prism-mcp/src/tools/mod.rs` | Create | #[tool_router] macro, tool registration |
| `crates/prism-mcp/src/tools/query.rs` | Create | query + explain_query stubs |
| `crates/prism-mcp/src/tools/schedule.rs` | Create | schedule tool stubs |
| `crates/prism-mcp/src/tools/health.rs` | Create | check_sensor_health stub |
| `crates/prism-mcp/src/tools/meta.rs` | Create | list_capabilities, get_help, get_diagnostics |
| `crates/prism-mcp/tests/bootstrap.rs` | Create | Integration tests for startup + tool list |

---

## Previous Story Intelligence

- S-1.08 (Feature Flags): The `FeatureFlag` resolver is available. Use it directly in
  conditional registration — do not re-implement flag logic.
- S-3.02 (Query Materialization): The query engine interface is stable. The `query` tool
  stub should accept the same parameters as the `QueryRequest` type from prism-query.
- S-4.01 (Schedule CRUD): Scheduler is available via `PrismContext`. The `create_schedule`
  tool wraps the scheduler's public API.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | MCP `initialize` received before `PrismContext` is fully initialised | Server returns protocol error; does not crash; startup is synchronous before transport binding |
| EC-002 | `tools/list` called with all feature flags disabled | Returns exactly 28 always-visible tools; no write tools appear |
| EC-003 | `tools/list` called with all write-capability flags enabled | Returns exactly 52 tools (28 always-visible + 24 capability-gated) |
| EC-004 | Second `prism serve` started while first holds RocksDB LOCK file | Second process fails at RocksDB open with lock error before MCP transport starts (DI-017) |
| EC-005 | `SIGTERM` received while a long-running query is in-flight | Server waits for in-flight request to complete or 5s hard timeout, then force-exits with code 1 |
| EC-006 | Tracing subscriber accidentally writes to stdout | Test assert: no log content on stdout; all tracing output routed to stderr |
| EC-007 | Config reload adds a new capability-gated tool | `notifications/tools/list_changed` emitted exactly once AFTER new capability set is effective |
| EC-008 | Tool stub returns `Err(PrismError::NotImplemented)` | MCP error response returned to client with `error_code: "E-NOT-IMPLEMENTED"`; server remains running |

## Dev Notes

- rmcp 1.4's `#[tool_router]` macro generates the dispatch table at compile time. All
  tools must be registered in a single `impl` block on `PrismServer` decorated with
  `#[tool_router]`.
- The tool stub bodies in this story should return `Err(PrismError::NotImplemented)`
  where the full logic lives in subsequent wave-5 stories. This keeps the bootstrap story
  focused on registration, not implementation.
- RocksDB's LOCK file prevents concurrent open. If a second `prism serve` is started
  while one is running, it will fail at RocksDB open (before MCP transport starts). This
  is intentional per DI-017.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.9 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.8 | pass-60-fix | 2026-04-20 | story-writer | Fixed HIGH-001: resolved 7 stem-only BC input paths to full slugged filenames (BC-2.04.014-tools-list-changed-notification, BC-2.10.001-server-handler-implementation, BC-2.10.002-tool-registration-via-tool-router, BC-2.10.003-conditional-tool-registration, BC-2.10.005-notifications-tools-list-changed, BC-2.10.006-stdio-transport, BC-2.10.010-graceful-shutdown). |
| 1.7 | pass-59-fix | 2026-04-20 | story-writer | Fixed HIGH-002 (anchor_capabilities: CAP-010 → [CAP-005, CAP-009, CAP-015, CAP-034] per BC-INDEX union: BC-2.04.014→CAP-005, BC-2.10.001/006/010→CAP-034, BC-2.10.002→CAP-005/015, BC-2.10.003→CAP-005, BC-2.10.005→CAP-005/009). |
| 1.6 | step5-remediation | 2026-04-20 | story-writer | IMP-001-A: Added S-6.01 to blocks (MCP bootstrap must complete before CLI startup can wire the serve subcommand). Closes IMP-001-A bidirectionality gap for S-5.01→S-6.01 edge. Also confirmed S-4.01 depends_on S-5.01 is already correctly set. |
| 1.5 | Wave-5-patch | 2026-04-20 | story-writer | Pre-build sweep: added level, points, blocks, anchor_bcs/capabilities/subsystem, assumption_validations, risk_mitigations, inputs frontmatter; added ## Edge Cases section (synthesized from BC-2.10.001/002/003/005/006/010 boundary cases); renamed ## Library and Framework Requirements → ## Library & Framework Requirements; renamed ## Notes → ## Dev Notes. |
| 1.4 | B-43 | 2026-04-19 | story-writer | Renamed set_credential → configure_credential_source in Task 3 tool registration list to match api-surface.md v1.3. Closes P3P41-A-HIGH-001. |
| 1.3 | B-50 | 2026-04-19 | state-manager | Version-pin propagation: api-surface.md v1.3 → v1.4 in Task 3 + AC-2 prose. Closes P3P49-A-HIGH-002. |
| 1.2 | B-45 | 2026-04-19 | story-writer | P3P44-A-HIGH-002: Renamed `health_check` → `check_sensor_health` in Task 3 and File Structure table. P3P44-A-MED-001: Replaced stale 34-tool enumeration in Task 3 with api-surface.md v1.3 reference (28 always-visible + 24 capability-gated = 52 total); rewrote AC-2 to assert 28/52 counts per BC-2.10.002 v2.2; updated Task 7 test assertion from "35+" to 28/52. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
