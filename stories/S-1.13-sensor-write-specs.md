---
story_id: S-1.13
title: "prism-spec-engine: Sensor Spec Write Endpoints"
wave: 1
level: "L4"
target_module: prism-spec-engine
subsystems: [SS-16]
priority: P0
depends_on: [S-1.11]
blocks: [S-3.06]
estimated_days: 2
points: 3
status: draft
document_type: story
version: "1.3"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "fbe3f7a"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-1"
phase: 2
behavioral_contracts: [BC-2.16.001, BC-2.16.009]
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.16.001, BC-2.16.009]
anchor_capabilities: [CAP-029]
anchor_subsystem: ["SS-16"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.16.001-sensor-spec-file-loading.md
  - .factory/specs/behavioral-contracts/BC-2.16.009-spec-file-validation.md
---

# S-1.13 — prism-spec-engine: Sensor Spec Write Endpoints

## Narrative

As a Prism platform implementer, I want sensor TOML specs extended with
`[write_endpoints]` sections in prism-spec-engine, so that write operations
(contain, acknowledge, tag, etc.) for CrowdStrike, Cyberint, Claroty, and Armis
can be declared declaratively alongside read specs — with validated risk tiers,
pipe verb uniqueness enforcement, and WriteEndpointRegistry descriptors consumed
by prism-query for DataFusion registration.

## Objective

Extend the sensor TOML spec format to support `[write_endpoints]` sections. Parse, validate,
and export write endpoint descriptors via a `WriteEndpointRegistry` for downstream DataFusion
registration by prism-query (S-3.07). Implement write endpoint specs for the four initial
sensors (CrowdStrike, Cyberint, Claroty, Armis). Per AD-022.

NOTE: prism-spec-engine does NOT depend on DataFusion — it exports data structures only.
DataFusion `TableProvider` registration is done by prism-query (S-3.07).

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.16.001 | Sensor Spec File Loading — Parse TOML, Validate Schema, Register Tables |
| BC-2.16.009 | Spec File Validation — Schema Validation, Variable Reference Resolution, OCSF Field Validation |

---

## Tasks

1. Extend `SensorSpec` TOML schema with `[write_endpoints.{verb}]` section:
   ```toml
   [write_endpoints.contain]
   pipe_verb = "contain"
   sql_table = "crowdstrike_contained_hosts"
   risk_tier = "irreversible"
   capability_path = "crowdstrike.hosts.write"
   batch_limit = 10
   batch_mode = "serial"
   record_id_field = "device_id"
   [[write_endpoints.contain.steps]]
   method = "POST"
   url = "/devices/entities/host-actions/v2"
   body_template = '{"action_name": "contain", "ids": ${record_ids}}'
   ```

2. Implement `WriteEndpointSpec` struct in `crates/prism-spec-engine/src/write_endpoint.rs`:
   - Fields: pipe_verb, sql_table, risk_tier (Reversible|Irreversible), capability_path,
     batch_limit, batch_mode (Serial|Parallel), record_id_field, steps (Vec<WriteStep>)
   - `WriteStep`: method, url, body_template, response_path (optional JSONPath)

3. Implement `WriteEndpointRegistry` in `crates/prism-spec-engine/src/write_endpoint.rs`:
   - Maps (sensor_type, verb) -> WriteEndpointSpec
   - `verbs_for_sensor(sensor: &str) -> Vec<&str>` for parser dynamic grammar
   - `get(sensor: &str, verb: &str) -> Option<&WriteEndpointSpec>` for execution dispatch
   - `WriteTableDescriptor` export: sql_table name, write_only flag, sensor, verb

4. Implement spec validation for write endpoints (BC-2.16.009):
   - `pipe_verb` must not collide with read-side reserved keywords (where, sort, limit,
     join, enrich, head) — return E-SPEC-011
   - `pipe_verb` must be unique across all sensors (no two sensors can claim "contain")
   - `risk_tier` must be "reversible" or "irreversible" only
   - `batch_limit = 0` + `risk_tier = "irreversible"`: emit structured warning (not error)
   - `steps` array must be non-empty
   - `record_id_field` must be a valid column name identifier (`[a-z0-9_]+`)

5. Export `WriteTableDescriptor` for each write endpoint:
   - Contains sql_table name, write_only flag, and write method routing metadata
   - DataFusion `TableProvider` registration (insert_into, update, delete_from) lives
     in prism-query (S-3.07), NOT prism-spec-engine

6. Implement variable interpolation for write templates in
   `crates/prism-spec-engine/src/interpolation.rs`:
   - `${record_ids}`: JSON array of record_id_field values from matched records
   - `${params.KEY}`: resolves to write_arg key=value from pipe stage
   - `${params.KEY|default:VALUE}`: uses VALUE if param not provided
   - Apply same safety rules as read-side: JSON-escape for body, percent-encode for URL

7. Add write endpoint specs to all four initial sensor TOML files:
   - `crowdstrike.sensor.toml`: contain (irreversible), uncontain (reversible),
     update_status (reversible), assign (reversible) — 4 endpoints
   - `cyberint.sensor.toml`: acknowledge (reversible), close_alert (irreversible) — 2 endpoints
   - `claroty.sensor.toml`: tag (reversible), remove_tag (reversible) — 2 endpoints
   - `armis.sensor.toml`: tag (reversible), remove_tag (reversible) — 2 endpoints
   - Total: 10 write endpoints across 4 sensors, matching AD-022 risk classification table

8. Write unit tests in `crates/prism-spec-engine/tests/write_endpoint_tests.rs`:
   - Valid spec load: all write verbs registered, WriteTableDescriptors present in registry
   - Reserved keyword rejection: pipe_verb = "where" -> E-SPEC-011
   - Risk tier rejection: risk_tier = "read" -> parse error
   - Batch limit + irreversible warning: emitted without failing load
   - Cross-sensor verb registry: `verbs_for_sensor("crowdstrike")` returns expected verbs

---

## Acceptance Criteria

**AC-1:** Given a valid `crowdstrike.sensor.toml` with `[write_endpoints.contain]`, When the
spec is loaded, Then `WriteEndpointRegistry.get("crowdstrike", "contain")` returns the
`WriteEndpointSpec` with risk_tier=Irreversible and batch_limit=10.

**AC-2:** Given a write endpoint with `pipe_verb = "where"` (reserved keyword), When
validation runs, Then E-SPEC-011 is returned.

**AC-3:** Given a write endpoint with `batch_limit = 0` and `risk_tier = "irreversible"`,
When validation runs, Then a structured warning is logged (spec load succeeds, no error
returned). (BC-2.16.009)

**AC-4:** Given a write endpoint with `sql_table = "crowdstrike_contained_hosts"`, When the
spec is loaded, Then `WriteEndpointRegistry` contains a `WriteTableDescriptor` with
`sql_table = "crowdstrike_contained_hosts"` and `write_only = true`. DataFusion catalog
registration is verified in S-3.07, not here. (BC-2.16.001)

**AC-5:** Given all four sensor specs loaded, When `WriteEndpointRegistry` is queried, Then
at least 10 write verbs are available across the 4 sensors.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Sensor write-spec schema and validator | `prism-spec-engine` (SS-16) | Mixed |
| Write operation security gates | `prism-security` (SS-04) | Mixed |
| Intent-log audit trail | `prism-audit` (SS-05) | Effectful |

`prism-spec-engine` extends its TOML spec parser to handle write-spec fields (endpoints,
HTTP methods, risk tiers, required confirmation). Validation is pure; filesystem reads are
effectful. `prism-security` provides risk-tier gate checks (AD-022). `prism-audit` records
write intent before execution. Per `architecture/module-decomposition.md`, `prism-query`
consumes write specs via `SpecEngine` during write-verb execution (S-3.07).

---

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Write endpoint `pipe_verb` collides with reserved keyword `where` | Validation returns `E-SPEC-011` — spec load fails (BC-2.16.009) |
| EC-002 | Two sensors declare the same `pipe_verb` (e.g., both claim `"contain"`) | Global uniqueness check fails at load time, second spec rejected (BC-2.16.009) |
| EC-003 | `batch_limit = 0` combined with `risk_tier = "irreversible"` | Structured warning emitted; spec load succeeds (BC-2.16.009) |
| EC-004 | `steps` array is empty for a write endpoint | Validation fails — at least one step required (BC-2.16.009) |
| EC-005 | `record_id_field` contains uppercase or special characters | Rejected — must match `[a-z0-9_]+` (BC-2.16.009) |

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-spec-engine` (write-spec parser) | Mixed | Pure TOML/schema validation; effectful for filesystem reads. Mixed per `architecture/purity-boundary-map.md`. |
| `prism-security` (risk-tier gates) | Mixed | Pure risk classification logic; effectful only for `SystemTime`. Mixed. |
| `prism-audit` (intent log) | Effectful | RocksDB writes for audit trail. Effectful-shell per `architecture/purity-boundary-map.md`. |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,500 |
| prism-spec-engine/src/write_endpoint.rs (WriteEndpointSpec, WriteStep, WriteEndpointRegistry, WriteTableDescriptor) | ~1,500 |
| prism-spec-engine/src/spec_parser.rs modifications ([write_endpoints] parsing) | ~600 |
| prism-spec-engine/src/validation.rs modifications (write endpoint validation rules) | ~700 |
| prism-spec-engine/src/interpolation.rs modifications (${record_ids}, ${params.KEY}) | ~600 |
| sensors/*.sensor.toml modifications (4 sensor files, 10 write endpoints total) | ~1,200 |
| BC files (2 BCs) | ~1,000 |
| Unit tests | ~1,200 |
| Total | ~9,300 |

Within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Previous Story Intelligence

S-1.11 (Spec Loading and Pipeline Execution) established `SpecParser`, `SensorSpec`,
and the validation infrastructure. This story extends those with `[write_endpoints]`
parsing and a new `WriteEndpointRegistry`. The separation is deliberate: S-1.11
handles read-side; S-1.13 handles write-side.

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|---------------|----------------------|--------------------|
| S-1.11 | `spec_parser.rs` is the entrypoint for all TOML parsing | Validation returns errors with line number references | prism-spec-engine MUST NOT import DataFusion — write endpoint descriptors exported as plain Rust structs; DataFusion integration in prism-query (S-3.07) |
| S-1.11 | Variable interpolation uses JSON-escape for body context and percent-encode for URL context | `CustomAdapter` trait is the escape hatch for complex sensors | `pipe_verb` must be globally unique across all sensors — per-sensor uniqueness is insufficient |

---

## Architecture Compliance Rules

- prism-spec-engine MUST NOT depend on DataFusion or Arrow — export descriptors only
- DataFusion `TableProvider` registration lives in prism-query (S-3.07), NOT here
- `WriteEndpointRegistry` is the single source of truth for registered write verbs
- Write verb uniqueness is enforced globally (no two sensors can register the same verb)

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| toml | 0.8.x | TOML spec file parsing |
| serde | 1.x | WriteEndpointSpec deserialization |
| prism-core | workspace | PrismError, RiskTier enum, CapabilityPath types |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-spec-engine/src/write_endpoint.rs` | Create | WriteEndpointSpec, WriteStep, WriteEndpointRegistry, WriteTableDescriptor |
| `crates/prism-spec-engine/src/spec_parser.rs` | Modify | Extend to parse [write_endpoints] sections |
| `crates/prism-spec-engine/src/validation.rs` | Modify | Add write endpoint validation rules |
| `crates/prism-spec-engine/src/interpolation.rs` | Modify | Add ${record_ids} and ${params.KEY} resolution |
| `sensors/crowdstrike.sensor.toml` | Modify | Add [write_endpoints] for contain, uncontain, update_status, assign |
| `sensors/cyberint.sensor.toml` | Modify | Add [write_endpoints] for acknowledge, close_alert |
| `sensors/claroty.sensor.toml` | Modify | Add [write_endpoints] for tag, remove_tag |
| `sensors/armis.sensor.toml` | Modify | Add [write_endpoints] for tag, remove_tag |
| `crates/prism-spec-engine/tests/write_endpoint_tests.rs` | Create | Unit tests for write endpoint loading and validation |

---

## Dev Notes

- This story exports `WriteTableDescriptor` and `WriteEndpointRegistry` data structures
  only — NO DataFusion code lives here. The actual `TableProvider` implementation
  (`WriteCapableTableProvider`) with `insert_into`/`update`/`delete_from` methods is
  created in prism-query by S-3.07 using these descriptors. prism-spec-engine does NOT
  depend on DataFusion.
- Sensor TOML write endpoint specs are co-located with read-side specs in the same file
- The `pipe_verb` registered for each sensor is what S-3.06 (parser) uses to build the
  dynamic Chumsky grammar production for write stages

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.4 | pass-64-fix | 2026-04-20 | story-writer | Filled [TODO: template scaffolding in Narrative/Token Budget/Previous Story Intelligence sections. Architecture Compliance Rules/Library & Framework Requirements/File Structure Requirements were already filled in prior sweep. Pre-build-sweep wave 2 over-claimed; this completes the remediation. |
| 1.3 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; renamed ## Library and Framework Requirements → ## Library & Framework Requirements; normalized ## Dev Notes heading form. |
