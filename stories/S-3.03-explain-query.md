---
story_id: S-3.03
title: "prism-query: Explain and Query Diagnostics"
wave: 3
target_module: prism-query
subsystems: [SS-11]
priority: P0
depends_on: [S-3.02]
blocks: []
estimated_days: 1
points: 2
status: draft
document_type: story
version: "1.9"
level: "L4"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "7017efc9"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-3"
phase: 2
behavioral_contracts: [BC-2.11.010]
verification_properties: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.11.010]
anchor_capabilities: [CAP-015]
anchor_subsystem: ["SS-11"]
inputs:
  - ".factory/specs/prd.md"
  - ".factory/specs/behavioral-contracts/BC-2.11.010-explain-query-tool.md"
---

# S-3.03 — prism-query: Explain and Query Diagnostics

## Narrative

As an analyst or LLM agent, I want an `explain_query` tool that shows me the execution
plan for a PrismQL query — what sources will be fetched, which predicates will be pushed
down to sensor APIs (as PrismQL-native predicate strings; sensor-native translation
deferred per TD-S303-PUSH-DOWN-TRANSLATION-001), and the AST-derived post-fetch
operations — without actually executing the query, so that I can diagnose performance
issues and validate query correctness before running expensive fan-out operations.

## Objective

Implement the `explain_query` MCP tool: parse the PrismQL string, resolve sources,
classify push-down predicates, derive AST-based `post_fetch_operations` (DataFusion
logical plan elided per TD-S303-DATAFUSION-PLAN-001), and return a structured JSON
`ExplainResult` with a cost estimate — all without issuing any sensor API calls.

---

## Behavioral Contracts

| BC ID | Title |
|-------|-------|
| BC-2.11.010 | `explain_query` MCP Tool |

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~1,500 |
| explain.rs (QueryEngine::explain, ExplainResult) | ~2,000 |
| tests/explain_tests.rs | ~1,000 |
| Total | ~4,500 |

Well within the 30% context window budget (~40k tokens for a 128k-context agent).

---

## Tasks

1. Implement `explain(query_str: &str, options: ExplainOptions) -> Result<ExplainResult, PrismError>`
   in `crates/prism-query/src/explain.rs` (BC-2.11.010):
   - `ExplainOptions` carries `clients: Option<Vec<OrgSlug>>`, `sensors: Option<Vec<SensorType>>`,
     `sources: Option<Vec<String>>`, `alias_registry: HashMap<String, String>`,
     `client_registry: Option<Arc<ClientRegistry>>`, and `audit_sink`
   - Parse `query_str` via `PrismQlParser::parse()` — return parse errors immediately without
     attempting further analysis
   - Resolve source refs to sensor specs (same logic as S-3.02 materialization, but no
     fan-out)
   - Resolve client scope via `resolve_clients()` from `scoping.rs` (no fan-out);
     populate `ExecutionPlan.clients_to_query` with the resolved list (AC-5)
   - Classify predicates via `classify_predicates()` from `pushdown.rs` for each source
   - DataFusion plan elision (I-LOCAL-001): use AST-derived `post_fetch_operations` list
     in place of a DataFusion `LogicalPlan`. Full DataFusion plan visibility deferred to
     TD-S303-DATAFUSION-PLAN-001. The BC postcondition is satisfied by the AST-derived list.
   - Collect results into `ExplainResult`

2. Define `ExplainResult` struct in `explain.rs`:
   ```
   ExplainResult {
     parsed_mode: String,            // "filter", "sql", or "pipe"
     original_query: String,         // raw query string as provided
     alias_expansion: HashMap<String, String>,
     expanded_query: String,
     field_resolution: HashMap<String, FieldResolution>,
     execution_plan: ExecutionPlan,
     estimated_cost: CostEstimate,
   }

   ExecutionPlan {
     sensors_to_query: Vec<ExplainSource>,
     post_fetch_operations: Vec<String>,   // AST-derived (DataFusion plan deferred: TD-S303-DATAFUSION-PLAN-001)
     clients_to_query: Vec<OrgSlug>,       // AC-5: all clients in scope (pure registry lookup)
   }

   ExplainSource {
     source_ref: String,             // e.g. "crowdstrike.detections"
     sensor_type: SensorType,
     api_filters_pushed: Vec<String>,      // predicates sent to sensor API
     post_filter_predicates: Vec<String>, // predicates applied after fetch
     estimated_row_count: Option<u64>,    // None if sensor doesn't support row count hints
   }
   ```

3. Serialize `ExplainResult` to JSON for MCP tool response. Use `serde_json::to_string_pretty`
   so that LLM consumers receive readable output.

4. Write unit tests in `crates/prism-query/src/tests/explain_tests.rs`:
   - Test: valid query → correct sources and push-down decisions
   - Test: query with push-downable predicates → api_filters_pushed populated
   - Test: invalid query → parse errors returned, no ExplainResult
   - Test: multi-client query → all clients listed in execution_plan.clients_to_query

---

## Acceptance Criteria

**AC-1:** Given a valid PrismQL query, When `explain_query` is called, Then an
`ExplainResult` is returned without issuing any sensor API calls (verified by asserting
zero calls to `AdapterRegistry`). (BC-2.11.010)

**AC-2:** Given a query with `WHERE severity_id >= 3` where `severity_id` is a REQUIRED
push-downable column, When explained, Then `ExplainSource.api_filters_pushed` contains
`"severity_id >= 3"` for the relevant source. (BC-2.11.010)

**AC-3:** Given a query targeting two sources (`crowdstrike.detections` and
`claroty.alerts`), When explained, Then `execution_plan.sensors_to_query` contains two
`ExplainSource` entries, one per source. (BC-2.11.010)

**AC-4:** Given a syntactically invalid PrismQL query, When `explain_query` is called,
Then a structured parse error is returned (not a panic), and `ExplainResult` is not
produced. (BC-2.11.010)

**AC-5:** Given a query with `clients: None`, When explained, Then `execution_plan.clients_to_query`
lists all configured client IDs without any sensor fan-out occurring. (BC-2.11.010)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| EXPLAIN query plan rendering | `prism-query` (SS-11) | Pure |
| MCP `explain_query` tool | `prism-mcp` (SS-10) | Effectful |

`prism-query` implements `QueryEngine::explain()` — it parses the PrismQL AST, classifies
push-down predicates, and derives `post_fetch_operations` without executing sensor fetches
(DataFusion logical plan elided per TD-S303-DATAFUSION-PLAN-001). Plan analysis is pure.
`prism-mcp` exposes `explain_query` as an MCP tool. Per `architecture/module-decomposition.md`,
`prism-query` exports `QueryEngine::explain()` consumed by `prism-mcp`.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-query` (EXPLAIN) | Pure | Plan rendering is deterministic AST→plan-tree transformation; no sensor I/O. Pure per `architecture/purity-boundary-map.md`. |
| `prism-mcp` (explain tool) | Effectful | MCP transport I/O. Effectful-shell. |

---

## Verification Properties

None for this story.

---

## Architecture Compliance Rules

- `explain_query` MUST NOT call `fan_out()` or any sensor adapter `fetch()` method —
  it is a read-only plan analysis operation; calling sensors would defeat its purpose
  and incur unnecessary API costs
- `ExplainResult` MUST be serializable to JSON without any custom serializer —
  all fields use standard types (`String`, `Vec<String>`, `u64`, `Option`)
- `post_fetch_operations` MUST be derived from the AST (not from DataFusion); DataFusion
  logical plan generation is elided per TD-S303-DATAFUSION-PLAN-001 — do not invoke
  `SessionContext::create_logical_plan()` or register MemTables
- `explain_query` reuses `classify_predicates()` from S-3.02 pushdown.rs directly —
  do NOT duplicate the push-down classification logic

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| datafusion | `"53.1"` | NOT used by explain path (LogicalPlan generation elided per TD-S303-DATAFUSION-PLAN-001); remains a workspace dependency for the execute path |
| serde | `"1"` (features = ["derive"]) | ExplainResult serialization to JSON |
| serde_json | 1.x | JSON pretty-printing for MCP response |
| prism-core | workspace | PrismError, TenantId |

**Workspace MSRV:** Rust 1.85 (driven by arrow/datafusion/rocksdb/proptest).

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-query/src/explain.rs` | Create | QueryEngine::explain(), ExplainResult, ExplainSource |
| `crates/prism-query/src/tests/explain_tests.rs` | Create | Unit tests for explain behavior |

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-11-050 | Query with no sensor sources resolved | `E-QUERY-001` returned; `ExplainResult` not produced |
| EC-11-051 | Query with all predicates non-push-downable | `ExplainSource.api_filters_pushed` is empty; `post_filter_predicates` contains all predicates |
| EC-11-052 | Multi-client query with `clients: None` | `execution_plan.clients_to_query` lists all configured client IDs without any sensor fan-out |
| EC-11-053 | Parser encounters query exceeding `PRISM_MAX_QUERY_SIZE` | Parse error returned before plan generation |

---

## Previous Story Intelligence

This story is a thin layer over S-3.02's infrastructure. It reuses:
- Parser from S-3.01 — same parse call, same error handling
- `resolve_clients()` from S-3.02 scoping.rs
- `classify_predicates()` from S-3.02 pushdown.rs

The only new code is the `explain()` entry point and the `ExplainResult` struct
(with AST-derived `post_fetch_operations`; DataFusion plan generation is elided per
TD-S303-DATAFUSION-PLAN-001).

Estimated implementation time is 1 day because all the complex logic (parsing, scoping,
push-down) is already implemented and tested in S-3.02.

---

## Dev Notes

- `CostEstimate` has four fields: `per_sensor_latency_ms` (rolling average API latency per
  sensor), `per_sensor_api_call_count` (expected pagination depth), `per_sensor_rate_limit_headroom`
  (remaining calls in current rate limit window), and `summary` (human-readable combined
  estimate). There is no `estimated_memory_mb` field in the implementation.
- If a sensor adapter exposes a `count_hint()` method (not required in S-2.06), use it
  to populate `ExplainSource.estimated_row_count`. Otherwise leave it `None`.
- There is no `datafusion_logical_plan` field in `ExplainResult` or `ExecutionPlan`. The
  implementation uses an AST-derived `post_fetch_operations` list (human-readable strings
  such as "GROUP BY 2 column(s)", "LIMIT 10"). Full DataFusion plan visibility is deferred
  to TD-S303-DATAFUSION-PLAN-001.
- This story intentionally has no Kani proofs or fuzz targets: the explain path is
  nearly identical to the execute path (which has VP-031) and the parser is already
  covered by VP-014, VP-015, VP-021.

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.9 | S-3.03-adversary-local-pass4 | 2026-05-07 | implementer | C-1: Whole-file sweep for elided datafusion/memory-budget references — Narrative updated (removed "DataFusion logical plan" and "memory budget"; replaced with AST-derived post-fetch ops + TD-S303-DATAFUSION-PLAN-001 deferral note); Objective updated (same elision); Architecture Mapping updated (removed "DataFusion logical and physical plan optimization"; replaced with AST-derived derivation); Architecture Compliance Rules updated (removed "schema-only MemTables" instruction; replaced with explicit DataFusion elision instruction); Library Requirements updated (datafusion row annotated as not-used-by-explain); Previous Story Intelligence updated (removed schema-only MemTable registration reference). input-hash recomputed to 7017efc9. |
| 1.8 | S-3.03-adversary-local-pass3 | 2026-05-07 | implementer | I-LOCAL-PASS3-1: Dev Notes fixed — removed stale `estimated_memory_mb` reference (field never existed; replaced with actual `CostEstimate` fields: `per_sensor_latency_ms`, `per_sensor_api_call_count`, `per_sensor_rate_limit_headroom`, `summary`); removed stale `datafusion_logical_plan` reference (elided per TD-S303-DATAFUSION-PLAN-001; replaced with note on AST-derived `post_fetch_operations`). Bumped input-hash to 6fefc774. |
| 1.7 | S-3.03-adversary-local-pass2 | 2026-05-07 | implementer | Fixed 8 sites of stale field names (C-LOCAL-NEW-1): ExplainSource.push_down_predicates → api_filters_pushed (5 sites: struct sketch, test bullet, AC-2, EC-11-051 table), ExplainResult.sources → execution_plan.sensors_to_query (AC-3), ExplainResult.clients → execution_plan.clients_to_query (3 sites: test bullet, AC-5, EC-11-052 table). Bumped input-hash to bcccca0 (I-LOCAL-NEW-6). |
| 1.6 | S-3.03-adversary-local-pass1 | 2026-05-07 | implementer | Updated task §1 signature to current ExplainOptions / OrgSlug API (I-LOCAL-005). Updated ExplainResult struct sketch to match implementation (clients_to_query: AC-5; DataFusion plan elision: I-LOCAL-001 → TD-S303-DATAFUSION-PLAN-001). |
| 1.5 | W3-spec-remediation | 2026-05-04 | story-writer | Version pins: datafusion "53.1", serde "1" with derive. MSRV Rust 1.85 added. |
| 1.4 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.3 | remediation-step5 | 2026-04-20 | story-writer | BLK-002/BLK-005: converted `inputs:` from dict format to YAML string-list; fixed legacy `architecture/behavioral-contracts/` prefix to `.factory/specs/behavioral-contracts/`; added actual BC filename slug. Reset `input-hash` to `[pending-recompute]`. |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-40 | 2026-04-19 | story-writer | Architecture Mapping table filled with subsystem + crate + purity classification (P3P25-A-L-002). Changelog section added retroactively in Burst 41 (P3P39-A-MED-002). |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |
