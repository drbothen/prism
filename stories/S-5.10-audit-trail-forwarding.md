---
story_id: S-5.10
title: "prism-audit: Audit Trail External Forwarding"
wave: 5
target_module: prism-audit
subsystems: [SS-05, SS-20]
priority: P1
depends_on: [S-2.04, S-5.09]
estimated_days: 3
status: draft
document_type: story
version: "1.9"
producer: story-writer
timestamp: "2026-04-18T00:00:00Z"
input-hash: "1e29f9d"
traces_to: []
cycle: "v1.0.0-greenfield"
epic_id: "E-5"
phase: 2
behavioral_contracts: [BC-2.05.001, BC-2.05.002, BC-2.05.003, BC-2.05.004, BC-2.05.006, BC-2.05.008, BC-2.05.011, BC-2.15.004]
verification_properties: [VP-039, VP-056]
level: "L4"
points: 5
blocks: []
assumption_validations: []
risk_mitigations: []
anchor_bcs: [BC-2.05.001, BC-2.05.002, BC-2.05.003, BC-2.05.004, BC-2.05.006, BC-2.05.008, BC-2.05.011, BC-2.15.004]
anchor_capabilities: [CAP-007]
anchor_subsystem: ["SS-05", "SS-20"]
inputs:
  - .factory/specs/prd.md
  - .factory/specs/behavioral-contracts/BC-2.05.001-audit-entry-per-tool-invocation.md
  - .factory/specs/behavioral-contracts/BC-2.05.002-audit-entry-structured-json-format.md
  - .factory/specs/behavioral-contracts/BC-2.05.003-secret-redaction-in-audit-entries.md
  - .factory/specs/behavioral-contracts/BC-2.05.004-write-operation-audit-detail.md
  - .factory/specs/behavioral-contracts/BC-2.05.006-audit-entry-immutability.md
  - .factory/specs/behavioral-contracts/BC-2.05.008-soc2-iso27001-field-requirements.md
  - .factory/specs/behavioral-contracts/BC-2.05.011-audit-forwarding-at-least-once.md
  - .factory/specs/verification-properties/vp-039-audit-forward-watermark-monotonic.md
  - .factory/specs/verification-properties/vp-056-audit-buffer-overflow-purge-preserves-newest.md
  - .factory/specs/behavioral-contracts/BC-2.15.004-audit-buffer-overflow.md
---

# S-5.10 — prism-audit: Audit Trail External Forwarding

## Narrative

As a Prism MSSP operator, I want the audit trail forwarded to external compliance and
SIEM destinations with at-least-once delivery guarantees and durable on-disk buffering,
so that no audit entries are lost during destination outages and compliance obligations
(SOC2, NIST) are met even when external systems are temporarily unavailable.

## Objective

Implement audit trail external forwarding per `config-schema.md` §Audit Settings
`[audit.forward]`: at-least-once forwarding from the RocksDB `audit_buffer` CF to
external destinations (`vector`, `syslog`, `webhook`); exponential backoff retry
(2s base, 60s max); forwarding state tracked in RocksDB (delivered entry watermark);
on-disk durability — entries remain in `audit_buffer` until acknowledged by destination;
24-hour outage tolerance with FIFO eviction at configured cap; `prism audit status`
diagnostic command.

This story is separate from diagnostic log forwarding (S-5.09):
- S-5.09: best-effort, in-memory queue, diagnostic logs
- S-5.10: at-least-once, RocksDB-backed, audit-grade entries

---

## Behavioral Contracts

| BC ID | Clause | Description |
|-------|--------|-------------|
| BC-2.05.001 | postcondition 1 | Every MCP tool invocation produces exactly one audit entry (fail-closed for writes) — this story forwards those entries; must not lose them |
| BC-2.05.002 | postcondition 1 | Audit entries use structured JSON format with complete fields — forwarder preserves this structure verbatim; no field stripping on transit |
| BC-2.05.003 | invariant | Credential values are never present in audit entries — forwarder must not inject credential values when building delivery payloads (e.g., destination auth headers must not echo into the entry body) |
| BC-2.05.004 | postcondition 1 | Write operations log capability check and execution outcome — forwarder delivers these entries intact; the write-outcome ordering is already established by S-2.04, forwarding must preserve it |
| BC-2.05.006 | invariant | Audit entries are append-only and immutable — forwarder reads entries from `audit_buffer` CF without modifying them; watermark advance is separate from the entry itself |
| BC-2.05.008 | postcondition 1 | Audit entries satisfy SOC 2 Type II and ISO 27001 requirements — forwarder ensures at-least-once delivery to compliance destinations so SOC2/NIST audit trail continuity is maintained |
| BC-2.05.011 | precondition + postconditions 1–2 + invariant | Audit Forwarding At-Least-Once: entry in `audit_buffer` CF precondition; postcondition 1 — delivered to all destinations or watermark not advanced (retried on restart); postcondition 2 — permanent failure skips entry with ERROR log, watermark advances past it; invariant — watermark monotonically increasing per destination (links to Tasks 3, 7, 8) |
| BC-2.15.004 | postconditions | Audit Buffer Overflow — Purge Oldest Entries When Exceeding 100K, Log Warning: this story's VP-056 proptest (Task 11) is the execution vehicle for formally verifying the `audit_buffer` overflow purge logic. The `compute_purge_targets` function must return exactly the oldest `(N - floor(threshold * 0.9))` entries when N > threshold; the purge-event record must always be included in output. |

**BC coverage note (Burst 2.75 — resolved):** BC-2.05.011 is the dedicated at-least-once
forwarding contract. It supersedes the earlier product-owner flag (which proposed BC-2.05.009;
the committed ID is BC-2.05.011 due to 009 and 010 already being occupied). VP-039 (Kani)
formally verifies the watermark monotonicity invariant.

---

## Token Budget Estimate

| Artifact | Estimated Tokens |
|----------|-----------------|
| This story spec | ~2,000 |
| audit/forward/mod.rs (AuditForwarder trait, ForwardState) | ~1,500 |
| audit/forward/vector.rs (Vector HTTP source forwarder) | ~1,500 |
| audit/forward/syslog.rs (RFC 5424 syslog forwarder) | ~1,200 |
| audit/forward/webhook.rs (generic HTTP webhook) | ~1,000 |
| audit/forward/watermark.rs (RocksDB delivery state) | ~1,500 |
| audit/forward/scheduler.rs (retry loop, backoff, overflow) | ~2,000 |
| cli/audit_status.rs (prism audit status command) | ~1,000 |
| tests/audit_forward_tests.rs | ~2,000 |
| BC-2.05.* files (referenced) | ~3,000 |
| Total | ~16,700 |

Within 30% context window budget.

---

## Tasks

1. Define `[audit.forward]` config schema in `crates/prism-audit/src/forward/mod.rs`:
   - `AuditForwardConfig { enabled: bool, destinations: Vec<AuditDestination>,
     buffer_cap_mb: u32, retry_max_attempts: u32, backoff_base_secs: u64,
     backoff_max_secs: u64 }`
   - Defaults: `buffer_cap_mb = 512`, `retry_max_attempts = 10`,
     `backoff_base_secs = 2`, `backoff_max_secs = 60`
   - `AuditDestination { name: String, destination_type: AuditDestType,
     endpoint: String, credentials: HashMap<String, CredentialRef>,
     min_severity: Option<SeverityId> }`
   - `AuditDestType`: `Vector | Syslog | Webhook`
   - Credentials: opaque `CredentialRef` per AD-017 — resolved at delivery time

2. Implement `AuditForwarder` trait in `crates/prism-audit/src/forward/mod.rs`:
   ```rust
   #[async_trait]
   pub trait AuditForwarder: Send + Sync {
       async fn deliver(&self, entries: &[AuditEntry]) -> Result<(), ForwardError>;
       fn name(&self) -> &str;
   }
   ```
   - `ForwardError`: `Transient(String)` (retry) | `Permanent(String)` (do not retry)
   - Permanent errors: invalid credentials (4xx auth errors), malformed entries
   - Transient errors: network errors, 5xx server errors, connection refused

3. Implement forwarding state tracker in `crates/prism-audit/src/forward/watermark.rs`:
   - Tracks which audit entries have been delivered to each destination
   - Uses a per-destination watermark key in the `audit_buffer` CF:
     `"forward_watermark:{destination_name}"` → last acknowledged entry ID (UUID v7)
   - `Watermark::advance(dest_name: &str, entry_id: EntryId) -> Result<(), StorageError>`:
     - Write the new watermark atomically ONLY after the destination has acknowledged
       receipt (2xx response or syslog TCP ACK)
   - `Watermark::last_delivered(dest_name: &str) -> Option<EntryId>`: read current watermark
   - On restart: read watermarks from RocksDB, resume forwarding from last-delivered+1
   - This provides at-least-once semantics: entries between crash and watermark advance
     are re-delivered after restart (destination must be idempotent or accept duplicates)

4. Implement `vector` forwarder in `crates/prism-audit/src/forward/vector.rs`:
   - Vector accepts log events via HTTP source (`POST {endpoint}/_bulk`)
   - Body: NDJSON (newline-delimited JSON), one audit entry per line
   - Authentication: `Authorization: Bearer {token}` (from credential ref)
   - HTTP 2xx → success; 4xx (except 429) → `ForwardError::Permanent`; others → `ForwardError::Transient`
   - Batch size: configurable, default 50 entries per POST

5. Implement `syslog` forwarder in `crates/prism-audit/src/forward/syslog.rs`:
   - RFC 5424 format with CEF/LEEF structured data extension for compliance systems
   - TCP transport with TLS (required — audit data must not transit in cleartext)
   - Acknowledgment: TCP ACK (connection-level; syslog RFC 5425 does not provide
     application-level ACK). Watermark advances after TCP send returns without error.
   - `ssl_ca_cert: Option<PathBuf>`: path to CA cert for server verification

6. Implement `webhook` forwarder in `crates/prism-audit/src/forward/webhook.rs`:
   - POST entries as JSON array to configured URL
   - `Authorization` header from credential ref
   - HTTP 2xx → advance watermark; other → ForwardError per HTTP status

7. Implement retry scheduler in `crates/prism-audit/src/forward/scheduler.rs`:
   - `AuditForwardScheduler::run(config: AuditForwardConfig, storage: Arc<RocksDB>)`:
     - Spawn one task per destination (independent — one destination's outage does not
       block others)
     - Each task: poll `audit_buffer` CF for entries since `last_delivered`, batch them,
       call `forwarder.deliver(batch)`
     - On success: advance watermark
     - On `Transient` error: exponential backoff
       - Attempt 1: wait 2s, attempt 2: 4s, ..., up to `backoff_max_secs` (60s)
       - After `retry_max_attempts`: log `ERROR`, continue retrying (audit delivery is
         critical — no dead-letter, no drop — keep trying until destination recovers)
     - On `Permanent` error: log `ERROR`, skip the offending entry (move watermark past
       it), continue with next entries. A permanently-undeliverable entry blocks nothing.
   - Poll interval when caught up (no pending entries): 5 seconds

8. Implement buffer overflow / FIFO eviction in `crates/prism-audit/src/forward/scheduler.rs`:
   - Monitor `audit_buffer` CF size against `buffer_cap_mb`
   - On exceeding cap: evict oldest entries (lowest UUID v7 keys) until below 90% of cap
   - Eviction skips entries with an unadvanced watermark for any destination (i.e., do
     not evict entries that have not yet been delivered to all destinations)
   - If the oldest entries have been delivered to all destinations, they are safe to evict
   - Log `WARN` on eviction: `"audit_buffer cap exceeded: evicting {n} oldest entries"`
   - This provides 24-hour tolerance at typical audit volumes (assumes `buffer_cap_mb`
     is sized appropriately by the operator)

9. Implement `prism audit status` diagnostic in `crates/prism-audit/src/cli/audit_status.rs`:
   - Output per destination:
     - `name`: destination name
     - `last_delivered_id`: most recently delivered entry ID (UUID v7)
     - `pending_count`: entries in `audit_buffer` since `last_delivered_id`
     - `last_error`: last retry error (if any)
     - `retry_count`: current retry attempt number (0 if healthy)
   - JSON output with `--format json`
   - This command reads directly from RocksDB (does not require the server to be running)

10. Write tests in `crates/prism-audit/tests/audit_forward_tests.rs`:
    - Test at-least-once: write 10 entries, simulate crash mid-delivery, restart →
      verify entries are re-delivered from watermark
    - Test destination outage: destination returns 503 → verify retry with backoff
    - Test FIFO eviction: fill to cap → verify oldest-delivered entries evicted first
    - Test permanent error: destination returns 400 on specific entry → entry skipped,
      watermark advances, subsequent entries delivered normally
    - Test 2 destinations: verify each destination maintains independent watermarks

11. Write VP-056 proptest in `crates/prism-audit/src/proofs/buffer_purge.rs`:
   - Generate audit buffers of N entries (N > threshold); assert `compute_purge_targets` returns exactly oldest `(N - floor(threshold*0.9))` entries
   - Assert newest `floor(threshold*0.9)` entries never appear in purge target set
   - Assert purge-event record always included in output
   - Method: proptest; traces to BC-2.15.004 postconditions

12. Write VP-039 Kani proof in `crates/prism-audit/src/proofs/watermark_monotonic.rs`:
    - Method: kani
    - Property: at-least-once delivery via monotonic watermark (never decrements; advances only on confirmed delivery)
    - Traces to: BC-2.05.011 postconditions

---

## Acceptance Criteria

**AC-1:** Given Prism crashes after writing an audit entry to `audit_buffer` but before
the watermark advances, When Prism restarts, Then the entry is re-delivered to the
destination (at-least-once guarantee holds).
(traces to BC-2.05.001, BC-2.05.002 postcondition 1)

**AC-2:** Given a destination returns 503 for 3 consecutive delivery attempts, When the
4th attempt succeeds, Then all pending entries are delivered and the watermark advances.
The retry backoff sequence is 2s, 4s, 8s for the 3 failed attempts.
(traces to BC-2.05.011 postcondition "Forwarding failure with backoff")

**AC-3:** Given the `audit_buffer` CF exceeds `buffer_cap_mb`, When the overflow check
runs, Then the oldest entries that have been delivered to ALL destinations are evicted
(FIFO), and a WARN log is emitted. Entries NOT yet delivered to any destination are
NOT evicted.
(traces to BC-2.05.011 postcondition "Buffer cap / FIFO eviction")

**AC-4:** Given destination A and destination B are both configured, When destination
A is in retry backoff (503), Then destination B continues delivering entries on its
normal schedule — the two destinations are independent.
(traces to BC-2.05.011 postcondition "Forwarding failure with backoff" — per-destination watermark independence; INV-AUDIT-FWD-003)

**AC-5:** Given `prism audit status`, When destination "vector-prod" has 150 pending
entries and is in retry attempt 3, Then the output shows `pending_count: 150`,
`retry_count: 3`, and the last error message.

**AC-6:** Given an audit entry with a permanent delivery error (destination returns 400),
When the scheduler processes it, Then the entry is skipped (watermark advances past it),
an ERROR is logged, and subsequent entries continue delivering normally.
(traces to BC-2.05.011 error case E-AUDIT-005)

**AC-7:** Given an audit entry whose `parameters` field contains values that were redacted
at construction time (e.g., fields ending in `_key`, `_secret`, `_token`), When the
forwarder builds the delivery payload for that entry, Then the delivered payload contains
only the redaction marker (e.g., `"[REDACTED]"`) in those fields — no credential value is
present in the forwarded entry body, result_summary, capability_checks, or any other field.
(traces to BC-2.05.003 postcondition "The `parameters` field in the audit entry replaces all credential values with a redaction marker")

**AC-8:** Given Prism executes a WRITE sensor operation and the audit entry records both
`capability_checks` (with `compile_time_enabled`, `runtime_enabled`, `result`) and
`result_summary` (execution outcome), When the forwarder delivers that entry, Then the
delivered payload preserves both the `capability_checks` array and the `result_summary`
field intact — neither field is stripped or truncated during forwarding.
(traces to BC-2.05.004 postcondition "The `capability_checks` array in the audit entry contains at least one entry" and "result_summary includes the execution outcome")

**AC-9:** Given the `audit_buffer` CF contains both delivered and undelivered audit entries,
When the forwarder reads entries for delivery, Then no audit entry in the CF is modified,
overwritten, or deleted as a result of the read — watermark advancement is recorded as a
separate key (`forward_watermark:{destination_name}`) and does not mutate the entry itself.
(traces to BC-2.05.006 postcondition "Once an audit entry is emitted, it cannot be modified, overwritten, or deleted by Prism" and "no API or code path exists to update a previously emitted entry")

**AC-10:** Given the forwarder delivers an audit entry to a SOC 2 / ISO 27001 compliance
destination, When the destination receives the entry, Then the delivered JSON contains all
required fields non-null: `user_identity` (who), `tool_name` and `parameters` (what),
`timestamp` in ISO 8601 UTC (when), `client_id` and `sensor` (where), `result_summary`
(outcome), and `capability_checks` (authorization evidence for write operations) — the
forwarder does not strip or omit any of these fields during serialization or batching.
(traces to BC-2.05.008 postcondition "SOC 2 Type II fields are present" and "ISO 27001 fields are present")

**AC-11:** VP-056 proptest passes: `compute_purge_targets` preserves the newest `floor(threshold*0.9)` entries and always includes a purge-event record in the output.
(traces to BC-2.15.004)

**AC-12:** VP-039 Kani proof passes (watermark monotonic, at-least-once delivery).
(traces to BC-2.05.011)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|----------------|
| Audit trail forwarding | `prism-audit` (SS-05) | Effectful |
| External forwarding config | `prism-mcp` (SS-20) | Effectful |

`prism-audit` extends `BufferedForwarder` to forward SOC 2 audit entries to external
SIEM/log aggregators (Splunk HEC, Datadog, Elasticsearch, syslog). Forwarding logic
runs on a background tokio task; batch formatting is mostly pure but flush/retry is
effectful. Per `architecture/module-decomposition.md`, `prism-audit` depends on
`prism-storage` for buffer persistence and provides `AuditEmitter` to all other crates.

---

## Purity Classification

| Module | Classification | Justification |
|--------|----------------|---------------|
| `prism-audit` (trail forwarding) | Effectful | HTTP/syslog I/O to external systems, RocksDB buffer reads. Effectful-shell per `architecture/purity-boundary-map.md`. |
| `prism-mcp` (forwarding config) | Effectful | MCP transport I/O for forwarding configuration tools. Effectful-shell. |

---

## Verification Properties

| VP ID | Method | Property |
|-------|--------|----------|
| VP-039 | kani | Audit forward watermark monotonicity: for every destination D, the watermark stored in RocksDB never decreases across calls to `Watermark::advance()`. Proves the at-least-once invariant in BC-2.05.011. |
| VP-056 | proptest | Audit Buffer Overflow Purge Preserves Newest Entries: `compute_purge_targets(entries, threshold)` returns exactly the oldest `(N - floor(threshold*0.9))` entries; newest `floor(threshold*0.9)` entries are never purged; purge-event record always included. Proves BC-2.15.004 postcondition. |

Durability guarantees (at-least-once restart behavior) are additionally validated through
restart-based integration tests with controlled crash injection (stop the scheduler task
mid-delivery, verify re-delivery on restart).

---

## Architecture Compliance Rules

- Watermark advance MUST happen AFTER the destination acknowledges receipt. Advancing
  the watermark before acknowledgment would create data loss on crash between advance
  and delivery.
- The `audit_buffer` CF MUST be the single source of truth for pending audit entries.
  Forwarder tasks read from it, advance watermarks against it, and evict from it. There
  is NO secondary queue or in-memory buffer that can become the source of truth.
- `AuditForwardScheduler` tasks MUST be independent per destination. A `tokio::task`
  per destination with no shared state between tasks (except the RocksDB handle,
  which is `Arc<RocksDB>`).
- Syslog forwarding MUST use TLS. Audit data contains security-sensitive events; unencrypted
  transit is prohibited. Enforce TLS in the syslog forwarder; do not accept `ssl_verify: false`
  unless explicitly documented as an insecure configuration.
- The `prism audit status` command MUST be able to run without the Prism server process
  running. It reads directly from the RocksDB file, not via an IPC socket. This is a
  safety property: operators need audit status even when the server is down.
- Credentials for destination authentication MUST be resolved at delivery time from the
  `CredentialStore`. Never store credential values in the `AuditForwarder` struct itself
  (they may rotate).

---

## Library & Framework Requirements

| Library | Version | Purpose |
|---------|---------|---------|
| rocksdb | 0.24 | audit_buffer CF, watermark state |
| reqwest | 0.12.x | Vector HTTP source, webhook delivery |
| tokio-rustls | 0.26.x | Syslog TCP+TLS |
| serde_json | 1.x | NDJSON batch serialization |
| tokio | 1.x | Per-destination task spawning, backoff intervals |
| bincode | 2.x | Watermark state serialization (consistent with other CFs) |
| prism-core | workspace | PrismError, AuditEntry, EntryId |
| prism-storage | workspace | RocksDB CF access, StorageDomain::AuditBuffer |
| prism-security | workspace | CredentialRef resolution |
| prism-audit | workspace (self) | AuditEntry types from S-2.04 |
| proptest | 1.x (dev) | VP-056: audit buffer overflow purge preserves newest entries |
| kani | (dev, cfg-gated) | VP-039 watermark monotonic proof |

---

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/prism-audit/src/forward/mod.rs` | Create | AuditForwarder trait, ForwardError, AuditForwardConfig |
| `crates/prism-audit/src/forward/watermark.rs` | Create | Per-destination delivery watermark in RocksDB |
| `crates/prism-audit/src/forward/scheduler.rs` | Create | Retry loop, backoff, overflow eviction |
| `crates/prism-audit/src/forward/vector.rs` | Create | Vector HTTP source forwarder |
| `crates/prism-audit/src/forward/syslog.rs` | Create | RFC 5424 / CEF syslog over TCP+TLS |
| `crates/prism-audit/src/forward/webhook.rs` | Create | Generic HTTP webhook forwarder |
| `crates/prism-audit/src/cli/audit_status.rs` | Create | prism audit status diagnostic command |
| `crates/prism-audit/src/lib.rs` | Modify | Export forward module, construct AuditForwardScheduler |
| `crates/prism-audit/tests/audit_forward_tests.rs` | Create | At-least-once durability tests |
| `crates/prism-audit/src/proofs/buffer_purge.rs` | Create | VP-056 proptest — purge preserves newest entries, purge-event always included |
| `crates/prism-audit/src/proofs/watermark_monotonic.rs` | Create | VP-039 Kani proof — watermark monotonic, at-least-once delivery (pass-89 F89-004) |

---

## Previous Story Intelligence

S-2.04 (Audit Entry Construction and Compliance) owns the `audit_buffer` CF, the
`AuditEntry` struct, and the write path. This story reads from `audit_buffer` and
advances watermarks against it — it does NOT own the write path.

S-2.02 (Audit Buffer and Watchdog) established the `audit_buffer` CF capacity watchdog.
The FIFO eviction in this story interacts with the watchdog: if the watchdog is already
evicting based on a different threshold, coordinate to avoid double-eviction. The
watermark-aware eviction in Task 8 is the authoritative eviction policy for forwarding.

S-1.06 (Credential Store) provides `CredentialStore::get()`. Resolve destination
credentials via the injected `Arc<dyn CredentialStore>` at delivery time.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Prism crashes after writing audit entry but before watermark advances | Entry re-delivered on restart; at-least-once guarantee holds |
| EC-002 | Destination returns 503 repeatedly | Exponential backoff (2s, 4s, 8s…); continues retrying indefinitely — no dead-letter, no drop |
| EC-003 | Destination returns 400 (permanent error) on specific entry | Entry skipped; watermark advances past it; ERROR logged; subsequent entries continue |
| EC-004 | `audit_buffer` CF exceeds `buffer_cap_mb` | Oldest entries delivered to ALL destinations evicted (FIFO); WARN logged; undelivered entries NOT evicted |
| EC-005 | Two destinations with independent watermarks; destination A in backoff | Destination B continues delivering on its own schedule; fully independent |
| EC-006 | `prism audit status` run while server is down | Reads RocksDB directly; returns per-destination watermark and pending count without server process |

---

## Dev Notes

- The `audit_buffer` CF uses UUID v7 keys (time-sortable). Iterating "since last_delivered"
  is efficient: use RocksDB's `seek_for_prev(watermark_id)` then iterate forward. This
  avoids full-table scans even with millions of audit entries.
- At-least-once semantics mean a destination that crashed mid-delivery may receive
  duplicate entries on recovery. This is acceptable and expected. Compliance logging
  systems (Vector, Splunk, Elasticsearch) are designed to handle duplicates via dedup
  on a unique audit entry ID.
- The 24-hour tolerance target requires `buffer_cap_mb` to be sized for the expected
  audit volume. A typical Prism deployment generating 10,000 audit entries/hour at ~1KB
  each needs: 10,000 * 24 * 1KB = 240MB. A 512MB default provides headroom.

---

## Changelog

| Version | Burst | Date | Author | Changes |
|---------|-------|------|--------|---------|
| 1.9 | pass-90 | 2026-04-21 | story-writer | F90-002: swapped physical order of Task 11 (VP-056 proptest) and Task 12 (VP-039 Kani) so Task 11 precedes Task 12 in text. F90-005: added vp-056-audit-buffer-overflow-purge-preserves-newest.md to inputs. |
| 1.8 | pass-89 | 2026-04-21 | story-writer | F89-004: added Task 12 (VP-039 Kani proof watermark_monotonic.rs), AC-12 (VP-039 traces to BC-2.05.011), File Structure row for watermark_monotonic.rs, and kani Library row for VP-039. |
| 1.7 | pass-88 | 2026-04-21 | story-writer | F88-003: added BC-2.15.004 to frontmatter behavioral_contracts, anchor_bcs, inputs, and body BC table. F88-004: renumbered duplicate task 9 (VP-056) → task 11. F88-006: added File Structure Requirements row for VP-056 proof file. F88-007: added proptest to Library & Framework Requirements. |
| 1.6 | pass-87-F87-003 | 2026-04-21 | story-writer | F87-003: body-propagated VP-056 — added VP table row, Task 9, AC-11. |
| 1.5 | pass-60-fix | 2026-04-20 | story-writer | Renumbered duplicate Changelog version rows for monotonicity (MED-001). |
| 1.4 | pass-59-fix | 2026-04-20 | story-writer | Fixed HIGH-001 (inputs: missing specs/ prefix + stem-only BC/VP paths → full slugged paths) and HIGH-002 (anchor_capabilities: CAP-005 → CAP-007 per BC-INDEX BC-2.05.xxx→CAP-007). Note: adversary recommended CAP-007/CAP-025; BC-INDEX shows all BCs→CAP-007, none→CAP-025. CAP-007 used. |
| 1.3 | step5-remediation | 2026-04-20 | story-writer | IMP-001-A: Added S-5.09 to depends_on (audit-trail forwarding requires the SIEM connector from S-5.09 to be present). Closes IMP-001-A bidirectionality gap for S-5.09→S-5.10 edge. |
| 1.2 | pre-build-sweep | 2026-04-20 | story-writer | Template-compliance sweep: added level/inputs/points/blocks/assumption_validations/risk_mitigations/anchor_bcs/anchor_capabilities/anchor_subsystem frontmatter; added ## Edge Cases scaffolding; normalized ## Dev Notes and ## Library & Framework Requirements heading form. |
| 1.1 | B-41 | 2026-04-19 | story-writer | P3P39-A-MED-001: Subsystem corrected SS-06 (Client Configuration) → SS-20 (Observability / Log Forwarding) per ARCH-INDEX:111. Architecture Mapping table row updated `prism-mcp (SS-06)` → `prism-mcp (SS-20)`. Crate owner unchanged (prism-mcp owns both SS-06 and SS-20 per ARCH-INDEX). Aligns with peer story S-5.09 which uses SS-20. |
| 1.0 | Phase 3 | 2026-04-16 | story-writer | Initial story creation. |

---

## Notes (Burst 2.75 Anchor Update)

**BC-2.05.011 and VP-039 committed (resolved):** The at-least-once forwarding contract is
now formally specified as BC-2.05.011 (the committed ID; 009 and 010 were already occupied).
VP-039 (Kani proof) verifies the watermark monotonicity invariant. Both are anchored in the
frontmatter, BC traceability table, and VP table above. No further product-owner action
required for this story.
