Test: test_BC_2_16_013_claroty_audit_logs_dtu_column_parity
Crate: prism-dtu-claroty
Location: crates/prism-dtu-claroty/src/routes/audit_log.rs (mod tests)
Command: cargo nextest run -p prism-dtu-claroty -E 'test(test_BC_2_16_013_claroty_audit_logs_dtu_column_parity)'
Story: S-DEMO-CLAROTY-AUDIT-DTU-001 v1.8
PR: #167 (S-DEMO-CLAROTY-AUDIT-DTU-001)
BCs: BC-2.16.013 v1.22 (postcondition §2 DTU-TOML-column-parity)
Protocol: SAP-2 — DTU↔TOML schema parity probe

AC Coverage:
  AC-005 — Each ClarotyAuditLogEntry field maps 1:1 to a declared column in claroty.sensor.toml
           audit_logs table block. All 5 TOML columns are present in the Rust struct:
             id        → String  (column_type = "string")
             action    → String  (column_type = "string")
             actor     → String  (column_type = "string")
             timestamp → String  (column_type = "datetime", ISO 8601 per ADR-028 §D8)
             resource  → String  (column_type = "string")

Result:
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.25s
────────────
 Nextest run ID e41681da-85ba-435c-8a36-0fbd01a68a4c with nextest profile: default
    Starting 1 test across 1 binary (6 tests skipped)
        PASS [   0.138s] (1/1) prism-dtu-claroty routes::audit_log::tests::test_BC_2_16_013_claroty_audit_logs_dtu_column_parity
────────────
     Summary [   0.139s] 1 test run: 1 passed, 6 skipped

PASS — 1/1 tests passed

Assertions exercised by this test:
  Part 1 (compile-time SAP-2 probe):
    - fixtures/audit-log.json deserializes into Vec<ClarotyAuditLogEntry> (serde structural check)
    - entries.len() >= 5 (AC-004 cross-check)
    - first.id is non-empty String (AC-005: id column)
    - first.action is non-empty String (AC-005: action column)
    - first.actor is non-empty String (AC-005: actor column)
    - first.timestamp is non-empty String containing 'T' and ending with 'Z' (AC-005: ISO 8601 per ADR-028 §D8)
    - first.resource is non-empty String (AC-005: resource column)
  Part 2 (HTTP round-trip parity):
    - DTU started, POST /api/v1/audit_log/get returns HTTP 200
    - response["audit_log"] array deserializes into Vec<ClarotyAuditLogEntry> (end-to-end schema parity)
    - response_entries is non-empty (AC-004)

SAP-2 gap: TOML column with no DTU field = P1 CRITICAL (would fail serde deserialization in Part 1).
No undeclared fields found — column parity confirmed: 5/5 TOML columns present in ClarotyAuditLogEntry.
