# Prism Workspace Crate Layout

> **Status:** TODO — populate during S-3.5.01 implementation phase.
> This file is a stub with headings and TODO placeholders (Red Gate scaffold).
> See `scripts/check-crate-layout.sh` and ADR-012 for authoritative rules.

---

## 1. Canonical Crate Shape

<!-- TODO(S-3.5.01): Insert directory tree showing canonical layout per ADR-012 §2.1.
     Example tree to document:
       crates/<name>/
         Cargo.toml
         build.rs          # optional; Cargo-mandated build script location
         fixtures/         # fixture data (NOT tests/fixtures/)
         src/
           lib.rs          # or main.rs for binaries
           tests/          # optional dedicated test submodule (src/tests/)
         tests/            # optional integration tests
-->

---

## 2. Layout Rules

<!-- TODO(S-3.5.01): Expand each rule with description, rationale, and examples.

| Rule | Description | Rationale |
|------|-------------|-----------|
| Rule 1 | `src/lib.rs` OR `src/main.rs` must exist | ... |
| Rule 2 | No `.rs` files at crate root except `build.rs` | ... |
| Rule 3 | Fixture data in `fixtures/` not `tests/fixtures/` | ... |
| Rule 4 | Inline `#[cfg(test)]` in `src/` is permitted | Not checked by script |
| Rule 5 | `src/tests/` submodule is permitted | Not checked by script |
| Rule 6 | `tests/` directory is optional | prism-ocsf exception |

Enforcement: `scripts/check-crate-layout.sh` checks Rules 1, 2, 3.
-->

---

## 3. Exceptions

### 3.1 `prism-ocsf` — No `tests/` Directory

<!-- TODO(S-3.5.01): Document that prism-ocsf has no tests/ directory and this
     is conformant. The tests/ directory is optional per ADR-012 §2.1 Rule 1.
     The script must NOT flag prism-ocsf for the absence of tests/.
     This exception is documented explicitly to prevent false violation reports
     (AC-005, BC-3.7.001 postcondition 5, edge case EC-003). -->

### 3.2 `build.rs` at Crate Root

<!-- TODO(S-3.5.01): Document that build.rs at a crate root is explicitly permitted
     and excluded from Rule 2 ("no loose .rs at crate root"). build.rs is the
     Cargo-mandated build script location; it cannot be placed elsewhere.
     (AC-007, BC-3.7.001 postcondition 7, edge case EC-004, ADR-012 §7 OQ-1). -->

---

## 4. FAQ

<!-- TODO(S-3.5.01): Write FAQ answers for:
  Q: Where do fixture files go?
  A: ...

  Q: Where do integration tests go?
  A: ...

  Q: Inline unit tests or src/tests/ submodule?
  A: ...

  Q: Is tests/ required?
  A: ...
-->

---

## 5. 22-Crate Conformance Table

<!-- TODO(S-3.5.01): Auto-populate this table via `just layout-report`
     (runs scripts/check-crate-layout.sh --markdown).

| Crate | src/lib.rs | No loose .rs | fixtures/ clean | Status |
|-------|-----------|--------------|-----------------|--------|
| ... (22 rows, one per workspace crate) |
-->

---

## 6. Usage

### Running the Layout Validator

```bash
# Plain output (exit 0 = all conformant, non-zero = violations listed)
just check-layout

# Markdown table output for updating this document
just layout-report
```

### Pre-Commit Hook

```yaml
# From lefthook.yml — runs automatically when crates/** files are staged:
layout:
  glob: "crates/**"
  run: scripts/check-crate-layout.sh
```

---

## 7. Adding a New Crate

<!-- TODO(S-3.5.01): Write checklist for conformant new-crate creation:
  1. Create crates/<name>/Cargo.toml
  2. Create crates/<name>/src/lib.rs (or src/main.rs)
  3. Place fixture data in crates/<name>/fixtures/ (not tests/fixtures/)
  4. Verify: bash scripts/check-crate-layout.sh
-->

---

*Generated by S-3.5.01 stub pass. Populated by implementation phase.*
*Traces to: BC-3.7.001, ADR-012, VP-134, VP-135, VP-136.*
