pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check {staged_files}
      stage_fixed: true
    clippy:
      glob: "*.rs"
      run: cargo clippy --all-features -- -D warnings

pre-push:
  commands:
    check:
      run: just check
