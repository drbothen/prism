---
document_type: adversarial-review-pass
story: S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001
pass_label: "LOCAL Pass 1 on 81372a22"
pass_number: 4
frozen_code_head: "81372a22"
diff_base: "903c8fcb"
story_version_reviewed: "v1.5"
adversary_agent: "orchestrator-directed (D-1340 fix-burst chain)"
date: "2026-06-25"
clean_strict: false
clean_pr_merge: false
streak_status: "RESET 0/3 (code HEAD moved to 9eb55cfe after fix-burst)"
findings_total: 4
findings_open: 0
findings_closed: 4
build_status: "just check EXIT=0 (full GREEN on diff 903c8fcb..81372a22; pre-pass verification)"
---

# Adversarial Review — LOCAL Pass 1 on 81372a22

**Story:** S-DEMO-PRISMQL-GRAMMAR-REMEDIATION-001 v1.5
**Frozen code HEAD reviewed:** `81372a22`
**Diff base:** `903c8fcb..81372a22`
**Story version read:** v1.5 (AC count 27, Red Gate count 20)
**`just check` on 81372a22:** EXIT=0 — full GREEN

## CLEAN verdict

- **CLEAN (strict):** NO — 2 HIGH + 2 MED/OBS findings
- **CLEAN (PR-merge):** NO — 2 HIGH findings present

## Summary

This pass reviewed `81372a22` (the implementer fix-burst HEAD following D-1340 label fix). The
pass revealed two HIGH-severity mode-bridge coverage gaps in the D1/D2 diagnostic contract
(BC-2.11.023) and two MED/OBS taxonomy drift items in the error-taxonomy. All four findings were
CLOSED in-scope via story-writer + product-owner fix-bursts. A new code commit (`9eb55cfe`)
followed the fix-burst, advancing the frozen HEAD. 3-CLEAN streak RESET to 0/3 on `9eb55cfe`.

## Findings

### HIGH-1 — BC-2.11.023 §D1 mode-bridge message content gap (CLOSED)

**ID:** HIGH-1
**Severity:** HIGH
**Finding:** The `filter_parser.rs` `parse_with_limits` SELECT-arm D1 mode-bridge error message
omitted the three verbatim substrings mandated by BC-2.11.023 §D1:
- (a) stage-keyword enumeration `(enrich, where, limit, sort, stats, dedup, fields)`,
- (b) numbered alternatives `1. SQL+pipe composition: …` / `2. Pipe mode only: …`,
- (c) `See prismql://reference for the complete grammar.` pointer.

AC-009 (story v1.4) asserted only a bare substring, not all three components — the assertion
underpinned a false-green test status.

**Root cause:** D1 message was written from memory, not verbatim from BC-2.11.023 §D1; AC-009
assertion scope was too narrow to catch the mismatch.

**Resolution:** CLOSED.
- Story-writer tightened AC-009 in story v1.5: now asserts all three verbatim BC-2.11.023 §D1
  substrings plus a negative control (no raw Chumsky token list).
- Implementer rewrote D1 message verbatim and updated test to assert all three substrings.
- Code commit: `a2fd18a4`.

### HIGH-2 — BC-2.11.023 §D2 mode-bridge diagnostic entirely unimplemented (CLOSED)

**ID:** HIGH-2
**Severity:** HIGH
**Finding:** The D2 mode-bridge diagnostic (SQL clause keyword in pipe-stage position, e.g.,
`FROM t | WHERE …` or `| ORDER BY …`) was entirely unimplemented. BC-2.11.023 §D2 and the
story Architecture Mapping both committed to this diagnostic, but no AC enumerated D2, no
production code implemented the detection, and no Red Gate test covered it.

**Root cause:** Story decomposition did not enumerate D2 as a separate AC; implementer had no
failing test to drive D2 implementation.

**Resolution:** CLOSED via orchestrator adjudication: "already-specced postcondition,
production-grade no-defer."
- Story-writer added AC-027 + Red Gate test stub `test_BC_2_11_023_mode_bridge_d2_sql_keyword_in_pipe_position`
  (Red Gate count 19→20, AC count 26→27; story v1.4→v1.5).
- Implementer added `rewrite_d2_sql_keyword_in_pipe_position` in `error_recovery.rs`, wired into
  `pipe_parser.rs`; verbatim D2 message; test
  `test_bc_2_11_023_mode_bridge_d2_sql_keyword_in_pipe_position` is load-bearing.
- Code commit: `9eb55cfe`. `WHERE`/`LIMIT` uppercase correctly excluded from D2 trigger per
  BC-2.11.023 §D2 intent.

### OBS-1 (MED) — E-QUERY-036 available_tables prose over-claimed (CLOSED)

**ID:** OBS-1
**Severity:** MED
**Finding:** E-QUERY-036 `available_tables` field in the error-taxonomy described its content as
"both sensor + internal tables," but the production implementation (`registered_sensor_ids()`)
populates only registered sensor source prefixes. The prose over-claimed coverage and would have
misled implementers of future query-planning logic.

**Resolution:** CLOSED.
- Product-owner reconciled error-taxonomy prose to "registered sensor source prefixes" in
  taxonomy v1.99.
- `Message Format` string unchanged (no code impact).

### OBS-2 (MED) — E-QUERY-001 row stale ParseErrorDetails reference (CLOSED)

**ID:** OBS-2
**Severity:** MED
**Finding:** E-QUERY-001 row in the error-taxonomy still referenced `ParseErrorDetails`, the
retired struct name. The canonical type has been `StructuredErrorFields` since BC-2.11.023 v1.2
(D-1329). Stale reference created a cross-document inconsistency.

**Resolution:** CLOSED.
- Product-owner synced E-QUERY-001 to `StructuredErrorFields` in taxonomy v1.99.

## Verified Clean (do NOT re-flag in subsequent passes)

The following items were explicitly verified clean during this pass. Any re-flagging requires
evidence of a NEW defect, not recurrence of these same checks:

- **Temporal NOW()/INTERVAL production wiring:** all 4 AST arms (SQL mode, SQL+Pipe composition,
  filter mode, SqlPipe head) have `inject_now` called with plan-time constant folding. Verified
  in code commit chain through `81372a22`.
- **FORBID-BOTH 0-row hoist Step 1b:** `plan_sqlpipe_query` correctly gates `E-QUERY-040`
  before DataFusion execution; data-independence verified with `EmptyVecAdapter` fixture.
- **Filter-mode load-bearing:** `test_crit1b_now_substituted_before_datafusion` is a
  discriminating test (not a no-op marker).
- **E-QUERY-036/040 verbatim:** error codes and message formats match error-taxonomy v1.98 prose
  (E-QUERY-036 label fix was the subject of D-1340; verified clean at `81372a22`).
- **SAP-1 catalog complete:** `filter.sql_lowering` and `filter.sql_planning_error` rows are
  present in BC-2.16.002 §Postconditions (added D-1340 PO burst). No uncatalogued `event_type =`
  values in `crates/prism-query/src/` or `crates/prism-mcp/src/`.
- **Arc-DI BC-2.10.015:** `OrgSlug::new` constructor and `slug_exists` gate wired correctly;
  no `new_unchecked` in production path.
- **BC-2.10.016/017 fast-fail guard order:** `NOT_YET_AVAILABLE_TOOLS` returns `-32003` before
  any blocking I/O; verified in test `test_MCP_01_partition_positive_coverage`.
- **Anti-tautology CI gate:** `tests/external/non-exhaustive-violation/` gate at `EXPECTED=87`
  in worktree (develop=84); no regression.
- **SID-1 PASS:** no `#[ignore]`'d integration tests without a concrete blocking dependency
  comment and future story anchor.
- **SAP-2 N/A:** no `.prism/specs/sensors/*.toml` changes in this diff.

## Fix-burst tracking

| Finding | Routed to | Code commit | Status |
|---------|-----------|-------------|--------|
| HIGH-1 D1 message + AC-009 | story-writer (AC-009) + implementer (code) | story v1.5 + `a2fd18a4` | CLOSED |
| HIGH-2 D2 unimplemented + AC-027 | orchestrator adjudication → story-writer (AC-027) + implementer (code) | story v1.5 + `9eb55cfe` | CLOSED |
| OBS-1 E-QUERY-036 prose | product-owner (taxonomy) | taxonomy v1.99 | CLOSED |
| OBS-2 E-QUERY-001 stale ref | product-owner (taxonomy) | taxonomy v1.99 | CLOSED |

## Post-pass state

- **New frozen code HEAD:** `9eb55cfe`
- **Story version:** v1.5 (AC count 27, Red Gate count 20)
- **error-taxonomy version:** v1.99
- **3-CLEAN streak:** RESET to 0/3 (code HEAD moved; DRIFT-ORCH-PRLEVEL-PUSH-001 rule)
- **`just check` on 9eb55cfe:** confirmation pending (prior HEADs all EXIT=0; background)
- **Next action:** confirm `just check` EXIT=0 on `9eb55cfe` → LOCAL adversary cascade
  Pass 1 on `9eb55cfe` (need 3 consecutive CLEAN(strict) on unchanged `9eb55cfe`)
