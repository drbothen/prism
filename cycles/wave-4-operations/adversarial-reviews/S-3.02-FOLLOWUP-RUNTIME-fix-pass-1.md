---
document_type: fix-pass-closure-report
target_artifact: S-3.02-FOLLOWUP-RUNTIME (story implementation)
fix_pass_n: 1
review_class: LOCAL
target_sha: 99d49b20229e798688bfe8ddb6a4dfb6db1f6cce
prior_target_sha: a6380143f04106e66d7952a9d0c3a20089b9f4d9
base_sha: c98a38b0
worktree: .worktrees/S-3.02-FOLLOWUP-RUNTIME
branch: feature/S-3.02-FOLLOWUP-RUNTIME
fix_pass_dispatcher: orchestrator (per Standing Rule 3)
implementer_agent: vsdd-factory:implementer
fix_timestamp: "2026-05-10T00:00:00Z"
backfill_timestamp: "2026-05-10T00:00:00Z"
backfill_dispatcher: orchestrator
findings_addressed: 5 CRIT + 7 HIGH + 5 MED + 4 LOW + 3 OBS (adversary pass-1)
verification_status: claimed_by_implementer (adversary pass-2 pending independent verification)
test_result: "874 prism-query tests pass; just check 3472/3472 pass"
prior_passes: ["S-3.02-FOLLOWUP-RUNTIME-pass-1.md"]
---

# LOCAL Fix-Pass-1 Closure Report — S-3.02-FOLLOWUP-RUNTIME

## Status: CLAIMED-CLOSED (pending adversary pass-2 verification per Standing Rule 3)

target_sha: `99d49b20229e798688bfe8ddb6a4dfb6db1f6cce`
prior_target_sha: `a6380143` (adversary pass-1 base)
base_sha: `c98a38b0`

## Commit Log (worktree)

```
99d49b20 style(prism-query): cargo fmt after HIGH-2 implementation
73d97726 fix(prism-query): close F-LP1 adversary findings — CRIT-4, HIGH-3/4/7, MED-3, plus pre-existing clippy fixes
a6380143 feat(prism-query): implement QueryEngine execution pipeline — S-3.02-FOLLOWUP-RUNTIME
```

## Verification Status

- `cargo nextest run -p prism-query --no-fail-fast`: **874 tests pass, 6 skipped**
- `just check` (full canonical): **3472 tests pass, 17 skipped**
- Test suite GREEN
- 8 new tests added in this fix-pass (CRIT-1, CRIT-4×2, HIGH-2, HIGH-3×2, HIGH-4, HIGH-7×2, MED-3)

## Per-Finding Closure (claimed by implementer)

### CRITICALs (5/5 claimed closed)

**F-LP1-CRIT-1** (register_internal_tables not invoked from QueryEngine::execute) — CLOSED
- Fix: `execute_inner` calls `register_internal_tables(&session_ctx, storage.clone())?` before materialization when storage is `Some`. `execute_scheduled` does the same.
- Evidence: `crates/prism-query/src/engine.rs:414-420`
- New test: `test_CRIT_1_internal_table_queryable_through_execute` verifies `prism_audit` queryable via `QueryEngine::execute` (not standalone)

**F-LP1-CRIT-2** (Cross-sensor auth downcast bug) — CLOSED
- Fix: `materialization.rs` calls `prism_sensors::fan_out()` with `Arc<dyn CredentialResolver>`. No placeholder `CrowdStrikeAuth`.
- Evidence: `crates/prism-query/src/materialization.rs:407-470`

**F-LP1-CRIT-3** (Production CrowdStrikeAdapter org_id mismatch) — CLOSED
- Fix: `resolve_org_id()` uses OrgRegistry lookup (production) or first registered adapter's OrgId (test fallback). No `OrgId::new()` randomness.
- Evidence: `crates/prism-query/src/materialization.rs:690-713`

**F-LP1-CRIT-4** (AC-1 and AC-7 vacuous-pass with empty registry) — CLOSED
- Fix: AC-1 and AC-7 register `StubAdapter` (3 rows each). Added precondition asserts `!result.batches.is_empty()` and `result.returned_results > 0` that fail fast on empty fixture regressions.
- Evidence: `crates/prism-query/tests/execute_integration_tests.rs:302-416` and `:684-800`

**F-LP1-CRIT-5** (sensor_errors silently discarded) — CLOSED
- Fix: `run_materialization_pipeline` returns `MaterializationOutput { batches, sensor_errors, registered_tables }`. `execute_inner` propagates `sensor_errors` to `QueryResult.sensor_errors`.
- Evidence: `crates/prism-query/src/materialization.rs:141-148`, `crates/prism-query/src/engine.rs:476`

### HIGHs (7/7 claimed closed)

**F-LP1-HIGH-1** (Filter and Pipe modes silently discard) — CLOSED
- Fix: `execute_against_session` returns `table_batches.into_values().flatten().collect()` for `Ast::Filter` and `Ast::Pipe`.
- Evidence: `crates/prism-query/src/materialization.rs:551-556`

**F-LP1-HIGH-2** (RocksDbTableProvider::scan does NOT deserialize bincode 2.x) — CLOSED
- Fix: `build_batch_from_kv` is domain-aware. For `StorageDomain::AuditBuffer`, deserializes `AuditEntry` via `bincode::serde::decode_from_slice`. Failed deserialization → empty strings (graceful degradation).
- Cargo dep: `bincode = {version = "2", features = ["serde"]}` added to prism-query
- New test: `test_HIGH_2_audit_entry_bincode_deserialization`
- Evidence: `crates/prism-query/src/internal_tables.rs:342-480`

**F-LP1-HIGH-3** (requires_audit_read capability gate unimplemented) — CLOSED
- Fix: `check_internal_table_capabilities(query_str, &options.capabilities)?` called in `execute_inner` before any storage scan. Parses AST, walks source refs, returns `PrismError::AuditTableAccessDenied` (E-QUERY-011) if `prism_audit` referenced without `Capability::AuditRead`.
- New tests: `test_HIGH_3_audit_read_capability_gate_deny` + `test_HIGH_3_audit_read_capability_gate_allow`
- Evidence: `crates/prism-query/src/engine.rs:586-627`

**F-LP1-HIGH-4** (Internal tables don't get virtual field injection) — CLOSED
- Fix: `InternalTableDescriptor.full_schema()` appends `_sensor`, `_client`, `_source_table`. `RocksDbTableProvider::schema()` returns `full_schema()`. `inject_internal_virtual_fields()` populates `_sensor="prism"`, `_client="<system>"`, `_source_table=<table_name>`.
- New test: `test_HIGH_4_internal_table_virtual_fields_present`
- Evidence: `crates/prism-query/src/internal_tables.rs:82-99, 155-158, 216-328`

**F-LP1-HIGH-5** (AC-4 push-down bypasses pushdown::classify_predicates) — CLOSED
- Fix: `extract_push_down_filters_as_map()` uses `collect_eq_filters()` walking the AST Predicate tree (same logic as `pushdown::classify_predicates` infrastructure).
- Evidence: `crates/prism-query/src/materialization.rs:748-790`

**F-LP1-HIGH-6** (AC-6 fan-out duplicates work via get_all_for_sensor_type) — CLOSED
- Fix: `fan_out()` uses `registry.get(target.org_id, target.sensor_type)` for per-org dispatch. `resolve_source_refs` produces one target per (org_id, sensor_type) pair via `get_all_for_sensor_type` only for "ALL clients" scope (no explicit client list), which is correct.
- Evidence: `crates/prism-query/src/materialization.rs:607-680`

**F-LP1-HIGH-7** (BC-2.11.001 max-1000 limit not enforced) — CLOSED
- Fix: Enforcement was already in `engine.rs:365-373`. Added tests `test_HIGH_7_limit_exceeds_1000_returns_error` (limit=1001 → E-QUERY-001) + `test_HIGH_7_limit_exactly_1000_is_allowed` (boundary).
- Evidence: `crates/prism-query/tests/execute_integration_tests.rs:808-860`

### MEDs (4/5 closed; 1 out-of-scope)

**F-LP1-MED-1** (step7_init_storage in prism-bin still todo!()) — OUT-OF-SCOPE (blocks S-5.01-FOLLOWUP-MCP-BOOT). Engine-side wiring functional via F-LP1-CRIT-1.
**F-LP1-MED-2** (in_query_cache allocated but never used) — CLOSED. Cache lookup/insert wired in fan-out loop.
**F-LP1-MED-3** (AC-3 narrowed to 1-row cap) — CLOSED. Added `test_AC_3_bis_size_limit_at_10k_boundary` — two adapters × 6K rows = 12K → E-QUERY-003.
**F-LP1-MED-4** (safety_check infrastructure not consulted) — CLOSED (already correct). Parse-time security guards (depth, size, stage count) enforced inside `PrismQlParser::parse` via `security::ParseLimits` in `filter_parser.rs`.
**F-LP1-MED-5** (Test registries don't share state) — PARTIALLY ADDRESSED. AC-1/AC-7 use proper StubAdapter registries; `make_mat_ctx_with_stub` and `make_engine` still use separate registries (test independence).

### LOWs / OBSs (triaged)

**F-LP1-LOW-1** — Factory artifact (story §Tasks step 8 timeout error code). Surfaced for product-owner.
**F-LP1-LOW-2** — Documentation correct (DataFusion requires backtick-quoting for dot-named tables).
**F-LP1-LOW-3** — Documented behavior, no action.
**F-LP1-LOW-4** — Deliberate MVP scope per story §Tasks step 6.
**F-LP1-OBS-1** — Error format leak risk: error messages now use `fan_err.error.error_code()` (code-only).
**F-LP1-OBS-2/3** — Process-gap items, not code fixes.

## Files Modified

**prism-query crate only — no cross-crate touches:**
- `crates/prism-query/Cargo.toml` — bincode 2 dep added
- `crates/prism-query/src/engine.rs` — capability gate, Capability enum, QueryOptions.capabilities, HIGH-7 tests
- `crates/prism-query/src/internal_tables.rs` — full_schema, virtual field injection, domain-aware deserialization
- `crates/prism-query/src/materialization.rs` — capability check fn pub(crate), cache_key format
- `crates/prism-query/tests/execute_integration_tests.rs` — 8 new tests
- Pre-existing clippy fixes: write_dispatch.rs, write_pipeline.rs, write_pipeline_tests.rs, vp013_cycle_detection.rs, pagination_tests.rs

## Implementer's Architectural Correction Acknowledgement

The implementer's prior pass-1 report claimed `prism-sensors::fanout::CredentialResolver` was sealed and could not be implemented externally. This claim was incorrect; only `SensorAuth` is sealed. `CredentialResolver` is publicly implementable. The existing `prism-sensors::fanout::fan_out()` API was sufficient to close CRIT-2/CRIT-3 without any modifications to `prism-sensors` or `prism-core`. This correction was applied in this fix-pass.

## Adversary Pass-2 Pending

Per Standing Rule 3 (Production-Grade Closure Discipline), implementer self-disclosure is NOT authoritative for fix verification. Adversary pass-2 with fresh context dispatched to independently verify all CLAIMED-CLOSED findings.

The two findings most at risk of false-CLOSE per pass-1 finding pattern:
- F-LP1-CRIT-2/CRIT-3 claimed "CLOSED in prior implementation" — but pass-1 explicitly cited `materialization.rs:400-404` placeholder `CrowdStrikeAuth` and `materialization.rs:384` `OrgId::new()` as defects in a6380143. Either pass-1 was wrong about a6380143, or 73d97726 silently fixed these without commit-message acknowledgement. Pass-2 must verify which.
- F-LP1-CRIT-5 same pattern (claimed prior-implementation CLOSED but pass-1 cited as defect at the same SHA).
