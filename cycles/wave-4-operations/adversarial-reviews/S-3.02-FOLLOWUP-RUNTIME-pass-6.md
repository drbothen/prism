---
document_type: adversarial-review-report
target_artifact: S-3.02-FOLLOWUP-RUNTIME (story implementation)
pass_n: 6
review_class: LOCAL
target_sha: dcc11f68
prior_target_sha: d7e32ab1
base_sha: c98a38b0
worktree: .worktrees/S-3.02-FOLLOWUP-RUNTIME
branch: feature/S-3.02-FOLLOWUP-RUNTIME
verdict: BLOCKED-soft
streak: 0/3
finding_summary:
  pass5_closures_verified_full: 1
  pass5_closures_no_code_action: 1
  novel_critical: 0
  novel_high: 0
  novel_medium: 0
  novel_low: 1
  novel_observation: 0
  kudos: 5
  process_gaps: 0
prior_passes: ["S-3.02-FOLLOWUP-RUNTIME-pass-1.md", "S-3.02-FOLLOWUP-RUNTIME-fix-pass-1.md", "S-3.02-FOLLOWUP-RUNTIME-pass-2.md", "S-3.02-FOLLOWUP-RUNTIME-fix-pass-2.md", "S-3.02-FOLLOWUP-RUNTIME-pass-3.md", "S-3.02-FOLLOWUP-RUNTIME-fix-pass-3.md", "S-3.02-FOLLOWUP-RUNTIME-pass-4.md", "S-3.02-FOLLOWUP-RUNTIME-fix-pass-4.md", "S-3.02-FOLLOWUP-RUNTIME-pass-5.md", "S-3.02-FOLLOWUP-RUNTIME-fix-pass-5.md"]
reviewer_agent: vsdd-factory:adversary
review_timestamp: "2026-05-10"
backfill_timestamp: "2026-05-10"
backfill_dispatcher: orchestrator (Standing Rule 1)
policy_rubric_version: "1.7"
standing_rule_3_violations: 1
---

# Adversarial Review Pass 6 — S-3.02-FOLLOWUP-RUNTIME (LOCAL)

**Verdict: BLOCKED-soft** — streak 0/3 (orchestrator reset on Standing Rule 3 #3
sub-clause (b) per user mandate). F-LP6-LOW-1 surfaced from explicit AST
exhaustiveness audit.

## KUDOs (5)

**K-LP6-1:** F-LP5-LOW-1 CLOSED — both `extract_source_names_shallow`
(materialization.rs:827-837) and `extract_source_names_recursive`
(materialization.rs:882-892) now walk `pipe.stages` for `PipeStage::Join` with
anchor comments citing F-LP5-LOW-1 / C-LOCAL-001 / explain.rs:489-499.

**K-LP6-2:** `test_LP5_LOW_1_pipe_join` at materialization.rs:1292-1348 is
non-vacuous bidirectional — 4 assertions across both walkers;
`SourceRefKind::Internal(InternalTable::Audit)` classifier verified. Reverting
either arm fails the test.

**K-LP6-3:** explain.rs:489-499 sibling unchanged; C-LOCAL-001 anchor preserved.

**K-LP6-4:** F-LP5-OBS-1 deferral honest; no false coverage claim.

**K-LP6-5:** Severity peak strictly decreasing:
CRIT → CRIT → CRIT → MED → LOW → LOW.

## Pass-5 Closure Verification

| Finding | Status | Evidence |
|---------|--------|----------|
| F-LP5-LOW-1 | VERIFIED-CLOSED | Both walker arms updated; anchor comments present; bidirectional test passes |
| F-LP5-OBS-1 | NO-CODE-ACTION (acceptable) | Process-gap deferred with honest rationale; no false claim of coverage |

## Novel Finding F-LP6-LOW-1

**`extract_source_names_recursive` and `extract_sources_from_ast` silently drop
`Ast::Sql(SqlStatement::Dml(...))`**

**Severity:** LOW (latent — currently unexploitable end-to-end through
`QueryEngine::execute` because Phase 3 fetch in write_pipeline.rs is stubbed;
activates once S-3.06/S-3.07 wires DML through QueryMaterializer).
**Confidence:** HIGH. **Standing Rule 3 sub-rule #3 sub-clause (b).**

### Locations

- materialization.rs:875-893 — `extract_source_names_recursive` matches only
  `Ast::Sql(SqlStatement::Select)`; `Dml` falls through wildcard `_`
- explain.rs:479-501 — `extract_sources_from_ast` same gap (matches only
  `SqlStatement::Select` at line 483; DML hits `_` at line 500)
- engine.rs:622 — single call site for the security gate; routes through
  `PrismQlParser::parse` which DOES produce `Ast::Sql(SqlStatement::Dml(DmlNode))`

### AST Evidence

- `Ast::Sql` wraps `SqlStatement` (ast.rs:47)
- `SqlStatement` variants: `Select(SqlQuery)` (ast.rs:88) + `Dml(DmlNode)` (ast.rs:95)
- `DmlNode` carries: `filter: Option<Predicate>` (write_ast.rs:126),
  `source_select: Option<SqlQuery>` (write_ast.rs:129)
- Visitor trait at visit.rs:170-171 already handles DML correctly via
  `visitor.visit_sql_query(source_select)` — ad-hoc walkers don't use it

### Exploit Path (Latent)

```sql
INSERT INTO crowdstrike_contained_hosts SELECT host_id FROM prism_audit WHERE severity = 'high'
DELETE FROM crowdstrike_contained_hosts WHERE host_id IN (SELECT trace_host FROM prism_audit)
```

Both parse into `SqlStatement::Dml`. Layer 1 capability gate at engine.rs:613-633
extracts ZERO source names; `AuditRead` unenforced against the `source_select`
subquery.

**Currently inert:** write_pipeline.rs:344-349 says "Phase 3: Fetch (stub —
QueryMaterializer integration is S-3.02). For S-3.07: fetched_records is empty";
`materialization::execute_against_session` at materialization.rs:565-567 returns
`Vec::new()` for non-Select/Filter/Pipe ASTs.

**Maturation path:** Once `WriteExecutor::execute` (write_pipeline.rs:271) wires
Phase 3 fetch to actually invoke `run_materialization_pipeline` on reconstructed
`source_select`, the gate is bypassed and `prism_audit` rows leak without
`Capability::AuditRead`.

### Doc-Drift Contradiction (POL-4)

- materialization.rs:852 declares "Recursively extract ALL source table names" —
  implies completeness
- engine.rs:594-598 declares "extract every referenced prism_* table name" —
  implies completeness
- DML branch silently dropped → POL-4 + Standing Rule 3 #3 violation

### Why LOW (not MED/HIGH)

1. Currently unexploitable end-to-end (Phase 3 stubbed; non-SELECT returns
   `Vec::new()`)
2. DML target_table write-protection rejects `prism_*` at parse time
   (sql_parser.rs:817) — partial defense on TARGET only, not `source_select`
3. Pre-existing latent gap; surfaced by explicit AST exhaustiveness audit

### Recommendation

Mirror `Ast::Sql` arm to handle `SqlStatement::Dml` in both walkers (~10-line change
each). For each `DmlNode`: walk `source_select` via `walk_sql_query` and `filter`
via `walk_predicate`. Plus parallel arm in explain.rs::extract_sources_from_ast (its
purpose is "sensors to query"; DML source_select sensors must show up in EXPLAIN
otherwise EXPLAIN under-reports). Plus 1 RED→GREEN test exercising
`Ast::Sql(SqlStatement::Dml { source_select: Some(SELECT FROM prism_audit), .. })`.

### Lineage Trace

F-LP3-CRIT-1 (WHERE InSubquery) → F-LP4-MED-1 (FuncCall args) → F-LP5-LOW-1
(PipeStage::Join sources) → **F-LP6-LOW-1 (DML source_select / filter)**. After
this closes, Layer 1 walker is genuinely exhaustive across all four query modes
(SELECT, DML, Pipe, Filter).

## Defense-in-Depth Integrity Restatement

| Layer | Query Mode | Status |
|-------|-----------|--------|
| Layer 1 | SQL SELECT | INTACT |
| Layer 1 | SQL DML | LATENT-GAP F-LP6-LOW-1 |
| Layer 1 | Pipe | INTACT (post fix-pass-5) |
| Layer 1 | Filter | INTACT (parser cannot construct InSubquery) |
| Layer 2 | All | INTACT |
| Layer 3 | All | INTACT |

**Aggregate:** HOLDS for SELECT/Pipe/Filter (all production read paths); LATENT GAP
for DML pending S-3.07 wiring.

## Policy Rubric

| Policy | Verdict | Note |
|--------|---------|------|
| POL-1 | PASS | |
| POL-2..POL-3 | N/A | |
| POL-4 | VIOLATION (soft) | F-LP6-LOW-1 doc claims "ALL"/"every"; DML silently dropped |
| POL-5..POL-12 | N/A or PASS | |
| POL-13 | NEEDS-VERIFICATION | Carryover F-LP5-OBS-1 |
| POL-14 | N/A | |
| POL-15 | VIOLATION (soft, accepted) | Carryover F-LP1-MED-1 |
| POL-16 | PASS | |
| POL-18 | PASS | |

## Standing Rule 3 Assessment

| Sub-Rule | Status | Note |
|----------|--------|------|
| 1. No surface-and-defer-via-error | CLEAN | |
| 2. No MVP-limitation deferrals without anchor | CLEAN | |
| 3. No documented-but-unenforced gates | PARTIAL VIOLATION | F-LP6-LOW-1 sub-clause (b) |
| 4. Architectural correction in scope | SATISFIED | |
| 5. Last-resort surface-for-approval | CLEAN | |

Net: 1 partial Standing Rule 3 violation (F-LP6-LOW-1, sub-rule #3 sub-clause (b),
LOW).

## Process Gaps

None new. F-LP5-OBS-1 standing process gap reinforced by F-LP6-LOW-1 second
manifestation. Visit trait at visit.rs:151,170-171 already correctly dispatches
`Dml` — ad-hoc walkers don't use the right machinery.

## Verification Performed

Files read (line ranges):

- materialization.rs (800-900, 1100-1135, 1280-1350, 311-350, 525-575, 770-800)
- explain.rs (460-505)
- engine.rs (350-400, 395-470, 588-635)
- ast.rs (40-180, 200-250, 260-310, 405-430, 685-750)
- write_ast.rs (95-135)
- filter_parser.rs (60-170, 190-260, 425-445)
- sql_parser.rs (800-900)
- write_pipeline.rs (1-80, 271-410)
- safety_check.rs (selected)
- internal_tables.rs (1-20, 640-680)

Grep verification:

- All five `SourceRef`-carrying positions in ast.rs enumerated and traced:
  `FilterExpr.source` (62), `FromClause.source` (210), `Join.source` (229 SQL),
  `PipeQuery.source` (268), `JoinStage.source` (415); all five reachable;
  `JoinStage.source` newly covered post fix-pass-5
- All nine `PipeStage` variants enumerated (ast.rs:282-301): Where, Sort, Limit,
  Tail, Stats, Dedup, Fields, Join, Enrich; only Join carries SourceRef directly
- pipe_parser.rs and filter_parser.rs cannot construct InSubquery (zero matches in
  grep)
- InSubquery constructed only by sql_parser.rs (sql_parser.rs:495 + 549)
- `SqlStatement::Dml` parser-reachable end-to-end via `PrismQlParser::parse` →
  filter_parser.rs:197-202 → `parse_dml_internal` at filter_parser.rs:140-142
- Visitor trait at visit.rs:151 matches `SqlStatement::Dml(dml)` →
  `v.visit_dml_node(dml)`; :170-171 walks `source_select` — codebase has the right
  machinery; ad-hoc walkers don't use it
- explain.rs sibling at :489-499 byte-for-byte unchanged; C-LOCAL-001 anchor
  preserved
- `test_LP5_LOW_1_pipe_join` (materialization.rs:1292-1348) non-vacuous: 4
  assertions across both walkers, `SourceRefKind::Internal(InternalTable::Audit)`,
  reverting either arm fails

Tests NOT RUN (read-only adversary; relying on implementer's `just check` PASS / 888
prism-query).

## Convergence Position

- **BLOCKED-soft**, streak 0/3 (orchestrator reset)
- Severity peak strictly decreasing
- After F-LP6-LOW-1 closes (mirror DML walking into both walkers + 1 mirror test on
  each + doc enumeration update at materialization.rs:852-869 and
  engine.rs:594-598), Layer 1 walker becomes genuinely exhaustive across all four
  query modes
- Pass-7 should be convergence-target pass; expected zero novel findings → streak 1/3

## Novelty Assessment

| Field | Value |
|-------|-------|
| **Pass** | 6 |
| **New findings** | 1 LOW |
| **Duplicate/variant** | 0 |
| **Novelty score** | 0.80 (sibling-pattern lineage but previously unflagged AST class — DML — uncovered by explicit final exhaustiveness audit) |
| **Median severity** | LOW |
| **Trajectory** | pass-1 12 → pass-2 8 → pass-3 3 → pass-4 1MED+3OBS → pass-5 1LOW+1OBS → pass-6 1LOW — strictly decreasing severity peak; finding count at low-severity floor |
| **Verdict** | FINDINGS_REMAIN-MINOR |
