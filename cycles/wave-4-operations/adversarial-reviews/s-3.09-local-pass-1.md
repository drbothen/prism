# S-3.09 LOCAL Adversarial Pass-1

**Date:** 2026-05-08
**Worktree:** /Users/jmagady/Dev/prism/.worktrees/S-3.09
**Reviewer:** adversary (fresh context)
**Status:** DIRTY — false convergence confirmed; same TD-S307-002/003 silent-shallow-test pattern recurs verbatim

## Severity Counts

- CRITICAL: 6
- HIGH: 5
- MEDIUM: 4
- LOW: 2
- OBSERVATION: 3

## Findings

### F-PASS1-CRIT-1: `QueryEngine::execute()` is `todo!()` — Task 2 fully unimplemented; instrumentation entirely absent

- **Category:** spec-compliance / silent-shallow-test (TD-S307-002 cluster recurrence)
- **Location:** `crates/prism-query/src/engine.rs:283-303`
- **Severity:** CRITICAL
- **Confidence:** HIGH
- **Problem:** Story Task 2 requires "Instrument `QueryEngine::execute()` in `engine.rs`" with `Instant` checkpoints. Implementation: `todo!("S-3.02 — QueryEngine::execute")`. `execute_scheduled()` is also `todo!()` (line 332-346). No `Instant::now()` calls exist in engine.rs. No `try_lock()` against `metrics_buffer` anywhere in production code.
- **Required Fix:** Implement Task 2 fully OR explicitly defer via TD entry with STORY-INDEX status downgrade.
- **Routing:** implementer + product-owner

### F-PASS1-CRIT-2: `metrics_buffer` field is dead code — never read or written by production paths

- **Category:** spec-compliance / dead-code
- **Location:** `crates/prism-query/src/engine.rs:185, 234, 246`
- **Severity:** CRITICAL
- **Problem:** `metrics_buffer: Arc<Mutex<QueryMetricsBuffer>>` field is constructed but no production code path calls `.lock()` or `.try_lock()` on it. No public accessor.
- **Required Fix:** Implement push path in `execute()` and `execute_scheduled()` per Task 4. Add public accessor.
- **Routing:** implementer

### F-PASS1-CRIT-3: `prism-mcp::diagnostics::get_fanout_diagnostics()` returns hardcoded zeros — Task 5 fully unimplemented

- **Category:** spec-compliance / silent-shallow-test
- **Location:** `crates/prism-mcp/src/diagnostics.rs:69-97`
- **Severity:** CRITICAL
- **Problem:** Task 5 requires feeding QueryMetrics into get_diagnostics(subsystem: "fanout"). Implementation hardcodes `recent_query_metrics: json!([])`, `aggregate_stats: { avg: 0.0, p50: 0, p95: 0 }`, `metrics_dropped_count: 0`. Two MCP tests assert only structural shape — they pass against hardcoded stub.
- **Required Fix:** Plumb `Arc<Mutex<QueryMetricsBuffer>>` into `get_fanout_diagnostics`. Replace zeros with `buffer.snapshot()` / `buffer.aggregate_stats()` / `buffer.dropped_count()`. Rewrite tests to push 5 metrics, then assert response array length == 5.
- **Routing:** implementer + test-writer

### F-PASS1-CRIT-4: `explain.rs` AC-6 actuals path is unimplemented; `ExplainResult` lacks any `actuals` field

- **Category:** spec-compliance / silent-shallow-test
- **Location:** `crates/prism-query/src/explain.rs:1051-1061, 161-187`
- **Severity:** CRITICAL
- **Problem:** Task 6 requires explain_query include_actuals lookup. Implementation is `let _ = &options.include_actuals;` (no-op). `ExplainResult` struct lacks any `actuals` field. AC-6 test pushes into a separate locally-constructed buffer and asserts struct field round-trip — never invokes explain function for actuals branch.
- **Required Fix:** Add `pub actuals: Option<QueryMetrics>` to `ExplainResult`. Plumb buffer reference. Implement lookup. Update test to call `engine.explain(...)` after pushing a metric and assert `result.actuals.is_some()`.
- **Routing:** implementer + test-writer

### F-PASS1-CRIT-5: `QueryResult.context` has no `metrics` field — `_meta.metrics` envelope is structurally impossible

- **Category:** spec-compliance / silent-shallow-test
- **Location:** `crates/prism-query/src/engine.rs:106-134`; `crates/prism-mcp/src/safety_envelope.rs:39-51`
- **Severity:** CRITICAL
- **Problem:** Task 3 requires QueryMetrics in `_meta` envelope. `QueryResult` has no `metrics` field. `ResponseMeta` has no `metrics` field. There is no plumbing between `QueryEngine::execute()` and the MCP envelope for metrics.
- **Required Fix:** Add `pub metrics: QueryMetrics` to `QueryResult` (or context). Add `pub metrics: Option<QueryMetrics>` to `ResponseMeta`. Wire engine to populate. Add E2E test serializing the envelope.
- **Routing:** implementer + test-writer

### F-PASS1-CRIT-6: All seven AC tests are silent-shallow tests — they manually populate `QueryMetrics` instead of exercising `QueryEngine::execute()`

- **Category:** test-quality / TD-S307-002 cluster recurrence
- **Location:** `crates/prism-query/tests/profiling_integration_tests.rs` (entire file); `crates/prism-query/src/tests/metrics_tests.rs`
- **Severity:** CRITICAL
- **Problem:** Every AC test uses `make_two_source_metrics()` helper that hand-builds `QueryMetrics` with hardcoded values. AC-1/2/3 consume this and assert their own inserted values are present (tautology). AC-4 builds local `QueryMetricsBuffer`, never calls `get_diagnostics`. AC-6 pushes into local buffer, asserts struct field round-trip. AC-7 loops over 3 references and assigns identical values via shared loop body — mathematically tautological. EC-11-081 contention test exercises atomic on a SECOND standalone buffer, not the contended one.
- **Required Fix:** Rewrite all integration tests as true E2E:
  - AC-1/2/3/7: build QueryEngine with mock AdapterRegistry, call `engine.execute(...)`, assert returned `QueryResult.metrics` populated.
  - AC-4: push 5 metrics via `engine.execute()`, call `get_diagnostics({subsystem: "fanout"})`, assert array length 5 with required fields.
  - AC-6: push metric via execute(), call `engine.explain(...)`, assert `result.actuals.is_some()`.
  - EC-11-081: hold mutex from helper thread, invoke `engine.execute()` (must use try_lock internally), assert `dropped_count() == 1` on same buffer.
- **Routing:** test-writer (primary) + implementer

### F-PASS1-HIGH-1: AC-1 boundary coverage absent — only 2 sensors tested

- **Category:** test-quality / coverage-gap
- **Location:** `crates/prism-query/tests/profiling_integration_tests.rs:63-104`
- **Required Fix:** Parametrize over N ∈ {0, 1, 2, 3, 5, 10} sensors and M ∈ {1, 2, 3} clients.
- **Routing:** test-writer

### F-PASS1-HIGH-2: AC-5 boundary cases (capacity 0, capacity 1) untested — `push()` may violate invariant on capacity 0

- **Category:** test-quality / code-quality
- **Location:** `crates/prism-query/src/ring_buffer.rs:82-87`
- **Required Fix:** `debug_assert!(capacity > 0)` in `new()` OR handle capacity=0 by returning early. Add tests for new(0), new(1), new(2).
- **Routing:** implementer + test-writer

### F-PASS1-HIGH-3: AC-7 mode-agnosticism is asserted tautologically

- **Category:** test-quality
- **Location:** `crates/prism-query/tests/profiling_integration_tests.rs:344-427`
- **Required Fix:** Replace with real engine-driven test executing filter/SQL/pipe variants.
- **Routing:** test-writer

### F-PASS1-HIGH-4: EC-11-081 contention test exercises atomic API on a different buffer than the contended one

- **Category:** test-quality / silent-shallow-test
- **Location:** `crates/prism-query/src/tests/metrics_tests.rs:112-177`
- **Required Fix:** Test must exercise production caller under contention, asserting same buffer's dropped_count increments.
- **Routing:** test-writer

### F-PASS1-HIGH-5: BC-2.11.001 anchor enrichment claim not verified by any BC contract test in this story

- **Category:** governance / spec-compliance
- **Location:** Story file, lines 62, 72
- **Required Fix:** Implement envelope wiring per CRIT-5, OR amend BC-2.11.001 with `_meta.metrics` extension postcondition, OR author dedicated BC-2.11.NNN.
- **Routing:** product-owner + implementer

### F-PASS1-MED-1: AC-6 + EC-11-082 do not test malformed/empty/non-UTF8 trace_id inputs

- **Category:** test-quality / coverage-gap
- **Required Fix:** Add EC variants for empty/whitespace trace_id; document treatment as missing.
- **Routing:** test-writer + story-writer

### F-PASS1-MED-2: `QueryMetrics` JSON field ordering is not stable — HashMap fields produce non-deterministic JSON

- **Category:** code-quality / catalog↔impl coherence
- **Location:** `crates/prism-query/src/metrics.rs:46-68`
- **Required Fix:** Replace `HashMap` with `BTreeMap` for `per_source_latency_ms` and `rate_limit_remaining`. Add determinism test.
- **Routing:** implementer + story-writer

### F-PASS1-MED-3: `aggregate_stats` percentile formula is documented inconsistently — test accepts ambiguous values

- **Category:** code-quality / catalog↔impl coherence
- **Location:** `crates/prism-query/src/ring_buffer.rs:117-125`
- **Required Fix:** Pin canonical percentile formula in spec. Tighten test from disjunction to equality.
- **Routing:** implementer + story-writer

### F-PASS1-MED-4: Story claims `verification_properties: []` while defining hard architecture invariants — VP coverage gap

- **Category:** governance / VP coverage
- **Required Fix:** Author 1-2 VPs (proptest tier) for non-blocking-push and key-format invariants. Update VP-INDEX.
- **Routing:** product-owner / VP author. (Closed by v1.6 amendment adding VP-014/VP-015.)

### F-PASS1-LOW-1: `QueryMetrics::new` minor allocation inefficiency

- **Category:** code-quality
- **Required Fix:** Add `with_capacity_hint(trace_id, fan_out_width)` constructor. Deferrable.
- **Routing:** implementer (deferrable)

### F-PASS1-LOW-2: `QueryMetricsBuffer::snapshot()` clones every entry — O(N) cost per diagnostics call

- **Category:** code-quality / performance
- **Required Fix:** Document as future optimization opportunity.
- **Routing:** implementer (deferrable)

## Observations (process-gap codification candidates)

### O-1: [process-gap] Implementer prompt allows "tests pass" status:ready without verifying test bodies actually exercise production paths

The same TD-S307-002/003 silent-shallow-test pattern recurred verbatim. Implementer prompt should require:
1. For every AC test, one assertion that its body invokes the AC's named SUT.
2. A "scaffolding test" tag for tests not invoking SUT, with hard ceiling.
3. Spec-compliance gate failing burst if any Task is "exercised by struct-level test" without production wiring present.

### O-2: [process-gap] `todo!()` in production paths should block ready-status via pre-flight lint

Add lefthook/justfile lint failing CI when `todo!()` / `unimplemented!()` appears outside `#[cfg(test)]` for ready-status commits. Currently only fails at runtime.

### O-3: [process-gap] Cycle-Closing Checklist should require a "production-path-executed" smoke test per Task

For each Task in a story, require a smoke test building the engine and calling `execute()` once with mocks before closing the burst.

## Hallucination Test

All CRIT/HIGH findings have explicit file:line citations. Direct file reads validated each. No plausible-but-non-existent defects identified.

## Process-Gap Codification

O-1, O-2, O-3 deferred to post-S-3.09-converge maintenance burst per orchestrator decision D-296. TDs to be filed:
- TD-S309-O1 P1: implementer prompt SUT-invocation check
- TD-S309-O2 P1: pre-flight todo!()/unimplemented!() lint
- TD-S309-O3 P2: Cycle-Closing Checklist production-path-executed smoke

---

**Convergence verdict: DIRTY — false convergence confirmed.**

Tasks 2, 3, 4 (push half), 5, 6 not implemented. "23/23 GREEN" reflects scaffolding-only test surface. Six of seven ACs cannot be verified against spec preconditions because production paths required don't exist (`execute()` is `todo!()`, `_meta.metrics` envelope absent, `get_fanout_diagnostics` hardcoded zeros, `ExplainResult.actuals` absent). Pass-1 must NOT advance to PR review.

**Recommended remediation:** Address F-PASS1-CRIT-1 (instrument execute()) first; CRIT-2/3/4/5 are downstream wiring; CRIT-6 (test rewrite) follows after SUTs exist.

**Resolution:** Orchestrator decision D-296 (Path A) re-scopes S-3.09 to absorb the engine materialization pipeline build-out (Phase A) plus the profiling instrumentation overlay (Phase B, formerly Tasks 1-6). Story spec amendment v1.5→v1.6 commit `4ab33e75` on factory-artifacts. AC-8..AC-13 added covering execute/execute_scheduled lifecycle, BC-2.11.005/006/007/011/012 transferred to S-3.09 ownership.
