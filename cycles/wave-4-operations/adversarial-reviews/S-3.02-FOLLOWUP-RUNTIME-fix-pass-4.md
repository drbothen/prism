---
document_type: fix-pass-closure-report
target_artifact: S-3.02-FOLLOWUP-RUNTIME (story implementation)
fix_pass_n: 4
target_sha: d7e32ab1
prior_target_sha: b749e6d7
base_sha: c98a38b0
worktree: .worktrees/S-3.02-FOLLOWUP-RUNTIME
branch: feature/S-3.02-FOLLOWUP-RUNTIME
addresses_pass: 4
addresses_findings: [F-LP4-MED-1, F-LP4-OBS-1, F-LP4-OBS-2]
deferred_findings: [F-LP4-OBS-3]
deferred_reason: process-gap (AST exhaustiveness proptest) tracked separately
implementer_agent: vsdd-factory:implementer
backfill_dispatcher: orchestrator (Standing Rule 1)
backfill_timestamp: "2026-05-10"
test_result: 887 passed in prism-query (one new test); 3485 total workspace
just_check_result: PASS exit 0
---

# Fix-Pass-4 Closure Report — S-3.02-FOLLOWUP-RUNTIME

## Preamble

Fix-pass-4 closes all 3 active pass-4 findings (F-LP4-MED-1, F-LP4-OBS-1,
F-LP4-OBS-2). F-LP4-OBS-3 (AST exhaustiveness proptest process-gap) is deferred
as a separate maintenance follow-up per Standing Rule 3 last-resort surface-for-
approval sub-rule (non-security-critical, no code path bypass, tracked separately).

Worktree advanced from `b749e6d7` (post fix-pass-3) to `d7e32ab1` (fix-pass-4
HEAD). `just check` PASS, exit 0. prism-query 887 passed / 0 failed (1 new test
added). Workspace total 3485.

## Per-Finding Closure Table

| Finding | Severity | Status | Evidence |
|---------|----------|--------|----------|
| F-LP4-MED-1 | MED | CLOSED | Extended `walk_expr` at materialization.rs:990-998 with `Expr::FuncCall(func_call)` arm matching both `FuncCall::Scalar { args }` and `FuncCall::Aggregate { args }`; `FuncCall::Window {}` explicitly no-op'd with S-3.06 anchor; wildcard arm comment updated to remove FuncCall from "no subqueries" list. New non-vacuous test `test_LP4_MED_1_func_call_args_subquery_discovered_by_layer1` at materialization.rs:1276-1345 covers BOTH variants. `walk_sql_query` doc at lines 884-894 enumerates "function-call argument lists". |
| F-LP4-OBS-1 | OBS | CLOSED | Doc fix at engine.rs:594-598: removed false "and CTEs" claim; new enumeration matches actual walker coverage. |
| F-LP4-OBS-2 | OBS | CLOSED | Comment fix at materialization.rs:333-335: replaced false "routes through classify_predicates / ADR-022 §C wiring-not-redesign" with honest "Build a flat FilterMap of equality predicates ... Per-sensor classify_predicates integration deferred to wave-5". |
| F-LP4-OBS-3 | OBS | NO-CODE-ACTION (process-gap) | AST exhaustiveness verified by manual enumeration — fragile but non-security-critical. Deferred as separate maintenance: recommend proptest in `crates/prism-query/src/proofs/`. No code change. |

## F-LP4-MED-1 Closure Detail

**Path taken:** Path (a) — extend `walk_expr` to recurse into FuncCall argument
lists without redesigning the traversal architecture (ADR-022 §C wiring-not-
redesign compliant).

**Code change at materialization.rs:990-998:**

```rust
Expr::FuncCall(func_call) => {
    match func_call {
        FuncCall::Scalar { args } | FuncCall::Aggregate { args } => {
            for arg in args {
                walk_expr(arg, visitor)?;
            }
        }
        // FuncCall::Window is a future extension anchor (S-3.06);
        // window frame bounds do not carry InSubquery in current AST.
        FuncCall::Window { .. } => {}
    }
}
```

**Wildcard arm comment update at materialization.rs:965-990:** Removed FuncCall
from the "no subqueries" enumeration in the wildcard arm. Updated to honest list
of truly-leaf-only variants.

**walk_sql_query doc at lines 884-894:** Added "Function call argument lists
(FuncCall::Scalar / Aggregate args — may contain InSubquery; F-LP4-MED-1)" to
the enumeration of nodes walked.

**New test `test_LP4_MED_1_func_call_args_subquery_discovered_by_layer1`
at materialization.rs:1276-1345:** Non-vacuous. Covers both FuncCall::Scalar
(lines 1289-1310) and FuncCall::Aggregate (1322-1344) variants with parallel
assertions. Reverting the walker arm would fail both halves.

## F-LP4-OBS-1 Closure Detail

**engine.rs:594-598 before:**
```
// Walks all subquery-carrying positions: SELECT projections, WHERE predicate
// tree (recursive Predicate::And/Or), JOIN ON conditions, GROUP BY, ORDER BY,
// and CTEs.
```

**engine.rs:594-598 after:**
```
// Walks all subquery-carrying positions: SELECT projections, WHERE predicate
// tree (recursive Predicate::And/Or), JOIN ON conditions, GROUP BY, ORDER BY,
// and function call argument lists (FuncCall::Scalar / Aggregate args).
// CTE support is a forward-coverage anchor; SqlQuery has no WITH field yet.
```

`SqlQuery` (ast.rs:101-114) has no CTE/WITH field — the old claim was false.
The new comment honestly enumerates walker coverage including F-LP4-MED-1's fix.

## F-LP4-OBS-2 Closure Detail

**materialization.rs:333-335 before:**
```
// Routes equality predicates through classify_predicates per ADR-022 §C
// wiring-not-redesign: sensor-level pushdown wired here, redesign deferred.
```

**materialization.rs:333-335 after:**
```
// Build a flat FilterMap of equality predicates for the WHERE clause.
// Per-sensor classify_predicates integration (ADR-022 §C pushdown wiring)
// deferred to wave-5 — not yet threaded through DataFusion plan.
```

This replaces the stale residue from the F-LP3-MED-1 fix that updated the
function-level doc but missed the call-site narration. The new comment is honest
about what the code actually does.

## F-LP4-OBS-3 Deferral

**No code action.** AST exhaustiveness verified by manual enumeration in
walk_expr. The proptest recommendation (verify exhaustiveness automatically by
generating arbitrary Expr trees containing InSubquery and asserting visitor
detects them) is tracked as a separate maintenance follow-up. This is a
process-gap only — not a security-critical bypass, not a correctness defect in
current code.

## Standing Rule 3 Self-Assessment

Each closure meets production-grade discipline per Standing Rule 3:

- F-LP4-MED-1: Path (a) real fix — extends walker without papering over. Test
  is non-vacuous (both Scalar and Aggregate variants, parallel assertions).
  No `MVP limitation` framing. No surface-and-defer pattern.
- F-LP4-OBS-1: Honest doc fix — removes false claim, adds honest enumeration.
- F-LP4-OBS-2: Honest comment fix — removes stale classify_predicates residue,
  replaces with accurate description of what code does.
- F-LP4-OBS-3: Process-gap deferral is legitimate — non-security-critical,
  non-blocking, tracked separately. Not a shortcut to avoid real work.

## Single-Commit Confirmation

All changes committed in a single commit on `feature/S-3.02-FOLLOWUP-RUNTIME`:

```
fix(prism-query): close S-3.02-FOLLOWUP-RUNTIME pass-4 findings — F-LP4-MED-1 + F-LP4-OBS-1 + F-LP4-OBS-2
```

HEAD advanced from `b749e6d7` to `d7e32ab1`. No Stage/backfill theme words.
Single-commit burst protocol (TD-VSDD-053) satisfied.

## Novelty Assessment

| **Field** | **Value** |
|-----------|-----------|
| **Pass** | fix-pass-4 |
| **New findings** | 0 — closure-only report |
| **Duplicate/variant** | 0 |
| **Novelty score** | N/A — closure-only |
| **Median severity** | N/A |
| **Trajectory** | pass-1 12 → pass-2 8 → pass-3 3 → pass-4 1MED+3OBS → fix-pass-4 0 (closure) |
| **Verdict** | FINDINGS_REMAIN (1 OBS process-gap deferred; no novel findings introduced) |
