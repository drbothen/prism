---
document_type: fix-pass-closure-report
target_artifact: S-3.02-FOLLOWUP-RUNTIME (story implementation)
fix_pass_n: 2
review_class: LOCAL
target_sha: 609d7d87
prior_target_sha: 99d49b20
base_sha: c98a38b0
worktree: .worktrees/S-3.02-FOLLOWUP-RUNTIME
branch: feature/S-3.02-FOLLOWUP-RUNTIME
fix_pass_dispatcher: orchestrator (per Standing Rule 3)
implementer_agent: vsdd-factory:implementer
fix_timestamp: "2026-05-10T00:00:00Z"
backfill_timestamp: "2026-05-10T00:00:00Z"
backfill_dispatcher: orchestrator
findings_addressed: 1 NEW CRIT + 1 NEW HIGH + 3 MED + 3 LOW (adversary pass-2)
verification_status: claimed_by_implementer (adversary pass-3 pending independent verification)
test_result: "884 prism-query tests pass; just check 3482 workspace tests pass"
prior_passes:
  - "S-3.02-FOLLOWUP-RUNTIME-pass-1.md"
  - "S-3.02-FOLLOWUP-RUNTIME-fix-pass-1.md"
  - "S-3.02-FOLLOWUP-RUNTIME-pass-2.md"
---

# LOCAL Fix-Pass-2 Closure Report — S-3.02-FOLLOWUP-RUNTIME

## Status: CLAIMED-CLOSED (pending adversary pass-3 verification per Standing Rule 3)

target_sha: `609d7d87`
prior_target_sha: `99d49b20` (adversary pass-2 base)
base_sha: `c98a38b0` (develop tip)

## Commit Log (worktree)

```
609d7d87 fix(prism-query): close F-LP2 adversary findings — CRIT-1 + HIGH-1 + MED-1/2/3 + LOW-1/2/3
99d49b20 style(prism-query): cargo fmt after HIGH-2 implementation
73d97726 fix(prism-query): close F-LP1 adversary findings — CRIT-4, HIGH-3/4/7, MED-3, plus pre-existing clippy fixes
a6380143 feat(prism-query): implement QueryEngine execution pipeline — S-3.02-FOLLOWUP-RUNTIME
```

## Verification Status

- `cargo nextest run -p prism-query --no-fail-fast`: **884 tests pass**
- `just check` (full canonical): **3482 workspace tests pass, 0 failures**

## Per-Finding Closure (claimed by implementer)

### F-LP2-CRIT-1 (Capability gate subquery bypass) — CLOSED via DEFENSE-IN-DEPTH

Three layers implemented per Standing Rule 3:

**Layer 1 (pre-execution recursive AST walk):**
- `extract_source_names_recursive()` in `materialization.rs` recursively walks `Predicate::InSubquery`, HAVING subqueries, SELECT projections, and nested `SqlQuery` bodies.
- `check_internal_table_capabilities()` in `engine.rs` now uses this recursive walker.

**Layer 2 (scan-time gate):**
- `RocksDbTableProvider::new_with_capabilities()` stores caller capabilities.
- `scan()` checks `descriptor.requires_audit_read` and returns `DataFusionError::Plan` with E-QUERY-011 if denied.
- `register_internal_tables_with_capabilities()` passes capabilities through.

**Layer 3 (descriptor-driven policy):**
- `INTERNAL_TABLE_SPECS` static array drives the policy for both layers.
- `table_requires_audit_read()` is the lookup function.
- Future tables with `requires_audit_read = true` are auto-gated (closes F-LP2-MED-3 dead-data issue).

6 new tests covering all three layers.

### F-LP2-HIGH-1 (AC-2 vacuous-pass) — CLOSED

- New `test_AC_2_materialization_pipeline_non_vacuous_assertion` asserts actual row count (3) and `table_exist("crowdstrike_detections")`.
- Sibling sweep result: existing AC-1, AC-3 through AC-8 already had non-vacuous assertions (StubAdapter or precondition checks) — no additional siblings found.

### F-LP2-MED-1 (HIGH-5 doc drift on pushdown routing) — CLOSED

- `collect_eq_filters` deleted.
- `extract_push_down_filters_as_map` now calls `pushdown::predicate_tree_to_filter_map` which uses `classify_predicates` infrastructure.
- AC-4 push-down test continues to pass.

### F-LP2-MED-2 (Cache key incomplete) — CLOSED

- Cache key format: `"client:sensor:source_table:json_filters"` — `serde_json::to_string(&where_filters)` appended.
- Two-scenario test proves same query hits cache, different filters do not.

### F-LP2-MED-3 (Doc drift + dead requires_audit_read) — CLOSED automatically by CRIT-1

`requires_audit_read` now consulted by all three layers; module doc updated.

### F-LP2-LOW-1 (E-QUERY-001 stuffed in wrong variant) — CLOSED

- `PrismError::QueryLimitExceeded { requested, max }` added to `prism-core/src/error.rs`.
- Display outputs `E-QUERY-001: limit {requested} exceeds maximum of {max}`.
- `engine.rs` uses the new variant.

### F-LP2-LOW-2 (`_all` sentinel client_slug) — CLOSED

- When OrgRegistry slug lookup fails: emits `tracing::warn!`; uses synthetic `"org-<hex8>"` slug instead of `"_all"`.
- No-adapters case: `tracing::debug!` + skip (no sentinel target added).

### F-LP2-LOW-3 (Empty registry in HIGH-7 boundary test) — CLOSED

- `test_HIGH_7_limit_exactly_1000_pipeline_success_with_stub` registers a `StubAdapter` returning 5 rows; asserts `returned_results > 0`.

### F-LP2-OBS-1/2/3 (process-gap items) — surfaced, no code changes

- OBS-2 (commit message): this commit enumerates all closed findings.
- OBS-1/3: process-gap items for orchestrator follow-up.

## Files Modified

**Cross-crate touch (justified per Standing Rule 3):**
- `crates/prism-core/src/error.rs` — `QueryLimitExceeded { requested, max }` variant added (F-LP2-LOW-1)

**prism-query crate:**
- `crates/prism-query/src/engine.rs` — capability gate uses recursive walker; QueryLimitExceeded usage
- `crates/prism-query/src/internal_tables.rs` — Layer 2 (scan-time gate); INTERNAL_TABLE_SPECS; register_internal_tables_with_capabilities
- `crates/prism-query/src/materialization.rs` — Layer 1 (recursive AST walk); cache key format; `_all` sentinel removed
- `crates/prism-query/src/pushdown.rs` — predicate_tree_to_filter_map (HIGH-5 closure)
- `crates/prism-query/tests/execute_integration_tests.rs` — AC-2 non-vacuous; LP2-CRIT-1 layer tests (6 new); LP2-MED-2 cache key test; LP2-LOW-3 boundary test

## Adversary Pass-3 Pending

Per Standing Rule 3, implementer self-disclosure NOT authoritative. Adversary pass-3 with fresh context will independently verify all CLAIMED-CLOSED findings + sweep for novel regressions.

Particular pass-3 verification priorities:
- F-LP2-CRIT-1 Layer 2 catches subquery bypass even if Layer 1 misses (defense-in-depth integrity test)
- F-LP2-MED-1 — verify `collect_eq_filters` actually deleted and `pushdown::predicate_tree_to_filter_map` is the new path
- AC-2 non-vacuous assertion is genuinely fixture-dependent
- No new regressions in 884 prism-query tests + 3482 workspace tests
