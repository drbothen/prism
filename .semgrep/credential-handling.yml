# TODO: S-0.02 stub — placeholder Semgrep rules for credential handling.
# Real rule logic will be implemented in the implementation pass.
rules:
  - id: prism-no-string-credentials
    # TODO: implement pattern matching for String credential storage
    patterns:
      - pattern: "TODO"
    message: "Use `secrecy::SecretString` for credential values, never `String`."
    languages: [rust]
    severity: ERROR

  - id: prism-no-log-secret
    # TODO: implement pattern matching for credential logging
    patterns:
      - pattern: "TODO"
    message: "Do not log potential credential values."
    languages: [rust]
    severity: WARNING
